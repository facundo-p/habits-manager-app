---
description: Reglas sobre la identidad visual "Cozy", el componente de fondo global y la estética de cuaderno.
globs: ["src/components/layout/**/*", "src/screens/**/*", "src/styles/ui.styles.ts"]
---

# App Vision: CozyHabit & Background Implementation

## 1. El Fondo "Cozy" (Global Layout)
- **Implementación**: Se debe usar un componente envolvente llamado `AppBackground`.
- **Estructura**:
  1. `ImageBackground`: Usa una imagen cálida/orgánica (ubicada en `assets/images/background.jpg`).
  2. `BlurView`: Capa superior con `intensity={20}` y `tint="light"` (usando `expo-blur`).
  3. `SafeAreaView`: Para contener la navegación y el contenido.
- **Regla**: Ninguna pantalla debe definir su propio fondo; todas deben ser transparentes para mostrar el fondo global.

## 2. Estética "Notebook" (Cuaderno)
- **Papel**: Los contenedores principales deben usar la clase `notebookPaper` de `ui.styles.ts` (blanco hueso, sombra suave, borde izquierdo simulando el margen de un cuaderno).
- **Margen Anillado**: El componente `notebookPaper` debe incluir un borde izquierdo decorativo que simule un anillado espiral (puedes usar un patrón de círculos pequeños o un borde segmentado).
- **Spiral Binding (Anillado)**: 
  - Ubicación: Margen izquierdo extremo.
  - Elementos: Una serie de "anillas" metálicas (óvalos con gradiente gris/plata).
  - Efecto: Cada anilla debe tener una pequeña sombra (`shadowOpacity: 0.2`) sobre el papel.
  - Margen: Una línea vertical roja (`rose-300`) a 40px del borde izquierdo.
- **TopSheet Modal**: Todos los modales de entrada (Reflection, HabitForm, AreaInfo) deben renderizarse en la parte SUPERIOR de la pantalla (`justify-content: flex-start`) con una animación de caída.
- **Checklist**: Al marcar un hábito, el texto debe aplicar un estilo `line-through` con un color sepia suave, simulando un tachado a mano.
- **Iconografía**: Usa `lucide-react-native` con trazos finos y colores tierra.

## 3. Mood Tracker UI
- **Control**: Slider horizontal con valores de 1.0 a 10.0.
- **Snap**: Debe permitir incrementos de 0.5.
- **Feedback**: El valor seleccionado debe mostrarse con una tipografía Serif cálida.

## 4. Navegación
- El botón físico "Atrás" debe cerrar modales abiertos antes de salir de la pantalla.
- Al terminar una edición histórica, el flujo debe retornar a la pantalla de origen (Stats).
- Al presionar la pestaña "Hoy", siempre debe ejecutarse `resetToToday()` para limpiar cualquier fecha histórica.