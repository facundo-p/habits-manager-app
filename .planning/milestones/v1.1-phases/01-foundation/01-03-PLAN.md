---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/config/mood.ts
  - src/components/shared/MoodPicker.tsx
  - src/components/shared/MoodPicker.styles.ts
  - src/components/modals/ReflectionModal.tsx
  - src/components/modals/ReflectionModal.styles.ts
  - src/__tests__/mood.test.ts
autonomous: true
requirements:
  - FOUND-02
  - FOUND-06

must_haves:
  truths:
    - "Existe componente shared <MoodPicker> con props {value, onChange, disabled?}"
    - "ReflectionModal renderiza <MoodPicker> en lugar de MoodSection inline"
    - "src/config/mood.ts es la SoT de la escala mood (re-exporta MOOD_MIN/MAX/STEP/DEFAULT y agrega MOOD_LABELS + moodLabelFor + MOOD_SCALE_VERSION)"
    - "El flow de habit completion + reflection se ve y funciona idéntico al usuario (FOUND-06)"
  artifacts:
    - path: "src/config/mood.ts"
      provides: "Single source of truth for mood scale, labels, scale version"
      exports: ["MOOD_MIN", "MOOD_MAX", "MOOD_STEP", "MOOD_DEFAULT_VALUE", "MOOD_LABELS", "MoodLabel", "moodLabelFor", "MOOD_SCALE_VERSION"]
      contains: "export const MOOD_SCALE_VERSION"
    - path: "src/components/shared/MoodPicker.tsx"
      provides: "Shared MoodPicker component"
      contains: "interface MoodPickerProps"
    - path: "src/components/shared/MoodPicker.styles.ts"
      provides: "Styles for MoodPicker extraídos de ReflectionModal.styles.ts"
      contains: "moodValue"
    - path: "src/components/modals/ReflectionModal.tsx"
      provides: "ReflectionModal refactored to compose <MoodPicker>"
      contains: "<MoodPicker"
  key_links:
    - from: "src/components/modals/ReflectionModal.tsx"
      to: "src/components/shared/MoodPicker.tsx"
      via: "JSX render"
      pattern: "<MoodPicker"
    - from: "src/components/shared/MoodPicker.tsx"
      to: "src/config/mood.ts"
      via: "import MOOD_MIN/MAX/STEP"
      pattern: "from.*config/mood"
---

<objective>
Extraer `<MoodPicker>` shared component desde `MoodSection` inline en `ReflectionModal.tsx` (D-02). Crear `src/config/mood.ts` como SoT de la escala. Refactorizar `ReflectionModal` para componer el nuevo MoodPicker manteniendo paridad UX estricta (D-03 / FOUND-06).

Purpose: Habilitar que Phase 2 surfaces (morning, evening, note, weekly review) consuman un único componente de mood input. Cumple FOUND-02 + FOUND-06.
Output: Componente reutilizable + config module + ReflectionModal con `<MoodPicker>` compuesto.
</objective>

<execution_context>
@/Users/facu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/facu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v1.1-phases/01-foundation/01-CONTEXT.md
@.planning/milestones/v1.1-phases/01-foundation/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@src/components/modals/ReflectionModal.tsx
@src/components/modals/ReflectionModal.styles.ts
@src/config/constants.ts
</context>

<interfaces>
<!-- API contract del componente nuevo. -->

```ts
// src/config/mood.ts — NEW
export { MOOD_MIN, MOOD_MAX, MOOD_STEP, MOOD_DEFAULT_VALUE } from './constants';

export const MOOD_LABELS = ['muyMal', 'mal', 'neutral', 'bien', 'muyBien'] as const;
export type MoodLabel = (typeof MOOD_LABELS)[number];

/** Pure: mapea [MOOD_MIN, MOOD_MAX] → label discreta en buckets equipartitos. */
export function moodLabelFor(value: number): MoodLabel;

/** Stamp persistido por migrationV2 INSERT...SELECT y por nuevos repos. */
export const MOOD_SCALE_VERSION = 'v1' as const;
```

```ts
// src/components/shared/MoodPicker.tsx — NEW
interface MoodPickerProps {
  value: number;                            // [MOOD_MIN, MOOD_MAX], step MOOD_STEP
  onChange: (v: number) => void;
  disabled?: boolean;
}
export function MoodPicker(props: MoodPickerProps): JSX.Element;
```

Layout interno del MoodPicker (extraído verbatim de `ReflectionModal.tsx:66-91`):
- Text "¿Cómo te sientes?"
- Text con `value.toFixed(1)`
- `<Slider>` de `@react-native-community/slider` con bounds + step + colors

CONTEXT D-02: SIN props `size`, `comment`, `sleep`. Cada surface compone su propio layout (Phase 2 envuelve este componente con su context propio).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/config/mood.ts (SoT escala + labels + helper puro)</name>
  <files>src/config/mood.ts, src/__tests__/mood.test.ts</files>
  <behavior>
- Test 1: `moodLabelFor(1)` === 'muyMal'; `moodLabelFor(10)` === 'muyBien'.
- Test 2: `moodLabelFor(5.5)` (mid) === 'neutral'.
- Test 3: `MOOD_SCALE_VERSION` === 'v1'.
- Test 4: `MOOD_LABELS.length` === 5; tipo `MoodLabel` es literal-string union (compile-time, no runtime check necesario más allá del export).
- Test 5: `MOOD_MIN`, `MOOD_MAX`, `MOOD_STEP`, `MOOD_DEFAULT_VALUE` se exportan con los mismos valores que `src/config/constants.ts`.
  </behavior>
  <action>
1. Crear `src/config/mood.ts`:
   ```ts
   export {
     MOOD_MIN,
     MOOD_MAX,
     MOOD_STEP,
     MOOD_DEFAULT_VALUE,
   } from './constants';

   export const MOOD_LABELS = ['muyMal', 'mal', 'neutral', 'bien', 'muyBien'] as const;
   export type MoodLabel = (typeof MOOD_LABELS)[number];

   /**
    * Mapea un valor numérico de mood [MOOD_MIN, MOOD_MAX] a una label discreta.
    * Buckets equipartitos: [1, 2.8) → muyMal, [2.8, 4.6) → mal, etc.
    * Pure function — testable sin DB ni UI.
    */
   export function moodLabelFor(value: number): MoodLabel {
     // ... implementación corta (≤15 líneas) con buckets calculados desde MOOD_MIN/MAX
   }

   /**
    * Versión de la escala persistida en mood_log.mood_scale_version.
    * Bump SOLO si la escala cambia shape (rango, step, semántica de los puntos).
    * NO bumpear por labels — labels son cosméticas.
    */
   export const MOOD_SCALE_VERSION = 'v1' as const;
   ```
   Si `moodLabelFor` supera 20 líneas, refactorizar a constante array de buckets + función mapper de 5 líneas (D-CLAUDE refactor rule).

2. Crear `src/__tests__/mood.test.ts` con los 5 tests del `<behavior>`. Sin mocks — pure unit tests.

3. **NO** tocar `src/config/constants.ts` — sigue siendo la fuente original; `mood.ts` re-exporta.

NOTA per research §3 Open Question: `MOOD_LABELS` se exporta para uso futuro (Phase 2 surfaces); Phase 1 no las consume visualmente. Documentar en comentario inline.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=mood</automated>
  </verify>
  <done>
- `src/config/mood.ts` existe con todos los exports.
- `npm test -- --testPathPattern=mood` verde con 5 tests pasando.
- `moodLabelFor` ≤ 20 líneas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear <MoodPicker> + styles, refactorizar ReflectionModal</name>
  <files>src/components/shared/MoodPicker.tsx, src/components/shared/MoodPicker.styles.ts, src/components/modals/ReflectionModal.tsx, src/components/modals/ReflectionModal.styles.ts</files>
  <action>
1. **Crear `src/components/shared/MoodPicker.tsx`**:
   - Copiar verbatim el JSX de `MoodSection` (`ReflectionModal.tsx:66-91`) — incluido el `<Text>¿Cómo te sientes?</Text>` label, `<Text>{value.toFixed(1)}</Text>`, y el `<Slider>` con bounds + colors.
   - Cambiar el nombre del componente a `MoodPicker`.
   - Props: `{ value: number; onChange: (v: number) => void; disabled?: boolean }`. Mapear `onChange` al `onValueChange` interno del Slider; pasar `disabled` al Slider via `disabled` prop nativa.
   - Imports: `MOOD_MIN/MAX/STEP` desde `'../../config/mood'` (NO desde `constants` directo — fuerza el camino canónico).
   - Sin inline styles (D-CLAUDE) — usar styles del archivo hermano.

2. **Crear `src/components/shared/MoodPicker.styles.ts`**:
   - Copiar las constantes `moodValue`, `sliderWrapper`, `sliderColors`, `nativeStyles.slider`, `label` desde `ReflectionModal.styles.ts` (sólo las que pertenecen al MoodSection).
   - Mantener nombres idénticos para minimizar diff visual.
   - Parametrizar colores desde el theme existente — NO hardcodear (D-CLAUDE rule).

3. **Editar `src/components/modals/ReflectionModal.tsx`**:
   - Borrar la función `MoodSection` (líneas 66-91).
   - Eliminar imports ahora unused (`Slider`, `MOOD_MIN/MAX/STEP`).
   - Importar: `import { MoodPicker } from '../shared/MoodPicker';`.
   - Reemplazar el render de `<MoodSection value={mood} onValueChange={setMood} />` por `<MoodPicker value={mood} onChange={setMood} />`.
   - Verificar visualmente (manual smoke) que el modal se ve idéntico — D-03 paridad estricta.

4. **Editar `src/components/modals/ReflectionModal.styles.ts`**:
   - Eliminar los style keys que ahora viven en `MoodPicker.styles.ts`: `moodValue`, `sliderWrapper`, `sliderColors`, `nativeStyles.slider`, `label` (si era exclusivo de MoodSection).
   - Mantener los style keys que ReflectionModal sigue usando para sus otras secciones (header, comment, actions, sectionGap).

5. **Sin test de UI** — `@testing-library/react-native` NO está instalado (research A5 confirmado: el grep al package.json no encontró la dep). FOUND-02 unit coverage = test de `moodLabelFor` del Task 1; paridad visual de ReflectionModal va a UAT (01-HUMAN-UAT.md Scenario 1).

Cumple D-CLAUDE "no duplicar código" (MoodSection ya no se duplica entre surfaces), "separar lógica y presentación" (config/mood.ts = lógica; MoodPicker.tsx = presentación), "sin inline styling".
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && grep -c "MoodPicker" src/components/modals/ReflectionModal.tsx</automated>
  </verify>
  <done>
- `src/components/shared/MoodPicker.tsx` + `.styles.ts` existen.
- `ReflectionModal.tsx` importa `MoodPicker` y lo renderiza (grep `<MoodPicker` ≥ 1).
- `MoodSection` function ya no existe en `ReflectionModal.tsx` (grep `function MoodSection` → 0).
- `npm test` full suite verde — los tests de mood + cualquier otro test indirecto pasan.
- Verificación visual UAT (deferida a Scenario 1 del 01-HUMAN-UAT.md, ejecutado en Wave 7).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MoodPicker → callers (Phase 2/4) | UI puro controlled; sin I/O. Sin trust boundary explícito. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Bug | Refactor de ReflectionModal rompe paridad UX → habit completion+reflection se ve distinta para el usuario | mitigate | UAT Scenario 1 del 01-HUMAN-UAT.md valida paridad visual + funcional. Diff de styles minimizado (mismas keys, mismos valores). |
| T-03-02 | Bug | `moodLabelFor` produce labels incorrectas en bucket boundaries → consumers futuros muestran label wrong | mitigate | Unit tests 1+2 cubren extremos y mid. Phase 2 puede agregar más bucket tests si descubre nuevos casos. |
</threat_model>

<verification>
- `npm test` full suite verde
- `src/config/mood.ts` exports completos
- `src/components/shared/MoodPicker.tsx` existe y se importa desde ReflectionModal
- UAT Scenario 1 (FOUND-06 paridad) queda en `01-HUMAN-UAT.md` para Wave 7
</verification>

<success_criteria>
Phase 2 puede importar `<MoodPicker>` desde `src/components/shared/MoodPicker` sin volver a inventar el slider. La escala vive en `src/config/mood.ts` única.
</success_criteria>

<output>
Create `.planning/milestones/v1.1-phases/01-foundation/01-03-SUMMARY.md` con: archivos creados/modificados, count de líneas borradas de ReflectionModal, link al Scenario 1 UAT.
</output>
