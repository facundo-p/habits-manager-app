---
phase: 01-foundation
plan: 08
type: execute
wave: 7
depends_on: [02, 03, 04, 05, 06, 07]
files_modified:
  - .planning/STATE.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONVENTIONS.md
  - tasks/lessons.md
  - tasks/todo.md
  - .planning/milestones/v1.1-phases/01-foundation/01-VERIFICATION.md
  - .planning/milestones/v1.1-phases/01-foundation/01-HUMAN-UAT.md
autonomous: false

requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "STATE.md refleja que Phase 1 v1.1 está closed (decisiones D-01..D-07 aplicadas, plans 01-07 done)"
    - "codebase/ARCHITECTURE.md menciona la capa nueva utils/date.ts + las 4 tablas v2 + el bootSequence module"
    - "codebase/STRUCTURE.md tiene los paths nuevos (src/utils/date.ts, src/components/shared/, src/repositories/{moodLog,textLibrary,weeklyReviews,drafts}Repository.ts, src/services/bootSequence.ts, src/components/screens/MigrationErrorScreen.tsx)"
    - "tasks/lessons.md capturó al menos 1 lesson nueva de Phase 1 (codemod strategy, atomic migration, UAT-vs-unit tradeoff con missing testing-library)"
    - "Todos los 3 UAT scenarios del 01-HUMAN-UAT.md están ejecutados OK; Scenario 3 (FOUND-05) es mandatorio — el DraftHarnessModal viene del Plan 05 Task 3"
    - "01-VERIFICATION.md existe con full-suite test summary + grep checks resultados"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated state: Phase 1 v1.1 closed"
      contains: "Phase 1 v1.1"
    - path: ".planning/milestones/v1.1-phases/01-foundation/01-VERIFICATION.md"
      provides: "Sign-off doc del phase con resultados de validation contract"
      contains: "FOUND-01"
---

<objective>
Wave 7 — Cierre del phase. Actualizar docs internos desactualizados (per D-CONV "actualizar archivos .md desactualizados"), ejecutar los UAT scenarios, capturar lessons, y producir el 01-VERIFICATION.md que sirve de sign-off para el phase.

Purpose: Cumplir D-CLAUDE "Document Results" (review section en todo.md) + "Capture Lessons" (tasks/lessons.md). Garantizar que Phase 2 arranca con docs en estado correcto.
Output: Docs actualizados + UAT ejecutado + lessons + VERIFICATION.md.
</objective>

<execution_context>
@/Users/facu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/facu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/milestones/v1.1-phases/01-foundation/01-CONTEXT.md
@.planning/milestones/v1.1-phases/01-foundation/01-HUMAN-UAT.md
@tasks/lessons.md
@tasks/todo.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar codebase maps + STATE.md + tasks/*.md</name>
  <files>.planning/STATE.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/CONVENTIONS.md, tasks/lessons.md, tasks/todo.md</files>
  <action>
1. **`.planning/STATE.md`**: leer el archivo actual; agregar entrada con la fecha de cierre de Phase 1 v1.1; listar decisiones aplicadas D-01..D-07. UAT Scenario 3 (FOUND-05) es MANDATORIO en Phase 1 — el dev harness viene de Plan 05 Task 3; no se difiere.

2. **`.planning/codebase/ARCHITECTURE.md`**: agregar al diagrama de capas:
   - Nueva utility layer: `src/utils/date.ts` (single source of truth para day-key).
   - Nuevo orchestrator: `src/services/bootSequence.ts` (boot lifecycle testable).
   - 4 tablas nuevas + 3 repos nuevos (mood_log, text_library, weekly_reviews, drafts) bajo schema v2.
   - Mencionar `BACKUP_VERSION = 2` con dispatcher v1→v2.

3. **`.planning/codebase/STRUCTURE.md`**: agregar paths nuevos a la árbol de archivos:
   - `src/utils/date.ts`
   - `src/config/mood.ts`
   - `src/components/shared/MoodPicker.tsx` + `.styles.ts`
   - `src/components/screens/MigrationErrorScreen.tsx` + `.styles.ts`
   - `src/repositories/{moodLog,textLibrary,weeklyReviews,drafts}Repository.ts`
   - `src/services/bootSequence.ts`
   - `src/services/migrations/migrationV2.ts`
   - `src/hooks/useDraftAutosave.ts`

4. **`.planning/codebase/CONVENTIONS.md`**: agregar (si no estaba):
   - "Para 'hoy' usar `getLocalDayKey()` desde `src/utils/date.ts`. NUNCA `new Date().toISOString().slice(0,10)`."
   - "Migrations versionadas: cada nueva = `migrationVN.ts` siguiendo template de `migrationV1.ts`. Si la migration es schema-breaking, RETHROW en catch (vs. silent log) para activar MigrationErrorScreen vía bootSequence."
   - "SQL constants al tope del file. No business logic en repos."

5. **`tasks/todo.md`**: marcar items de Phase 1 como done. Agregar review section:
   ```markdown
   ## Phase 1 v1.1 — Review (closed YYYY-MM-DD)

   ### What worked
   - Codemod date helpers en una wave aislada (Plan 02) — bajo blast radius, todo verde al primer intento.
   - migrationV2 reusando template de migrationV1 — pattern probado.
   - Wave 0 de tests skeleton previno tasks RED-MISSING en waves siguientes (Nyquist compliance).

   ### What was bumpy
   - `@testing-library/react-native` no instalado → FOUND-02 UI degradada a UAT (planeado).
   - Pre-v2 snapshot + buildV1Snapshot requirió preservar SQL legacy de moods temporalmente — tradeoff aceptado.

   ### Deferred
   - Ninguno por defecto. UAT Scenario 3 es mandatorio en Phase 1 (dev harness viene de Plan 05 Task 3).
   ```

6. **`tasks/lessons.md`**: agregar lessons nuevas (D-CONV "Self-improvement loop"):
   ```markdown
   ## Lesson — codemod-before-migration
   **Date:** YYYY-MM-DD (Phase 1 v1.1)
   **Pattern:** Renames y refactors cross-cutting (codemod) deben preceder migraciones de schema en una wave aislada. Mezclar codemod + migration en una sola wave aumenta riesgo de rollback y rinde diffs no-revisables.
   **Rule:** Para futuras phases que mezclen refactor + migration, separar en wave 1 = codemod, wave 2 = migration.

   ## Lesson — testing-library availability check
   **Date:** YYYY-MM-DD
   **Pattern:** El planner asumió A5 (`@testing-library/react-native` available) y el grep al package.json reveló que NO. Resultado: FOUND-02 unit test degradado a UAT.
   **Rule:** RESEARCH stage debe ejecutar `cat package.json | grep <dep>` para CADA test infra dep asumida — no solo declararla en Assumptions Log.
   ```

D-CONV "actualizar .md desactualizados" + "Self-improvement loop" cubiertos.
  </action>
  <verify>
    <automated>grep -c "Phase 1 v1.1" .planning/STATE.md && grep -c "getLocalDayKey" .planning/codebase/CONVENTIONS.md && grep -c "Phase 1" tasks/lessons.md</automated>
  </verify>
  <done>
- 4 codebase docs actualizados.
- STATE.md refleja phase close.
- tasks/todo.md + lessons.md actualizados.
- Greps confirman keywords nuevos presentes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ejecutar UAT scenarios del 01-HUMAN-UAT.md</name>
  <what-built>
- Build APK local via `eas build --local` con todos los waves 1-6 aplicados.
- 3 escenarios de UAT a ejecutar (ver `01-HUMAN-UAT.md`):
  - Scenario 1: FOUND-06 paridad — completar hábito + reflexión y verificar UX idéntica.
  - Scenario 2: D-05 error screen — usar `__DEV_FORCE_MIGRATION_FAIL` para forzar fallo y validar Restore + Retry.
  - Scenario 3: FOUND-05 draft survives kill — usar `DraftHarnessModal` dev-only (entrega obligatoria del Plan 05 Task 3, accesible desde Settings bajo `__DEV__`).
  </what-built>
  <how-to-verify>
1. `eas build --local --platform android` → instalar APK en device físico (force-kill require device real, no Expo Go).
2. Ejecutar los 3 scenarios siguiendo los steps del 01-HUMAN-UAT.md.
3. Marcar las checkboxes de cada Assertion en el UAT doc.
4. Firmar la sección "Sign-off" (tester=facu, date, build SHA).
5. Si algún scenario falla → NO marcar el phase como complete; abrir un plan de gap closure (`/gsd-plan-phase --gaps`).
6. Los 3 scenarios son mandatorios; ningún defer silencioso. Si el dev harness no se entregó en Plan 05 → bloquear sign-off y volver a Wave 4.
  </how-to-verify>
  <resume-signal>Type "approved" si los 3 scenarios pasan, o describir el fallo.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Producir 01-VERIFICATION.md (sign-off del phase)</name>
  <files>.planning/milestones/v1.1-phases/01-foundation/01-VERIFICATION.md, .planning/milestones/v1.1-phases/01-foundation/01-HUMAN-UAT.md</files>
  <action>
1. Ejecutar:
   ```bash
   npm test 2>&1 | tee /tmp/phase1-fullsuite.log | tail -20
   grep -rn "getTodayPrefix" src/ ; echo "exit=$?"
   grep -rn "new Date().toISOString().slice(0,10)" src/ ; echo "exit=$?"
   grep -rn "BACKUP_VERSION" src/config/constants.ts
   ```
   Capturar outputs.

2. Crear `.planning/milestones/v1.1-phases/01-foundation/01-VERIFICATION.md`:
   ```markdown
   # Phase 1 — Verification Sign-Off

   **Closed:** YYYY-MM-DD
   **Builder:** Claude + facu (UAT)
   **Build SHA:** <commit>

   ## Validation Contract Results (per 01-VALIDATION.md)

   | Req ID | Behavior | Command | Result |
   |--------|----------|---------|--------|
   | FOUND-01 | getLocalDayKey + grep ban | `npm test -- --testPathPattern=dateUtils` + grep | ✅ |
   | FOUND-02 | MoodPicker shared + moodLabelFor unit | `npm test -- --testPathPattern=mood` | ✅ (UI via UAT Scenario 1) |
   | FOUND-03 forward | migration v2 forward | `... migrationV2 -t forward` | ✅ |
   | FOUND-03 idempotency | 2x runMigrations | `... -t idempotency` | ✅ |
   | FOUND-03 rollback | forced throw | `... -t rollback` | ✅ |
   | FOUND-03 integrity | INSERT...SELECT count match | `... -t "data integrity"` | ✅ |
   | FOUND-04 v1→v2 | mood_entries → mood_log mapping | `... backupV1toV2 -t "v1 forward"` | ✅ |
   | FOUND-04 v2 roundtrip | buildBackupData round-trip | `... -t "round-trip"` | ✅ |
   | FOUND-04 v3 reject | future version error | `... -t "future"` | ✅ |
   | FOUND-05 drafts CRUD | upsert/find/delete/purge | `... drafts` | ✅ |
   | FOUND-05 autosave | debounce 500ms | `... drafts -t debounce` | ✅ |
   | FOUND-05 survives kill | UAT Scenario 3 | manual | <result> |
   | FOUND-06 paridad | UAT Scenario 1 | manual | <result> |
   | D-05 error screen | UAT Scenario 2 | manual | <result> |
   | D-06 pre-v2 snapshot | snapshot pre-transaction | `... migrationV2 -t snapshot` | ✅ |
   | D-06 cleanup 30d | stale snapshots purged | `... -t cleanup` | ✅ |

   ## Grep Checks

   - `grep -rn "getTodayPrefix" src/` → **0 matches** ✅
   - `grep -rn "new Date().toISOString().slice(0,10)" src/` → **0 matches** ✅
   - `grep "BACKUP_VERSION" src/config/constants.ts` → **`= 2`** ✅

   ## Full-suite

   - `npm test` → **N tests passing**, 0 failing, M todos (intencional para Phase 2/4)

   ## Threat Model Outcomes

   Refer to threat model en cada plan 01-04 — todas las T-XX-YY entries con disposition `mitigate` están cubiertas por tests automáticos. Las `accept` están documentadas en CONTEXT Deferred Ideas.

   ## Deferred to Phase 2

   - <list any deferred items>

   ## Sign-off

   - [ ] Tester: facu — Date: ____ — Result: ____
   ```

3. Editar `01-HUMAN-UAT.md` para marcar los checkboxes de sign-off (resultado del Task 2).
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.1-phases/01-foundation/01-VERIFICATION.md && grep -c "FOUND-" .planning/milestones/v1.1-phases/01-foundation/01-VERIFICATION.md</automated>
  </verify>
  <done>
- 01-VERIFICATION.md existe con tabla completa.
- Todas las rows tienen resultado (✅/❌/deferred).
- Sign-off firmado por humano.
- UAT doc tiene checkboxes marcados.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Docs desactualizados → Phase 2 hace decisiones basadas en info obsoleta | mitigate | Task 1 actualiza 4 codebase maps + STATE.md. Wave 7 NO cierra si falta uno. |
| T-08-02 | Bug | UAT firmado sin ejecutar realmente (sign-off fake) | accept | Confianza interna; el proyecto es solo-dev. Build SHA en el sign-off ata el resultado a un commit específico. |
</threat_model>

<verification>
- 01-VERIFICATION.md existe con todas las rows
- UAT scenarios ejecutados (los 3 OK; ningún defer)
- codebase maps + STATE.md + tasks/* actualizados
</verification>

<success_criteria>
Phase 1 v1.1 cierra con docs internos coherentes con el estado del código + UAT firmado + lessons capturadas. Phase 2 puede arrancar con confianza.
</success_criteria>

<output>
Create `.planning/milestones/v1.1-phases/01-foundation/01-08-SUMMARY.md` con: full-suite test count, lista de deferred items, sign-off ref, commit SHAs por wave.
</output>
