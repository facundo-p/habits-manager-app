---
phase: 01-foundation
plan: 06
type: execute
wave: 5
depends_on: [04]
files_modified:
  - src/services/bootSequence.ts
  - src/components/screens/MigrationErrorScreen.tsx
  - src/components/screens/MigrationErrorScreen.styles.ts
  - App.tsx
  - src/__tests__/bootSequence.test.ts
autonomous: false

requirements:
  - FOUND-03

must_haves:
  truths:
    - "Si runMigrations propaga un throw, App.tsx renderiza MigrationErrorScreen en lugar del NavigationContainer"
    - "MigrationErrorScreen muestra 'No se pudo actualizar la base de datos.' + botón Restore + botón Reintentar"
    - "Reintentar re-ejecuta initDatabase y, si pasa, renderiza el Home normal"
    - "Restore navega al flow existente de Drive restore (Phase 3 v1.0) — opcionalmente con un pre-step que ofrece el snapshot pre-v2 local primero"
    - "El navigator principal está bloqueado mientras migrationState !== 'ok'"
    - "Bajo __DEV__ existe un flag/hook que permite forzar el fallo para UAT"
  artifacts:
    - path: "src/services/bootSequence.ts"
      provides: "Testable boot orchestrator: initDatabase → backfill → migration state"
      exports: ["bootSequence", "MigrationState"]
      contains: "MigrationState"
    - path: "src/components/screens/MigrationErrorScreen.tsx"
      provides: "Blocking error screen with Restore + Retry actions"
      contains: "Reintentar migración"
    - path: "App.tsx"
      provides: "Conditional render basado en migrationState"
      contains: "MigrationErrorScreen"
  key_links:
    - from: "App.tsx"
      to: "src/services/bootSequence.ts"
      via: "useEffect calls bootSequence"
      pattern: "bootSequence"
    - from: "App.tsx"
      to: "src/components/screens/MigrationErrorScreen.tsx"
      via: "conditional render"
      pattern: "<MigrationErrorScreen"
---

<objective>
Implementar la error UX para fallos de migrationV2 (D-05): pantalla bloqueante con Restore + Retry buttons. Extraer la lógica de boot de App.tsx a `src/services/bootSequence.ts` para hacerla testeable en aislamiento. Agregar un dev-only hook `__DEV_FORCE_MIGRATION_FAIL` para que el Scenario 2 del UAT pueda forzar el fallo.

Purpose: Cumplir D-05 (la app NO arranca a un home roto si migration v2 falla). El usuario tiene una salida accionable (Restore o Retry).
Output: Error screen + bootSequence module + App.tsx con render condicional + tests unitarios del bootSequence.
</objective>

<execution_context>
@/Users/facu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/facu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.1-phases/01-foundation/01-CONTEXT.md
@.planning/milestones/v1.1-phases/01-foundation/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@App.tsx
</context>

<interfaces>
```ts
// src/services/bootSequence.ts — NEW

export type MigrationState =
  | { status: 'pending' }
  | { status: 'ok' }
  | { status: 'failed'; error: unknown };

/**
 * Boot orchestrator separado de App.tsx para testability.
 * Acepta dependencias inyectables (initDatabase, checkAndBackfillHistory) para mock en tests.
 */
export async function bootSequence(deps: {
  initDatabase: () => Promise<void>;
  checkAndBackfillHistory: () => Promise<void>;
}): Promise<MigrationState>;
```

```ts
// src/components/screens/MigrationErrorScreen.tsx — NEW
interface MigrationErrorScreenProps {
  error: unknown;
  onRetry: () => void;
  onRestore: () => void;   // navega a Drive restore o pre-v2 snapshot picker (planner decide UX)
}
export function MigrationErrorScreen(props: MigrationErrorScreenProps): JSX.Element;
```

```ts
// App.tsx — render condicional
const [migrationState, setMigrationState] = useState<MigrationState>({ status: 'pending' });

useEffect(() => {
  bootSequence({ initDatabase, checkAndBackfillHistory }).then(setMigrationState);
}, []);

if (migrationState.status === 'pending' || (!fontsLoaded && !fontError)) return <LoadingScreen />;
if (migrationState.status === 'failed') return <MigrationErrorScreen
  error={migrationState.error}
  onRetry={() => { setMigrationState({status:'pending'}); bootSequence(...).then(setMigrationState); }}
  onRestore={() => { /* navigate to Drive restore screen */ }}
/>;
return <SafeAreaProvider><NavigationContainer>...</NavigationContainer></SafeAreaProvider>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extraer bootSequence + tests (initDatabase throw → MigrationState 'failed')</name>
  <files>src/services/bootSequence.ts, src/__tests__/bootSequence.test.ts</files>
  <behavior>
- Test 1: `bootSequence` con `initDatabase` que resuelve OK + `checkAndBackfillHistory` que resuelve OK → returns `{status:'ok'}`.
- Test 2: `initDatabase` rejects con Error('migration v2 falló') → returns `{status:'failed', error: <that error>}`.
- Test 3: `initDatabase` OK pero `checkAndBackfillHistory` rejects → returns `{status:'failed', error: ...}` (backfill failure también bloquea — research §6 contradice: backfill no es schema-breaking → decisión planner: backfill SÍ degrada a OK con warning log, NO bloquea). **Decisión:** Test 3 espera `{status:'ok'}` con un `console.warn` (backfill is best-effort, no blocking). Documentar inline.
- Test 4: Errors no incluyen PII en mensaje serializado (verificar que `error.message` no contiene mood values, comments, etc. — el error de migration solo dice "schema migration falló").
  </behavior>
  <action>
1. **Crear `src/services/bootSequence.ts`**:
   ```ts
   export type MigrationState =
     | { status: 'pending' }
     | { status: 'ok' }
     | { status: 'failed'; error: unknown };

   export interface BootDeps {
     initDatabase: () => Promise<void>;
     checkAndBackfillHistory: () => Promise<void>;
   }

   export async function bootSequence(deps: BootDeps): Promise<MigrationState> {
     try {
       await deps.initDatabase();
     } catch (err) {
       console.error('[bootSequence] init falló:', err instanceof Error ? err.message : 'unknown');
       return { status: 'failed', error: err };
     }
     try {
       await deps.checkAndBackfillHistory();
     } catch (err) {
       console.warn('[bootSequence] backfill falló (no bloqueante):', err instanceof Error ? err.message : 'unknown');
       // Backfill no bloquea — schema OK suficiente.
     }
     return { status: 'ok' };
   }
   ```

2. **Poblar `src/__tests__/bootSequence.test.ts`** con tests 1-4. Mocks via `jest.fn()`.

D-CLAUDE "separar lógica y presentación": App.tsx queda como pure render; bootSequence es lógica testable.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=bootSequence</automated>
  </verify>
  <done>
- 4 tests verdes.
- `bootSequence` exportado.
  </done>
</task>

<task type="auto">
  <name>Task 2: MigrationErrorScreen component + styles</name>
  <files>src/components/screens/MigrationErrorScreen.tsx, src/components/screens/MigrationErrorScreen.styles.ts</files>
  <action>
1. **Crear `src/components/screens/MigrationErrorScreen.tsx`**:
   - Componente funcional simple (research §6 esqueleto).
   - Props: `{ error, onRetry, onRestore }`.
   - Layout: full-screen View centrado.
     - `<Text>` headline: "No se pudo actualizar la base de datos."
     - `<Text>` subhead: "Tu información está a salvo — podés restaurar desde un backup o reintentar la actualización."
     - `<Pressable onPress={onRestore}>` con label "Restaurar desde backup".
     - `<Pressable onPress={onRetry}>` con label "Reintentar migración".
     - Dev-only (`if (__DEV__)`): mostrar `error instanceof Error ? error.message : 'Error desconocido'` en texto chico.
   - Usar tone-of-voice (Wave 6 entrega doc) — empática, neutral, sin culpar. Copy ya alineado. Si Wave 6 todavía no entregó, copy provisional con ese tono.

2. **Crear `src/components/screens/MigrationErrorScreen.styles.ts`**:
   - StyleSheet con keys `container`, `headline`, `subhead`, `actions`, `primaryButton`, `secondaryButton`, `buttonText`, `errorDetail`.
   - Colores parametrizados desde theme existente (`ui.styles` o equivalente — grep `colors\.\|palette` en el codebase para confirmar el path).
   - NO inline styling (D-CLAUDE).
   - Layout: `flex: 1, justifyContent: 'center', alignItems: 'center', padding: 24`.

3. **No tests UI** (research A5 — `@testing-library/react-native` missing). Verificación via UAT Scenario 2 (Wave 7).

D-CLAUDE: "sin inline styling, CSS separado, parametrizar colores" — explícitamente cubierto.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/screens/MigrationErrorScreen.tsx 2>&1 | head -10; test -f src/components/screens/MigrationErrorScreen.styles.ts</automated>
  </verify>
  <done>
- Component + styles existen.
- TypeScript compila sin errores.
- Verificación UX deferida a UAT Scenario 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Wire App.tsx + dev flag for UAT + manual smoke test</name>
  <files>App.tsx</files>
  <what-built>
- `App.tsx` modificado para usar `bootSequence` + state `migrationState` + render condicional (LoadingScreen / MigrationErrorScreen / NavigationContainer).
- Dev-only flag `__DEV_FORCE_MIGRATION_FAIL` exportado para que UAT Scenario 2 lo pueda flip:
  ```ts
  // En migrationV2.ts (top of file):
  // @ts-expect-error: dev-only global
  export const __DEV_FORCE_MIGRATION_FAIL = { value: false };

  // Dentro del transaction, primer step:
  if (__DEV__ && __DEV_FORCE_MIGRATION_FAIL.value) {
    throw new Error('FORCED FAIL FOR UAT');
  }
  ```
- onRetry handler resets migrationState → 'pending' → re-invokes bootSequence.
- onRestore handler navega al Drive restore flow existente (`SettingsScreen` o `RestoreFromDriveScreen` — re-usar Phase 3 v1.0 flow). Si el snapshot pre-v2 existe en `documentDirectory`, **planner aterriza UX** (research §6 Open Q #6): recomendación es offer-first del snapshot local (one-tap) y fallback a Drive. **Implementación mínima Phase 1:** botón único "Restaurar" navega a una nueva mini-screen `RestoreOptionsScreen` que lista (a) snapshot pre-v2 local (si existe — leer `documentDirectory` para archivos `pre-v2-snapshot-*.json`) y (b) "Importar desde Google Drive" (lleva al flow existente). Si NO hay snapshot local, navega directo al flow Drive.
  </what-built>
  <how-to-verify>
1. Ejecutar `npm test` — full suite verde (los tests que mockean `initDatabase` siguen funcionando porque App.tsx pasa la función via deps al bootSequence).
2. Ejecutar `eas build --local` (per skill `build-apk-local`) → instalar APK en device/emulator.
3. Abrir la app normalmente → debe llegar al Home sin error screen (migrationState='ok').
4. Activar `__DEV_FORCE_MIGRATION_FAIL.value = true` via dev console (o un toggle en SettingsScreen dev section — planner aterriza el cómo).
5. Force-kill la app + reabrir → debe aparecer MigrationErrorScreen.
6. Tap "Reintentar migración" → vuelve a fallar (flag sigue ON) → mismo screen.
7. Toggle flag OFF + tap "Reintentar migración" → debe pasar al Home.
8. Confirmar visualmente: pantalla legible, botones tappables, copy empático (alineado a tone-of-voice).

Cumple D-CONV "Planning obligatorio antes de código" — este checkpoint pausa para verificación humana antes de cerrar la wave.
  </how-to-verify>
  <resume-signal>Type "approved" si la pantalla rendering OK + retry funciona + flag dev expuesto, o describir issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration throw → App.tsx render | Error message puede contener detalles técnicos. |
| Dev flag → production build | El flag SOLO debe ser efectivo bajo `__DEV__`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Info Disclosure | `error.message` mostrado en MigrationErrorScreen incluye detalles de schema/data | mitigate | Mostrar solo `error.message` (no `error` raw). Gated por `__DEV__` para no mostrar a usuarios finales. En prod build: "Error técnico — contactá soporte". |
| T-06-02 | Elevation of Privilege | `__DEV_FORCE_MIGRATION_FAIL` accesible en producción → DoS de la app | mitigate | Toda lectura del flag está envuelta en `if (__DEV__)`. La constante existe pero su efecto es no-op en prod. Verificar bundle: `grep "__DEV_FORCE_MIGRATION_FAIL" dist/` post-build → no debe aparecer en runtime code. |
</threat_model>

<verification>
- `npm test -- --testPathPattern=bootSequence` verde
- `App.tsx` importa `bootSequence` y `MigrationErrorScreen`
- Manual smoke (Task 3) confirmado por humano
- UAT Scenario 2 listo para Wave 7
</verification>

<success_criteria>
Una migration v2 fallida resulta en una experiencia accionable (Restore o Retry), no en un Home roto ni en un crash silencioso.
</success_criteria>

<output>
Create `.planning/milestones/v1.1-phases/01-foundation/01-06-SUMMARY.md` con: decisión final sobre Restore UX (auto-pre-v2 vs picker), screenshot del error screen, flag dev location.
</output>
