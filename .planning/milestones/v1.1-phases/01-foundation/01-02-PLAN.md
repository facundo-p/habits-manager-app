---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/utils/date.ts
  - src/utils/dateHelpers.ts
  - src/services/db.ts
  - src/services/assignmentService.ts
  - src/services/moodService.ts
  - src/services/driveBackupService.ts
  - src/hooks/useDriveActions.ts
  - src/screens/SettingsScreen.tsx
  - src/screens/RestoreFromDriveScreen.tsx
  - src/__tests__/dateUtils.test.ts
  - src/__tests__/db.test.ts
  - src/__tests__/dailyAssignments.test.ts
  - src/__tests__/habitService.test.ts
  - src/__tests__/sanitize.test.ts
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "src/utils/date.ts exporta getLocalDayKey, isFutureDate, nextDay, formatDateStr, getNowTimestamp, getTimestampForDate"
    - "Cero call sites de getTodayPrefix en src/"
    - "Cero ocurrencias de `new Date().toISOString().slice(0,10)` en src/"
    - "El test cross-midnight existente sigue verde post-codemod, ahora bajo dateUtils.test.ts"
  artifacts:
    - path: "src/utils/date.ts"
      provides: "Único módulo de date helpers para el proyecto"
      exports: ["getLocalDayKey", "isFutureDate", "nextDay", "formatDateStr", "getNowTimestamp", "getTimestampForDate"]
      contains: "export function getLocalDayKey"
    - path: "src/services/db.ts"
      provides: "DB bootstrap + migrations puros (sin date helpers)"
      contains: "initDatabase"
  key_links:
    - from: "src/services/assignmentService.ts"
      to: "src/utils/date.ts"
      via: "import getLocalDayKey, isFutureDate, nextDay"
      pattern: "from.*utils/date"
    - from: "src/services/moodService.ts"
      to: "src/utils/date.ts"
      via: "import getLocalDayKey, getNowTimestamp"
      pattern: "from.*utils/date"
---

<objective>
Codemod completo `getTodayPrefix → getLocalDayKey` (D-01). Mover los 6 date helpers (getLocalDayKey, isFutureDate, nextDay, formatDateStr, getNowTimestamp, getTimestampForDate) desde `src/services/db.ts` + `src/services/assignmentService.ts` + `src/utils/dateHelpers.ts` a un único `src/utils/date.ts`. Reescribir `dateToPrefix` con getters UTC (sin `toISOString().slice(0,10)`) para que el grep ban sea absoluto.

Purpose: Eliminar el antipattern day-key drift del día 1 (Pitfall #1). Cumple FOUND-01.
Output: Un único módulo date.ts; db.ts queda como bootstrap + migrations puros.
</objective>

<execution_context>
@/Users/facu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/facu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v1.1-phases/01-foundation/01-CONTEXT.md
@.planning/milestones/v1.1-phases/01-foundation/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@src/services/db.ts
@src/utils/dateHelpers.ts
</context>

<interfaces>
<!-- API canónica del nuevo módulo (consumida por todos los call sites). -->

```ts
// src/utils/date.ts — NEW

/** "Hoy" en local TZ, formato YYYY-MM-DD. Reemplaza getTodayPrefix. */
export function getLocalDayKey(): string;

/** datePrefix > getLocalDayKey() */
export function isFutureDate(datePrefix: string): boolean;

/** Avanza un día sobre un string YYYY-MM-DD, UTC arithmetic (DST-safe). */
export function nextDay(dateStr: string): string;

/** Formatea Date → YYYY-MM-DD usando getters UTC (NO toISOString().slice). */
export function formatDateStr(d: Date): string;

/** "YYYY-MM-DD HH:MM:SS" actual. */
export function getNowTimestamp(): string;

/** "<datePrefix> HH:MM:SS" con hora actual. */
export function getTimestampForDate(datePrefix: string): string;
```

CONTEXT D-01 dice "rename total" → SIN alias `getTodayPrefix`. Symbolic rename absoluto.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/utils/date.ts y unpoblar db.ts/assignmentService.ts/dateHelpers.ts</name>
  <files>src/utils/date.ts, src/services/db.ts, src/services/assignmentService.ts, src/utils/dateHelpers.ts, src/__tests__/dateUtils.test.ts, src/__tests__/db.test.ts</files>
  <behavior>
- Test 1: `getLocalDayKey()` a las 23:59 local devuelve día N; a las 00:01 local devuelve día N+1 (cross-midnight) — port literal de `db.test.ts`.
- Test 2: `getLocalDayKey()` NUNCA delega en UTC (no internal `.toISOString().slice(0,10)`).
- Test 3: `formatDateStr(new Date('2026-03-10T00:00:00Z'))` === '2026-03-10' usando UTC getters (DST-stable).
- Test 4: `nextDay('2026-03-09')` === '2026-03-10' incluso cruzando DST spring-forward.
- Test 5: `isFutureDate('2099-01-01')` === true; `isFutureDate(getLocalDayKey())` === false.
- Test 6: `getNowTimestamp()` matches /^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}$/.
- Test 7: `getTimestampForDate('2026-05-12')` matches /^2026-05-12 \d{2}:\d{2}:\d{2}$/.
  </behavior>
  <action>
1. **Crear `src/utils/date.ts`** con las 6 funciones. Copiar implementación verbatim de `db.ts:31-51` para `getLocalDayKey` (renombrado), `isFutureDate`, `getNowTimestamp`, `getTimestampForDate`. Mover `nextDay` desde `assignmentService.ts:374-378`. Reescribir `dateToPrefix` (de `dateHelpers.ts:27-29`) como `formatDateStr` usando getters UTC explícitos:
   ```ts
   export function formatDateStr(d: Date): string {
     const y = d.getUTCFullYear();
     const m = String(d.getUTCMonth() + 1).padStart(2, '0');
     const day = String(d.getUTCDate()).padStart(2, '0');
     return `${y}-${m}-${day}`;
   }
   ```
   Esta reescritura es deliberada (research §2): elimina la última ocurrencia de `toISOString().slice(0,10)` y permite que el grep ban sea absoluto. Comentar inline: `// UTC getters — DST-safe; permite que el grep ban sobre toISOString().slice(0,10) sea absoluto (research §2)`.

2. **Editar `src/services/db.ts`**: borrar las 4 funciones (líneas 31-51). El header del archivo queda solo con bootstrap + migrations. Eliminar el comment "Date helpers (compartidos entre services)".

3. **Editar `src/services/assignmentService.ts`**: borrar la función `nextDay` local (líneas 374-378). Cambiar imports al tope: `import { getLocalDayKey, isFutureDate, nextDay, getNowTimestamp, getTimestampForDate } from '../utils/date';` reemplazando los imports actuales desde `./db`.

4. **Editar `src/utils/dateHelpers.ts`**: borrar `dateToPrefix` (líneas 27-29). Si `dateHelpers.ts` queda con UI formatters (`formatTodayDate`, `formatHistoricDate`), mantener el archivo. Si queda vacío, eliminarlo (decisión inline al ver el contenido restante).

5. **Poblar `src/__tests__/dateUtils.test.ts`**: convertir los `it.todo` de Wave 0 en tests reales (port del cross-midnight de `db.test.ts` con `jest.useFakeTimers().setSystemTime(...)`, más los 5 tests del bloque `<behavior>` arriba).

6. **Reescribir `src/__tests__/db.test.ts`**: eliminar los tests de `getTodayPrefix` (ahora viven en dateUtils.test.ts). Si el file queda con tests no-date (sanitize, etc.), mantener; sino borrar el file.

Cumple D-CLAUDE "refactor si función >20 líneas" — las funciones movidas son chicas; no requieren split. Cumple "no duplicar código" — la duplicación previa entre `db.ts` y `dateHelpers.ts` se elimina.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='dateUtils' && grep -rn "new Date().toISOString().slice(0,10)" src/ ; test $? -ne 0</automated>
  </verify>
  <done>
- `src/utils/date.ts` exporta las 6 funciones.
- `npm test -- --testPathPattern=dateUtils` verde con todos los tests de behavior pasando (sin `it.todo`).
- `grep -rn "new Date().toISOString().slice(0,10)" src/` retorna 0 matches.
- `db.ts` no contiene las funciones de date helpers (grep "function getTodayPrefix\|function getNowTimestamp\|function isFutureDate\|function getTimestampForDate" src/services/db.ts → 0).
  </done>
</task>

<task type="auto">
  <name>Task 2: Codemod de todos los call sites + ajuste de mocks en tests</name>
  <files>src/services/moodService.ts, src/services/driveBackupService.ts, src/hooks/useDriveActions.ts, src/screens/SettingsScreen.tsx, src/screens/RestoreFromDriveScreen.tsx, src/__tests__/dailyAssignments.test.ts, src/__tests__/habitService.test.ts, src/__tests__/sanitize.test.ts</files>
  <action>
Para cada archivo de la lista, hacer find-replace EN ORDEN:

1. **`src/services/moodService.ts`** (líneas 10, 20, 29, 38 per research §2):
   - Reemplazar `import { getTodayPrefix, getNowTimestamp, getTimestampForDate } from './db';` con `import { getLocalDayKey, getNowTimestamp, getTimestampForDate } from '../utils/date';`.
   - Find-replace `getTodayPrefix` → `getLocalDayKey` en todo el archivo.

2. **`src/services/driveBackupService.ts`** (líneas 31, 188):
   - `import { ..., getTodayPrefix } from './db';` → import `getLocalDayKey` from `../utils/date` (mantener otros imports de `./db` si son del `getDatabase`/`generateId`).
   - Find-replace `getTodayPrefix` → `getLocalDayKey`.

3. **`src/hooks/useDriveActions.ts`** (líneas 11, 83):
   - Mismo patrón: extract `getTodayPrefix` import a un import desde `../utils/date` con `getLocalDayKey`.
   - Find-replace identificador.

4. **`src/screens/SettingsScreen.tsx`** (líneas 31, 117): mismo patrón.

5. **`src/screens/RestoreFromDriveScreen.tsx`** (líneas 22, 127): mismo patrón.

6. **`src/__tests__/dailyAssignments.test.ts`** (líneas 10, 26, 83, 101, 113, 128, 151, 176, 177, 200, 210, 239, 270 per research §2):
   - Cambiar `jest.mock('../services/db', ...)` → `jest.mock('../utils/date', ...)` SI el mock target era para los date helpers. Si el mock era para `getDatabase`/`generateId`, mantenerlo y agregar un segundo `jest.mock('../utils/date', ...)` para los helpers.
   - Find-replace `getTodayPrefix` → `getLocalDayKey` en imports y call sites.

7. **`src/__tests__/habitService.test.ts`**: mismo patrón (líneas con `getTodayPrefix, getNowTimestamp, getTimestampForDate`).

8. **`src/__tests__/sanitize.test.ts`**: solo un comentario referencia `getTodayPrefix` — actualizarlo a `getLocalDayKey`.

**Verificación intermedia tras cada archivo**: `npm test -- --testPathPattern=<archivo>` debería seguir verde. Si un mock target no fue actualizado, fallará — corregir antes de seguir.

**Orden estricto**: services primero (moodService, driveBackupService), luego hooks, luego screens, luego tests. Esto minimiza el ventana de inconsistencia.

Cumple D-CONV "no duplicar código" (single import path) y D-CLAUDE "tasks pequeñas" (un archivo a la vez).
  </action>
  <verify>
    <automated>grep -rn "getTodayPrefix" src/ ; test $? -ne 0 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>
- `grep -rn "getTodayPrefix" src/` retorna 0 matches.
- `grep -rn "from.*services/db'" src/ | grep -i "getNow\|getTimestamp\|isFutureDate\|nextDay\|getLocal" | wc -l` retorna 0.
- `npm test` (full suite) verde — todos los tests que tocaban date helpers ahora pasan via `../utils/date` mock target.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| utils/date.ts → callers | Función pura, sin I/O ni state. Sin trust boundary real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Bug | Codemod parcial deja un archivo con import roto → runtime crash en producción | mitigate | `npm test` (full suite) tras cada archivo + grep final `getTodayPrefix → 0 matches`. |
| T-02-02 | Bug | `formatDateStr` reescrito cambia semántica vs. `dateToPrefix` original | mitigate | Test 3 del behavior verifica equivalencia sobre Date UTC. Adicionalmente, el caller único de `dateToPrefix` (en `assignmentService.nextDay`) sigue funcionando bajo los tests existentes de `dailyAssignments.test.ts` |
</threat_model>

<verification>
- `npm test` verde.
- `grep -rn "getTodayPrefix" src/` → 0 matches.
- `grep -rn "new Date().toISOString().slice(0,10)" src/` → 0 matches.
- `grep -rn "function getTodayPrefix\|function getNowTimestamp\|function isFutureDate\|function getTimestampForDate" src/services/db.ts` → 0 matches.
- `src/utils/date.ts` existe con 6 exports.
</verification>

<success_criteria>
Wave 2+ pueden consumir `import { getLocalDayKey } from '../utils/date'` confiando en un módulo único. El día-key drift queda eliminado del codebase como antipattern.
</success_criteria>

<output>
Create `.planning/milestones/v1.1-phases/01-foundation/01-02-SUMMARY.md` con: lista de archivos tocados, count final de grep `getTodayPrefix` (debe ser 0), test results de dateUtils suite.
</output>
