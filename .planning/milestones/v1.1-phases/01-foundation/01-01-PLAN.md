---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/setup/testDatabase.ts
  - src/__tests__/dateUtils.test.ts
  - src/__tests__/migrationV2.test.ts
  - src/__tests__/backupV1toV2.test.ts
  - src/__tests__/drafts.test.ts
  - .planning/milestones/v1.1-phases/01-foundation/01-HUMAN-UAT.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "Existen fixtures testDatabase.createPreMigrationV2TestDatabase() y createPostMigrationV2TestDatabase()"
    - "Cada test file (dateUtils, migrationV2, backupV1toV2, drafts) existe con tests RED (skeleton .todo o expect failing) que listan los casos a cubrir"
    - "01-HUMAN-UAT.md describe escenarios FOUND-06 paridad + D-05 error screen + FOUND-05 draft survives kill"
  artifacts:
    - path: "src/__tests__/setup/testDatabase.ts"
      provides: "Pre/post migration v2 fixture builders"
      contains: "createPreMigrationV2TestDatabase"
    - path: "src/__tests__/dateUtils.test.ts"
      provides: "Skeleton test cases for getLocalDayKey family (cross-midnight, nextDay, isFutureDate)"
      contains: "describe('getLocalDayKey'"
    - path: "src/__tests__/migrationV2.test.ts"
      provides: "Skeleton tests forward/idempotency/rollback/data-integrity/snapshot/cleanup"
      contains: "describe('migrationV2'"
    - path: "src/__tests__/backupV1toV2.test.ts"
      provides: "Skeleton tests v1 forward, v2 roundtrip, future version reject"
      contains: "v1 forward"
    - path: "src/__tests__/drafts.test.ts"
      provides: "Skeleton tests CRUD, purge, debounce hook"
      contains: "useDraftAutosave"
    - path: ".planning/milestones/v1.1-phases/01-foundation/01-HUMAN-UAT.md"
      provides: "UAT scenarios for paridad + error screen + draft survival"
      contains: "FOUND-06"
  key_links:
    - from: "src/__tests__/migrationV2.test.ts"
      to: "src/__tests__/setup/testDatabase.ts"
      via: "import createPreMigrationV2TestDatabase"
      pattern: "createPreMigrationV2TestDatabase"
    - from: "src/__tests__/backupV1toV2.test.ts"
      to: "src/__tests__/setup/testDatabase.ts"
      via: "fixture import"
      pattern: "from.*setup/testDatabase"
---

<objective>
Wave 0 — Establecer la infraestructura de tests y los UAT scripts ANTES de implementar nada. Cada test file que las waves 1–5 van a poblar debe existir con su esqueleto de casos (describe/it.todo o tests que fallan rojo intencionalmente con `expect(false).toBe(true)`), de modo que ninguna wave subsiguiente pueda landearse sin un harness automático listo.

Purpose: Cumplir con la regla Nyquist (`<verify><automated>` no puede ser MISSING) y permitir que cada plan posterior corra `npm test -- --testPathPattern=<area>` desde el primer commit.
Output: 5 test files con esqueletos + 1 UAT script + extensiones al fixture testDatabase.
</objective>

<execution_context>
@/Users/facu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/facu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.1-phases/01-foundation/01-CONTEXT.md
@.planning/milestones/v1.1-phases/01-foundation/01-RESEARCH.md
@.planning/milestones/v1.1-phases/01-foundation/01-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@src/__tests__/setup/testDatabase.ts
@src/__tests__/migrationV1.test.ts
@src/__tests__/db.test.ts
</context>

<interfaces>
<!-- El fixture actual (src/__tests__/setup/testDatabase.ts) ya expone helpers reusables.
     Estas tareas EXTIENDEN ese módulo, no lo reemplazan. -->

Convención existente (resumen extraído del fixture):
- `createTestDatabase()` → DB en memoria con schema v0 (pre-migration) actual
- `seedHabits(db, habits[])`, `seedAssignments(db, rows[])` helpers ya disponibles
- Tests usan `better-sqlite3` via `__mocks__/expo-sqlite.ts`

Pre-v2 schema = schema actual de `db.ts` (habits, performed_habits, mood_entries, daily_assignments) + `user_version=1` + UNIQUE INDEX (post-migrationV1).

Post-v2 schema = pre-v2 MINUS mood_entries PLUS mood_log + text_library + weekly_reviews + drafts (per research/ARCHITECTURE.md §2).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extender testDatabase.ts con fixtures pre/post migration v2</name>
  <files>src/__tests__/setup/testDatabase.ts</files>
  <action>
Agregar dos funciones nuevas exportadas al final del archivo (per D-08 v1.0 pattern: SQL constants al tope, helpers abajo):

1. `export async function createPreMigrationV2TestDatabase()`:
   - Crea DB in-memory con schema actual (habits, performed_habits, mood_entries, daily_assignments).
   - Aplica `PRAGMA user_version = 1` + CREATE UNIQUE INDEX idx_unique_habit_date (resultado post-migrationV1).
   - Devuelve la DB lista para que migrationV2 corra contra ella.
   - Acepta opcionalmente `{ moodEntries?: MoodEntry[] }` para poblar la tabla legacy.

2. `export async function createPostMigrationV2TestDatabase()`:
   - Crea DB in-memory con schema v2 final: habits, performed_habits, daily_assignments, mood_log, text_library, weekly_reviews, drafts.
   - SQL exacto: cargar de research/ARCHITECTURE.md §2 (NO inventar columnas — copiar verbatim).
   - Aplica `PRAGMA user_version = 2`.
   - Devuelve DB con 4 tablas nuevas vacías + index `idx_mood_log_one_per_day` partial UNIQUE + `idx_drafts_kind_key`.

Mantener convención: SQL constants `SQL_CREATE_MOOD_LOG_V2`, `SQL_CREATE_TEXT_LIBRARY_V2`, `SQL_CREATE_WEEKLY_REVIEWS_V2`, `SQL_CREATE_DRAFTS_V2` al tope del file, reusables luego por migrationV2 (Wave 3 importa estas constantes). Implementación cumple D-CONV "SQL constants al tope" y D-CLAUDE "no duplicar código".

Aplicar D-CLAUDE refactor si la función `createPostMigrationV2TestDatabase` supera 20 líneas (separar en mini-helpers `createV2Tables(db)` + `createV2Indexes(db)`).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=setup 2>/dev/null; echo "fixture compiles:"; npx tsc --noEmit src/__tests__/setup/testDatabase.ts 2>&1 | head -20</automated>
  </verify>
  <done>
Fixture exporta ambas funciones; `npx tsc --noEmit` sobre el file compila sin errores; suite existente (`npm test -- --testPathPattern=migrationV1`) sigue verde — no se rompieron consumers actuales.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear esqueletos de test files (RED phase)</name>
  <files>src/__tests__/dateUtils.test.ts, src/__tests__/migrationV2.test.ts, src/__tests__/backupV1toV2.test.ts, src/__tests__/drafts.test.ts</files>
  <action>
Crear los 4 test files con esqueletos. Cada describe corresponde a una entrada en `01-VALIDATION.md` "Per-Requirement Verification Map". Cada test es `it.todo(...)` o `it(...) { expect(true).toBe(false); /* TODO Wave X */ }` (RED intencional). NUNCA stubear logic — el objetivo es el harness.

**dateUtils.test.ts** (consumido por Wave 1):
```ts
describe('getLocalDayKey', () => {
  it.todo('returns YYYY-MM-DD in local TZ at 23:59 → next day at 00:01 (cross-midnight)');
  it.todo('NEVER returns a UTC-rolled date (port of db.test.ts cross-midnight)');
});
describe('isFutureDate', () => { it.todo('"YYYY-MM-DD" > getLocalDayKey() → true'); });
describe('nextDay', () => { it.todo('returns date+1 in UTC arithmetic, DST-safe'); });
describe('formatDateStr (ex-dateToPrefix)', () => { it.todo('UTC getters rewrite produces same YYYY-MM-DD'); });
describe('getNowTimestamp', () => { it.todo('returns "YYYY-MM-DD HH:MM:SS" current'); });
describe('getTimestampForDate', () => { it.todo('returns "<dateKey> HH:MM:SS" with current time'); });
```

**migrationV2.test.ts** (consumido por Wave 3):
```ts
describe('migrationV2 — forward', () => {
  it.todo('pre: user_version=1 + mood_entries with N rows; post: user_version=2 + mood_log kind=reflection count=N');
  it.todo('creates 4 tables: mood_log, text_library, weekly_reviews, drafts');
  it.todo('creates partial UNIQUE INDEX idx_mood_log_one_per_day');
  it.todo('drops mood_entries table');
});
describe('migrationV2 — idempotency', () => {
  it.todo('runMigrations × 2 → second run is no-op (user_version stays 2)');
});
describe('migrationV2 — rollback', () => {
  it.todo('forced throw inside INSERT...SELECT → user_version stays 1 AND mood_entries intacta');
});
describe('migrationV2 — data integrity', () => {
  it.todo('count(mood_log kind=reflection) === pre-count(mood_entries) (assertion DENTRO de la transaction)');
});
describe('migrationV2 — pre-v2 snapshot (D-06)', () => {
  it.todo('writes JSON to documentDirectory/pre-v2-snapshot-<ts>.json BEFORE BEGIN TRANSACTION');
  it.todo('if writeAsStringAsync fails → migration NOT executed; user_version stays 1');
});
describe('migrationV2 — cleanup 30d (D-06)', () => {
  it.todo('snapshots with mtime > 30d removed on subsequent boot post-success');
  it.todo('snapshots within 30d preserved');
});
```

**backupV1toV2.test.ts** (consumido por Wave 3):
```ts
describe('backupService — v1 forward', () => {
  it.todo('v1 backup JSON with mood_entries[N] → restoreData → mood_log has N rows kind=reflection');
  it.todo('v1 mood_entries.timestamp → mood_log.date_key (slice 0,10) and occurred_at preserved');
  it.todo('mood_scale_version = "v1" stamped on all migrated rows');
});
describe('backupService — v2 round-trip', () => {
  it.todo('buildBackupData → JSON → parseAndValidate → restoreData round-trip preserves mood_log + text_library + weekly_reviews');
  it.todo('drafts EXCLUDED from buildBackupData output (transient — FOUND-04)');
});
describe('backupService — future version reject', () => {
  it.todo('parseAndValidate({version:3,...}) throws actionable error "Backup más nuevo que la app"');
});
```

**drafts.test.ts** (consumido por Wave 4):
```ts
describe('draftsRepository', () => {
  it.todo('upsert + find round-trip preserves payload_json');
  it.todo('UNIQUE(kind, key) enforced → second upsert overwrites, no duplicate row');
  it.todo('delete removes the row');
  it.todo('purgeOlderThan(cutoffIso) removes rows with updated_at < cutoff');
});
describe('useDraftAutosave hook', () => {
  it.todo('debounce 500ms — multiple changes within 500ms result in ONE upsert call');
  it.todo('uses jest.useFakeTimers()');
  it.todo('cleanup on unmount cancels pending timer');
});
```

NO implementar lógica — solo `describe` + `it.todo()`. La idea es que `npm test` corra y muestre N todos pendientes (jest los reporta como passing-with-todo).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern='dateUtils|migrationV2|backupV1toV2|drafts' 2>&1 | tail -20</automated>
  </verify>
  <done>
Los 4 test files existen; `npm test --testPathPattern='dateUtils|migrationV2|backupV1toV2|drafts'` ejecuta y reporta N `todo` (no fallos). `npm test` (full suite) sigue verde porque `it.todo` no rompe.
  </done>
</task>

<task type="auto">
  <name>Task 3: Crear 01-HUMAN-UAT.md con escenarios paridad + error screen + draft survival</name>
  <files>.planning/milestones/v1.1-phases/01-foundation/01-HUMAN-UAT.md</files>
  <action>
Crear `.planning/milestones/v1.1-phases/01-foundation/01-HUMAN-UAT.md` con tres escenarios numerados (uno por behavior manual-only listado en VALIDATION.md "Manual-Only Verifications"):

```markdown
# Phase 1 — Human UAT Scenarios

**Run:** at end of Wave 7 (before /gsd-verify-work).
**Build flavor:** `eas build --local` (Android APK) — per MEMORY/`build-apk-local` skill. NO usar Expo Go para D-05 (force-kill test requiere standalone build).

---

## Scenario 1 — FOUND-06: Habit reflection paridad (per D-03)

**Goal:** El flow de habit completion + reflection sigue idéntico desde la perspectiva del usuario.

**Pre-conditions:**
- App en versión post-Wave-5 (migration v2 aplicada).
- Al menos 1 hábito activo en Home.

**Steps:**
1. Tap un hábito desde Home → ReflectionModal abre.
2. Mover slider de mood a 7.5 — verificar que el número muestra "7.5" arriba del slider (mismo visual que pre-refactor).
3. Escribir comentario "Test paridad post-MoodPicker" en el TextInput.
4. Tap "Guardar reflexión".
5. Verificar: toast/feedback de éxito + puntos del hábito sumados al total visible en Home.

**Assertions:**
- [ ] Visual del modal idéntico a pre-refactor (label "¿Cómo te sientes?", value, slider, comentario, Guardar/Omitir).
- [ ] Points awarded === base_points del hábito (no cambia).
- [ ] Abrir DB (via dev tool o backup export JSON) → existe row en `mood_log` con kind='reflection', habit_id=<id>, mood_value=7.5, comment="Test paridad...", mood_scale_version='v1'.
- [ ] La row NO existe en `mood_entries` (tabla droppeada).

---

## Scenario 2 — D-05: Migration error screen + Restore + Retry

**Goal:** Si la migration v2 falla, la app muestra MigrationErrorScreen bloqueante con Restore + Retry funcionales.

**Pre-conditions:**
- Build standalone (APK) con dev flag `__DEV_FORCE_MIGRATION_FAIL` expuesto (Wave 5 lo agrega).
- App fresh install (sin migration v2 aplicada) — o resetear `user_version` a 1 via dev hook.

**Steps:**
1. Habilitar flag dev → cerrar app.
2. Abrir app → migration v2 dispara, `__DEV_FORCE_MIGRATION_FAIL` throws desde dentro del transaction.
3. Verificar: en lugar del Home, aparece MigrationErrorScreen con texto "No se pudo actualizar la base de datos.".
4. Verificar: botones "Restaurar desde backup" y "Reintentar migración" visibles.
5. Tap "Reintentar migración" → flag dev sigue activo → vuelve a fallar → mismo screen.
6. Deshabilitar flag (dev override) → tap "Reintentar migración" otra vez → migration completa OK → Home renderiza.

**Assertions:**
- [ ] La pantalla bloquea acceso al NavigationContainer (no se puede swipe-back ni acceder a otros screens).
- [ ] Tap "Restaurar desde backup" abre el flow de restore (Drive o pre-v2 snapshot local, según Wave 5 UX).
- [ ] El snapshot pre-v2 está presente en `${FileSystem.documentDirectory}/pre-v2-snapshot-*.json` (verificable con dev file viewer).
- [ ] Retry funciona luego de remover la condición de fallo.

---

## Scenario 3 — FOUND-05: Draft survives app kill

**Goal:** Un draft autosaved sobrevive un force-kill del proceso de la app.

**Pre-conditions:**
- Build standalone (APK) — Wave 4 ya implementó `useDraftAutosave`, pero Phase 1 NO tiene surface user-facing que lo consuma. Este scenario usa un **dev harness modal** opcional (Wave 4 puede agregar uno temporal, planner decide) O se difiere a Phase 2.

**Steps:**
1. Abrir dev harness modal para draft kind='note' (Wave 4 agrega ruta dev).
2. Escribir "Texto borrador no submiteado".
3. Esperar 600ms (>500ms debounce) — el draft se escribe a SQLite.
4. Force-kill app desde recent apps tray.
5. Reabrir app → reabrir el dev harness modal con mismo kind/key → debería mostrar el texto restaurado.

**Assertions:**
- [ ] El texto "Texto borrador no submiteado" aparece prepoblado en el TextInput.
- [ ] SQLite query `SELECT * FROM drafts WHERE kind='note'` muestra 1 row con payload_json conteniendo el texto.

**Defer condition:** Si Wave 4 no agrega dev harness, este scenario se difiere a Phase 2 (donde existen surfaces reales que consumen el hook). Documentar el defer en SUMMARY de plan 05.

---

## Sign-off

- [ ] Scenario 1 ✓
- [ ] Scenario 2 ✓
- [ ] Scenario 3 ✓ or deferred to Phase 2

**Tester:** ________  **Date:** ________  **APK build SHA:** ________
```

Aplica D-CLAUDE "actualizar archivos .md desactualizados" — este archivo es **nuevo**, no actualiza otros.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.1-phases/01-foundation/01-HUMAN-UAT.md && grep -c "Scenario" .planning/milestones/v1.1-phases/01-foundation/01-HUMAN-UAT.md</automated>
  </verify>
  <done>
Archivo existe; contiene 3 scenarios numerados con steps, assertions y sign-off; menciona explícitamente FOUND-06, D-05, FOUND-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → production code | El fixture genera SQL idéntico al de migrationV2.ts (Wave 3). Si divergen, los tests darían falsos positivos. Mitigación: en Wave 3, importar las SQL constants DE testDatabase.ts (single source of truth) o viceversa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | testDatabase fixture SQL diverges from real migrationV2 SQL → tests pass but production breaks | mitigate | En Wave 3 importar las SQL constants desde `testDatabase.ts` (al tope, exported), o invocar `migrationV2.runOn(db)` desde el fixture post-Wave-3 |
| T-01-02 | Info Disclosure | UAT script menciona dev flag `__DEV_FORCE_MIGRATION_FAIL` | accept | Solo en builds dev; gateado por `if (__DEV__)` en Wave 5 |
</threat_model>

<verification>
- `npm test` (full suite) verde
- `npm test -- --testPathPattern='dateUtils|migrationV2|backupV1toV2|drafts'` reporta N todos pendientes
- Los 4 test files + UAT existen
- `npx tsc --noEmit` sobre el fixture extendido pasa sin errores
</verification>

<success_criteria>
Wave 1–5 pueden ahora referenciar tests preexistentes en sus `<verify><automated>` blocks. Sin esta wave, todos los plans posteriores tendrían `<automated>MISSING — needs Wave 0</automated>`.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.1-phases/01-foundation/01-01-SUMMARY.md` listing: which fixtures were added, todos count per file, link to UAT.
</output>
