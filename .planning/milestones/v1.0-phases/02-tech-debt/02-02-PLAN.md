---
phase: 02-tech-debt
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/screens/DailySheetScreen.tsx
  - src/screens/HabitLibraryScreen.tsx
  - src/components/modals/HabitFormModal.tsx
  - src/services/statsService.ts
  - src/services/db.ts
  - src/__tests__/sanitize.test.ts
autonomous: true
requirements:
  - DEBT-02
  - DEBT-03

must_haves:
  truths:
    - "Cuatro call sites (DailySheetScreen, HabitLibraryScreen, HabitFormModal, statsService) importan y usan parseAndValidateCategories"
    - "No queda ningún `import { parseJsonArray }` en el codebase ni ningún `JSON.parse` directo de categorías en los 4 archivos UI/service"
    - "src/services/db.ts contiene dos funciones explícitas: sanitizeHabitDefaultCategories y sanitizePerformedCategoriesUsed"
    - "sanitizeTable y filterValidIds están eliminadas de db.ts"
    - "Las dos funciones de sanitización usan SQL estático (sin template strings con dollar-brace para nombres de tabla/columna) y tipos explícitos por tabla (sin [key: string]: any)"
    - "Las dos funciones de sanitización están exportadas para tests directos"
    - "sanitizeCategories sigue siendo el caller en initDatabase y ahora invoca las dos funciones nuevas"
    - "Tests de sanitize.test.ts ahora ejercen las dos funciones contra una DB in-memory con datos sucios"
  artifacts:
    - path: "src/services/db.ts"
      provides: "sanitizeHabitDefaultCategories y sanitizePerformedCategoriesUsed con SQL estático y tipos explícitos"
      contains: "sanitizeHabitDefaultCategories"
    - path: "src/__tests__/sanitize.test.ts"
      provides: "Tests reales que cubren ambas funciones de sanitización con datos in-memory"
      contains: "sanitizeHabitDefaultCategories"
    - path: "src/services/statsService.ts"
      provides: "Migrado a parseAndValidateCategories sin filtro VALID_AREA_IDS redundante"
      contains: "parseAndValidateCategories"
  key_links:
    - from: "src/services/db.ts"
      to: "src/utils/parsing.ts"
      via: "import parseAndValidateCategories"
      pattern: "import.*parseAndValidateCategories.*from.*utils/parsing"
    - from: "src/screens/DailySheetScreen.tsx"
      to: "src/utils/parsing.ts"
      via: "import parseAndValidateCategories"
      pattern: "import.*parseAndValidateCategories.*from.*utils/parsing"
    - from: "src/screens/HabitLibraryScreen.tsx"
      to: "src/utils/parsing.ts"
      via: "import parseAndValidateCategories"
      pattern: "import.*parseAndValidateCategories.*from.*utils/parsing"
    - from: "src/components/modals/HabitFormModal.tsx"
      to: "src/utils/parsing.ts"
      via: "import parseAndValidateCategories"
      pattern: "import.*parseAndValidateCategories.*from.*utils/parsing"
    - from: "src/services/statsService.ts"
      to: "src/utils/parsing.ts"
      via: "import parseAndValidateCategories"
      pattern: "import.*parseAndValidateCategories.*from.*utils/parsing"
---

<objective>
Migrar los 4 call sites de `parseJsonArray` al nuevo parser central `parseAndValidateCategories` y reemplazar el `sanitizeTable` genérico de `db.ts` por dos funciones explícitas (`sanitizeHabitDefaultCategories`, `sanitizePerformedCategoriesUsed`) con SQL estático y tipos por tabla.

Purpose: Cerrar DEBT-02 (eliminando todo JSON.parse disperso de categorías) y DEBT-03 (eliminando el SQL dinámico y el shape `[key: string]: any`). Tras este plan, todo el flujo de categorías —lectura desde DB, validación en write, sanitización defensiva en boot— pasa por una sola función.
Output: 4 archivos UI/service migrados; `sanitizeTable` y `filterValidIds` eliminadas; dos funciones explícitas exportadas y testeadas; `sanitize.test.ts` con tests reales contra DB in-memory.
</objective>

<execution_context>
@/Users/facu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/facu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tech-debt/02-CONTEXT.md
@.planning/phases/02-tech-debt/02-RESEARCH.md
@.planning/phases/02-tech-debt/02-PATTERNS.md
@.planning/phases/02-tech-debt/02-VALIDATION.md
@.planning/phases/02-tech-debt/02-01-SUMMARY.md

<interfaces>
From src/utils/parsing.ts (creado en plan 02-01):
- `export function parseAndValidateCategories(json: string): string[]` — parsea, filtra contra VALID_AREA_IDS, console.warn de descartados.

Current src/services/db.ts §Sanitización (líneas 141-181, A REEMPLAZAR):
- `async function sanitizeCategories(db)` — caller que invoca dos veces a `sanitizeTable`
- `async function sanitizeTable(db, table, column)` — usa `getAllAsync<{ id: string; [key: string]: any }>` y SQL con template strings dinámicos
- `function filterValidIds(json: string): string` — filtra contra VALID_AREA_IDS y serializa con JSON.stringify

Schema (verified — espejo exacto del SQL de producción):
- habits table: `{ id: string; default_categories: string | null }`
- performed_habits table: `{ id: string; categories_used: string | null }`

Test setup from src/__tests__/setup/testDatabase.ts:
- `createTestDatabase(): Database.Database`
- `resetTestDatabase(): void`
- `insertTestHabit(db, { id, name, default_categories?, ... }): void`

The test file `src/__tests__/sanitize.test.ts` exists from plan 02-01 with `it.todo` placeholders that this plan replaces.

Call site lines (verified in research):
- DailySheetScreen.tsx — import line 34, usage line 102: `const ids = parseJsonArray(categories);`
- HabitLibraryScreen.tsx — import line 19, usage line 202: `const cats = parseJsonArray(habit.default_categories);`
- HabitFormModal.tsx — import line 19 (`from '../../utils/parsing'`), usage line 178: `setCats(parseJsonArray(habit.default_categories));`
- statsService.ts — imports lines 11-12, usage lines 124-125:
  - `const rawCats = parseJsonArray(row.categories_used);`
  - `const cats = [...new Set(rawCats)].filter((id) => VALID_AREA_IDS.has(id as never));`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrar los 4 call sites de parseJsonArray a parseAndValidateCategories</name>
  <files>
    - src/screens/DailySheetScreen.tsx (MODIFY — import + 1 usage)
    - src/screens/HabitLibraryScreen.tsx (MODIFY — import + 1 usage)
    - src/components/modals/HabitFormModal.tsx (MODIFY — import + 1 usage)
    - src/services/statsService.ts (MODIFY — import + collapse 2 lines into 1)
  </files>
  <read_first>
    - src/screens/DailySheetScreen.tsx (líneas 30-40 e líneas 95-110)
    - src/screens/HabitLibraryScreen.tsx (líneas 15-25 e líneas 195-210)
    - src/components/modals/HabitFormModal.tsx (líneas 15-25 e líneas 170-185)
    - src/services/statsService.ts (líneas 1-30 e líneas 110-135)
    - src/utils/parsing.ts (creado en 02-01 — confirma firma del nuevo parser)
    - .planning/phases/02-tech-debt/02-PATTERNS.md (sección "Call-site migrations" líneas 233-284)
  </read_first>
  <behavior>
    - Cada uno de los 4 archivos importa parseAndValidateCategories desde '../utils/parsing' (o '../../utils/parsing' para HabitFormModal)
    - Ningún archivo importa parseJsonArray después de la migración
    - Cada archivo invoca parseAndValidateCategories(arg) en el lugar exacto donde antes llamaba a parseJsonArray(arg)
    - statsService.ts: las líneas 124-125 originales colapsan a una sola línea sin el filtro redundante VALID_AREA_IDS.has(id as never) (el parser ya filtra)
    - statsService.ts: si tras eliminar el filtro VALID_AREA_IDS no se usa en otra línea, eliminar también su import
    - El comportamiento UI es idéntico desde la perspectiva del usuario; cambia solo el origen del filtrado
    - npx jest (suite completa) sigue verde — sin regresiones
  </behavior>
  <action>
**Archivo 1 — `src/screens/DailySheetScreen.tsx`**:
- Línea 34: cambiar `import { parseJsonArray } from '../utils/parsing';` por `import { parseAndValidateCategories } from '../utils/parsing';`
- Línea 102: cambiar `const ids = parseJsonArray(categories);` por `const ids = parseAndValidateCategories(categories);`

**Archivo 2 — `src/screens/HabitLibraryScreen.tsx`**:
- Línea 19: cambiar `import { parseJsonArray } from '../utils/parsing';` por `import { parseAndValidateCategories } from '../utils/parsing';`
- Línea 202: cambiar `const cats = parseJsonArray(habit.default_categories);` por `const cats = parseAndValidateCategories(habit.default_categories);`

**Archivo 3 — `src/components/modals/HabitFormModal.tsx`**:
- Línea 19: cambiar `import { parseJsonArray } from '../../utils/parsing';` por `import { parseAndValidateCategories } from '../../utils/parsing';`
- Línea 178: cambiar `setCats(parseJsonArray(habit.default_categories));` por `setCats(parseAndValidateCategories(habit.default_categories));`

**Archivo 4 — `src/services/statsService.ts`**:
- Línea 12: cambiar `import { parseJsonArray } from '../utils/parsing';` por `import { parseAndValidateCategories } from '../utils/parsing';`
- Reemplazar las líneas 124-125 (las dos juntas):
  - Antes:
    - `const rawCats = parseJsonArray(row.categories_used);`
    - `const cats = [...new Set(rawCats)].filter((id) => VALID_AREA_IDS.has(id as never));`
  - Después (una sola línea, parser ya filtra):
    - `const cats = [...new Set(parseAndValidateCategories(row.categories_used))];`
- Verificar si VALID_AREA_IDS se usa en alguna otra línea: `grep -n "VALID_AREA_IDS" src/services/statsService.ts`. Si la única referencia restante es el import línea 11 → eliminar ese import también. Si hay otras referencias → mantener el import.

**Verificación post-migración (a ejecutar antes de cerrar la task):** `grep -rn "parseJsonArray" src/` debe retornar 0 resultados. Si aparece alguno, esa migración quedó incompleta.
  </action>
  <verify>
    <automated>bash -c '! grep -rn "parseJsonArray" src/ && npx jest'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rn "parseJsonArray" src/` succeeds (zero references remain anywhere in src/)
    - `grep -c "parseAndValidateCategories" src/screens/DailySheetScreen.tsx` returns 2 (import + usage)
    - `grep -c "parseAndValidateCategories" src/screens/HabitLibraryScreen.tsx` returns 2
    - `grep -c "parseAndValidateCategories" src/components/modals/HabitFormModal.tsx` returns 2
    - `grep -c "parseAndValidateCategories" src/services/statsService.ts` returns 2
    - `! grep -q "VALID_AREA_IDS.has(id as never)" src/services/statsService.ts` succeeds (redundant filter removed)
    - `npx jest` returns exit code 0 (full suite green — Phase 1 + parsing + habitService tests)
  </acceptance_criteria>
  <done>4 archivos migrados; parseJsonArray ausente de todo src/; filtro redundante de statsService eliminado; suite completa de Jest verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reemplazar sanitizeTable por sanitizeHabitDefaultCategories y sanitizePerformedCategoriesUsed</name>
  <files>
    - src/services/db.ts (MODIFY — eliminar sanitizeTable + filterValidIds, agregar 2 funciones explícitas exportadas, actualizar caller)
    - src/__tests__/sanitize.test.ts (REWRITE — reemplazar `it.todo` por tests reales contra DB in-memory)
  </files>
  <read_first>
    - src/services/db.ts (líneas 1-30 imports, líneas 140-182 sección sanitización)
    - src/utils/parsing.ts (creado en 02-01 — el consumidor del parser central)
    - src/__tests__/dailyAssignments.test.ts (líneas 16-63 — patrón jest.mock + better-sqlite3)
    - src/__tests__/setup/testDatabase.ts (createTestDatabase, insertTestHabit)
    - src/__tests__/sanitize.test.ts (creado en 02-01 con `it.todo` — base a reescribir)
    - .planning/phases/02-tech-debt/02-RESEARCH.md (sección DEBT-03, líneas 396-454)
  </read_first>
  <behavior>
    - db.ts exporta sanitizeHabitDefaultCategories(db) y sanitizePerformedCategoriesUsed(db)
    - sanitizeTable y filterValidIds ya no existen en db.ts
    - sanitizeCategories(db) (caller en initDatabase) invoca las dos funciones nuevas en serie
    - SQL en ambas funciones es estático (string literal) — no hay template strings con dollar-brace en las queries
    - Cada getAllAsync usa shape explícito por tabla — sin [key: string]: any en la sección de sanitización
    - Habit con `default_categories='["salud_fisica","fake_id"]'` → tras sanitizeHabitDefaultCategories(db) la fila queda con `default_categories='["salud_fisica"]'`
    - performed_habit con `categories_used='["mental","invalid"]'` → tras sanitizePerformedCategoriesUsed(db) la fila queda con `categories_used='["mental"]'`
    - Filas con la columna NULL son ignoradas por la cláusula `WHERE X IS NOT NULL` y por `if (row.X == null) continue`
    - npx jest --testPathPattern="sanitize" pasa con 6 tests verdes
    - npx jest (suite completa) sigue verde
  </behavior>
  <action>
**Modificación 1 — `src/services/db.ts`**:

Agregar al bloque de imports (cerca del top, junto a otros imports relativos):

`import { parseAndValidateCategories } from '../utils/parsing';`

Reemplazar la sección completa de sanitización (líneas 141-181 actuales) con este bloque exacto:

```ts
// Sanitización de categorías
//
// Recorre habits.default_categories y performed_habits.categories_used.
// Elimina cualquier ID que NO exista en VALID_AREA_IDS.
async function sanitizeCategories(db: SQLite.SQLiteDatabase): Promise<void> {
  await sanitizeHabitDefaultCategories(db);
  await sanitizePerformedCategoriesUsed(db);
}

// Limpia habits.default_categories eliminando IDs de área inválidos.
// Defensivo: corre en initDatabase() y no rompe ante datos legacy.
// Exportada para tests directos (Wave 0).
export async function sanitizeHabitDefaultCategories(
  db: SQLite.SQLiteDatabase,
): Promise<void> {
  const rows = await db.getAllAsync<{ id: string; default_categories: string | null }>(
    'SELECT id, default_categories FROM habits WHERE default_categories IS NOT NULL',
  );
  for (const row of rows) {
    if (row.default_categories == null) continue;
    const cleaned = JSON.stringify(parseAndValidateCategories(row.default_categories));
    if (cleaned !== row.default_categories) {
      await db.runAsync(
        'UPDATE habits SET default_categories = ? WHERE id = ?',
        [cleaned, row.id],
      );
    }
  }
}

// Limpia performed_habits.categories_used eliminando IDs de área inválidos.
// Defensivo: corre en initDatabase() y no rompe ante datos legacy.
// Exportada para tests directos (Wave 0).
export async function sanitizePerformedCategoriesUsed(
  db: SQLite.SQLiteDatabase,
): Promise<void> {
  const rows = await db.getAllAsync<{ id: string; categories_used: string | null }>(
    'SELECT id, categories_used FROM performed_habits WHERE categories_used IS NOT NULL',
  );
  for (const row of rows) {
    if (row.categories_used == null) continue;
    const cleaned = JSON.stringify(parseAndValidateCategories(row.categories_used));
    if (cleaned !== row.categories_used) {
      await db.runAsync(
        'UPDATE performed_habits SET categories_used = ? WHERE id = ?',
        [cleaned, row.id],
      );
    }
  }
}
```

(El executor debe usar JSDoc en español de bloque `/** ... */` para cada función — el formato de comentarios mostrado arriba es solo para evitar que el closing tag XML haga parsing raro de este action block. Patrón JSDoc de referencia: `src/services/db.ts` líneas 143-146 actuales.)

**Notas críticas:**
- AMBAS funciones se exportan (`export async function ...`) — esto resuelve el open question de PATTERNS.md/RESEARCH.md a favor de testeabilidad directa. Razón: testear vía initDatabase requiere setup adicional y oculta los assertions; el patrón de Phase 1 es testear funciones puras directamente.
- Eliminar las funciones sanitizeTable y filterValidIds por completo — no quedan stubs ni aliases.
- Si VALID_AREA_IDS solo se usaba en filterValidIds dentro de db.ts, su import puede quedar sin usar; verificar con `grep -n "VALID_AREA_IDS" src/services/db.ts`. Si tras eliminar filterValidIds no hay más referencias, eliminar VALID_AREA_IDS del import del top del archivo (línea 10: cambiar `import { DB_NAME, SEED_HABITS, VALID_AREA_IDS } from '../config/constants';` → `import { DB_NAME, SEED_HABITS } from '../config/constants';`).
- Cada función tiene ~14 líneas — dentro del límite del proyecto de 20 líneas (D-08 — sin helper compartido).
- Las dos funciones tienen estructura paralela; mantener orden de campos y comentarios idénticos para fácil revisión.

**Modificación 2 — `src/__tests__/sanitize.test.ts`** (reemplazar el contenido completo del archivo creado en 02-01). Estructura del archivo final:

1. Header JSDoc en español: "Tests para las dos funciones de sanitización defensiva (DEBT-03). Usa DB SQLite in-memory (better-sqlite3)."
2. Imports: `Database` type-import desde `'better-sqlite3'`; `createTestDatabase`, `resetTestDatabase`, `insertTestHabit` desde `'./setup/testDatabase'`.
3. `jest.mock('../services/db', () => { const actual = jest.requireActual('../services/db'); return { ...actual }; });` — necesario para que el mock de `__mocks__/expo-sqlite.ts` se active al hacer `setMockDatabase` en `createTestDatabase`.
4. Imports de `sanitizeHabitDefaultCategories` y `sanitizePerformedCategoriesUsed` desde `'../services/db'` (DESPUÉS del jest.mock).
5. `let db: Database.Database;` + `beforeEach(() => { db = createTestDatabase(); });` + `afterEach(() => { resetTestDatabase(); jest.clearAllMocks(); });`
6. Helpers locales (no exportar — privados al test):
   - `function readHabitCats(habitId: string): string` — `db.prepare('SELECT default_categories FROM habits WHERE id = ?').get(habitId)`
   - `function insertPerformed(opts: { id: string; habit_id: string; categories_used: string | null }): void` — INSERT directo a performed_habits con timestamp fijo `'2026-04-26 10:00:00'`, points_earned `1`, habit_description `null`
   - `function readPerformedCats(performedId: string): string | null`
7. `describe('sanitizeHabitDefaultCategories', () => { ... })` con 3 tests:
   - **test "elimina IDs inválidos en habits.default_categories"**: insertTestHabit con `default_categories: '["salud_fisica","fake_id"]'`; spy de console.warn; await llamada (con cast: `db as unknown as Parameters<typeof sanitizeHabitDefaultCategories>[0]`); expect `readHabitCats('h1') === '["salud_fisica"]'`; warnSpy.mockRestore().
   - **test "no modifica filas con todas las categorías válidas"**: insertTestHabit con `default_categories: '["salud_fisica","mental"]'`; await llamada; expect `readHabitCats('h2') === '["salud_fisica","mental"]'`.
   - **test "ignora filas con default_categories NULL"**: INSERT directo con NULL; await llamada; expect el valor leído sea null.
8. `describe('sanitizePerformedCategoriesUsed', () => { ... })` con 3 tests análogos:
   - "elimina IDs inválidos en performed_habits.categories_used" — categories_used `'["mental","invalid"]'` → `'["mental"]'`
   - "no modifica filas con todas las categorías válidas"
   - "ignora filas con categories_used NULL"

**Cast Database → SQLiteDatabase:** `db as unknown as Parameters<typeof sanitizeHabitDefaultCategories>[0]`. Necesario porque las funciones declaran el tipo de expo-sqlite pero en tests se usa better-sqlite3 envuelto por el mock. Es consistente con el approach de `dailyAssignments.test.ts` (que usa el mock vía `getDatabase()` interno; acá las funciones reciben db como param y necesitan el cast).
  </action>
  <verify>
    <automated>bash -c 'npx jest --testPathPattern="sanitize" && npx jest && ! grep -nE "(sanitizeTable\b|filterValidIds\b)" src/services/db.ts && ! grep -nE "\[key: string\]: any" src/services/db.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --testPathPattern="sanitize"` returns exit code 0 with 6 passing tests (3 per function)
    - `npx jest` returns exit code 0 (full suite — Phase 1 + parsing + habitService + sanitize)
    - `grep -c "export async function sanitizeHabitDefaultCategories" src/services/db.ts` returns 1
    - `grep -c "export async function sanitizePerformedCategoriesUsed" src/services/db.ts` returns 1
    - `! grep -nE "sanitizeTable\b" src/services/db.ts` succeeds (function removed)
    - `! grep -nE "filterValidIds\b" src/services/db.ts` succeeds (function removed)
    - `! grep -nE "\[key: string\]: any" src/services/db.ts` succeeds (no `[key: string]: any` in file)
    - `grep -q "import.*parseAndValidateCategories.*from.*utils/parsing" src/services/db.ts` succeeds
    - In the sanitize section of db.ts, all SELECT/UPDATE strings are single-quoted literals — no `${table}` or `${column}` interpolation in SQL strings: `! grep -nE "FROM \\\$\\{|SET \\\$\\{|UPDATE \\\$\\{" src/services/db.ts`
    - `grep -q "sanitizeHabitDefaultCategories" src/__tests__/sanitize.test.ts` succeeds (test file rewritten with real assertions)
    - `! grep -q "it.todo" src/__tests__/sanitize.test.ts` succeeds (no more placeholders)
  </acceptance_criteria>
  <done>db.ts contiene únicamente las dos funciones de sanitización exportadas con SQL estático y tipos por tabla; sanitizeTable y filterValidIds eliminadas; sanitize.test.ts ejerce ambas funciones contra DB in-memory; suite completa verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → DB | Las dos funciones de sanitización ejecutan SQL directo. Antes: SQL dinámico con nombres de tabla/columna interpolados. Después: SQL estático verificable en compile time. |
| DB → parser | Filas leídas de la DB pasan por `parseAndValidateCategories` antes del UPDATE — la validación es la misma que aplican los call sites de UI/service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | sanitización defensiva en boot (db.ts) | mitigate | Task 2: ambas funciones consumen `parseAndValidateCategories` que filtra contra `VALID_AREA_IDS` y emite console.warn. Tests verifican que `'["salud_fisica","fake_id"]'` se reduce a `'["salud_fisica"]'`. |
| T-02-02 | Tampering | sanitizeTable (SQL injection surface) | mitigate | Task 2: `sanitizeTable` con SQL dinámico (`${table}`, `${column}`) se elimina; reemplazada por dos funciones con string literals exclusivamente. Acceptance criterion verifica ausencia de template strings en SQL via grep. |

T-02-03 (write-time validation) ya fue mitigado en plan 02-01 (D-15 en habitService).
</threat_model>

<verification>
- `! grep -rn "parseJsonArray" src/` → función eliminada del codebase completo (DEBT-02)
- `grep -c "parseAndValidateCategories" src/services/db.ts` → 2 (import + uso en cada función × 1 import = realmente 1 import + 2 usos = 3; valor mínimo 2)
- `! grep -nE "sanitizeTable\b|filterValidIds\b" src/services/db.ts` → ambas funciones eliminadas
- `! grep -nE "\[key: string\]: any" src/services/db.ts` → tipo dinámico eliminado (DEBT-03)
- `! grep -nE "FROM \\\${|SET \\\${|UPDATE \\\${" src/services/db.ts` → SQL estático en sanitización
- `npx jest --testPathPattern="sanitize"` → 6 tests passing
- `npx jest` → suite completa verde (29 Phase 1 + 5 parsing + 4 habitService + 6 sanitize = 44+ tests)
</verification>

<success_criteria>
- DEBT-02 cerrado: 4 call sites migrados, `parseJsonArray` eliminada del codebase, sanitización en db.ts también consume el parser central. Único punto de parsing+validación de categorías es `parseAndValidateCategories`.
- DEBT-03 cerrado: dos funciones explícitas con tipos por tabla y SQL estático; `sanitizeTable` y `filterValidIds` eliminadas; tests cubren ambas funciones contra DB in-memory.
- Phase 1 (29 tests) sigue verde — sin regresiones.
- T-02-01 mitigado en boot (sanitización) además de read (call sites) y write (habitService).
- T-02-02 mitigado: ya no hay SQL dinámico en db.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tech-debt/02-02-SUMMARY.md` documentando:
- Estado final de los 4 call sites migrados (líneas exactas modificadas)
- Decisión sobre import VALID_AREA_IDS en statsService.ts y db.ts (eliminado o mantenido según uso restante)
- Estado final de db.ts §Sanitización (cuántas líneas, qué exporta)
- 6 tests nuevos en sanitize.test.ts y cómo se relacionan con las dos funciones
- Hand-off para 02-03: 02-02 NO toca useSpeechRecognition.ts ni backupService.ts — esos están reservados para 02-03 (que puede correr en paralelo a 02-02 dado que no comparten archivos).
</output>
