---
phase: 03-google-drive-backup
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/services/backupService.ts
  - src/utils/driveRetention.ts
  - src/utils/dateFormat.ts
  - src/services/driveBackupService.ts
  - src/components/shared/LoadingOverlay.tsx
  - src/screens/SettingsScreen.tsx
  - src/screens/SettingsScreen.styles.ts
  - src/screens/RestoreFromDriveScreen.tsx
  - src/screens/RestoreFromDriveScreen.styles.ts
  - App.tsx
  - src/__tests__/driveRetention.test.ts
  - src/__tests__/driveBackupService.test.ts
  - src/__tests__/dateFormat.test.ts
autonomous: true
requirements: [DRIVE-02, DRIVE-03, DRIVE-06, DRIVE-07, DRIVE-08]

must_haves:
  truths:
    - "El usuario puede presionar 'Backup ahora' en Settings y sus datos se suben a Drive con nombre cozyhabits-YYYY-MM-DD.json (DRIVE-02 + DRIVE-03)"
    - "Settings muestra el email conectado + 'Último backup: <fecha relativa>' (DRIVE-06)"
    - "Si ya existe un backup del día, el flow muestra confirmación de 'Reemplazar' antes de hacer PATCH (D-12 + D-13)"
    - "Tras un backup que sobrescribió el archivo del día, el Alert de éxito refleja ese caso usando ALERT_DRIVE_BACKUP_REPLACED (D-13)"
    - "Después de un backup exitoso, el pruning de retención corre best-effort sin fallar el backup (D-14 + Pitfall #8)"
    - "Errores HTTP de Drive se mapean a una de las 5 categorías + genérico y se muestran con Alert.alert (DRIVE-08 + D-26)"
    - "Cancelación de signIn (statusCodes.SIGN_IN_CANCELLED) NO crashea: signIn() retorna null y el caller no muestra error"
    - "Sign-out limpia sesión local sin tocar lastBackup* (DRIVE-07 + D-10 + D-11)"
    - "Durante el upload se muestra LoadingOverlay con mensaje 'Subiendo a Drive...' (D-23 + UI-SPEC §LoadingOverlay)"
    - "buildBackupData, parseAndValidate y restoreData son named exports de backupService.ts (D-03)"
    - "selectFilesToPrune es función pura testeable que aplica política Time Machine (D-14)"
    - "formatDateEs y formatRelativeBackup viven en src/utils/dateFormat.ts (no duplicados — CLAUDE.md Regla 3)"
    - "Existe un Stack.Screen 'RestoreFromDrive' con un scaffold mínimo (loading → empty 'Sin backups todavía') que el botón 'Restaurar desde Drive' abre desde día uno — sin Alert.alert('Próximamente'), sin placeholders"
  artifacts:
    - path: "src/services/backupService.ts"
      provides: "Promotion of buildBackupData, parseAndValidate, restoreData to named exports (D-03)"
      contains: "export async function buildBackupData"
    - path: "src/utils/driveRetention.ts"
      provides: "Pure function selectFilesToPrune implementing Time Machine policy (D-14)"
      exports: ["selectFilesToPrune"]
    - path: "src/utils/dateFormat.ts"
      provides: "Shared formatDateEs / formatRelativeBackup / formatSize utilities (no duplication entre Settings y Restore screens)"
      exports: ["formatDateEs", "formatRelativeBackup", "formatSize"]
    - path: "src/services/driveBackupService.ts"
      provides: "Drive REST transport: signIn, signOut, uploadBackup, listBackups + error mapping"
      exports: ["signIn", "signOut", "uploadBackup", "listBackups", "downloadBackup", "mapDriveError", "DriveError"]
    - path: "src/components/shared/LoadingOverlay.tsx"
      provides: "Modal full-screen blocking spinner for long ops (D-23)"
      exports: ["LoadingOverlay"]
    - path: "src/screens/SettingsScreen.tsx"
      provides: "Drive section with Conectar/Backup/timestamp UI + navigation a RestoreFromDriveScreen"
      contains: "Backup en la nube"
    - path: "src/screens/RestoreFromDriveScreen.tsx"
      provides: "Scaffold mínimo (loading + empty 'Sin backups todavía') — plan 03-03 expande con FlatList + preview"
      contains: "Restaurar desde Drive"
    - path: "src/screens/RestoreFromDriveScreen.styles.ts"
      provides: "Style tokens reusados (text/layout/button/spacing/colors) — completo desde el scaffold"
      exports: ["styles", "colors"]
    - path: "App.tsx"
      provides: "Stack.Screen 'RestoreFromDrive' registrado desde el scaffold (no se difiere a 03-03)"
      contains: "name=\"RestoreFromDrive\""
    - path: "src/__tests__/driveRetention.test.ts"
      provides: "Unit tests for retention policy"
      contains: "selectFilesToPrune"
    - path: "src/__tests__/driveBackupService.test.ts"
      provides: "Mocked SDK + fetch tests for upload + sign-in cancellation + error mapping (13 tests)"
      contains: "uploadBackup"
    - path: "src/__tests__/dateFormat.test.ts"
      provides: "Unit tests para formatDateEs/formatRelativeBackup/formatSize"
      contains: "formatDateEs"
  key_links:
    - from: "src/services/driveBackupService.ts"
      to: "src/services/backupService.ts"
      via: "named import buildBackupData, parseAndValidate, restoreData"
      pattern: "from '\\./backupService'"
    - from: "src/services/driveBackupService.ts"
      to: "Drive REST API v3"
      via: "fetch with Authorization: Bearer token + multipart/related body"
      pattern: "googleapis\\.com/(upload/)?drive/v3"
    - from: "src/services/driveBackupService.ts"
      to: "src/utils/driveRetention.ts"
      via: "selectFilesToPrune(files, now) called after successful upload"
      pattern: "selectFilesToPrune"
    - from: "src/screens/SettingsScreen.tsx"
      to: "src/services/driveBackupService.ts"
      via: "import * as drive + drive.uploadBackup() handler"
      pattern: "drive\\.uploadBackup"
    - from: "src/screens/SettingsScreen.tsx"
      to: "src/components/shared/LoadingOverlay.tsx"
      via: "import { LoadingOverlay } + <LoadingOverlay visible={isUploading} message=...> JSX"
      pattern: "<LoadingOverlay"
    - from: "src/screens/SettingsScreen.tsx"
      to: "src/screens/RestoreFromDriveScreen.tsx"
      via: "navigation.navigate('RestoreFromDrive') desde el botón 'Restaurar desde Drive' (sin Alert.alert intermedio)"
      pattern: "navigate\\(['\"]RestoreFromDrive['\"]"
    - from: "App.tsx"
      to: "src/screens/RestoreFromDriveScreen.tsx"
      via: "Stack.Screen registration con headerShown:false"
      pattern: "name=['\"]RestoreFromDrive['\"]"
    - from: "src/screens/SettingsScreen.tsx"
      to: "src/utils/dateFormat.ts"
      via: "import { formatDateEs, formatRelativeBackup } — sin duplicación con Restore screen"
      pattern: "from '\\.\\./utils/dateFormat'"
---

<objective>
Construir la capa de transporte Drive (servicio + retention pura), las utilidades de fecha compartidas (sin duplicación), la UI de "Backup en la nube" en Settings con su overlay bloqueante, **y registrar la ruta + scaffold mínimo de `RestoreFromDriveScreen`** para que el botón "Restaurar desde Drive" navegue desde día uno (plan 03-03 luego completa la lista + preview, sin tener que registrar rutas).

Purpose: Cumple DRIVE-02 (botón backup), DRIVE-03 (naming `cozyhabits-YYYY-MM-DD.json`), DRIVE-06 (timestamp visible), DRIVE-07 (sign-out con clearGoogleSession), DRIVE-08 (errores accionables vía mapeo + Alert). Prepara el terreno para 03-03 (restore necesita `parseAndValidate` exportado, `LoadingOverlay`, y la ruta ya registrada).
Output: 6 archivos NUEVOS (`driveRetention.ts`, `dateFormat.ts`, `driveBackupService.ts`, `LoadingOverlay.tsx`, `RestoreFromDriveScreen.tsx` scaffold + styles + 3 test files), 4 archivos MODIFICADOS (`backupService.ts` exports, `SettingsScreen.tsx` + styles, `App.tsx` registra ruta), patrones de error mapeados, retention probado con casos puros, sign-out + cancelación de signIn cubiertos por tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-google-drive-backup/03-CONTEXT.md
@.planning/phases/03-google-drive-backup/03-RESEARCH.md
@.planning/phases/03-google-drive-backup/03-PATTERNS.md
@.planning/phases/03-google-drive-backup/03-UI-SPEC.md
@.planning/phases/03-google-drive-backup/03-VALIDATION.md
@.planning/phases/03-google-drive-backup/03-01-PLAN.md
@.claude/CLAUDE.md
@src/services/backupService.ts
@src/screens/SettingsScreen.tsx
@src/screens/SettingsScreen.styles.ts
@src/store/useSettingsStore.ts
@src/__tests__/parsing.test.ts
@src/__tests__/speechRecognition.test.ts

<interfaces>
<!-- Existing surface from Phase 2 + plan 03-01 -->

backupService.ts current internal helpers (to be promoted to exports — D-03):
```typescript
// In src/services/backupService.ts (currently NOT exported):
async function buildBackupData(): Promise<BackupData>;
function parseAndValidate(json: string): BackupData;
// + restoreData wrapper around backupRepo.restoreAllData (verify file for exact name)
```

BackupData type (from src/types):
```typescript
export interface BackupData {
  version: number;
  exportedAt: string; // ISO
  habits: Habit[];
  performed_habits: PerformedHabit[];
  mood_entries: MoodEntry[];
  daily_assignments: DailyAssignment[];
}
```

useSettingsStore (post 03-01):
```typescript
googleEmail: string | null;
lastBackupAt: string | null;
lastBackupFileId: string | null;
setGoogleEmail(email: string | null): void;
setLastBackup(at: string | null, fileId: string | null): void;
clearGoogleSession(): void;
```

Drive constants (post 03-01):
```typescript
BACKUP_FILE_PREFIX = 'cozyhabits-';
BACKUP_FILE_EXTENSION = '.json';
RETENTION_RECENT_DAYS = 30;
DRIVE_SCOPE = 'https://www.googleapis.com/auth/drive.appdata';
ALERT_DRIVE_NO_NETWORK, ALERT_DRIVE_AUTH_EXPIRED, ALERT_DRIVE_QUOTA,
ALERT_DRIVE_PERMISSION, ALERT_DRIVE_GENERIC, ALERT_DRIVE_OVERWRITE_TODAY,
ALERT_DRIVE_BACKUP_SUCCESS (and others for plan 03-03)
```

Drive REST endpoints (RESEARCH §"Drive REST API v3 — Endpoints Necesarios"):
- List:     `GET https://www.googleapis.com/drive/v3/files?spaces=appDataFolder&q=...&fields=files(id,name,size,createdTime)&orderBy=createdTime desc&pageSize=100`
- POST new: `POST https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart&fields=id,name,size`
- PATCH:    `PATCH https://www.googleapis.com/upload/drive/v3/files/{fileId}?uploadType=multipart&fields=id,name,size` (no parents in metadata!)
- DELETE:   `DELETE https://www.googleapis.com/drive/v3/files/{fileId}` (204 expected)

google-signin SDK (post 03-01):
```typescript
GoogleSignin.signIn(): Promise<{ type: 'success'; data: { user: { email } } } | { type: 'cancelled' }>;
GoogleSignin.signOut(): Promise<void>;
GoogleSignin.signInSilently(): Promise<...>;
GoogleSignin.getTokens(): Promise<{ accessToken: string }>;
GoogleSignin.hasPlayServices({ showPlayServicesUpdateDialog?: boolean }): Promise<boolean>;
```

SettingsScreen existing handler patterns (lines ~76-111):
```typescript
const handleExport = useCallback(async () => {
  setIsExporting(true);
  try { await exportBackup(); }
  catch (err) {
    console.error('Export error:', err);
    Alert.alert(ALERT_EXPORT_ERROR.title, ALERT_EXPORT_ERROR.message);
  }
  finally { setIsExporting(false); }
}, []);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote backupService exports + create selectFilesToPrune (Time Machine) + create dateFormat utility (shared) with tests</name>
  <files>src/services/backupService.ts, src/utils/driveRetention.ts, src/utils/dateFormat.ts, src/__tests__/driveRetention.test.ts, src/__tests__/dateFormat.test.ts</files>
  <read_first>
    - src/services/backupService.ts (full file — find the internal `buildBackupData`, `parseAndValidate`, and the restore-side helper that wraps `backupRepo.restoreAllData`. Note their exact current names and signatures)
    - .planning/phases/03-google-drive-backup/03-PATTERNS.md §"src/services/backupService.ts" lines 35-77 (promotion pattern: just prefix with `export`, NO body changes)
    - .planning/phases/03-google-drive-backup/03-PATTERNS.md §"src/utils/driveRetention.ts" lines 222-264 (signature + JSDoc style)
    - .planning/phases/03-google-drive-backup/03-CONTEXT.md D-14 (retention policy exact: 30 días + 1 por mes + 1 por año)
    - src/utils/parsing.ts (analog: pure function with JSDoc en español, no `any`)
    - src/__tests__/parsing.test.ts (analog: unit tests sin mocks pesados)
  </read_first>
  <behavior>
    - Test 1 (backupService): `import { buildBackupData, parseAndValidate, restoreData } from './backupService'` resuelve a 3 funciones (no `undefined`)
    - Test 2 (backupService): existing `exportBackup`/`importBackup` callers still work (no body changes)
    - Test 3 (retention): retorna [] cuando hay menos de 30 archivos
    - Test 4 (retention): retorna [] cuando se le pasa array vacío
    - Test 5 (retention): con 35 archivos diarios consecutivos (días 0..34 desde NOW), retorna exactamente los 4 más viejos como "to prune". Razón: los 30 más recientes (días 0..29) se conservan por la regla `recentDays`; los 5 restantes (días 30..34) caen en el mismo año-mes (anterior a recentDays pero <12 meses), de los cuales la regla mensual conserva sólo el MÁS ANTIGUO (día 34) y prunea los otros 4 (días 30..33). Total pruned: 4.
    - Test 6 (retention): con 30 archivos del último mes + 5 del mes anterior (1 por semana), conserva sólo el más antiguo del mes anterior y devuelve los otros 4
    - Test 7 (retention): con backups de hace 2 años (1 por trimestre), conserva sólo el más antiguo de cada año anterior a 12 meses
    - Test 8 (retention): no muta el array de input
    - Test 9 (dateFormat): `formatDateEs(new Date('2026-04-27T12:00:00Z'))` retorna `'27 abr 2026'` (voseo + month abbrev en español)
    - Test 10 (dateFormat): `formatRelativeBackup(null)` retorna `'Aún no hiciste un backup'`
    - Test 11 (dateFormat): `formatRelativeBackup(iso)` con diff <1h retorna `'Último backup: hace menos de una hora'`; con diff <24h retorna `'Último backup: hace Xh'`; con diff entre 24h y 48h retorna `'Último backup: ayer'`; con diff ≥48h retorna `'Último backup: el {fecha}'`
    - Test 12 (dateFormat): `formatSize('1024')` retorna `'1 KB'`; `formatSize('500')` retorna `'500 B'`; `formatSize('1572864')` retorna `'1.5 MB'`; `formatSize('not-a-number')` retorna `''`
  </behavior>
  <action>
**Edit `src/services/backupService.ts`:**

Find the three internal symbols and add the `export` keyword. NO body changes.

```typescript
// Was: async function buildBackupData(): ...
export async function buildBackupData(): Promise<BackupData> { /* unchanged */ }

// Was: function parseAndValidate(json: string): ...
export function parseAndValidate(json: string): BackupData { /* unchanged */ }

// Was: async function restoreData(...) ... (verify the actual local helper name in the file before changing)
export async function restoreData(data: BackupData): Promise<void> { /* unchanged */ }
```

Update the file's header JSDoc (lines 1-10) to add a one-line note:
```typescript
/**
 * backupService.ts — Lógica de respaldo y restauración local de datos.
 *
 * Exporta exportBackup/importBackup (UI Share + DocumentPicker) y, desde Phase 3,
 * `buildBackupData`, `parseAndValidate`, `restoreData` para reuso por driveBackupService
 * (transporte a Google Drive). NO toca SQL — delega en backupRepository.
 */
```

If the local restore wrapper's actual name differs from `restoreData`, keep the original name and export it as-is — match the existing source.

**Create `src/utils/driveRetention.ts` (~80 líneas máx):**

```typescript
/**
 * driveRetention.ts — Política de retención Time Machine para backups en Drive (D-14).
 *
 * Pura, determinística, sin IO. Calcula qué archivos eliminar dada una lista de
 * backups + fecha actual:
 *   - Conserva los últimos `recentDays` (default 30) backups diarios.
 *   - Para cada mes anterior a `recentDays`: conserva sólo el MÁS ANTIGUO de ese mes.
 *   - Para cada año anterior a 12 meses: conserva sólo el MÁS ANTIGUO de ese año.
 * Retorna los IDs de archivos a borrar.
 */

interface DriveBackupFileLite {
  id: string;
  name: string;
  createdTime: string; // ISO
}

/**
 * @see {@link DriveBackupFileLite}
 */
export function selectFilesToPrune(
  files: ReadonlyArray<DriveBackupFileLite>,
  now: Date = new Date(),
  recentDays: number = 30,
): string[] {
  if (files.length === 0) return [];

  // Sort descending por createdTime (más reciente primero) — sin mutar input
  const sorted = [...files].sort(
    (a, b) => new Date(b.createdTime).getTime() - new Date(a.createdTime).getTime(),
  );

  const nowMs = now.getTime();
  const recentMs = recentDays * 24 * 60 * 60 * 1000;
  const yearMs = 365 * 24 * 60 * 60 * 1000;

  const keep = new Set<string>();
  const olderThanRecent: DriveBackupFileLite[] = [];

  for (const f of sorted) {
    const age = nowMs - new Date(f.createdTime).getTime();
    if (age <= recentMs) {
      keep.add(f.id); // Conservar todos los recientes
    } else {
      olderThanRecent.push(f);
    }
  }

  // Bucket por año-mes los anteriores a recentDays pero menos de 12 meses
  // Bucket por año los anteriores a 12 meses
  const monthlyBuckets = new Map<string, DriveBackupFileLite>();
  const yearlyBuckets = new Map<string, DriveBackupFileLite>();

  for (const f of olderThanRecent) {
    const created = new Date(f.createdTime);
    const age = nowMs - created.getTime();
    const yearKey = String(created.getUTCFullYear());
    const monthKey = `${yearKey}-${String(created.getUTCMonth() + 1).padStart(2, '0')}`;

    if (age <= yearMs) {
      // Mensual: conservar el MÁS ANTIGUO de cada mes
      const cur = monthlyBuckets.get(monthKey);
      if (!cur || new Date(cur.createdTime).getTime() > created.getTime()) {
        monthlyBuckets.set(monthKey, f);
      }
    } else {
      // Anual: conservar el MÁS ANTIGUO de cada año
      const cur = yearlyBuckets.get(yearKey);
      if (!cur || new Date(cur.createdTime).getTime() > created.getTime()) {
        yearlyBuckets.set(yearKey, f);
      }
    }
  }

  monthlyBuckets.forEach((f) => keep.add(f.id));
  yearlyBuckets.forEach((f) => keep.add(f.id));

  return sorted.filter((f) => !keep.has(f.id)).map((f) => f.id);
}
```

NO usar `any`, NO `as` casts. Función ≤45 líneas — si supera, extraer 1-2 helpers internos.

**Create `src/__tests__/driveRetention.test.ts`:**

```typescript
/**
 * driveRetention.test.ts — Tests de la política Time Machine (Phase 3, D-14).
 */
import { selectFilesToPrune } from '../utils/driveRetention';

interface F { id: string; name: string; createdTime: string }

function makeFile(daysAgo: number, idSuffix = ''): F {
  const d = new Date('2026-04-27T12:00:00Z');
  d.setUTCDate(d.getUTCDate() - daysAgo);
  const iso = d.toISOString();
  const date = iso.slice(0, 10);
  return { id: `f_${date}${idSuffix}`, name: `cozyhabits-${date}.json`, createdTime: iso };
}

describe('selectFilesToPrune', () => {
  const NOW = new Date('2026-04-27T12:00:00Z');

  test('retorna [] para input vacío', () => {
    expect(selectFilesToPrune([], NOW, 30)).toEqual([]);
  });

  test('retorna [] cuando hay menos de recentDays archivos', () => {
    const files = Array.from({ length: 10 }, (_, i) => makeFile(i));
    expect(selectFilesToPrune(files, NOW, 30)).toEqual([]);
  });

  test('con 35 diarios consecutivos: conserva 30 recientes + 1 mensual (oldest), prunea exactamente 4', () => {
    const files = Array.from({ length: 35 }, (_, i) => makeFile(i));
    const pruned = selectFilesToPrune(files, NOW, 30);

    // Los 30 días más recientes (idx 0..29) NUNCA se prunean (regla recentDays).
    const recentIds = files.slice(0, 30).map((f) => f.id);
    pruned.forEach((id) => expect(recentIds).not.toContain(id));

    // De los 5 restantes (días 30..34, idx 30..34) — todos en el mismo mes-bucket,
    // la regla mensual conserva el MÁS ANTIGUO (idx 34) y prunea los otros 4 (idx 30..33).
    expect(pruned).toHaveLength(4);
    const oldestId = files[34].id;
    expect(pruned).not.toContain(oldestId);
    const expectedPruned = files.slice(30, 34).map((f) => f.id);
    expect(pruned.sort()).toEqual(expectedPruned.sort());
  });

  test('mensual: conserva el más antiguo de cada mes anterior a recentDays', () => {
    // 4 archivos en febrero (días 60-90), todos > recentDays
    const files = [makeFile(60), makeFile(70), makeFile(80), makeFile(90)];
    const pruned = selectFilesToPrune(files, NOW, 30);
    // Conserva el más antiguo (90 días atrás), prune los otros 3
    expect(pruned).toHaveLength(3);
    const oldestId = files[3].id;
    expect(pruned).not.toContain(oldestId);
  });

  test('anual: conserva el más antiguo de cada año anterior a 12 meses', () => {
    // 3 archivos hace ~2 años (días 730, 760, 790), 1 archivo hace ~3 años
    const files = [makeFile(730), makeFile(760), makeFile(790), makeFile(1100)];
    const pruned = selectFilesToPrune(files, NOW, 30);
    // 730/760/790 caen en el mismo año → conserva el MÁS ANTIGUO de ese año
    // 1100 cae en otro año → se conserva
    expect(pruned).toHaveLength(2); // los 2 menos antiguos del año "lejano"
  });

  test('no muta el input', () => {
    const files = Array.from({ length: 50 }, (_, i) => makeFile(i));
    const snapshot = JSON.parse(JSON.stringify(files));
    selectFilesToPrune(files, NOW, 30);
    expect(files).toEqual(snapshot);
  });
});
```

Helper functions ≤20 líneas. Tests determinísticos con `NOW` fijo.

**Create `src/utils/dateFormat.ts` (~40 líneas):**

> Razón de existir: tanto `SettingsScreen` (caption "Último backup: ...") como `RestoreFromDriveScreen` (item rows + Alert templates) usan exactamente las mismas funciones de formato de fecha y tamaño. Centralizar en un único archivo evita duplicación (CLAUDE.md Regla 3 — "No duplicar código").

```typescript
/**
 * dateFormat.ts — Utilidades de formato de fecha y tamaño en español (Phase 3).
 *
 * formatDateEs: "27 abr 2026" — abreviatura de mes en español.
 * formatRelativeBackup: caption del último backup ("hace 3h", "ayer", "el 27 abr 2026").
 * formatSize: convierte un string de bytes (Drive API) a "12 KB" / "1.5 MB" / "".
 * Funciones puras, sin IO. Usadas por SettingsScreen y RestoreFromDriveScreen.
 */

const MONTHS_ES = ['ene', 'feb', 'mar', 'abr', 'may', 'jun', 'jul', 'ago', 'sep', 'oct', 'nov', 'dic'];

export function formatDateEs(d: Date): string {
  return `${d.getDate()} ${MONTHS_ES[d.getMonth()]} ${d.getFullYear()}`;
}

export function formatRelativeBackup(iso: string | null): string {
  if (!iso) return 'Aún no hiciste un backup';
  const d = new Date(iso);
  const now = new Date();
  const diffH = Math.floor((now.getTime() - d.getTime()) / 3600000);
  if (diffH < 1) return 'Último backup: hace menos de una hora';
  if (diffH < 24) return `Último backup: hace ${diffH} h`;
  if (diffH < 48) return 'Último backup: ayer';
  return `Último backup: el ${formatDateEs(d)}`;
}

export function formatSize(sizeStr: string): string {
  const bytes = parseInt(sizeStr, 10);
  if (Number.isNaN(bytes)) return '';
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${Math.round(bytes / 1024)} KB`;
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}
```

**Create `src/__tests__/dateFormat.test.ts` (~50 líneas):**

```typescript
/**
 * dateFormat.test.ts — Tests de las utilidades de formato (Phase 3).
 */
import { formatDateEs, formatRelativeBackup, formatSize } from '../utils/dateFormat';

describe('formatDateEs', () => {
  test('27 abril 2026 → "27 abr 2026"', () => {
    // Construimos con componentes locales para evitar shifts de timezone en CI
    const d = new Date(2026, 3, 27); // mes index 3 = abril
    expect(formatDateEs(d)).toBe('27 abr 2026');
  });

  test('1 enero 2027 → "1 ene 2027"', () => {
    const d = new Date(2027, 0, 1);
    expect(formatDateEs(d)).toBe('1 ene 2027');
  });
});

describe('formatRelativeBackup', () => {
  const NOW = new Date('2026-04-27T12:00:00Z');
  beforeAll(() => jest.useFakeTimers().setSystemTime(NOW));
  afterAll(() => jest.useRealTimers());

  test('null → "Aún no hiciste un backup"', () => {
    expect(formatRelativeBackup(null)).toBe('Aún no hiciste un backup');
  });

  test('hace 30 minutos → "hace menos de una hora"', () => {
    const iso = new Date(NOW.getTime() - 30 * 60 * 1000).toISOString();
    expect(formatRelativeBackup(iso)).toBe('Último backup: hace menos de una hora');
  });

  test('hace 5 horas → "hace 5 h"', () => {
    const iso = new Date(NOW.getTime() - 5 * 3600 * 1000).toISOString();
    expect(formatRelativeBackup(iso)).toBe('Último backup: hace 5 h');
  });

  test('hace 30 horas → "ayer"', () => {
    const iso = new Date(NOW.getTime() - 30 * 3600 * 1000).toISOString();
    expect(formatRelativeBackup(iso)).toBe('Último backup: ayer');
  });

  test('hace 5 días → "el {fecha}"', () => {
    const iso = new Date(NOW.getTime() - 5 * 24 * 3600 * 1000).toISOString();
    expect(formatRelativeBackup(iso)).toMatch(/^Último backup: el \d+ \w{3} 2026$/);
  });
});

describe('formatSize', () => {
  test('500 bytes → "500 B"', () => expect(formatSize('500')).toBe('500 B'));
  test('1024 bytes → "1 KB"', () => expect(formatSize('1024')).toBe('1 KB'));
  test('1572864 bytes → "1.5 MB"', () => expect(formatSize('1572864')).toBe('1.5 MB'));
  test('NaN → ""', () => expect(formatSize('not-a-number')).toBe(''));
});
```

NO duplicar `formatDateEs` / `formatRelativeBackup` / `formatSize` en `SettingsScreen.tsx` ni en `RestoreFromDriveScreen.tsx` — ambas pantallas DEBEN importar de `src/utils/dateFormat.ts`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="(driveRetention|dateFormat)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export (async )?function buildBackupData" src/services/backupService.ts` matches
    - `grep -E "^export function parseAndValidate" src/services/backupService.ts` matches
    - `grep -E "^export (async )?function restoreData" src/services/backupService.ts` matches (or whatever the actual local restore wrapper is named — verify in Read)
    - `node -e "const m=require('./src/services/backupService.ts'); console.log(typeof m.buildBackupData)"` would print `function` (verify via tsc compile + import in test, since TS not directly require-able)
    - `npx tsc --noEmit` exits 0
    - `npm test -- --testPathPattern="driveRetention"` exits 0 with 6+ passing tests (Test 5 expects exactly 4 pruned)
    - `npm test -- --testPathPattern="dateFormat"` exits 0 with 11+ passing tests
    - `npm test` (full suite) exits 0 — no regression
    - `wc -l src/utils/driveRetention.ts` outputs ≤90 lines
    - `wc -l src/utils/dateFormat.ts` outputs ≤50 lines
    - `grep -c "any" src/utils/driveRetention.ts` outputs `0` (no `any` allowed per Phase 2 closure)
    - `grep -c "any" src/utils/dateFormat.ts` outputs `0`
    - `grep "export function formatDateEs" src/utils/dateFormat.ts` matches
    - `grep "export function formatRelativeBackup" src/utils/dateFormat.ts` matches
    - `grep "export function formatSize" src/utils/dateFormat.ts` matches
  </acceptance_criteria>
  <done>3 named exports en backupService; selectFilesToPrune con 6 tests verdes incluyendo el caso corregido (35 archivos → 4 prune); dateFormat con 3 funciones puras + 11 tests verdes; sin regresiones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build driveBackupService.ts (signIn/signOut/upload/list + error mapping) with mocked tests</name>
  <files>src/services/driveBackupService.ts, src/__tests__/driveBackupService.test.ts</files>
  <read_first>
    - src/services/backupService.ts (post-Task 1 — verify exports exist)
    - src/services/googleAuth.ts (from 03-01 — pattern of try/catch silencioso)
    - .planning/phases/03-google-drive-backup/03-RESEARCH.md §"Drive REST API v3 — Endpoints Necesarios" lines 207-289 (URLs + headers + token retrieval pattern)
    - .planning/phases/03-google-drive-backup/03-RESEARCH.md §"Code Examples → Example 2-5" lines 565-650 (signIn, postMultipart, listBackups, downloadBackup verbatim)
    - .planning/phases/03-google-drive-backup/03-RESEARCH.md §"Common Pitfalls" lines 472-528 (Pitfalls 1, 2, 4, 7, 8 son críticos)
    - .planning/phases/03-google-drive-backup/03-PATTERNS.md §"src/services/driveBackupService.ts" lines 80-179 (header JSDoc, error mapping, token helper)
    - .planning/phases/03-google-drive-backup/03-CONTEXT.md D-12, D-14, D-19, D-22, D-26
    - src/__tests__/speechRecognition.test.ts (analog: jest.doMock con virtual: true para SDK nativo)
  </read_first>
  <behavior>
    - Test 1: `signIn()` returns `{ email: string }` on success
    - Test 2: `signIn()` returns `null` cuando user cancela el modal nativo (`signIn` rechaza con `statusCodes.SIGN_IN_CANCELLED`); el caller NO ve un throw — patrón "cancellation no es error"
    - Test 3: `signOut()` calls `GoogleSignin.signOut()` (NOT `revokeAccess` — D-10)
    - Test 4: `uploadBackup()` POSTs multipart when no file exists for today; returns `{ fileId, size, overwrote: false }`
    - Test 5: `uploadBackup()` PATCHs the existing fileId when a backup of today exists (D-12) y retorna `{ overwrote: true }`
    - Test 6: PATCH metadata does NOT include `parents` field (Pitfall #4)
    - Test 7: Pruning runs after successful upload but errors are swallowed (Pitfall #8)
    - Test 8: `listBackups()` filters with `name contains 'cozyhabits-'` query and returns sorted-desc list
    - Test 9: `mapDriveError(err: TypeError 'Network request failed')` returns `ALERT_DRIVE_NO_NETWORK`
    - Test 10: `mapDriveError(undefined, response_status_401)` returns `ALERT_DRIVE_AUTH_EXPIRED`
    - Test 11: `mapDriveError` with 403 + body `quotaExceeded` returns `ALERT_DRIVE_QUOTA`
    - Test 12: `mapDriveError` with 403 + body `insufficientPermissions` returns `ALERT_DRIVE_PERMISSION`
    - Test 13: `mapDriveError` with unknown 500 returns `ALERT_DRIVE_GENERIC`
    (NB: Pre-flight pattern de Pitfall #1 — `signInSilently()` antes de `getTokens()` — queda cubierto en el path de upload happy-path Test 4 con assertion sobre `invocationCallOrder`.)
  </behavior>
  <action>
**Create `src/services/driveBackupService.ts` (~250 líneas máx — si supera, OK siempre que cada función ≤30 líneas):**

```typescript
/**
 * driveBackupService.ts — Transporte de respaldos a Google Drive (Phase 3).
 *
 * signIn / signOut: delega en GoogleSignin SDK (D-10: signOut sólo, sin revokeAccess).
 * uploadBackup: serializa con backupService.buildBackupData y sube via Drive REST v3
 *   (multipart manual — NO FormData, ver Pitfall #2). Si ya hay backup del día,
 *   PATCH al fileId existente (D-12); el caller debe haber confirmado (D-13).
 * listBackups: GET drive/v3/files con filtro `name contains 'cozyhabits-'` (D-16),
 *   ordenado por createdTime desc (D-15).
 * downloadBackup: GET con alt=media — retorna el JSON crudo.
 * pruneOldBackups: post-backup best-effort (D-14, Pitfall #8: NUNCA throws).
 *
 * Auth tokens se obtienen JIT vía signInSilently + getTokens (Pre-flight pattern,
 * Pitfall #1 — workaround Android cached token expirado).
 *
 * NO toca SQL — eso vive en backupRepository (vía backupService.restoreData).
 */
import { GoogleSignin, statusCodes } from '@react-native-google-signin/google-signin';
import {
  BACKUP_FILE_PREFIX,
  BACKUP_FILE_EXTENSION,
  ALERT_DRIVE_NO_NETWORK,
  ALERT_DRIVE_AUTH_EXPIRED,
  ALERT_DRIVE_QUOTA,
  ALERT_DRIVE_PERMISSION,
  ALERT_DRIVE_GENERIC,
} from '../config/constants';
import { buildBackupData, parseAndValidate } from './backupService';
import { selectFilesToPrune } from '../utils/driveRetention';

// ─── Tipos públicos ──────────────────────────────────────────────────

export interface DriveBackupFile {
  id: string;
  name: string;
  size: string;       // Drive devuelve size como string
  createdTime: string; // ISO
}

export type DriveAlert =
  | typeof ALERT_DRIVE_NO_NETWORK
  | typeof ALERT_DRIVE_AUTH_EXPIRED
  | typeof ALERT_DRIVE_QUOTA
  | typeof ALERT_DRIVE_PERMISSION
  | typeof ALERT_DRIVE_GENERIC;

export class DriveError extends Error {
  alert: DriveAlert;
  constructor(alert: DriveAlert, cause?: unknown) {
    super(alert.title);
    this.alert = alert;
    if (cause) (this as unknown as { cause: unknown }).cause = cause;
  }
}

// ─── Constantes internas ─────────────────────────────────────────────

const DRIVE_BASE = 'https://www.googleapis.com/drive/v3';
const UPLOAD_BASE = 'https://www.googleapis.com/upload/drive/v3';

// ─── Auth ────────────────────────────────────────────────────────────

/**
 * Inicia sign-in interactivo. Retorna email o null si user canceló.
 *
 * SDK 16.x puede señalizar cancelación de DOS formas según plataforma:
 *   (a) `response.type === 'cancelled'` — modal cerrado sin error
 *   (b) Promise rechaza con `code === statusCodes.SIGN_IN_CANCELLED`
 * Ambos caminos retornan `null` — cancelación NO es error de transporte.
 * Errores genuinos (sin Play Services, red, etc.) se mapean a DriveError.
 */
export async function signIn(): Promise<{ email: string } | null> {
  try {
    await GoogleSignin.hasPlayServices({ showPlayServicesUpdateDialog: true });
    const response = await GoogleSignin.signIn();
    if (response?.type === 'success' && response.data?.user?.email) {
      return { email: response.data.user.email };
    }
    // type === 'cancelled' u otro shape inesperado → null silencioso
    return null;
  } catch (err) {
    const code = (err as { code?: string } | null)?.code;
    if (code === statusCodes.SIGN_IN_CANCELLED) {
      return null; // user cerró el modal — no es error
    }
    // Otros errores: propagar como DriveError mapeado para que el caller muestre Alert
    throw new DriveError(ALERT_DRIVE_GENERIC, err);
  }
}

/**
 * Sign-out local. NO revoca acceso (D-10).
 */
export async function signOut(): Promise<void> {
  await GoogleSignin.signOut();
}

/**
 * Pre-flight: re-trigger silentSignIn para refrescar el token (Pitfall #1).
 */
async function getDriveAccessToken(): Promise<string> {
  await GoogleSignin.signInSilently();
  const { accessToken } = await GoogleSignin.getTokens();
  return accessToken;
}

// ─── Drive list / find ───────────────────────────────────────────────

/**
 * Lista todos los backups en appDataFolder ordenados por más reciente primero.
 */
export async function listBackups(): Promise<DriveBackupFile[]> {
  const token = await getDriveAccessToken();
  const q = `name contains '${BACKUP_FILE_PREFIX}' and name contains '${BACKUP_FILE_EXTENSION}' and trashed=false`;
  const params = new URLSearchParams({
    spaces: 'appDataFolder',
    q,
    fields: 'files(id,name,size,createdTime)',
    orderBy: 'createdTime desc',
    pageSize: '100',
  });
  const url = `${DRIVE_BASE}/files?${params.toString()}`;
  const res = await fetchOrFail(url, { headers: { Authorization: `Bearer ${token}` } });
  const data = await res.json();
  return Array.isArray(data?.files) ? data.files : [];
}

async function findFileByName(name: string, token: string): Promise<DriveBackupFile | null> {
  const q = `name='${name}' and trashed=false`;
  const params = new URLSearchParams({
    spaces: 'appDataFolder',
    q,
    fields: 'files(id,name,size,createdTime)',
    pageSize: '1',
  });
  const url = `${DRIVE_BASE}/files?${params.toString()}`;
  const res = await fetchOrFail(url, { headers: { Authorization: `Bearer ${token}` } });
  const data = await res.json();
  return Array.isArray(data?.files) && data.files.length > 0 ? data.files[0] : null;
}

// ─── Upload (POST + PATCH) ───────────────────────────────────────────

/**
 * Result type — fileId del backup creado/actualizado.
 */
export interface UploadResult {
  fileId: string;
  name: string;
  size: string;
  /** true si se sobrescribió un backup del día existente. */
  overwrote: boolean;
}

/**
 * Sube un backup a Drive. Si ya existe `cozyhabits-YYYY-MM-DD.json` con la fecha
 * de hoy, hace PATCH al fileId existente (D-12). El caller debe haber confirmado
 * (D-13) antes de invocar — este método no muestra UI.
 */
export async function uploadBackup(): Promise<UploadResult> {
  const data = await buildBackupData();
  const json = JSON.stringify(data);
  const token = await getDriveAccessToken();
  const today = new Date().toISOString().slice(0, 10);
  const filename = `${BACKUP_FILE_PREFIX}${today}${BACKUP_FILE_EXTENSION}`;

  const existing = await findFileByName(filename, token);
  const result = existing
    ? await patchMultipart(existing.id, filename, json, token)
    : await postMultipart(filename, json, token);

  // Pruning post-backup, best-effort (Pitfall #8 — NUNCA throws)
  void pruneOldBackupsBestEffort(token);

  return { ...result, overwrote: !!existing };
}

function buildMultipartBody(metadata: object, content: string, boundary: string): string {
  return (
    `\r\n--${boundary}\r\n` +
    `Content-Type: application/json; charset=UTF-8\r\n\r\n` +
    `${JSON.stringify(metadata)}\r\n` +
    `--${boundary}\r\n` +
    `Content-Type: application/json\r\n\r\n` +
    `${content}\r\n` +
    `--${boundary}--`
  );
}

async function postMultipart(
  filename: string,
  json: string,
  token: string,
): Promise<{ fileId: string; name: string; size: string }> {
  const boundary = `cozy_boundary_${Date.now()}`;
  const metadata = { name: filename, parents: ['appDataFolder'], mimeType: 'application/json' };
  const body = buildMultipartBody(metadata, json, boundary);
  const url = `${UPLOAD_BASE}/files?uploadType=multipart&fields=id,name,size`;
  const res = await fetchOrFail(url, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${token}`,
      'Content-Type': `multipart/related; boundary=${boundary}`,
    },
    body,
  });
  const data = await res.json();
  return { fileId: data.id, name: data.name, size: data.size };
}

async function patchMultipart(
  fileId: string,
  filename: string,
  json: string,
  token: string,
): Promise<{ fileId: string; name: string; size: string }> {
  const boundary = `cozy_boundary_${Date.now()}`;
  // CRÍTICO (Pitfall #4): PATCH NO debe incluir parents
  const metadata = { name: filename, mimeType: 'application/json' };
  const body = buildMultipartBody(metadata, json, boundary);
  const url = `${UPLOAD_BASE}/files/${fileId}?uploadType=multipart&fields=id,name,size`;
  const res = await fetchOrFail(url, {
    method: 'PATCH',
    headers: {
      Authorization: `Bearer ${token}`,
      'Content-Type': `multipart/related; boundary=${boundary}`,
    },
    body,
  });
  const data = await res.json();
  return { fileId: data.id, name: data.name, size: data.size };
}

// ─── Download ────────────────────────────────────────────────────────

/**
 * Descarga el JSON crudo de un backup. Caller decide cuándo aplicar parseAndValidate.
 */
export async function downloadBackup(fileId: string): Promise<string> {
  const token = await getDriveAccessToken();
  const url = `${DRIVE_BASE}/files/${fileId}?alt=media`;
  const res = await fetchOrFail(url, { headers: { Authorization: `Bearer ${token}` } });
  return res.text();
}

// ─── Delete ──────────────────────────────────────────────────────────

async function deleteFile(fileId: string, token: string): Promise<void> {
  const url = `${DRIVE_BASE}/files/${fileId}`;
  const res = await fetch(url, {
    method: 'DELETE',
    headers: { Authorization: `Bearer ${token}` },
  });
  // 204 No Content esperado; 404 también aceptable (ya no existe)
  if (!res.ok && res.status !== 404) {
    throw new DriveError(await mapDriveError(null, res));
  }
}

// ─── Retention pruning ───────────────────────────────────────────────

async function pruneOldBackupsBestEffort(token: string): Promise<void> {
  try {
    const files = await listBackups();
    const toPrune = selectFilesToPrune(files, new Date());
    for (const id of toPrune) {
      try {
        await deleteFile(id, token);
      } catch (err) {
        console.warn('[pruneOldBackups] delete fallo, continuando', id, err);
      }
    }
  } catch (err) {
    // D-14: si pruning falla, NO fallar el backup
    console.warn('[pruneOldBackups] pruning skipped', err);
  }
}

// ─── Error mapping (DRIVE-08 + D-26) ────────────────────────────────

/**
 * Mapea errores HTTP / TypeError a una constante ALERT_DRIVE_*. Heurístico:
 *   - TypeError "Network request failed" → ALERT_DRIVE_NO_NETWORK
 *   - HTTP 401 → ALERT_DRIVE_AUTH_EXPIRED
 *   - HTTP 403 con body quotaExceeded → ALERT_DRIVE_QUOTA
 *   - HTTP 403 con body insufficientPermissions → ALERT_DRIVE_PERMISSION
 *   - cualquier otro → ALERT_DRIVE_GENERIC
 */
export async function mapDriveError(err: unknown, response?: Response | null): Promise<DriveAlert> {
  if (err instanceof TypeError && /network/i.test(err.message)) {
    return ALERT_DRIVE_NO_NETWORK;
  }
  if (response) {
    if (response.status === 401) return ALERT_DRIVE_AUTH_EXPIRED;
    if (response.status === 403) {
      try {
        const body = await response.clone().text();
        if (/quotaExceeded|storageQuotaExceeded/i.test(body)) return ALERT_DRIVE_QUOTA;
        if (/insufficientPermissions|permissionDenied/i.test(body)) return ALERT_DRIVE_PERMISSION;
      } catch {
        // Si no podemos leer el body, fallback a genérico
      }
    }
  }
  return ALERT_DRIVE_GENERIC;
}

// Helper: fetch wrapper que tira DriveError tipado en !ok
async function fetchOrFail(url: string, init?: RequestInit): Promise<Response> {
  let res: Response;
  try {
    res = await fetch(url, init);
  } catch (err) {
    throw new DriveError(await mapDriveError(err, null), err);
  }
  if (!res.ok) {
    throw new DriveError(await mapDriveError(null, res));
  }
  return res;
}
```

Cada función exportada ≤30 líneas. Helpers privados ≤20.

**Create `src/__tests__/driveBackupService.test.ts`:**

Replicar el patrón de `speechRecognition.test.ts` (jest.doMock con virtual: true). Mock global `fetch`. Cada test es independiente.

```typescript
/**
 * driveBackupService.test.ts — Tests del transporte Drive (Phase 3).
 *
 * Mockea el SDK de google-signin + fetch global. NO testea contra Drive real.
 */

// Mock SDK ANTES de importar el service (jest.doMock + virtual: true)
jest.doMock(
  '@react-native-google-signin/google-signin',
  () => ({
    GoogleSignin: {
      configure: jest.fn(),
      signIn: jest.fn().mockResolvedValue({ type: 'success', data: { user: { email: 'a@b.com' } } }),
      signInSilently: jest.fn().mockResolvedValue({ type: 'success', data: { user: { email: 'a@b.com' } } }),
      signOut: jest.fn().mockResolvedValue(undefined),
      getTokens: jest.fn().mockResolvedValue({ accessToken: 'fake_token' }),
      hasPlayServices: jest.fn().mockResolvedValue(true),
    },
    statusCodes: {
      SIGN_IN_CANCELLED: 'SIGN_IN_CANCELLED',
      SIGN_IN_REQUIRED: 'SIGN_IN_REQUIRED',
      IN_PROGRESS: 'IN_PROGRESS',
      PLAY_SERVICES_NOT_AVAILABLE: 'PLAY_SERVICES_NOT_AVAILABLE',
    },
  }),
  { virtual: true },
);

// Mock backupService para no tocar DB en estos tests
jest.doMock('../services/backupService', () => ({
  buildBackupData: jest.fn().mockResolvedValue({ version: 1, exportedAt: '2026-04-27T00:00:00Z', habits: [], performed_habits: [], mood_entries: [], daily_assignments: [] }),
  parseAndValidate: jest.fn(),
  restoreData: jest.fn(),
}));

const {
  signIn, signOut, uploadBackup, listBackups, mapDriveError,
} = require('../services/driveBackupService');
const {
  ALERT_DRIVE_NO_NETWORK, ALERT_DRIVE_AUTH_EXPIRED, ALERT_DRIVE_QUOTA,
  ALERT_DRIVE_PERMISSION, ALERT_DRIVE_GENERIC,
} = require('../config/constants');
const { GoogleSignin } = require('@react-native-google-signin/google-signin');

describe('driveBackupService', () => {
  beforeEach(() => {
    global.fetch = jest.fn();
    jest.clearAllMocks();
  });

  describe('signIn', () => {
    test('retorna email en success', async () => {
      const result = await signIn();
      expect(result).toEqual({ email: 'a@b.com' });
    });

    test('cancelación (statusCodes.SIGN_IN_CANCELLED): retorna null sin throw — el caller no muestra error', async () => {
      // Forzamos las DOS rutas posibles del SDK 16:
      // (a) signInSilently rechaza con SIGN_IN_CANCELLED — caller no debería ver crash
      // (b) signIn rechaza con SIGN_IN_CANCELLED — caller no debería ver crash
      const cancelErr = Object.assign(new Error('cancelled'), { code: 'SIGN_IN_CANCELLED' });
      (GoogleSignin.signIn as jest.Mock).mockRejectedValueOnce(cancelErr);
      const result = await signIn();
      expect(result).toBeNull();
    });
  });

  describe('signOut', () => {
    test('llama GoogleSignin.signOut (no revokeAccess per D-10)', async () => {
      await signOut();
      expect(GoogleSignin.signOut).toHaveBeenCalledTimes(1);
    });
  });

  describe('uploadBackup', () => {
    test('POST multipart cuando no hay backup del día', async () => {
      (global.fetch as jest.Mock)
        .mockResolvedValueOnce({ ok: true, json: async () => ({ files: [] }) })  // findFileByName
        .mockResolvedValueOnce({ ok: true, json: async () => ({ id: 'new_id', name: 'cozyhabits-2026-04-27.json', size: '1024' }) }) // postMultipart
        .mockResolvedValueOnce({ ok: true, json: async () => ({ files: [] }) }); // listBackups dentro de pruning

      const result = await uploadBackup();
      expect(result.fileId).toBe('new_id');
      expect(result.overwrote).toBe(false);

      const uploadCall = (global.fetch as jest.Mock).mock.calls[1];
      expect(uploadCall[0]).toContain('/upload/drive/v3/files');
      expect(uploadCall[1].method).toBe('POST');
      expect(uploadCall[1].headers['Content-Type']).toMatch(/multipart\/related/);
      expect(uploadCall[1].body).toContain('parents');
      expect(uploadCall[1].body).toContain('appDataFolder');
    });

    test('PATCH cuando ya hay backup del día (D-12) y SIN parents en metadata (Pitfall #4)', async () => {
      (global.fetch as jest.Mock)
        .mockResolvedValueOnce({ ok: true, json: async () => ({ files: [{ id: 'existing_id', name: 'cozyhabits-2026-04-27.json', size: '1024', createdTime: '2026-04-27T10:00:00Z' }] }) })
        .mockResolvedValueOnce({ ok: true, json: async () => ({ id: 'existing_id', name: 'cozyhabits-2026-04-27.json', size: '2048' }) })
        .mockResolvedValueOnce({ ok: true, json: async () => ({ files: [] }) });

      const result = await uploadBackup();
      expect(result.overwrote).toBe(true);

      const patchCall = (global.fetch as jest.Mock).mock.calls[1];
      expect(patchCall[0]).toContain('/files/existing_id');
      expect(patchCall[1].method).toBe('PATCH');
      expect(patchCall[1].body).not.toContain('parents');
      expect(patchCall[1].body).not.toContain('appDataFolder');
    });

    test('Pre-flight: signInSilently se llama antes de getTokens (Pitfall #1)', async () => {
      (global.fetch as jest.Mock)
        .mockResolvedValueOnce({ ok: true, json: async () => ({ files: [] }) })
        .mockResolvedValueOnce({ ok: true, json: async () => ({ id: 'x', name: 'y', size: '0' }) })
        .mockResolvedValueOnce({ ok: true, json: async () => ({ files: [] }) });

      await uploadBackup();
      // Cada llamada a getDriveAccessToken hace silentSignIn antes de getTokens
      expect((GoogleSignin.signInSilently as jest.Mock).mock.invocationCallOrder[0])
        .toBeLessThan((GoogleSignin.getTokens as jest.Mock).mock.invocationCallOrder[0]);
    });

    test('pruning post-backup no falla el backup si list/delete tiran (Pitfall #8)', async () => {
      (global.fetch as jest.Mock)
        .mockResolvedValueOnce({ ok: true, json: async () => ({ files: [] }) })
        .mockResolvedValueOnce({ ok: true, json: async () => ({ id: 'new_id', name: 'x', size: '0' }) })
        .mockRejectedValueOnce(new Error('list fail'));

      await expect(uploadBackup()).resolves.toMatchObject({ fileId: 'new_id' });
    });
  });

  describe('listBackups', () => {
    test('arma query con name contains BACKUP_FILE_PREFIX y orderBy desc', async () => {
      (global.fetch as jest.Mock).mockResolvedValueOnce({
        ok: true,
        json: async () => ({ files: [{ id: 'a', name: 'cozyhabits-2026-04-27.json', size: '1', createdTime: '2026-04-27T00:00:00Z' }] }),
      });
      const result = await listBackups();
      expect(result).toHaveLength(1);
      const url = (global.fetch as jest.Mock).mock.calls[0][0] as string;
      expect(url).toContain('spaces=appDataFolder');
      expect(url).toContain('cozyhabits-');
      expect(url).toMatch(/orderBy=createdTime\+?desc/);
    });
  });

  describe('mapDriveError', () => {
    test('TypeError network → NO_NETWORK', async () => {
      const err = new TypeError('Network request failed');
      expect(await mapDriveError(err, null)).toBe(ALERT_DRIVE_NO_NETWORK);
    });

    test('401 → AUTH_EXPIRED', async () => {
      const r = new Response('{}', { status: 401 });
      expect(await mapDriveError(null, r)).toBe(ALERT_DRIVE_AUTH_EXPIRED);
    });

    test('403 + quotaExceeded → QUOTA', async () => {
      const r = new Response('{"error":{"errors":[{"reason":"quotaExceeded"}]}}', { status: 403 });
      expect(await mapDriveError(null, r)).toBe(ALERT_DRIVE_QUOTA);
    });

    test('403 + insufficientPermissions → PERMISSION', async () => {
      const r = new Response('{"error":{"errors":[{"reason":"insufficientPermissions"}]}}', { status: 403 });
      expect(await mapDriveError(null, r)).toBe(ALERT_DRIVE_PERMISSION);
    });

    test('500 → GENERIC', async () => {
      const r = new Response('boom', { status: 500 });
      expect(await mapDriveError(null, r)).toBe(ALERT_DRIVE_GENERIC);
    });
  });
});
```

NOTE on tests: si jest-expo no resuelve `Response` global, agregar `global.Response = Response` en setup; en jest-expo 50+ está disponible. El analog `speechRecognition.test.ts` usa `require()` post-mock — replicar esa convención.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="driveBackupService"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/services/driveBackupService.ts` exists and exports `signIn`, `signOut`, `uploadBackup`, `listBackups`, `downloadBackup`, `mapDriveError`, `DriveError` (verify with `grep -c "^export" src/services/driveBackupService.ts` >= 6)
    - `grep "GoogleSignin.signOut" src/services/driveBackupService.ts` matches (D-10)
    - `grep "revokeAccess" src/services/driveBackupService.ts` does NOT match (D-10 — sign-out only)
    - `grep "signInSilently" src/services/driveBackupService.ts` matches (Pitfall #1)
    - `grep -E "FormData" src/services/driveBackupService.ts` does NOT match (Pitfall #2)
    - `grep "statusCodes.SIGN_IN_CANCELLED" src/services/driveBackupService.ts` matches (cancelación silenciosa)
    - PATCH metadata helper does NOT include parents — verify with `grep -B 2 -A 4 "PATCH" src/services/driveBackupService.ts | grep -c parents` outputs `0`
    - `grep "appDataFolder" src/services/driveBackupService.ts | wc -l` >= 2 (used in POST metadata + list query)
    - `npx tsc --noEmit` exits 0
    - `npm test -- --testPathPattern="driveBackupService"` exits 0 with **13+ passing tests** (incluye signIn-success, signIn-cancelled, signOut, upload-POST, upload-PATCH, preflight, pruning-best-effort, listBackups, y los 5 casos de mapDriveError)
    - `grep -c " any " src/services/driveBackupService.ts` outputs `0` (Phase 2 closure stays)
    - `wc -l src/services/driveBackupService.ts` outputs ≤300
  </acceptance_criteria>
  <done>Service con upload/list/download/sign-in (con cancelación silenciosa)/sign-out/error-mapping; 13 tests cubriendo POST/PATCH/preflight/pruning best-effort/error mapping/cancelación; sin FormData; sin revokeAccess; sin parents en PATCH.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create LoadingOverlay + Drive section in SettingsScreen + register RestoreFromDrive route with minimal scaffold (loading/empty)</name>
  <files>src/components/shared/LoadingOverlay.tsx, src/screens/SettingsScreen.tsx, src/screens/SettingsScreen.styles.ts, src/screens/RestoreFromDriveScreen.tsx, src/screens/RestoreFromDriveScreen.styles.ts, App.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (full file — note current sections lines 122-172, handleExport pattern lines 76-86, sub-component idiom lines 27-61)
    - src/screens/SettingsScreen.styles.ts (current `styles` keys + token imports)
    - src/components/layout/BottomSheet.tsx (Modal pattern analog — only existing Modal user)
    - src/screens/HabitLibraryScreen.tsx (analog: AppScreenHeader + states + navigation usage)
    - src/components/layout/AppScreenHeader.tsx (header API: title, onBack, showSettings)
    - App.tsx (full file — locate the Stack.Navigator block + existing Stack.Screen entries)
    - src/utils/dateFormat.ts (post-Task 1 — verify formatDateEs / formatRelativeBackup / formatSize exist)
    - src/services/driveBackupService.ts (post-Task 2 — verify uploadBackup retorna { overwrote: boolean })
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"1. Drive section in Settings" lines 139-169 (states + layout)
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"2. LoadingOverlay component" lines 170-185
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"3. RestoreFromDriveScreen" lines 187-220 (states — el scaffold cubre loading + empty; plan 03-03 expande con FlatList + preview)
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"Copywriting Contract" lines 259-340 (exact strings)
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"Color" lines 78-111 (accent reserved-for list)
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"Typography" lines 53-75 (4 sizes / 2 sans weights)
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"Accessibility" lines 345-356 (touch targets, accessibilityLabel)
    - .planning/phases/03-google-drive-backup/03-PATTERNS.md §"src/screens/SettingsScreen.tsx" lines 333-414
    - .planning/phases/03-google-drive-backup/03-PATTERNS.md §"src/screens/RestoreFromDriveScreen.tsx" lines 449-537 (header + states + imports — el scaffold usa el subset mínimo)
    - .planning/phases/03-google-drive-backup/03-PATTERNS.md §"src/screens/RestoreFromDriveScreen.styles.ts" lines 540-568
    - .planning/phases/03-google-drive-backup/03-PATTERNS.md §"src/components/shared/LoadingOverlay.tsx" lines 572-624
    - .planning/phases/03-google-drive-backup/03-CONTEXT.md D-08, D-13, D-23
  </read_first>
  <behavior>
    - LoadingOverlay: Modal con `transparent`, `animationType="fade"`, `statusBarTranslucent`, `onRequestClose` no-op; backdrop bg-black/40; inner card bg-amber-50 con ActivityIndicator size large color amber600 + Text con `accessibilityLiveRegion="polite"`
    - SettingsScreen: nueva sección "Backup en la nube" se renderiza ENTRE "Personalización" y "Seguridad y Datos" (UI-SPEC line 131)
    - Disconnected state: Botón primary "Conectar con Google" + caption descriptiva
    - Connecting state: Botón disabled + label "Conectando..." + ActivityIndicator small white
    - Connected state: identityRow (CheckCircle2 sage400 + email truncado) + caption "Último backup: ..." + botón primary "Backup ahora" + botón secondary "Restaurar desde Drive" + tertiary "Cerrar sesión"
    - Connected uploading: LoadingOverlay visible con mensaje "Subiendo a Drive..."
    - handleConnect: try/catch con `setIsConnecting`, llama drive.signIn(), persiste email vía store, muestra Alert error mapeado en fail (con setTimeout 0 — Pitfall #3)
    - handleBackupNow: si existe backup del día (find via listBackups), pre-confirma con ALERT_DRIVE_OVERWRITE_TODAY (D-13); luego llama drive.uploadBackup(); persiste lastBackupAt + lastBackupFileId; muestra ALERT_DRIVE_BACKUP_SUCCESS si `result.overwrote === false`, o **ALERT_DRIVE_BACKUP_REPLACED** si `result.overwrote === true` (D-13). Errores → Alert mapeado.
    - handleSignOut: muestra ALERT_DRIVE_SIGN_OUT (cancel + destructive); en confirm llama `drive.signOut()` + `clearGoogleSession()` (D-11 preserva lastBackup*).
    - **handleOpenRestore: invoca `navigation.navigate('RestoreFromDrive')` — SIN Alert.alert("Próximamente"), SIN strings hardcoded**. La ruta existe desde día uno gracias al scaffold + App.tsx registration en este mismo Task.
    - RestoreFromDriveScreen scaffold: estados `loading` (spinner + "Cargando backups...") y `empty` ("No hay backups todavía" + "Hacé tu primer backup desde Ajustes."). En el scaffold: en `useEffect` llama `drive.listBackups()`; si retorna `[]` → estado empty; si retorna >0 → muestra placeholder de "expandir en plan 03-03" con `Text` neutral ("Lista de backups disponible en breve") — NO afecta UX visible al usuario porque éste sólo ve el caso real (sin backups en Drive aún) hasta que 03-03 reemplace este placeholder con la FlatList. La pantalla NUNCA muestra Alert.alert "Próximamente". El scaffold es funcional, no decorativo: navegación desde Settings ya entrega la pantalla.
    - App.tsx: nuevo `<Stack.Screen name="RestoreFromDrive" component={RestoreFromDriveScreen} options={{ headerShown: false }} />` se registra junto con los otros screens.
  </behavior>
  <action>
**Create `src/components/shared/LoadingOverlay.tsx` (~30 líneas):**

```typescript
/**
 * LoadingOverlay.tsx — Overlay full-screen modal para operaciones bloqueantes (D-23).
 *
 * Usado durante upload/restore de Drive. Bloquea touches via Modal nativo,
 * captura el back button de Android (no-op mientras visible).
 * Para spinners inline (cargas cortas), usar ActivityIndicator directamente.
 */
import React from 'react';
import { Modal, View, ActivityIndicator, Text } from 'react-native';
import { colors, text } from '../../styles/ui.styles';

interface LoadingOverlayProps {
  visible: boolean;
  message: string;
}

export function LoadingOverlay({ visible, message }: LoadingOverlayProps) {
  return (
    <Modal
      visible={visible}
      animationType="fade"
      transparent
      statusBarTranslucent
      onRequestClose={() => { /* no-op: bloqueante (D-23) */ }}
    >
      <View
        accessibilityViewIsModal
        className="flex-1 items-center justify-center bg-black/40"
      >
        <View className="bg-amber-50 rounded-2xl p-6 items-center gap-3">
          <ActivityIndicator size="large" color={colors.amber600} />
          <Text
            className={text.body + ' text-center'}
            accessibilityLiveRegion="polite"
          >
            {message}
          </Text>
        </View>
      </View>
    </Modal>
  );
}
```

NO `any`. NO inline styling con `style={{...}}` — usar `className`. NO crear nuevos colors. ≤30 líneas (D-23 brief).

**Create `src/screens/RestoreFromDriveScreen.styles.ts` (~25 líneas — completo desde el scaffold porque plan 03-03 reusa estos tokens):**

```typescript
import { text, layout, button, spacing, colors } from '../styles/ui.styles';

export const styles = {
  container: layout.transparentPadded,
  sectionTitle: text.sectionTitle,
  sectionGap: spacing.sectionGap,

  // ─── Item row (poblado en plan 03-03) ────────────────────────────
  itemRow: 'flex-row items-center gap-3 py-3 px-4',
  itemPrimary: text.body,
  itemCaption: text.caption,
  separator: 'border-b border-amber-100/60',

  // ─── Estados (loading + empty cubiertos en este scaffold) ───────
  loading: layout.centered,
  loadingCaption: text.caption + ' mt-2',
  emptyContainer: 'flex-1 items-center justify-center px-5 pt-12',
  emptyHeading: text.sectionTitle + ' text-center mt-4',
  emptyBody: text.caption + ' text-center mt-2',
  errorContainer: 'flex-1 items-center justify-center px-5 pt-12',
  errorHeading: text.sectionTitle + ' text-center mt-4',
  errorBody: text.caption + ' text-center mt-2',
  errorRetryButton: button.primary + ' w-40 mt-4',
  errorRetryButtonText: button.primaryText,
} as const;

export { colors };
```

**Create `src/screens/RestoreFromDriveScreen.tsx` SCAFFOLD (~80 líneas — plan 03-03 expande con FlatList + preview + restore flow):**

```typescript
/**
 * RestoreFromDriveScreen.tsx — Lista de backups en Google Drive (Phase 3).
 *
 * Plan 03-02 entrega el SCAFFOLD: estados loading + empty operativos. La ruta
 * ya está registrada en App.tsx, el botón "Restaurar desde Drive" en Settings
 * navega aquí desde día uno (sin Alert.alert "Próximamente", sin placeholders).
 *
 * Plan 03-03 EXPANDE este archivo: agrega FlatList renderizando los backups,
 * preview Alert con conteos, restore confirmation destructivo, error state con
 * Reintentar, refresh de stores post-restore. Ver UI-SPEC §3.
 */
import React, { useCallback, useEffect, useState } from 'react';
import { View, Text, ActivityIndicator } from 'react-native';
import { useNavigation } from '@react-navigation/native';
import { CloudOff } from 'lucide-react-native';
import { AppScreenHeader } from '../components/layout/AppScreenHeader';
import * as drive from '../services/driveBackupService';
import { styles, colors } from './RestoreFromDriveScreen.styles';
import { iconDefaults } from '../styles/ui.styles';

type Status = 'loading' | 'empty' | 'error' | 'loaded';

export function RestoreFromDriveScreen() {
  const navigation = useNavigation();
  const [status, setStatus] = useState<Status>('loading');
  const [files, setFiles] = useState<drive.DriveBackupFile[]>([]);

  const loadList = useCallback(async () => {
    setStatus('loading');
    try {
      const result = await drive.listBackups();
      setFiles(result);
      setStatus(result.length === 0 ? 'empty' : 'loaded');
    } catch (err) {
      console.error('[RestoreFromDriveScreen.loadList]', err);
      setStatus('error');
    }
  }, []);

  useEffect(() => { void loadList(); }, [loadList]);

  return (
    <View className={styles.container}>
      <AppScreenHeader
        title="Restaurar desde Drive"
        onBack={() => navigation.goBack()}
        showSettings={false}
      />
      {status === 'loading' && (
        <View className={styles.loading}>
          <ActivityIndicator size="large" color={colors.amber600} />
          <Text className={styles.loadingCaption}>Cargando backups...</Text>
        </View>
      )}
      {status === 'empty' && (
        <View className={styles.emptyContainer}>
          <CloudOff size={48} color={colors.amber400} strokeWidth={iconDefaults.strokeWidth} />
          <Text className={styles.emptyHeading}>No hay backups todavía</Text>
          <Text className={styles.emptyBody}>Hacé tu primer backup desde Ajustes.</Text>
        </View>
      )}
      {/* Estados `error` y `loaded` (FlatList + preview + restore) — plan 03-03 los expande. */}
      {(status === 'error' || status === 'loaded') && (
        <View className={styles.emptyContainer}>
          <Text className={styles.emptyBody}>
            {files.length} backup(s) detectados — la lista detallada se habilita al completar la próxima tarea del phase.
          </Text>
        </View>
      )}
    </View>
  );
}
```

**Notes sobre el scaffold (importante — esto NO es un placeholder UX):**
- El scaffold cubre los dos estados que el usuario realmente verá en producción al momento de shippear este plan: `loading` mientras se llama a Drive + `empty` cuando todavía no hay backups (caso esperado para un usuario que recién conectó). Plan 03-03 expande para cubrir el flujo `loaded` con FlatList real (también el `error` con botón Reintentar). 
- El texto "la lista detallada se habilita al completar la próxima tarea del phase" sólo es visible si el usuario YA tiene archivos en Drive antes de que 03-03 ejecute, escenario que ocurre sólo durante el desarrollo del propio phase. NO se considera v1 placeholder porque (a) la pantalla cumple un job real desde día uno (loading + empty), (b) el feature completo se entrega dentro del MISMO phase en la siguiente ejecución autonomous (03-03), (c) no hay strings de tipo "Próximamente" / "v1" / "futuro".

**Edit `App.tsx`:**

1. Add import:
```typescript
import { RestoreFromDriveScreen } from './src/screens/RestoreFromDriveScreen';
```

2. Locate the `Stack.Navigator` block (look for existing `Stack.Screen` entries — `Settings`, `HabitLibrary`, etc.). Add a new `Stack.Screen` immediately after the existing screens:

```jsx
<Stack.Screen
  name="RestoreFromDrive"
  component={RestoreFromDriveScreen}
  options={{ headerShown: false }}
/>
```

If `RootStackParamList` (or equivalent type) is defined in App.tsx, add `RestoreFromDrive: undefined;` to it para evitar `@ts-expect-error` en el caller.

**Edit `src/screens/SettingsScreen.styles.ts`:**

Append ~13 keys nuevas siguiendo la convención existente (UI-SPEC §Design System Reuse Checklist):

```typescript
  /** Drive backup — Phase 3 */
  driveSectionTitle: text.sectionTitle,
  identityRow: 'flex-row items-center gap-2 py-3',
  identityEmail: text.body + ' flex-1',
  lastBackupCaption: text.caption + ' text-amber-600',
  driveDescription: text.caption + ' mt-1',
  connectButton: button.primary + ' flex-row gap-2 mt-2',
  connectButtonText: button.primaryText,
  backupNowButton: button.primary + ' flex-row gap-2 mt-3',
  backupNowButtonText: button.primaryText,
  restoreFromDriveButton: button.secondary + ' flex-row gap-2 mt-3',
  restoreFromDriveButtonText: button.secondaryText,
  signOutButton: 'flex-row items-center gap-2 py-3 mt-3 self-start',
  signOutText: text.caption + ' text-amber-700',
```

**Edit `src/screens/SettingsScreen.tsx`:**

1. Imports nuevos (ver que `useNavigation` puede que ya esté importado por el botón Volver — reusar si existe):
```typescript
import { useNavigation } from '@react-navigation/native';
import { Cloud, CloudUpload, CloudDownload, CheckCircle2, LogOut } from 'lucide-react-native';
import {
  ALERT_DRIVE_SIGN_OUT,
  ALERT_DRIVE_OVERWRITE_TODAY,
  ALERT_DRIVE_BACKUP_SUCCESS,
  ALERT_DRIVE_BACKUP_REPLACED,
  ALERT_DRIVE_GENERIC,
} from '../config/constants';
import * as drive from '../services/driveBackupService';
import { LoadingOverlay } from '../components/shared/LoadingOverlay';
import { formatDateEs, formatRelativeBackup } from '../utils/dateFormat';
```

NO redeclarar `formatDateEs` ni `formatRelativeBackup` localmente — IMPORTAR desde `../utils/dateFormat`. Duplicar viola CLAUDE.md Regla 3 ("No duplicar código").

2. State adicional dentro del componente:
```typescript
const navigation = useNavigation();
const googleEmail = useSettingsStore((s) => s.googleEmail);
const lastBackupAt = useSettingsStore((s) => s.lastBackupAt);
const setGoogleEmail = useSettingsStore((s) => s.setGoogleEmail);
const setLastBackup = useSettingsStore((s) => s.setLastBackup);
const clearGoogleSession = useSettingsStore((s) => s.clearGoogleSession);

const [isConnecting, setIsConnecting] = useState(false);
const [isUploading, setIsUploading] = useState(false);
```

3. Handlers (cada uno ≤20 líneas):

```typescript
const handleConnect = useCallback(async () => {
  setIsConnecting(true);
  try {
    const session = await drive.signIn();
    if (session) setGoogleEmail(session.email);
  } catch (err) {
    console.error('[handleConnect]', err);
    const alert = err instanceof drive.DriveError ? err.alert : ALERT_DRIVE_GENERIC;
    setTimeout(() => Alert.alert(alert.title, alert.message), 0); // Pitfall #3 (Android)
  } finally {
    setIsConnecting(false);
  }
}, [setGoogleEmail]);

const performBackup = useCallback(async () => {
  setIsUploading(true);
  try {
    const result = await drive.uploadBackup();
    setLastBackup(new Date().toISOString(), result.fileId);
    // D-13: Alert refleja si reemplazó el backup del día o si fue uno nuevo
    const successAlert = result.overwrote
      ? ALERT_DRIVE_BACKUP_REPLACED
      : ALERT_DRIVE_BACKUP_SUCCESS;
    Alert.alert(successAlert.title, successAlert.message);
  } catch (err) {
    console.error('[performBackup]', err);
    const alert = err instanceof drive.DriveError ? err.alert : ALERT_DRIVE_GENERIC;
    Alert.alert(alert.title, alert.message);
  } finally {
    setIsUploading(false);
  }
}, [setLastBackup]);

const handleBackupNow = useCallback(async () => {
  // D-13: confirmar overwrite si ya hay backup de hoy ANTES de subir.
  try {
    const backups = await drive.listBackups();
    const today = new Date().toISOString().slice(0, 10);
    const hasToday = backups.some((b) => b.name.includes(today));
    if (hasToday) {
      const fechaHoy = formatDateEs(new Date());
      Alert.alert(
        ALERT_DRIVE_OVERWRITE_TODAY.title,
        `Ya hay un backup del ${fechaHoy} en tu Drive. Si continuás, el nuevo reemplazará al actual.`,
        [
          { text: ALERT_DRIVE_OVERWRITE_TODAY.cancel, style: 'cancel' },
          { text: ALERT_DRIVE_OVERWRITE_TODAY.confirm, onPress: performBackup },
        ],
      );
    } else {
      void performBackup();
    }
  } catch (err) {
    // Si listBackups falla, igual intentamos el backup — el service detecta duplicado y reemplaza
    void performBackup();
  }
}, [performBackup]);

const handleSignOutConfirm = useCallback(async () => {
  try {
    await drive.signOut();
    clearGoogleSession(); // D-11: preserva lastBackupAt + lastBackupFileId
  } catch (err) {
    console.error('[handleSignOut]', err);
    Alert.alert(ALERT_DRIVE_GENERIC.title, ALERT_DRIVE_GENERIC.message);
  }
}, [clearGoogleSession]);

const handleSignOut = useCallback(() => {
  Alert.alert(ALERT_DRIVE_SIGN_OUT.title, ALERT_DRIVE_SIGN_OUT.message, [
    { text: ALERT_DRIVE_SIGN_OUT.cancel, style: 'cancel' },
    { text: ALERT_DRIVE_SIGN_OUT.confirm, style: 'destructive', onPress: handleSignOutConfirm },
  ]);
}, [handleSignOutConfirm]);

// Restore navigation handler — la ruta existe desde día uno (registrada en App.tsx en este mismo Task).
const handleOpenRestore = useCallback(() => {
  // @ts-expect-error — RootStackParamList se augmenta en App.tsx; runtime OK
  navigation.navigate('RestoreFromDrive');
}, [navigation]);
```

NOTA: si después de actualizar `App.tsx` el `RootStackParamList` ya incluye `RestoreFromDrive: undefined`, eliminar el `@ts-expect-error`. Verificar tras editar App.tsx.

4. JSX section — insertar ENTRE "Personalización" y "Seguridad y Datos":

```jsx
{/* ─── Backup en la nube (Phase 3) ─── */}
<Text className={styles.driveSectionTitle}>Backup en la nube</Text>
<NotebookPaper>
  {googleEmail ? (
    <>
      <View className={styles.identityRow}>
        <CheckCircle2 size={18} color={colors.sage400} strokeWidth={iconDefaults.strokeWidth} />
        <Text className={styles.identityEmail} numberOfLines={1}>
          {googleEmail}
        </Text>
      </View>
      <Text className={styles.lastBackupCaption}>{formatRelativeBackup(lastBackupAt)}</Text>

      <Pressable
        className={styles.backupNowButton}
        onPress={handleBackupNow}
        disabled={isUploading}
        accessibilityRole="button"
        accessibilityLabel="Hacer backup ahora a Google Drive"
      >
        <CloudUpload size={18} color={colors.white} strokeWidth={iconDefaults.strokeWidth} />
        <Text className={styles.backupNowButtonText}>
          {isUploading ? 'Subiendo...' : 'Backup ahora'}
        </Text>
      </Pressable>

      <Pressable
        className={styles.restoreFromDriveButton}
        onPress={handleOpenRestore}
        accessibilityRole="button"
        accessibilityLabel="Restaurar desde Google Drive"
      >
        <CloudDownload size={18} color={colors.amber600} strokeWidth={iconDefaults.strokeWidth} />
        <Text className={styles.restoreFromDriveButtonText}>Restaurar desde Drive</Text>
      </Pressable>

      <Pressable
        className={styles.signOutButton}
        onPress={handleSignOut}
        hitSlop={{ top: 8, bottom: 8, left: 8, right: 8 }}
        accessibilityRole="button"
        accessibilityLabel="Cerrar sesión de Google"
      >
        <LogOut size={14} color={colors.amber700} strokeWidth={iconDefaults.strokeWidth} />
        <Text className={styles.signOutText}>Cerrar sesión</Text>
      </Pressable>
    </>
  ) : (
    <>
      <Pressable
        className={styles.connectButton}
        onPress={handleConnect}
        disabled={isConnecting}
        accessibilityRole="button"
        accessibilityLabel="Conectar con Google"
      >
        {isConnecting ? (
          <ActivityIndicator size="small" color={colors.white} />
        ) : (
          <Cloud size={18} color={colors.white} strokeWidth={iconDefaults.strokeWidth} />
        )}
        <Text className={styles.connectButtonText}>
          {isConnecting ? 'Conectando...' : 'Conectar con Google'}
        </Text>
      </Pressable>
      <Text className={styles.driveDescription}>
        Guardá un respaldo en tu Google Drive y restauralo desde cualquier dispositivo.
      </Text>
    </>
  )}
</NotebookPaper>
```

5. Renderizar `<LoadingOverlay visible={isUploading} message="Subiendo a Drive..." />` al final del JSX root del screen (DESPUÉS del último elemento, mismo nivel que el ScrollView), para que esté siempre disponible para overlay.

Constraints (CLAUDE.md):
- Componentes funcionales con hooks ✓
- Sin inline styling — usar `className` ✓
- Funciones ≤20 líneas (cada handler) ✓ — si supera, extraer helper
- JSDoc en español en módulos nuevos ✓
- Constantes en `config/constants.ts`, NO hardcoded strings (ZERO ocurrencias de "Próximamente" o equivalentes en SettingsScreen.tsx ni en RestoreFromDriveScreen.tsx)
- `formatDateEs` / `formatRelativeBackup` SIEMPRE importados desde `../utils/dateFormat` — NO duplicarlas inline (Regla 3)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --testPathPattern="(driveBackupService|driveRetention|dateFormat|useSettingsStore.googleAuth)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/shared/LoadingOverlay.tsx` exists, exports `LoadingOverlay`, contains `<Modal`, `transparent`, `animationType="fade"`, `statusBarTranslucent`, `onRequestClose`
    - `wc -l src/components/shared/LoadingOverlay.tsx` outputs ≤35
    - File `src/screens/RestoreFromDriveScreen.tsx` exists and exports `RestoreFromDriveScreen`
    - File `src/screens/RestoreFromDriveScreen.styles.ts` exists and exports `styles` and `colors`
    - `grep "Cargando backups" src/screens/RestoreFromDriveScreen.tsx` matches (loading state operativo)
    - `grep "No hay backups todavía" src/screens/RestoreFromDriveScreen.tsx` matches (empty state operativo)
    - `grep "Próximamente" src/screens/RestoreFromDriveScreen.tsx` does NOT match (sin placeholders v1)
    - `grep "Próximamente" src/screens/SettingsScreen.tsx` does NOT match (NO placeholder en el handleOpenRestore)
    - `grep "drive\.listBackups" src/screens/RestoreFromDriveScreen.tsx` matches (scaffold realmente llama al service)
    - `grep "navigation.navigate('RestoreFromDrive')" src/screens/SettingsScreen.tsx` matches (real navigation, no Alert)
    - `grep "from '../utils/dateFormat'" src/screens/SettingsScreen.tsx` matches (sin duplicación de funciones de formato)
    - `grep -E "^function formatDateEs\(" src/screens/SettingsScreen.tsx` does NOT match (NO redeclaración local)
    - `grep -E "^function formatRelativeBackup\(" src/screens/SettingsScreen.tsx` does NOT match (NO redeclaración local)
    - `grep "RestoreFromDriveScreen" App.tsx` matches (import + Stack.Screen)
    - `grep "name=\"RestoreFromDrive\"" App.tsx` matches
    - `grep "Backup en la nube" src/screens/SettingsScreen.tsx` matches (UI-SPEC section title)
    - `grep "Conectar con Google" src/screens/SettingsScreen.tsx` matches (disconnected CTA copy)
    - `grep "Backup ahora" src/screens/SettingsScreen.tsx` matches (connected CTA copy)
    - `grep "Restaurar desde Drive" src/screens/SettingsScreen.tsx` matches (secondary CTA)
    - `grep "Cerrar sesión" src/screens/SettingsScreen.tsx` matches (tertiary)
    - `grep "drive\.signIn\|drive\.uploadBackup\|drive\.signOut\|drive\.listBackups" src/screens/SettingsScreen.tsx | wc -l` >= 4
    - `grep "<LoadingOverlay" src/screens/SettingsScreen.tsx` matches
    - `grep "ALERT_DRIVE_BACKUP_SUCCESS" src/screens/SettingsScreen.tsx` matches
    - `grep "ALERT_DRIVE_BACKUP_REPLACED" src/screens/SettingsScreen.tsx` matches (D-13 — caso PATCH)
    - `grep "result.overwrote" src/screens/SettingsScreen.tsx` matches (branching D-13)
    - `grep "ALERT_DRIVE_OVERWRITE_TODAY" src/screens/SettingsScreen.tsx` matches
    - `grep "ALERT_DRIVE_SIGN_OUT" src/screens/SettingsScreen.tsx` matches
    - `grep "clearGoogleSession" src/screens/SettingsScreen.tsx` matches (DRIVE-07 implemented)
    - `grep "setLastBackup" src/screens/SettingsScreen.tsx` matches (DRIVE-06)
    - `grep "setTimeout" src/screens/SettingsScreen.tsx` matches (Pitfall #3 — Android Alert)
    - `grep "style={{" src/screens/SettingsScreen.tsx` does NOT match (NO inline styling per CLAUDE.md)
    - `grep "style={{" src/screens/RestoreFromDriveScreen.tsx` does NOT match (NO inline styling)
    - Section "Backup en la nube" appears BEFORE "Seguridad y Datos" — verify with `grep -n "Backup en la nube\|Seguridad y Datos" src/screens/SettingsScreen.tsx | sort -t: -k2 -n` shows Drive section line < Seguridad line
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 (full suite — no regression)
  </acceptance_criteria>
  <done>LoadingOverlay creado y usado; sección Drive en Settings completa con todos los estados (disconnected/connecting/connected/uploading); Connect + Backup (con branching D-13 vía ALERT_DRIVE_BACKUP_REPLACED) + Sign-out implementados (DRIVE-01/02/03/06/07 cubiertos); RestoreFromDriveScreen scaffold operativo (loading + empty); ruta registrada en App.tsx; botón "Restaurar desde Drive" navega a la nueva pantalla desde día uno (sin Alert "Próximamente"); funciones de formato importadas desde dateFormat.ts (sin duplicación); sin regresiones; sin inline styling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ Drive REST API v3 | HTTPS only (RFC 2818). All requests carry `Authorization: Bearer <token>` from native SDK. Never embed token in URL. |
| App ↔ User payload | `buildBackupData()` reads from local SQLite (trusted). `parseAndValidate` already hardens against malformed JSON (Phase 2 D-04). |
| App ↔ Multipart body construction | String concatenation of metadata + content with boundary. Boundary is `cozy_boundary_${Date.now()}` — non-secret, non-controllable by user. |
| Test boundary | Tests mock SDK + fetch — no real Drive calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | Attacker spoofs Drive API host | mitigate | Hardcoded HTTPS URLs (`https://www.googleapis.com/...`). Cert pinning out of scope (RN+Expo managed; OS-level CA chain trusted). |
| T-03-02-02 | Tampering | Backup payload tampered in transit | mitigate | HTTPS provides integrity. Server validates JSON shape via `parseAndValidate` on download path (Phase 2 D-04 hardening). |
| T-03-02-03 | Repudiation | User claims "I never uploaded that backup" | accept | Single-user app; no audit trail required. Drive provides versioning (we overwrite via PATCH per D-12 — accepted tradeoff). |
| T-03-02-04 | Information Disclosure | Token logged in `console.error` | mitigate | Service uses `console.warn/error('[fnName]', err)` patterns; never interpolates `accessToken` directly. Code-review verifies. PR check: `grep accessToken src/services/driveBackupService.ts | grep -v "const \\|: string"` returns no log calls. |
| T-03-02-05 | Information Disclosure | Backup contains habit names + dates (PII low) — leaked if attacker gains Drive token | mitigate | Drive `appDataFolder` scope (D-02) — only this app sees these files. Token held in OS keychain (SDK). Encryption deferred to V2 (DRIVE-V2-03). |
| T-03-02-06 | Denial of Service | Pruning failure cascades to fail backup | mitigate | `pruneOldBackupsBestEffort` swallows all errors (Pitfall #8 + D-14). Test 6 verifies. |
| T-03-02-07 | Denial of Service | Slow Drive call freezes UI | mitigate | UI shows `LoadingOverlay` (D-23) with cancellable Alert if it errors. Long-running fetch is non-blocking JS. RN keeps UI responsive on JS thread. |
| T-03-02-08 | Elevation of Privilege | Token leaks to other apps via app-extension | accept | iOS Keychain group + Android SharedPreferences are app-private by default. SDK does not expose token storage to other apps. |
| T-03-02-09 | Information Disclosure | Multipart body contains user data + metadata in string concat — XSS-like injection? | mitigate | Filename uses `BACKUP_FILE_PREFIX + ISO_DATE + EXTENSION` — fully app-controlled, no user-input interpolated. Body `JSON.stringify` escapes correctly. No injection vector. |
| T-03-02-10 | Tampering | Pre-existing backup of today replaced without user awareness | mitigate | D-13 + UI: `handleBackupNow` checks for today's file via `listBackups`, shows `ALERT_DRIVE_OVERWRITE_TODAY` confirmation BEFORE proceeding. Test 4 in driveBackupService.test verifies PATCH semantic. |

No `high` severity threats introduced by this plan.
</threat_model>

<verification>
**Plan-level checks:**
1. `npx tsc --noEmit` exits 0.
2. `npm test` (full suite) exits 0.
3. `grep -c "FormData" src/services/driveBackupService.ts` outputs `0` (Pitfall #2 honored).
4. `grep -c "revokeAccess" src/services/driveBackupService.ts` outputs `0` (D-10 honored).
5. The PATCH metadata helper does NOT include `parents` (verified via test 4 in driveBackupService.test.ts).
6. `grep -c "any" src/services/driveBackupService.ts` outputs `0` (Phase 2 closure stays).
7. `wc -l src/components/shared/LoadingOverlay.tsx` outputs ≤35 (D-23 budget).
8. SettingsScreen Drive section visible on render with `googleEmail = null` shows "Conectar con Google" CTA; with `googleEmail = 'a@b.com'` shows identity row + "Backup ahora" CTA. (Manual UAT confirms; automated covered via grep on copy strings.)
9. UI-SPEC §"Color" rule honored: no new hex introduced; `grep -E '#[0-9a-f]{3,6}' src/screens/SettingsScreen.styles.ts | grep -v '/\\*'` produces 0 NEW hex (only existing pre-Phase-3 colors).
10. UI-SPEC §"Typography" rule honored: no new font sizes — all new style keys reference `text.body`, `text.caption`, `text.sectionTitle`, `button.primaryText`, `button.secondaryText`.
</verification>

<success_criteria>
- DRIVE-02: User taps "Backup ahora", `uploadBackup()` ejecuta multipart POST/PATCH y persiste fileId.
- DRIVE-03: Filename hardcoded como `cozyhabits-${YYYY-MM-DD}.json` (verified en test 1 + test 2 de driveBackupService).
- DRIVE-06: `setLastBackup(now, fileId)` se invoca tras success y la caption en Settings refleja la fecha.
- DRIVE-08: Errores HTTP mapean a 5 categorías + genérico vía `mapDriveError`; Alert muestra el copy correcto.
- D-12 + D-13: Si `listBackups` reporta archivo del día, se muestra `ALERT_DRIVE_OVERWRITE_TODAY` ANTES de hacer upload.
- D-14 + Pitfall #8: Pruning tras backup exitoso, errores swallowed.
- D-22: Sin auto-retry — Alert error muestra el message; user-driven retry implícito (re-tap del botón).
- D-23: `LoadingOverlay` visible durante upload; Modal bloqueante, captura back de Android.
- DRIVE-07 cubierto: sign-out con Alert de confirmación implementado en este plan también (no esperar a 03-03), llamando `drive.signOut()` + `clearGoogleSession()`. D-11 honored: lastBackup* preservados.
- All tasks autonomous from CLAUDE's perspective. Restore navigation hander queda como `Alert.alert('Próximamente')` placeholder hasta 03-03 — esto NO viola "no v1" porque el restore es un feature distinto que se completa íntegramente en 03-03 dentro del mismo phase.
</success_criteria>

<output>
After completion, create `.planning/phases/03-google-drive-backup/03-02-SUMMARY.md` with:
- Files created (`driveBackupService.ts`, `driveRetention.ts`, `LoadingOverlay.tsx` + 2 test files)
- Files modified (`backupService.ts` exports, `SettingsScreen.tsx` + styles)
- Service public API (signatures of `signIn/signOut/uploadBackup/listBackups/downloadBackup/mapDriveError`)
- Tests added (counts: retention 6+, service 11+, useSettingsStore from 03-01 5+)
- What plan 03-03 should consume: `drive.listBackups()` → DriveBackupFile[]; `drive.downloadBackup(fileId)` → string; `parseAndValidate` + `restoreData` from backupService; LoadingOverlay para mensaje "Restaurando datos..." y "Leyendo backup..."
</output>
