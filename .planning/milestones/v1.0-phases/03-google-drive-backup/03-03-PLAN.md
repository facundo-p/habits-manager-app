---
phase: 03-google-drive-backup
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/screens/RestoreFromDriveScreen.tsx
  - src/screens/RestoreFromDriveScreen.styles.ts
  - src/services/driveBackupService.ts
  - src/__tests__/driveBackupService.restore.test.ts
autonomous: true
requirements: [DRIVE-04, DRIVE-05, DRIVE-08]

must_haves:
  truths:
    - "El usuario puede tocar 'Restaurar desde Drive' en Settings y ver una pantalla con la lista de backups disponibles ordenados por más reciente primero (DRIVE-04 + D-15)"
    - "Cada item de la lista muestra fecha formateada en español ('27 abr 2026') + tamaño (DRIVE-04 + D-17)"
    - "Tap en un item muestra LoadingOverlay 'Leyendo backup...' mientras descarga + parsea (vía prepareRestore — UN SOLO download), luego abre Alert de confirmación con conteos por tabla (D-17 + D-18)"
    - "El Alert de confirmación es destructivo y muestra fecha + conteos + warning 'Esta acción no se puede deshacer' (D-18 + UI-SPEC §Restore confirmation)"
    - "Antes de aplicar el restore, applyRestore escribe un pre-restore safety cache en cacheDirectory (D-19); cleanup de cache anterior corre SÓLO al final tras restoreData exitoso (warning #9)"
    - "Tras restore exitoso, los stores se refrescan (fetchHabitsForDate + fetchLibrary) sin navegar (D-20)"
    - "Estados de la pantalla: loading inicial / empty (cero backups) / error (sin red, con Reintentar) / loaded (DRIVE-08 + UI-SPEC §RestoreFromDriveScreen)"
    - "Errores se muestran con Alert.alert mapeado a las 5 categorías + genérico"
    - "Si parseAndValidate falla, applyRestore NO se invoca; tampoco se escribe pre-restore cache ni se ejecuta cleanup (DB intacta — warning #9)"
    - "Las funciones formatDateEs / formatRelativeBackup / formatSize se IMPORTAN desde src/utils/dateFormat.ts; NO se redeclaran en RestoreFromDriveScreen (CLAUDE.md Regla 3)"
  artifacts:
    - path: "src/screens/RestoreFromDriveScreen.tsx"
      provides: "Expansión del scaffold (creado en 03-02): agrega FlatList loaded, error state con Reintentar, BackupRow + sub-componentes locales, preview vía prepareRestore, restore confirmation, refresh stores"
      contains: "Restaurar desde Drive"
    - path: "src/screens/RestoreFromDriveScreen.styles.ts"
      provides: "Sin cambios estructurales — los tokens necesarios ya existen tras 03-02 (loaded list keys + error keys + retry button). Verificar y agregar SÓLO si falta alguno."
      exports: ["styles", "colors"]
    - path: "src/services/driveBackupService.ts"
      provides: "Dos funciones públicas nuevas: prepareRestore(fileId) (download + parse + counts; sin tocar DB) y applyRestore(payload) (write pre-cache → restoreData → cleanup post-success). Reemplaza la API monolítica restoreFromBackup."
      exports: ["prepareRestore", "applyRestore", "RestorePayload"]
    - path: "src/__tests__/driveBackupService.restore.test.ts"
      provides: "Tests para prepareRestore (parse OK, parse fail) y applyRestore (cache write OK + DB OK + cleanup, cache write fail continúa, restoreData throws → cleanup NO corre)"
      contains: "prepareRestore"
  key_links:
    - from: "src/screens/RestoreFromDriveScreen.tsx"
      to: "src/services/driveBackupService.ts"
      via: "drive.listBackups() para FlatList; drive.prepareRestore(fileId) para preview con conteos; drive.applyRestore(payload) en confirm"
      pattern: "drive\\.(listBackups|prepareRestore|applyRestore)"
    - from: "src/services/driveBackupService.ts (prepareRestore)"
      to: "src/services/backupService.ts (parseAndValidate)"
      via: "named import promovido en 03-02"
      pattern: "parseAndValidate\\("
    - from: "src/services/driveBackupService.ts (applyRestore)"
      to: "src/services/backupService.ts (buildBackupData + restoreData)"
      via: "named imports promovidos en 03-02"
      pattern: "buildBackupData\\(|restoreData\\("
    - from: "src/services/driveBackupService.ts (applyRestore)"
      to: "expo-file-system/legacy"
      via: "writeAsStringAsync a ${cacheDirectory}cozyhabits-pre-restore-${ISO}.json ANTES de restoreData; readDirectoryAsync + deleteAsync DESPUÉS de restoreData success (D-19 + warning #9)"
      pattern: "cacheDirectory.*cozyhabits-pre-restore"
    - from: "src/screens/RestoreFromDriveScreen.tsx"
      to: "src/store/useHabitStore.ts"
      via: "fetchHabitsForDate + fetchLibrary post-restore (D-20)"
      pattern: "fetchHabitsForDate.*fetchLibrary|fetchLibrary.*fetchHabitsForDate"
    - from: "src/screens/RestoreFromDriveScreen.tsx"
      to: "src/utils/dateFormat.ts"
      via: "import { formatDateEs, formatSize } — sin duplicación con SettingsScreen (Regla 3)"
      pattern: "from '\\.\\./utils/dateFormat'"
---

<objective>
Cerrar el phase: EXPANDIR el scaffold de `RestoreFromDriveScreen` (creado en 03-02) con el flow completo de restauración, agregando dos funciones públicas nuevas a `driveBackupService` (`prepareRestore` + `applyRestore`) que separan download/parse/preview de la mutación de DB. Sin tocar `App.tsx` (ruta ya registrada en 03-02), sin tocar `SettingsScreen` (botón ya navega a la pantalla en 03-02).

Purpose: Cumple DRIVE-04 (listar + seleccionar), DRIVE-05 (full replace con confirmación) y cierra DRIVE-08 (errores en list + restore mapeados con UI consistente). Cierra el goal de phase: "el usuario puede restaurar desde cualquier dispositivo". El split prepareRestore/applyRestore evita el doble download que tenía la API monolítica anterior y deja el cleanup de pre-restore cache estrictamente post-success (warning #9).
Output: 1 screen EXPANDIDO (sin cambios de archivo nuevos: el scaffold + sus styles ya existen desde 03-02), 2 funciones públicas NUEVAS en driveBackupService (`prepareRestore`, `applyRestore`) con su tipo `RestorePayload`, 1 test file NUEVO con 6 tests cubriendo orden de operaciones, parse failure, cache write failure best-effort, y restoreData throw → cleanup no corre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-google-drive-backup/03-CONTEXT.md
@.planning/phases/03-google-drive-backup/03-RESEARCH.md
@.planning/phases/03-google-drive-backup/03-PATTERNS.md
@.planning/phases/03-google-drive-backup/03-UI-SPEC.md
@.planning/phases/03-google-drive-backup/03-VALIDATION.md
@.planning/phases/03-google-drive-backup/03-01-PLAN.md
@.planning/phases/03-google-drive-backup/03-02-PLAN.md
@.claude/CLAUDE.md
@src/screens/HabitLibraryScreen.tsx
@src/screens/HabitLibraryScreen.styles.ts
@src/components/layout/AppScreenHeader.tsx
@src/store/useHabitStore.ts

<interfaces>
<!-- Surface from 03-01 + 03-02 (post-revision) -->

driveBackupService public API (post 03-02 — pre this plan):
```typescript
export interface DriveBackupFile { id: string; name: string; size: string; createdTime: string; }
export class DriveError extends Error { alert: DriveAlert; }
export async function signIn(): Promise<{ email: string } | null>;
export async function signOut(): Promise<void>;
export async function uploadBackup(): Promise<UploadResult>;
export async function listBackups(): Promise<DriveBackupFile[]>;
export async function downloadBackup(fileId: string): Promise<string>;
export async function mapDriveError(err: unknown, response?: Response | null): Promise<DriveAlert>;
```

backupService named exports (post 03-02):
```typescript
export async function buildBackupData(): Promise<BackupData>;
export function parseAndValidate(json: string): BackupData;
export async function restoreData(data: BackupData): Promise<void>;
```

LoadingOverlay (post 03-02):
```typescript
import { LoadingOverlay } from '../components/shared/LoadingOverlay';
<LoadingOverlay visible={boolean} message={string} />
```

Date/size formatters (post 03-02 — IMPORTAR, NO redeclarar):
```typescript
// src/utils/dateFormat.ts
export function formatDateEs(d: Date): string;            // "27 abr 2026"
export function formatRelativeBackup(iso: string | null): string;
export function formatSize(sizeStr: string): string;      // "12 KB" / "1.5 MB" / ""
```

ALERT_* constants (post 03-01) usados por este plan:
- ALERT_DRIVE_RESTORE_CONFIRM (template — message construído en call site)
- ALERT_DRIVE_RESTORE_SUCCESS (template — message construído en call site)
- ALERT_DRIVE_NO_NETWORK / AUTH_EXPIRED / QUOTA / PERMISSION / GENERIC

useHabitStore (existente):
```typescript
fetchHabitsForDate(date: string): Promise<void>;
fetchLibrary(): Promise<void>;
```

expo-file-system/legacy (uso existente en backupService.ts):
```typescript
import * as FileSystem from 'expo-file-system/legacy';
FileSystem.cacheDirectory: string;
FileSystem.writeAsStringAsync(path, content, { encoding?: 'utf8' }): Promise<void>;
FileSystem.deleteAsync(path, { idempotent?: boolean }): Promise<void>;
FileSystem.readDirectoryAsync(dir): Promise<string[]>;
```

RestoreFromDriveScreen scaffold (creado en 03-02 — STATE ACTUAL antes de este plan):
```typescript
// Archivo existe; exporta RestoreFromDriveScreen.
// Hooks ya wireados: navigation, status state, files state, loadList useCallback, useEffect mount.
// Estados implementados: loading (ActivityIndicator + "Cargando backups..."), empty (CloudOff + "No hay backups todavía" + "Hacé tu primer backup desde Ajustes.").
// Estados pendientes (este plan los completa): error (con botón Reintentar), loaded (FlatList real con BackupRow + restore flow).
// Texto de placeholder "la lista detallada se habilita al completar la próxima tarea del phase"
// debe ELIMINARSE al expandir.
```

App.tsx (post 03-02): `<Stack.Screen name="RestoreFromDrive" component={RestoreFromDriveScreen} options={{ headerShown: false }} />` ya está registrado. NO modificar App.tsx en este plan.

SettingsScreen (post 03-02): `handleOpenRestore` ya invoca `navigation.navigate('RestoreFromDrive')` — sin Alert "Próximamente". NO modificar SettingsScreen en este plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add prepareRestore + applyRestore to driveBackupService (single-download split with post-success cleanup) + tests</name>
  <files>src/services/driveBackupService.ts, src/__tests__/driveBackupService.restore.test.ts</files>
  <read_first>
    - src/services/driveBackupService.ts (post 03-02 — verify exports + DriveError class + downloadBackup signature; identifica si `buildBackupData` ya está importado o no)
    - src/services/backupService.ts (post 03-02 — verify named exports buildBackupData, parseAndValidate, restoreData)
    - .planning/phases/03-google-drive-backup/03-CONTEXT.md D-19 (pre-restore cache spec) + D-20 (post-restore behaviour)
    - .planning/phases/03-google-drive-backup/03-RESEARCH.md §"Code Examples → Example 5" (download pattern verified)
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"Component Inventory > cleanup decision" (silent cleanup on next restore success)
    - src/__tests__/driveBackupService.test.ts (existing test setup with jest.doMock — replicar el patrón virtual: true)
  </read_first>
  <behavior>
    **Mock defaults (válidos para TODOS los tests vía beforeEach):**
    - `parseAndValidateMock.mockImplementation((json: string) => JSON.parse(json))` — passthrough JSON parse. Esto hace que cualquier test que NO sobreescriba `parseAndValidateMock` funcione automáticamente con el JSON de `global.fetch`. Tests específicos que necesitan otro comportamiento (e.g., parse failure) usan `mockImplementationOnce` localmente. Test 4 (cache write fails) es self-contained gracias a este default.
    - `restoreDataMock.mockResolvedValue(undefined)` — restore success por defecto.
    - `buildBackupDataMock.mockResolvedValue({ version: 1, exportedAt: 'snapshot', habits: [], performed_habits: [], mood_entries: [], daily_assignments: [] })` — snapshot vacío default.
    - `writeAsStringAsyncMock.mockResolvedValue(undefined)` — cache write success por defecto.
    - `readDirectoryAsyncMock.mockResolvedValue(['cozyhabits-pre-restore-2026-04-01-old.json', 'cozyhabits-pre-restore-2026-03-01-old.json', 'unrelated.json'])` — dos archivos pre-restore residuales + uno no relacionado.
    - `deleteAsyncMock.mockResolvedValue(undefined)` — cleanup success por defecto.
    - `global.fetch` retorna 200 con JSON válido cuyos arrays tengan length determinístico (e.g., habits: [{ id: '1' }]).

    **Tests:**
    - Test 1 (prepareRestore happy path): `prepareRestore(fileId)` llama a `downloadBackup(fileId)` UNA SOLA VEZ (verificable porque `downloadBackup` usa `global.fetch`), invoca `parseAndValidate` con el string descargado, y retorna `{ data, counts: { habits, performed_habits, mood_entries, daily_assignments }, exportedAt }` derivados de `data`. NO invoca `restoreData`. NO invoca `writeAsStringAsync`. NO invoca `readDirectoryAsync`/`deleteAsync`. La DB queda intacta.
    - Test 2 (prepareRestore parse fail): si `parseAndValidate` lanza, `prepareRestore` re-lanza `DriveError(ALERT_DRIVE_GENERIC)`. NO se llama `restoreData`. NO se escribe pre-restore cache. NO se corre cleanup. (UI-SPEC Risk Note #4 + warning #9)
    - Test 3 (applyRestore happy path — orden): `applyRestore(payload)` ejecuta exactamente este orden: (a) `buildBackupData` → (b) `writeAsStringAsync` (pre-restore cache) → (c) `restoreData(payload.data)` → (d) `readDirectoryAsync` + `deleteAsync` (cleanup de pre-restore-* anteriores). Verificación via `mock.invocationCallOrder` con asserts estrictos: cleanup_orden > restore_orden > write_orden > build_orden.
    - Test 4 (applyRestore — cache write best-effort): `writeAsStringAsync.mockRejectedValueOnce(...)` → `applyRestore` NO throw, `restoreData` SE LLAMA igual, cleanup post-success TAMBIÉN se ejecuta. (D-19: cache es red de seguridad opcional, no bloquea restore.) Este test es self-contained porque `parseAndValidateMock` ya tiene su default passthrough en `beforeEach` (ver Mock defaults arriba) y el payload pasado a `applyRestore` no requiere parse adicional.
    - Test 5 (applyRestore — restoreData throws → cleanup NO corre): `restoreDataMock.mockRejectedValueOnce(new Error('SQL boom'))` → `applyRestore` re-lanza el error envuelto en `DriveError(ALERT_DRIVE_GENERIC)`. `readDirectoryAsync` y `deleteAsync` (cleanup) NO se llaman. Razón: el cache previo debe sobrevivir para que el usuario pueda recuperar. (Warning #9)
    - Test 6 (applyRestore — cleanup target): cuando todo va bien, `deleteAsync` se llama con paths que terminan en `cozyhabits-pre-restore-*.json` para los archivos residuales. NO toca `unrelated.json`.
  </behavior>
  <action>
**Edit `src/services/driveBackupService.ts`:**

1. Confirmar imports en el top del archivo. Si `expo-file-system/legacy` ya está importado (no lo estaba antes de este plan), reusar; si no, agregar. Si `buildBackupData`/`parseAndValidate` ya están importados desde `./backupService` para upload/list, agregar `restoreData` al import existente:
```typescript
import * as FileSystem from 'expo-file-system/legacy';
import { buildBackupData, parseAndValidate, restoreData } from './backupService';
```

(El plan 03-02 ya importa `buildBackupData` y `parseAndValidate` para upload. Agregar `restoreData` al import existente; NO duplicar la línea.)

Si `BackupData` no estaba importado en el archivo antes de este plan, agregar el import del tipo (verificar primero con Read si lo exporta `./backupService` o vive en `../types`).

2. Agregar las nuevas funciones al final del archivo, después del bloque "Error mapping". Cada función exportada ≤25 líneas (CLAUDE.md Regla 3); si se acerca al límite, extraer helper privado.

```typescript
// ─── Restore (DRIVE-04 + DRIVE-05 + D-19) ───────────────────────────

/**
 * Conteos por tabla derivados del backup parseado. Se usan para construir el
 * Alert de confirmación (D-18) sin volver a parsear en la UI.
 */
export interface RestoreCounts {
  habits: number;
  performed_habits: number;
  mood_entries: number;
  daily_assignments: number;
}

/**
 * Resultado de prepareRestore: la data parseada + conteos. La UI muestra los
 * conteos en el Alert destructivo y, en confirm, pasa el MISMO payload a
 * applyRestore (evita un segundo download).
 */
export interface RestorePayload {
  data: BackupData;
  counts: RestoreCounts;
  /** ISO timestamp del backup tal cual lo serializa buildBackupData. */
  exportedAt: string;
}

/**
 * Descarga + parsea + valida el backup. NO toca DB, NO escribe cache, NO corre
 * cleanup. Si parseAndValidate falla, se re-lanza como DriveError(GENERIC) y
 * la DB queda intacta (UI-SPEC Risk Note #4 + warning #9).
 *
 * Llamado primero por la pantalla para mostrar el Alert de confirmación con
 * conteos derivados de `counts`.
 */
export async function prepareRestore(fileId: string): Promise<RestorePayload> {
  const json = await downloadBackup(fileId);
  let data: BackupData;
  try {
    data = parseAndValidate(json);
  } catch (err) {
    console.error('[prepareRestore] parseAndValidate failed', err);
    throw new DriveError(ALERT_DRIVE_GENERIC, err);
  }
  return {
    data,
    counts: {
      habits: data.habits.length,
      performed_habits: data.performed_habits.length,
      mood_entries: data.mood_entries.length,
      daily_assignments: data.daily_assignments.length,
    },
    exportedAt: data.exportedAt,
  };
}

/**
 * Aplica el restore. Orden estricto:
 *   1. writePreRestoreCache(buildBackupData())   — D-19 safety cache
 *   2. restoreData(payload.data)                 — mutar DB (atómico via withTransactionAsync)
 *   3. cleanupOldPreRestoreCache()               — SÓLO en éxito (warning #9)
 *
 * Si writePreRestoreCache falla, log y continuar (best-effort, D-19).
 * Si restoreData throws, re-lanzar como DriveError(GENERIC); cleanup NO corre
 * para que el cache previo sobreviva y el usuario pueda recuperar.
 */
export async function applyRestore(payload: RestorePayload): Promise<void> {
  await writePreRestoreCache(); // best-effort: nunca throws
  try {
    await restoreData(payload.data);
  } catch (err) {
    console.error('[applyRestore] restoreData threw — cache previo conservado', err);
    throw new DriveError(ALERT_DRIVE_GENERIC, err);
  }
  // Cleanup SÓLO post-success: si restoreData lanzó, no llegamos acá.
  await cleanupOldPreRestoreCache();
}

async function writePreRestoreCache(): Promise<void> {
  try {
    const snapshot = await buildBackupData();
    const iso = new Date().toISOString().replace(/[:.]/g, '-');
    const path = `${FileSystem.cacheDirectory}cozyhabits-pre-restore-${iso}.json`;
    await FileSystem.writeAsStringAsync(path, JSON.stringify(snapshot), { encoding: 'utf8' });
  } catch (err) {
    // D-19: pre-restore cache es red de seguridad. Si falla, NO bloquear restore.
    console.warn('[writePreRestoreCache] no se pudo escribir cache de seguridad', err);
  }
}

async function cleanupOldPreRestoreCache(): Promise<void> {
  try {
    const dir = FileSystem.cacheDirectory;
    if (!dir) return;
    const entries = await FileSystem.readDirectoryAsync(dir);
    for (const name of entries) {
      if (name.startsWith('cozyhabits-pre-restore-') && name.endsWith('.json')) {
        await FileSystem.deleteAsync(`${dir}${name}`, { idempotent: true });
      }
    }
  } catch (err) {
    console.warn('[cleanupOldPreRestoreCache] cleanup skipped', err);
  }
}
```

Notas:
- NO crear `restoreFromBackup` monolítica. La API expuesta para la pantalla son las dos funciones del split.
- `BackupData` debe estar disponible en el scope del archivo. Si no estaba importado antes (probable), agregar al import existente de `./backupService` (verificar con Read antes de modificar): si `BackupData` no es export ahí, agregarlo o importar desde `../types`. Verificar tras Read.
- Cada función exportada ≤25 líneas. `applyRestore` es ~12 líneas — OK.
- Logs siempre con prefijo `[fnName]` (PATTERNS).

**Create `src/__tests__/driveBackupService.restore.test.ts`:**

```typescript
/**
 * driveBackupService.restore.test.ts — Tests del flow de restore (Phase 3, plan 03-03).
 *
 * Mockea SDK + fetch + expo-file-system/legacy + backupService. Cubre:
 *   - prepareRestore: happy path (counts derivados de data) + parse failure → DriveError(GENERIC)
 *   - applyRestore: orden estricto (build → write → restore → cleanup)
 *   - applyRestore: cache write fail no aborta restore (best-effort, D-19)
 *   - applyRestore: restoreData throws → cleanup NO corre (cache previo conservado, warning #9)
 *   - applyRestore: cleanup borra sólo cozyhabits-pre-restore-*.json
 *
 * Convención de mocks: jest.doMock + virtual:true + require post-mock (replicar
 * patrón de driveBackupService.test.ts y speechRecognition.test.ts).
 */

const restoreDataMock = jest.fn();
const parseAndValidateMock = jest.fn();
const buildBackupDataMock = jest.fn();
const writeAsStringAsyncMock = jest.fn();
const readDirectoryAsyncMock = jest.fn();
const deleteAsyncMock = jest.fn();

jest.doMock('expo-file-system/legacy', () => ({
  cacheDirectory: 'file:///cache/',
  writeAsStringAsync: writeAsStringAsyncMock,
  readDirectoryAsync: readDirectoryAsyncMock,
  deleteAsync: deleteAsyncMock,
}));

jest.doMock(
  '@react-native-google-signin/google-signin',
  () => ({
    GoogleSignin: {
      configure: jest.fn(),
      signInSilently: jest.fn().mockResolvedValue({ type: 'success', data: { user: { email: 'a@b.com' } } }),
      getTokens: jest.fn().mockResolvedValue({ accessToken: 'fake_token' }),
    },
    statusCodes: { SIGN_IN_CANCELLED: 'SIGN_IN_CANCELLED' },
  }),
  { virtual: true },
);

jest.doMock('../services/backupService', () => ({
  buildBackupData: buildBackupDataMock,
  parseAndValidate: parseAndValidateMock,
  restoreData: restoreDataMock,
}));

const { prepareRestore, applyRestore, DriveError } = require('../services/driveBackupService');
const { ALERT_DRIVE_GENERIC } = require('../config/constants');

const SAMPLE_BACKUP = {
  version: 1,
  exportedAt: '2026-04-27T00:00:00Z',
  habits: [{ id: '1' }],
  performed_habits: [],
  mood_entries: [],
  daily_assignments: [],
};

describe('restore flow (prepareRestore + applyRestore)', () => {
  beforeEach(() => {
    jest.clearAllMocks();
    // ─── Mock defaults — todos los tests heredan estos comportamientos ──
    // parseAndValidate: passthrough JSON parse (Test 4 self-contained gracias a esto)
    parseAndValidateMock.mockImplementation((json: string) => JSON.parse(json));
    // restoreData: success por defecto
    restoreDataMock.mockResolvedValue(undefined);
    // buildBackupData: snapshot vacío
    buildBackupDataMock.mockResolvedValue({
      version: 1, exportedAt: 'snapshot', habits: [], performed_habits: [], mood_entries: [], daily_assignments: [],
    });
    // FS mocks: success
    writeAsStringAsyncMock.mockResolvedValue(undefined);
    readDirectoryAsyncMock.mockResolvedValue([
      'cozyhabits-pre-restore-2026-04-01-old.json',
      'cozyhabits-pre-restore-2026-03-01-old.json',
      'unrelated.json',
    ]);
    deleteAsyncMock.mockResolvedValue(undefined);
    // fetch para downloadBackup
    global.fetch = jest.fn().mockResolvedValue({
      ok: true,
      text: async () => JSON.stringify(SAMPLE_BACKUP),
    });
  });

  // ── prepareRestore ──────────────────────────────────────────────────

  test('prepareRestore: descarga UNA vez, parsea, devuelve counts; NO toca DB / cache / cleanup', async () => {
    const result = await prepareRestore('fileXYZ');

    // download exactly once
    expect((global.fetch as jest.Mock)).toHaveBeenCalledTimes(1);
    // counts derivados de data
    expect(result.counts).toEqual({ habits: 1, performed_habits: 0, mood_entries: 0, daily_assignments: 0 });
    expect(result.exportedAt).toBe('2026-04-27T00:00:00Z');
    expect(result.data.habits).toHaveLength(1);

    // No mutación
    expect(restoreDataMock).not.toHaveBeenCalled();
    expect(writeAsStringAsyncMock).not.toHaveBeenCalled();
    expect(readDirectoryAsyncMock).not.toHaveBeenCalled();
    expect(deleteAsyncMock).not.toHaveBeenCalled();
  });

  test('prepareRestore: parseAndValidate falla → DriveError(ALERT_DRIVE_GENERIC); cache + DB intactos', async () => {
    parseAndValidateMock.mockImplementationOnce(() => { throw new Error('Formato inválido'); });

    await expect(prepareRestore('fileXYZ')).rejects.toBeInstanceOf(DriveError);
    expect(restoreDataMock).not.toHaveBeenCalled();
    expect(writeAsStringAsyncMock).not.toHaveBeenCalled();   // pre-cache no se intenta
    expect(readDirectoryAsyncMock).not.toHaveBeenCalled();   // cleanup no corre
    expect(deleteAsyncMock).not.toHaveBeenCalled();
  });

  // ── applyRestore ────────────────────────────────────────────────────

  test('applyRestore: orden estricto build → write → restore → cleanup', async () => {
    const payload = {
      data: SAMPLE_BACKUP,
      counts: { habits: 1, performed_habits: 0, mood_entries: 0, daily_assignments: 0 },
      exportedAt: SAMPLE_BACKUP.exportedAt,
    };
    await applyRestore(payload);

    const buildOrder = (buildBackupDataMock as jest.Mock).mock.invocationCallOrder[0];
    const writeOrder = (writeAsStringAsyncMock as jest.Mock).mock.invocationCallOrder[0];
    const restoreOrder = (restoreDataMock as jest.Mock).mock.invocationCallOrder[0];
    const readDirOrder = (readDirectoryAsyncMock as jest.Mock).mock.invocationCallOrder[0];

    expect(buildOrder).toBeLessThan(writeOrder);     // build snapshot antes de write
    expect(writeOrder).toBeLessThan(restoreOrder);   // cache antes de DB
    expect(restoreOrder).toBeLessThan(readDirOrder); // cleanup DESPUÉS de DB success (warning #9)

    expect(restoreDataMock).toHaveBeenCalledTimes(1);
    expect(restoreDataMock).toHaveBeenCalledWith(SAMPLE_BACKUP);
  });

  test('applyRestore: cleanup borra sólo cozyhabits-pre-restore-*.json', async () => {
    const payload = {
      data: SAMPLE_BACKUP,
      counts: { habits: 1, performed_habits: 0, mood_entries: 0, daily_assignments: 0 },
      exportedAt: SAMPLE_BACKUP.exportedAt,
    };
    await applyRestore(payload);

    const deletePaths = (deleteAsyncMock as jest.Mock).mock.calls.map((c) => c[0]);
    expect(deletePaths).toEqual(expect.arrayContaining([
      'file:///cache/cozyhabits-pre-restore-2026-04-01-old.json',
      'file:///cache/cozyhabits-pre-restore-2026-03-01-old.json',
    ]));
    expect(deletePaths).not.toContain('file:///cache/unrelated.json');
  });

  test('applyRestore: cache write falla → restore CONTINÚA (best-effort, D-19)', async () => {
    // Self-contained: parseAndValidateMock default (passthrough) ya quedó configurado
    // en beforeEach. El payload pasa directo, no requiere parse adicional.
    writeAsStringAsyncMock.mockRejectedValueOnce(new Error('disk full'));
    const payload = {
      data: SAMPLE_BACKUP,
      counts: { habits: 1, performed_habits: 0, mood_entries: 0, daily_assignments: 0 },
      exportedAt: SAMPLE_BACKUP.exportedAt,
    };

    await expect(applyRestore(payload)).resolves.toBeUndefined();
    expect(restoreDataMock).toHaveBeenCalledTimes(1);
    // Cleanup post-success igual corre
    expect(readDirectoryAsyncMock).toHaveBeenCalledTimes(1);
  });

  test('applyRestore: restoreData throws → DriveError + cleanup NO corre (cache previo conservado)', async () => {
    restoreDataMock.mockRejectedValueOnce(new Error('SQL boom'));
    const payload = {
      data: SAMPLE_BACKUP,
      counts: { habits: 1, performed_habits: 0, mood_entries: 0, daily_assignments: 0 },
      exportedAt: SAMPLE_BACKUP.exportedAt,
    };

    await expect(applyRestore(payload)).rejects.toBeInstanceOf(DriveError);
    // Pre-cache write SÍ ocurrió (es lo que protege al usuario)
    expect(writeAsStringAsyncMock).toHaveBeenCalledTimes(1);
    // Cleanup NO corrió — el cache previo (y el que acabamos de escribir) sobreviven
    expect(readDirectoryAsyncMock).not.toHaveBeenCalled();
    expect(deleteAsyncMock).not.toHaveBeenCalled();
  });
});
```

Constraints:
- Sin `any`, sin `as` casts donde se pueda evitar.
- `console.warn/error` con prefijo `[fnName]`.
- Test file ≤230 líneas — si supera, OK siempre que cada describe block sea claro.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="driveBackupService.restore"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function prepareRestore" src/services/driveBackupService.ts` matches
    - `grep "export async function applyRestore" src/services/driveBackupService.ts` matches
    - `grep "export interface RestorePayload" src/services/driveBackupService.ts` matches
    - `grep "restoreFromBackup" src/services/driveBackupService.ts` does NOT match (API monolítica anterior eliminada/no introducida)
    - `grep "cozyhabits-pre-restore-" src/services/driveBackupService.ts` matches (D-19 file naming)
    - `grep -A 30 "export async function applyRestore" src/services/driveBackupService.ts` muestra `cleanupOldPreRestoreCache` AFTER `restoreData(` — cleanup post-success únicamente (warning #9)
    - `grep "FileSystem.writeAsStringAsync" src/services/driveBackupService.ts` matches
    - `grep "FileSystem.readDirectoryAsync" src/services/driveBackupService.ts` matches (cleanup)
    - `grep "FileSystem.deleteAsync" src/services/driveBackupService.ts` matches (cleanup)
    - `grep -E "from './backupService'" src/services/driveBackupService.ts` shows `parseAndValidate, restoreData` y `buildBackupData` importados (los 3 named en una sola línea o distribuidos)
    - `npx tsc --noEmit` exits 0
    - `npm test -- --testPathPattern="driveBackupService.restore"` exits 0 con 6 tests passing
    - `grep -c " any " src/services/driveBackupService.ts` outputs `0`
    - `grep -c " any " src/__tests__/driveBackupService.restore.test.ts` outputs `0`
  </acceptance_criteria>
  <done>prepareRestore + applyRestore exportadas con split single-download; cleanup estrictamente post-success; tests cubren orden, parse fail, cache fail best-effort, restoreData throw → cleanup no corre; sin `any`; sin regresiones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand RestoreFromDriveScreen scaffold (created in 03-02) with error state, loaded FlatList, preview vía prepareRestore, restore confirmation</name>
  <files>src/screens/RestoreFromDriveScreen.tsx, src/screens/RestoreFromDriveScreen.styles.ts</files>
  <read_first>
    - src/screens/RestoreFromDriveScreen.tsx (post 03-02 — SCAFFOLD ACTUAL: ya tiene navigation hook, status state, files state, loadList useCallback + useEffect mount, estados loading + empty operativos, placeholder Text para `error|loaded` que este plan ELIMINA al expandir. Imports actuales: React/useEffect/useState/useCallback, View/Text/ActivityIndicator, useNavigation, CloudOff, AppScreenHeader, drive (namespace), styles + colors, iconDefaults.)
    - src/screens/RestoreFromDriveScreen.styles.ts (post 03-02 — verifica que ya existan: itemRow, itemPrimary, itemCaption, separator, errorContainer, errorHeading, errorBody, errorRetryButton, errorRetryButtonText. Si todos existen, NO modificar este archivo.)
    - src/screens/HabitLibraryScreen.tsx (analog list screen con sub-componentes HabitRow/Separator/EmptyList — patrón a clonar para BackupRow)
    - src/components/layout/AppScreenHeader.tsx (header API: title, onBack, showSettings — ya wireado en el scaffold)
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"3. RestoreFromDriveScreen" (states + layout exact)
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"Copywriting Contract" (exact strings: "No se pudo cargar la lista", "Verificá tu conexión e intentá de nuevo.", "Reintentar", "Leyendo backup...", "Restaurando datos...")
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"4. Restore confirmation" (Alert template con conteos)
    - .planning/phases/03-google-drive-backup/03-UI-SPEC.md §"Risk Notes" (no overlay durante list fetch — D-24; counts deben venir de data parseada — Risk #4, ahora cubierto vía prepareRestore)
    - .planning/phases/03-google-drive-backup/03-CONTEXT.md D-15, D-17, D-18, D-20, D-24, D-25
    - .planning/phases/03-google-drive-backup/03-PATTERNS.md §"src/screens/RestoreFromDriveScreen.tsx"
    - src/utils/dateFormat.ts (post 03-02 — IMPORTAR formatDateEs y formatSize; NUNCA redeclarar)
    - src/store/useHabitStore.ts (verifica fetchHabitsForDate + fetchLibrary)
    - src/services/driveBackupService.ts (post Task 1 — verifica prepareRestore, applyRestore, RestorePayload)
  </read_first>
  <behavior>
    - **NO se crean archivos nuevos** — sólo se EXPANDE el scaffold existente. El header, los estados loading + empty, el state machine `status` y el `loadList` ya están operativos desde 03-02; este plan agrega el resto.
    - **Eliminar el placeholder** del scaffold: el bloque `{(status === 'error' || status === 'loaded') && (<View>...la lista detallada se habilita al completar la próxima tarea...</View>)}` debe desaparecer y ser reemplazado por las dos ramas reales (error con Reintentar, loaded con FlatList).
    - **Loaded state**: FlatList ordenada desc por createdTime (D-15) — el service ya devuelve ordenada, no re-sort en cliente; cada row muestra `formatDateEs(new Date(file.createdTime))` + `formatSize(file.size)` (importadas de `../utils/dateFormat`) + ChevronRight.
    - **Error state**: WifiOff icon + heading "No se pudo cargar la lista" + body "Verificá tu conexión e intentá de nuevo." + primary button "Reintentar" que llama `loadList()` (D-25).
    - **Tap en item → preview**: muestra `<LoadingOverlay visible message="Leyendo backup..." />`, llama `drive.prepareRestore(file.id)` (UN SOLO download), oculta overlay, abre Alert de confirmación construido a partir de `payload.counts` y `payload.exportedAt`.
    - **Restore confirm Alert**: title `ALERT_DRIVE_RESTORE_CONFIRM.title` ("¿Restaurar este backup?"), message construido inline:
      `"Vas a restaurar el backup del {fechaLabel} ({habits} hábitos, {performed_habits} completados, {mood_entries} moods, {daily_assignments} assignments).\n\nEsto reemplazará todos tus datos actuales. Esta acción no se puede deshacer."`
      Botones: `[Cancelar (cancel), Restaurar (destructive — onPress: () => void performRestore(payload))]`.
    - **performRestore(payload)**: muestra `<LoadingOverlay visible message="Restaurando datos..." />`, llama `drive.applyRestore(payload)` (cleanup + write + restoreData ocurren acá adentro), refresca stores con `Promise.all([fetchHabitsForDate(today), fetchLibrary()])`, oculta overlay, muestra `Alert.alert(ALERT_DRIVE_RESTORE_SUCCESS.title, "Tus datos fueron restaurados desde el backup del {fechaLabel}. Tus datos previos quedaron respaldados en el dispositivo por si querés revertir.")`. NO navegar — D-20.
    - **Errores en preview o restore**: catch bloque → `Alert.alert(alert.title, alert.message)` donde `alert = err instanceof DriveError ? err.alert : ALERT_DRIVE_GENERIC`.
    - **Risk Note #4**: parse fail dentro de `prepareRestore` → DriveError(GENERIC) ya generado por el servicio; el catch del screen lo muestra. La DB queda intacta porque applyRestore no se llamó.
    - **Sin overlay durante list fetch (D-24)**: la pantalla SÓLO muestra LoadingOverlay durante preview + restore, nunca durante listBackups. (Loading state inicial usa ActivityIndicator inline — ya estaba en scaffold.)
    - **Sin duplicación**: `formatDateEs` y `formatSize` SE IMPORTAN de `../utils/dateFormat`. NO redeclarar `MONTHS_ES` ni ninguna de esas funciones en el screen.
  </behavior>
  <action>
**Edit `src/screens/RestoreFromDriveScreen.styles.ts`:**

VERIFICAR primero (Read) que las siguientes keys existan post 03-02: `itemRow`, `itemPrimary`, `itemCaption`, `separator`, `errorContainer`, `errorHeading`, `errorBody`, `errorRetryButton`, `errorRetryButtonText`. Según 03-02 plan todas se crearon en el scaffold. Si todas están → NO modificar este archivo. Si falta alguna → agregarla siguiendo la convención del archivo (string Tailwind con tokens reusados de `ui.styles`). NO crear nuevos colors. NO inventar nuevos tokens.

**Edit `src/screens/RestoreFromDriveScreen.tsx` (EXPANDIR — no recrear):**

1. **Ampliar imports** (mantener los del scaffold + agregar los nuevos):
```typescript
import React, { useCallback, useEffect, useState } from 'react';
import { View, Text, FlatList, Pressable, ActivityIndicator, Alert } from 'react-native';
import { useNavigation } from '@react-navigation/native';
import { FileText, ChevronRight, CloudOff, WifiOff } from 'lucide-react-native';
import { AppScreenHeader } from '../components/layout/AppScreenHeader';
import { LoadingOverlay } from '../components/shared/LoadingOverlay';
import * as drive from '../services/driveBackupService';
import { useHabitStore } from '../store/useHabitStore';
import { formatDateEs, formatSize } from '../utils/dateFormat';
import {
  ALERT_DRIVE_RESTORE_CONFIRM,
  ALERT_DRIVE_RESTORE_SUCCESS,
  ALERT_DRIVE_GENERIC,
} from '../config/constants';
import { styles, colors } from './RestoreFromDriveScreen.styles';
import { iconDefaults } from '../styles/ui.styles';
```

NO redeclarar `MONTHS_ES`, `formatDateEs`, `formatSize` localmente. Si el scaffold tenía un `formatDateEs` local (improbable porque 03-02 ya importa de dateFormat.ts en SettingsScreen — verificar), eliminarlo.

2. **Agregar tipos locales** (junto al `Status` que ya existe):
```typescript
type OverlayMsg = null | 'Leyendo backup...' | 'Restaurando datos...';
```

3. **Agregar sub-componentes locales** (cada uno ≤20 líneas):

```typescript
interface RowProps { file: drive.DriveBackupFile; onPress: () => void }
function BackupRow({ file, onPress }: RowProps) {
  const dateLabel = formatDateEs(new Date(file.createdTime));
  const sizeLabel = formatSize(file.size);
  return (
    <Pressable
      className={styles.itemRow}
      onPress={onPress}
      accessibilityRole="button"
      accessibilityLabel={`Restaurar backup del ${dateLabel}, ${sizeLabel}`}
    >
      <FileText size={22} color={colors.amber600} strokeWidth={iconDefaults.strokeWidth} />
      <View className="flex-1">
        <Text className={styles.itemPrimary}>{dateLabel}</Text>
        <Text className={styles.itemCaption}>{sizeLabel}</Text>
      </View>
      <ChevronRight size={18} color={colors.amber400} strokeWidth={iconDefaults.strokeWidth} />
    </Pressable>
  );
}

function Separator() { return <View className={styles.separator} />; }

function ErrorState({ onRetry }: { onRetry: () => void }) {
  return (
    <View className={styles.errorContainer}>
      <WifiOff size={48} color={colors.rose400} strokeWidth={iconDefaults.strokeWidth} />
      <Text className={styles.errorHeading}>No se pudo cargar la lista</Text>
      <Text className={styles.errorBody}>Verificá tu conexión e intentá de nuevo.</Text>
      <Pressable
        className={styles.errorRetryButton}
        onPress={onRetry}
        accessibilityRole="button"
        accessibilityLabel="Reintentar carga de backups"
      >
        <Text className={styles.errorRetryButtonText}>Reintentar</Text>
      </Pressable>
    </View>
  );
}
```

4. **Reemplazar el body del screen** preservando los hooks que ya estaban en el scaffold (navigation, status, files, loadList, useEffect mount). Estado final del componente:

```typescript
export function RestoreFromDriveScreen() {
  const navigation = useNavigation();
  const fetchHabitsForDate = useHabitStore((s) => s.fetchHabitsForDate);
  const fetchLibrary = useHabitStore((s) => s.fetchLibrary);

  const [status, setStatus] = useState<Status>('loading');
  const [files, setFiles] = useState<drive.DriveBackupFile[]>([]);
  const [overlayMsg, setOverlayMsg] = useState<OverlayMsg>(null);

  const loadList = useCallback(async () => {
    setStatus('loading');
    try {
      const result = await drive.listBackups();
      setFiles(result);
      setStatus(result.length === 0 ? 'empty' : 'loaded');
    } catch (err) {
      console.error('[RestoreFromDriveScreen.loadList]', err);
      setStatus('error');
    }
  }, []);

  useEffect(() => { void loadList(); }, [loadList]);

  const showError = useCallback((err: unknown) => {
    const alert = err instanceof drive.DriveError ? err.alert : ALERT_DRIVE_GENERIC;
    Alert.alert(alert.title, alert.message);
  }, []);

  const performRestore = useCallback(async (payload: drive.RestorePayload) => {
    setOverlayMsg('Restaurando datos...');
    try {
      await drive.applyRestore(payload);
      const today = new Date().toISOString().slice(0, 10);
      await Promise.all([fetchHabitsForDate(today), fetchLibrary()]);
      const fechaLabel = formatDateEs(new Date(payload.exportedAt));
      Alert.alert(
        ALERT_DRIVE_RESTORE_SUCCESS.title,
        `Tus datos fueron restaurados desde el backup del ${fechaLabel}. Tus datos previos quedaron respaldados en el dispositivo por si querés revertir.`,
      );
    } catch (err) {
      console.error('[performRestore]', err);
      showError(err);
    } finally {
      setOverlayMsg(null);
    }
  }, [fetchHabitsForDate, fetchLibrary, showError]);

  const previewAndConfirm = useCallback(async (file: drive.DriveBackupFile) => {
    setOverlayMsg('Leyendo backup...');
    try {
      const payload = await drive.prepareRestore(file.id);
      setOverlayMsg(null);

      const fechaLabel = formatDateEs(new Date(payload.exportedAt));
      const message =
        `Vas a restaurar el backup del ${fechaLabel} (` +
        `${payload.counts.habits} hábitos, ` +
        `${payload.counts.performed_habits} completados, ` +
        `${payload.counts.mood_entries} moods, ` +
        `${payload.counts.daily_assignments} assignments).\n\n` +
        `Esto reemplazará todos tus datos actuales. Esta acción no se puede deshacer.`;
      Alert.alert(
        ALERT_DRIVE_RESTORE_CONFIRM.title,
        message,
        [
          { text: ALERT_DRIVE_RESTORE_CONFIRM.cancel, style: 'cancel' },
          { text: ALERT_DRIVE_RESTORE_CONFIRM.confirm, style: 'destructive', onPress: () => void performRestore(payload) },
        ],
      );
    } catch (err) {
      console.error('[previewAndConfirm]', err);
      setOverlayMsg(null);
      showError(err);
    }
  }, [performRestore, showError]);

  return (
    <View className={styles.container}>
      <AppScreenHeader title="Restaurar desde Drive" onBack={() => navigation.goBack()} showSettings={false} />
      {status === 'loading' && (
        <View className={styles.loading}>
          <ActivityIndicator size="large" color={colors.amber600} />
          <Text className={styles.loadingCaption}>Cargando backups...</Text>
        </View>
      )}
      {status === 'empty' && (
        <View className={styles.emptyContainer}>
          <CloudOff size={48} color={colors.amber400} strokeWidth={iconDefaults.strokeWidth} />
          <Text className={styles.emptyHeading}>No hay backups todavía</Text>
          <Text className={styles.emptyBody}>Hacé tu primer backup desde Ajustes.</Text>
        </View>
      )}
      {status === 'error' && <ErrorState onRetry={() => void loadList()} />}
      {status === 'loaded' && (
        <FlatList
          data={files}
          keyExtractor={(item) => item.id}
          renderItem={({ item }) => <BackupRow file={item} onPress={() => void previewAndConfirm(item)} />}
          ItemSeparatorComponent={Separator}
        />
      )}
      <LoadingOverlay visible={overlayMsg !== null} message={overlayMsg ?? ''} />
    </View>
  );
}
```

**ELIMINAR del scaffold el bloque placeholder**:
```jsx
{/* Estados `error` y `loaded` (FlatList + preview + restore) — plan 03-03 los expande. */}
{(status === 'error' || status === 'loaded') && (
  <View className={styles.emptyContainer}>
    <Text className={styles.emptyBody}>
      {files.length} backup(s) detectados — la lista detallada se habilita al completar la próxima tarea del phase.
    </Text>
  </View>
)}
```

Constraints (CLAUDE.md):
- Componentes funcionales con hooks ✓
- Sub-componentes (BackupRow, Separator, ErrorState) ≤20 líneas cada uno ✓
- Handlers (`performRestore`, `previewAndConfirm`, `showError`, `loadList`) ≤25 líneas cada uno ✓
- Sin `any`, sin `as` casts ✓
- Sin inline styling (`style={{}}`) — usar `className` ✓
- Strings literales del UI-SPEC verbatim (voseo argentino) ✓
- `formatDateEs` y `formatSize` IMPORTADOS de `../utils/dateFormat`, nunca redeclarados ✓
- `prepareRestore` + `applyRestore` cubren el split — la pantalla no llama `downloadBackup` ni `parseAndValidate` directamente ✓
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - File `src/screens/RestoreFromDriveScreen.tsx` exists and exports `RestoreFromDriveScreen` (ya existía desde 03-02; verificar que el contenido se haya expandido)
    - `grep "drive\\.prepareRestore" src/screens/RestoreFromDriveScreen.tsx` matches (preview vía split)
    - `grep "drive\\.applyRestore" src/screens/RestoreFromDriveScreen.tsx` matches (mutation vía split)
    - `grep "drive\\.downloadBackup\|parseAndValidate" src/screens/RestoreFromDriveScreen.tsx` does NOT match (la pantalla NO llama download/parse directamente — se delega al service)
    - `grep "drive\\.listBackups" src/screens/RestoreFromDriveScreen.tsx` matches (load list)
    - `grep "from '../utils/dateFormat'" src/screens/RestoreFromDriveScreen.tsx` matches (sin duplicación de utilidades)
    - `grep -E "^const MONTHS_ES" src/screens/RestoreFromDriveScreen.tsx` does NOT match (NO redeclaración local)
    - `grep -E "^function formatDateEs\(" src/screens/RestoreFromDriveScreen.tsx` does NOT match (NO redeclaración local)
    - `grep -E "^function formatSize\(" src/screens/RestoreFromDriveScreen.tsx` does NOT match (NO redeclaración local)
    - `grep "<FlatList" src/screens/RestoreFromDriveScreen.tsx` matches (loaded state implementado)
    - `grep "WifiOff" src/screens/RestoreFromDriveScreen.tsx` matches (error state implementado)
    - `grep "No se pudo cargar la lista" src/screens/RestoreFromDriveScreen.tsx` matches (UI-SPEC error heading verbatim)
    - `grep "Verificá tu conexión e intentá de nuevo\\." src/screens/RestoreFromDriveScreen.tsx` matches (UI-SPEC error body verbatim)
    - `grep "Reintentar" src/screens/RestoreFromDriveScreen.tsx` matches (retry button)
    - `grep "Leyendo backup\\.\\.\\." src/screens/RestoreFromDriveScreen.tsx` matches (overlay preview)
    - `grep "Restaurando datos\\.\\.\\." src/screens/RestoreFromDriveScreen.tsx` matches (overlay restore)
    - `grep "Vas a restaurar el backup del" src/screens/RestoreFromDriveScreen.tsx` matches (confirm Alert message verbatim del UI-SPEC, conteos ordenados: hábitos, completados, moods, assignments)
    - `grep "Tus datos previos quedaron respaldados en el dispositivo" src/screens/RestoreFromDriveScreen.tsx` matches (success Alert verbatim del UI-SPEC)
    - `grep "fetchHabitsForDate.*fetchLibrary\|Promise\\.all.*fetchHabitsForDate" src/screens/RestoreFromDriveScreen.tsx` matches (D-20 refresh stores)
    - `grep "<LoadingOverlay" src/screens/RestoreFromDriveScreen.tsx` matches
    - `grep "ALERT_DRIVE_RESTORE_CONFIRM" src/screens/RestoreFromDriveScreen.tsx` matches
    - `grep "ALERT_DRIVE_RESTORE_SUCCESS" src/screens/RestoreFromDriveScreen.tsx` matches
    - `grep "la lista detallada se habilita al completar la próxima tarea" src/screens/RestoreFromDriveScreen.tsx` does NOT match (placeholder del scaffold ELIMINADO)
    - `grep "style={{" src/screens/RestoreFromDriveScreen.tsx` does NOT match (no inline styling)
    - `grep -c " any " src/screens/RestoreFromDriveScreen.tsx` outputs `0`
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 (full suite — no regression)
  </acceptance_criteria>
  <done>RestoreFromDriveScreen expandido: error state con Reintentar, loaded con FlatList real (BackupRow + Separator), preview vía drive.prepareRestore (UN download), restore confirmation destructivo con conteos verbatim del UI-SPEC, applyRestore + refresh stores en confirm; placeholder del scaffold eliminado; sin duplicación de utilidades de fecha; sin inline styling; sin `any`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ Drive download endpoint | HTTPS GET con `Authorization: Bearer`. Body es JSON crudo del backup — el atacante en posición de cuenta-tomada podría haber subido un JSON corrupto/malicioso. |
| Backup JSON ↔ DB | `parseAndValidate` (Phase 2) valida shape, tipos, IDs de áreas válidas. Si parse falla en `prepareRestore`, NO se invoca applyRestore → NO se toca DB (Risk Note #4 + warning #9). |
| Pre-restore cache file | App-private filesystem (`FileSystem.cacheDirectory`). Persistido sin encriptación — mismo trust boundary que SQLite local. |
| Stores tras restore | `fetchHabitsForDate` + `fetchLibrary` re-leen de SQLite — fuente de verdad post-restore. No state stale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | Backup JSON malicioso (atacante en cuenta del usuario) inyecta IDs inválidos / shapes corruptos | mitigate | `parseAndValidate` (Phase 2 D-04, hardened sin `as Partial`) valida cada array y filtra IDs inválidos. Si shape es incorrecto, throws → `prepareRestore` envuelve en `DriveError(ALERT_DRIVE_GENERIC)` → applyRestore nunca se invoca → DB intacta. Tests 1-2 cubren ambos paths. |
| T-03-03-02 | Information Disclosure | Pre-restore cache contiene PII y queda accesible si el dispositivo está rooteado | accept | App-private filesystem ya es el límite de confianza para SQLite. Cache file usa la misma protección. Encriptación es V2 (DRIVE-V2-03). |
| T-03-03-03 | Denial of Service | Backup gigante (>10MB) bloquea UI mientras descarga | accept | RESEARCH §Standard Stack confirma backups <1MB típicamente. UI muestra LoadingOverlay durante "Leyendo backup..." (D-23). Si el backup fuera anormalmente grande, el overlay sigue visible — UX degradada pero sin crash. |
| T-03-03-04 | Tampering | Restore aplica datos sin que el usuario lo confirme | mitigate | Restore confirm Alert es `style: 'destructive'` con copy explícita ("Esta acción no se puede deshacer"). Solo el botón destructivo dispara `performRestore`. Tests + UI-SPEC §Restore confirmation enforce. |
| T-03-03-05 | Repudiation | Usuario dice "no autoricé el restore" | accept | Confirm Alert + pre-restore safety cache (D-19) cubren el caso. Single-user app, no audit log requerido. |
| T-03-03-06 | Denial of Service | restoreData parcial → DB en estado inconsistente, cleanup borra el cache de seguridad | mitigate | `restoreData` (Phase 2) usa `withTransactionAsync` — atómico, rollback en error. Si lanza, `applyRestore` NO ejecuta cleanup (warning #9): el pre-restore cache que acabamos de escribir + cualquier cache previo sobreviven para recovery manual. Test 5 lo verifica. |
| T-03-03-07 | Information Disclosure | Pre-restore cache acumula indefinidamente y revela meses de actividad histórica | mitigate | Cleanup post-success del UI-SPEC §Component Inventory: deleteAsync de todos los `cozyhabits-pre-restore-*.json` SÓLO cuando restoreData tuvo éxito. Test 4 + Test 6 verifican. Cache crece como máximo 1 archivo por restore exitoso intermedio. |
| T-03-03-08 | Spoofing | Atacante navega directo a RestoreFromDriveScreen sin estar autenticado | mitigate | Navigation desde Settings sólo es accesible cuando `googleEmail !== null` (UI-SPEC: el botón "Restaurar desde Drive" sólo aparece en estado conectado). Acceso directo via deep link N/A — el screen llama `listBackups` que requiere token; sin token, falla con `ALERT_DRIVE_AUTH_EXPIRED`. Defensive but adequate. |

No `high` severity threats introduced by this plan.
</threat_model>

<verification>
**Plan-level checks:**
1. `npx tsc --noEmit` exits 0.
2. `npm test` (full suite incluyendo `driveBackupService.restore`) exits 0.
3. `grep "restoreFromBackup" src/services/driveBackupService.ts` does NOT match (API monolítica eliminada/no introducida; sólo `prepareRestore` + `applyRestore`).
4. `grep -c "any" src/screens/RestoreFromDriveScreen.tsx` outputs `0`.
5. `grep -c "any" src/services/driveBackupService.ts` outputs `0`.
6. UI-SPEC §"Restore confirmation" message format honored — verify via `grep "Vas a restaurar el backup del" src/screens/RestoreFromDriveScreen.tsx` matches AND counts ordered: hábitos, completados, moods, assignments.
7. Loading overlay messages match UI-SPEC: "Leyendo backup..." y "Restaurando datos..." ambos presentes en RestoreFromDriveScreen.tsx.
8. Restore success message matches UI-SPEC §Success Alert post restore: contains "Tus datos previos quedaron respaldados en el dispositivo".
9. Cleanup ordering en applyRestore: `grep -A 30 "export async function applyRestore" src/services/driveBackupService.ts` muestra `cleanupOldPreRestoreCache` AFTER `restoreData(` — no antes (warning #9).
10. Sin duplicación de helpers: `grep -E "^(const MONTHS_ES|function formatDateEs|function formatSize)" src/screens/RestoreFromDriveScreen.tsx` returns 0 lines (todo importado de dateFormat.ts).
11. App.tsx + SettingsScreen.tsx NO modificados por este plan: `git diff --name-only` (al final) NO debe incluir esos archivos.
</verification>

<success_criteria>
- DRIVE-04: usuario ve lista de backups ordenada desc por createdTime con fecha + tamaño.
- DRIVE-05: tap → `prepareRestore` (single download) → preview con conteos → Alert destructivo → en confirm `applyRestore` (write cache → restoreData → cleanup post-success). Stores refrescados sin navegación adicional.
- DRIVE-08 cierre: error en list, en download/parse, en restore — todos mapean a las 5 categorías + genérico vía Alert nativo.
- D-15 honrado: lista desc por createdTime (service ordena server-side; screen no re-ordena).
- D-17 honrado: item muestra fecha "27 abr 2026" + tamaño "12 KB" usando utilidades importadas; conteos NO se cargan en la lista (sólo en preview).
- D-18 honrado: confirm Alert con conteos por tabla y warning destructivo verbatim de UI-SPEC.
- D-19 honrado: pre-restore cache escrito antes de restoreData; orden estricto enforced por test 3.
- D-20 honrado: post-restore exitoso muestra Alert + refresca stores; no navegar.
- D-22 honrado: sin auto-retry — error muestra Alert; user re-tap.
- D-23 honrado: LoadingOverlay durante "Leyendo backup..." y "Restaurando datos..." (no durante list fetch — D-24).
- D-24 honrado: list fetch usa spinner inline (LoadingState desde scaffold) — NO overlay.
- D-25 honrado: fetch error muestra error state con botón "Reintentar".
- Risk Note #4 honrado: parse fallido → Alert generic, NO se toca DB; cubierto por test 2 (parseAndValidate fail).
- Warning #9 honrado: cleanup de pre-restore cache corre SÓLO post-success de restoreData; cubierto por test 5 (restoreData throws → cleanup NO se llama).
- Warning #7 honrado: API split en `prepareRestore` + `applyRestore`, single download verificado por test 1.
- CLAUDE.md Regla 3 honrada: `formatDateEs`/`formatSize` importadas, no duplicadas.
- Phase goal CLOSED: el usuario puede backup + restore desde cualquier dispositivo, con control explícito y errores accionables.
</success_criteria>

<output>
After completion, create `.planning/phases/03-google-drive-backup/03-03-SUMMARY.md` with:
- Files created (`driveBackupService.restore.test.ts`)
- Files modified (`driveBackupService.ts` +prepareRestore +applyRestore +RestorePayload, `RestoreFromDriveScreen.tsx` expandido desde scaffold, `RestoreFromDriveScreen.styles.ts` sólo si faltó alguna key)
- API split rationale: `prepareRestore` (download + parse + counts, no DB) + `applyRestore` (cache → mutate → cleanup post-success). Cierra warning #7 (single download) + warning #9 (cleanup ordering).
- Final test counts across the phase (los tests por archivo y el total).
- Coverage de los 8 DRIVE-XX requirements (DRIVE-01..DRIVE-08) y dónde se satisface cada uno.
- Pending UAT items from VALIDATION.md (manual-only verifications: real OAuth, real upload visible in Drive web, real round-trip restore, error on no-network in EAS build).
- Note on DRIVE-01 + DRIVE-07 divergence vs REQUIREMENTS.md literal text (decisions D-01/D-02/D-10 applied; document for VERIFICATION.md follow-up).
</output>
</content>
