---
phase: 04-habit-creation-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/services/migrations/migrationV1.ts
  - src/repositories/assignmentRepository.ts
  - src/services/db.ts
  - src/__tests__/migrationV1.test.ts
  - App.tsx
autonomous: true
requirements:
  - REQ-04-04
  - REQ-04-05
  - REQ-04-06
  - REQ-04-07
  - REQ-04-08
  - REQ-04-09
threat_model_summary: "Riesgo MEDIO: data-loss durante hard-delete (D-04). Mitigación: orden atómico D-08 (detect → assert no duplicates remain → CREATE INDEX → set user_version) en transacción única; rollback automático en throw. Backup JSON pre-migración del usuario es la red de seguridad explícita (CONTEXT §specifics). Riesgo BAJO de inconsistencia en boot: la migración corre dentro de initDatabase ANTES de checkAndBackfillHistory (Pitfall #4), garantizando que ningún write-path de assignments toque la tabla hasta post-migración."
notes: "Wave 2 — depende de 04-01 (consume createPreMigrationTestDatabase + seedDuplicates + insertTestPerformed para los tests de migración). Implementa la migración versionada via PRAGMA user_version + CTE+ROW_NUMBER dedup + partial UNIQUE INDEX. Toca App.tsx solo para verificar boot order (no agrega nueva lógica)."

must_haves:
  truths:
    - "Después del boot, PRAGMA user_version = 1"
    - "Después del boot, sqlite_master contiene 'idx_unique_habit_date' como UNIQUE partial index sobre daily_assignments"
    - "Si la DB pre-migración tenía duplicados (habit_id, date), post-migración hay exactamente 1 row por (habit_id, date)"
    - "El winner respeta D-03: completed > has_performed > más-antigua-por-rowid"
    - "Si la migración corre 2 veces, la segunda ejecución no hace nada (PRAGMA user_version ya = 1)"
    - "Si la migración throws en cualquier paso, la DB queda en versión 0 con rollback completo y el boot continúa (console.error, no throw)"
    - "checkAndBackfillHistory en App.tsx corre DESPUÉS de runMigrations (verificado por inspección de App.tsx)"
  artifacts:
    - path: "src/services/migrations/migrationV1.ts"
      provides: "runMigrations(db) + migrationV1_dedupeAndIndex(db)"
      exports: ["runMigrations"]
      contains: "PRAGMA user_version"
    - path: "src/repositories/assignmentRepository.ts"
      provides: "SQL_DEDUPE_VIA_CTE, SQL_FIND_DUPLICATES, SQL_CREATE_UNIQUE_INDEX, SQL_ASSERT_NO_DUPLICATES + thin wrappers findDuplicates(), countByHabitAndDate()"
      contains: "ROW_NUMBER() OVER"
    - path: "src/services/db.ts"
      provides: "initDatabase con runMigrations(db) en la secuencia"
      contains: "await runMigrations(db)"
    - path: "src/__tests__/migrationV1.test.ts"
      provides: "Tests REQ-04-04..09 (D-03 priority, atomicity, idempotency, silent failure, index defense)"
      contains: "describe('migration v1"
    - path: "App.tsx"
      provides: "Boot order verificado: initDatabase().then(checkAndBackfillHistory) garantiza que migrations corren primero"
      contains: "initDatabase()"
  key_links:
    - from: "src/services/db.ts initDatabase()"
      to: "src/services/migrations/migrationV1.ts runMigrations"
      via: "import { runMigrations }"
      pattern: "await runMigrations\\(db\\)"
    - from: "src/services/migrations/migrationV1.ts"
      to: "src/repositories/assignmentRepository.ts SQL constants"
      via: "import { SQL_DEDUPE_VIA_CTE, SQL_FIND_DUPLICATES, SQL_CREATE_UNIQUE_INDEX } from '../../repositories/assignmentRepository'"
      pattern: "SQL_DEDUPE_VIA_CTE|SQL_CREATE_UNIQUE_INDEX"
    - from: "App.tsx useEffect"
      to: "initDatabase → checkAndBackfillHistory chain"
      via: ".then() chain garantiza orden secuencial"
      pattern: "initDatabase\\(\\)\\s*\\.then\\(\\(\\)\\s*=>\\s*checkAndBackfillHistory"
---

<objective>
Implementar la migración v1 versionada que (a) detecta y borra duplicados existentes en `daily_assignments` con la heurística D-03, (b) crea el partial UNIQUE INDEX `idx_unique_habit_date` como defensa en profundidad, (c) marca `PRAGMA user_version = 1`. Todo dentro de una sola transacción atómica (D-08); en falla, console.error + continuar boot (D-06).

Purpose: Cierra REQ-04-04..REQ-04-09. Una vez aplicada, la DB del usuario queda alineada con el schema que `setup/testDatabase.ts:67-72` ya pre-modela; cualquier futura inserción duplicada es rechazada por el motor (defensa en profundidad). Los tests existentes en `dailyAssignments.test.ts:333-362` ("Prevención de duplicados") quedan como verificación viva post-migración.

Output: módulo nuevo de migraciones, SQL constants nuevas en el repo, integración en initDatabase, suite de tests cubriendo D-03 priority + atomicity + idempotency + silent failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-habit-creation-audit/04-CONTEXT.md
@.planning/phases/04-habit-creation-audit/04-RESEARCH.md
@.planning/phases/04-habit-creation-audit/04-PATTERNS.md
@.planning/phases/04-habit-creation-audit/04-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@src/services/db.ts
@src/repositories/assignmentRepository.ts
@src/repositories/backupRepository.ts
@src/__tests__/setup/testDatabase.ts
@src/__tests__/dailyAssignments.test.ts
@App.tsx

<interfaces>
<!-- Contratos consumidos por este plan (de plan 04-01 y del codebase). -->

From src/__tests__/setup/testDatabase.ts (post 04-01):
```typescript
export function createPreMigrationTestDatabase(): Database.Database;
export function seedDuplicates(
  db: Database.Database,
  habitId: string,
  date: string,
  opts?: { count?: number; withCompleted?: boolean; withPerformedLink?: boolean },
): string[];
export function insertTestPerformed(db: Database.Database, opts: TestPerformedOpts): void;
```

From src/services/db.ts (existing — secuencia init a extender):
```typescript
export async function initDatabase(): Promise<void> {
  const db = await getDatabase();
  await executeSchema(db);
  await migrateSchema(db);          // legacy ad-hoc (PRAGMA table_info)
  await sanitizeCategories(db);
  await seedHabits(db);
}
```

From App.tsx líneas 102-107 (boot chain — debe permanecer secuencial):
```typescript
useEffect(() => {
  initDatabase()
    .then(() => checkAndBackfillHistory())
    .then(() => console.log('DB inicializada y backfill completado'))
    .catch((err) => console.error('Error inicializando DB:', err));
}, []);
```

From src/repositories/assignmentRepository.ts (SQL constants pattern a extender):
```typescript
const SQL_BY_DATE = '...';
const SQL_INSERT = '...';
// ... más constantes
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agregar SQL constants y wrappers para dedup en assignmentRepository.ts</name>
  <files>src/repositories/assignmentRepository.ts</files>
  <read_first>
    - src/repositories/assignmentRepository.ts (archivo completo — extender, no reescribir)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Pattern 2: Single-Query Dedup via CTE + ROW_NUMBER" (incluye SQL completo a usar literalmente)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Code Examples" Example 1 (SQL_FIND_DUPLICATES)
    - .planning/phases/04-habit-creation-audit/04-PATTERNS.md §"src/repositories/assignmentRepository.ts (MOD)"
    - src/services/db.ts líneas 64-100 (schemas — para confirmar nombres de columnas: `daily_assignments.is_completed`, `performed_habits.habit_id`, `performed_habits.timestamp`)
  </read_first>
  <behavior>
    - Test 1: `findDuplicates()` sobre DB con 0 duplicados → `[]`
    - Test 2: `findDuplicates()` sobre DB con duplicados sembrados → array con `{habit_id, date, count}` para cada grupo
    - Test 3: `countByHabitAndDate('h1', '2026-03-15')` retorna número de rows con ese par
    - Las constantes SQL deben ser exportadas para que migrationV1 las consuma
  </behavior>
  <action>
**Extender** `src/repositories/assignmentRepository.ts`. Agregar al final de la sección "SQL Constants" (después del último const existente, antes del header "// ─── Consultas ───"):

```typescript
// ─── SQL Constants para dedup migration (REQ-04-04..09) ─────────────

/**
 * SQL_DEDUPE_VIA_CTE — single-statement DELETE que aplica D-03 priority.
 *
 * Heurística (RESEARCH §Pattern 2):
 *   1. is_completed DESC  (1 wins)
 *   2. has performed_habit linked DESC (1 wins)
 *   3. da.rowid ASC       (más antigua wins — rowid es monotónico)
 *
 * Usa ROW_NUMBER() OVER (PARTITION BY habit_id, date ORDER BY ...) para
 * etiquetar la fila ganadora con rn=1; borra las rn>1.
 *
 * Sólo aplica a habit_id IS NOT NULL (spontaneous se preservan, D-07).
 */
export const SQL_DEDUPE_VIA_CTE = `
DELETE FROM daily_assignments
WHERE id IN (
  SELECT id FROM (
    SELECT
      da.id,
      ROW_NUMBER() OVER (
        PARTITION BY da.habit_id, da.date
        ORDER BY
          da.is_completed DESC,
          (CASE WHEN EXISTS (
            SELECT 1 FROM performed_habits ph
            WHERE ph.habit_id = da.habit_id
              AND substr(ph.timestamp, 1, 10) = da.date
          ) THEN 1 ELSE 0 END) DESC,
          da.rowid ASC
      ) AS rn
    FROM daily_assignments da
    WHERE da.habit_id IS NOT NULL
  )
  WHERE rn > 1
)
`;

/** Encuentra grupos (habit_id, date) con count > 1 — SOLO regulares. */
export const SQL_FIND_DUPLICATES = `
  SELECT habit_id, date, COUNT(*) as count
  FROM daily_assignments
  WHERE habit_id IS NOT NULL
  GROUP BY habit_id, date
  HAVING COUNT(*) > 1
`;

/**
 * Invariante post-DELETE: cuenta cuántos grupos siguen duplicados.
 * Si > 0, la migración debe abortar (RESEARCH §Pitfall #1).
 */
export const SQL_ASSERT_NO_DUPLICATES = `
  SELECT COUNT(*) as count FROM (
    SELECT 1 FROM daily_assignments
    WHERE habit_id IS NOT NULL
    GROUP BY habit_id, date HAVING COUNT(*) > 1
  )
`;

/** Partial UNIQUE INDEX (D-07). IF NOT EXISTS para idempotencia. */
export const SQL_CREATE_UNIQUE_INDEX = `
  CREATE UNIQUE INDEX IF NOT EXISTS idx_unique_habit_date
  ON daily_assignments(habit_id, date)
  WHERE habit_id IS NOT NULL
`;

const SQL_COUNT_BY_HABIT_AND_DATE =
  'SELECT COUNT(*) as count FROM daily_assignments WHERE habit_id = ? AND date = ?';
```

Y en la sección "// ─── Consultas ───" agregar:

```typescript
/** Cuenta rows para un (habit_id, date). Útil para invariantes runtime (REQ-04-02). */
export async function countByHabitAndDate(
  habitId: string,
  datePrefix: string,
): Promise<number> {
  const db = await getDatabase();
  const row = await db.getFirstAsync<{ count: number }>(
    SQL_COUNT_BY_HABIT_AND_DATE,
    [habitId, datePrefix],
  );
  return row?.count ?? 0;
}

/** Lista grupos duplicados (habit_id, date, count). Util para invariantes y tests. */
export async function findDuplicates(): Promise<{ habit_id: string; date: string; count: number }[]> {
  const db = await getDatabase();
  return db.getAllAsync<{ habit_id: string; date: string; count: number }>(SQL_FIND_DUPLICATES);
}
```

**No tocar** funciones existentes (`findByDate`, `insert`, `setCompleted`, etc.) — solo extender. La verificación de las wrappers nuevas sucede en task 3 (vía tests de migration).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export const SQL_DEDUPE_VIA_CTE" src/repositories/assignmentRepository.ts` retorna 1 línea
    - `grep -E "^export const SQL_FIND_DUPLICATES" src/repositories/assignmentRepository.ts` retorna 1 línea
    - `grep -E "^export const SQL_CREATE_UNIQUE_INDEX" src/repositories/assignmentRepository.ts` retorna 1 línea
    - `grep -E "^export const SQL_ASSERT_NO_DUPLICATES" src/repositories/assignmentRepository.ts` retorna 1 línea
    - `grep "ROW_NUMBER() OVER" src/repositories/assignmentRepository.ts` retorna ≥1 línea
    - `grep "WHERE habit_id IS NOT NULL" src/repositories/assignmentRepository.ts | wc -l` ≥ 3 (presente en SQL_DEDUPE_VIA_CTE, SQL_FIND_DUPLICATES, SQL_CREATE_UNIQUE_INDEX)
    - `grep -E "^export async function findDuplicates" src/repositories/assignmentRepository.ts` retorna 1 línea
    - `grep -E "^export async function countByHabitAndDate" src/repositories/assignmentRepository.ts` retorna 1 línea
    - `git diff src/repositories/assignmentRepository.ts | grep -c '^-export'` retorna 0 (no se eliminan exports)
    - `npm test` exit 0 (suite preexistente sin regresiones)
  </acceptance_criteria>
  <done>SQL constants y wrappers nuevos exportados; suite verde; el plan task 2 puede importar `SQL_DEDUPE_VIA_CTE` y compañía.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear src/services/migrations/migrationV1.ts con runMigrations + migrationV1_dedupeAndIndex y tests completos</name>
  <files>src/services/migrations/migrationV1.ts, src/__tests__/migrationV1.test.ts</files>
  <read_first>
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Pattern 1: Versioned Migration via PRAGMA user_version" (incluye implementación de referencia)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Code Examples" Example 2 (migrationV1_dedupeAndIndex completo a copiar)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Pitfall #1: CREATE UNIQUE INDEX falla porque quedaron duplicados" (justifica el invariante post-DELETE)
    - .planning/phases/04-habit-creation-audit/04-CONTEXT.md §D-05/D-06/D-08 (versioned migration, silent failure, atomic order)
    - .planning/phases/04-habit-creation-audit/04-PATTERNS.md §"src/services/migrations/migrationV1.ts" (analog: migrateSchema en db.ts:130-140 + withTransactionAsync en backupRepository.ts:69-103)
    - src/services/db.ts (file header pattern + SQL constant placement; el archivo será modificado en task 4)
    - src/repositories/backupRepository.ts (líneas 63-104 — patrón withTransactionAsync analog)
    - src/__tests__/setup/testDatabase.ts (post 04-01, exports createPreMigrationTestDatabase, seedDuplicates, insertTestPerformed)
    - src/__tests__/dailyAssignments.test.ts líneas 54-90 (patrón de setup beforeEach/afterEach + countAssignments helper)
  </read_first>
  <behavior>
    - Test 1 (REQ-04-04 D-03 step 1): seedDuplicates con `withCompleted: true` → post-migration sobrevive la row con `is_completed=1`
    - Test 2 (REQ-04-04 D-03 step 2): seedDuplicates con `withCompleted: false, withPerformedLink: true` (insertando un performed_habit cuyo (habit_id, date) matchea) → sobrevive una row del grupo (no se garantiza cuál entre las que comparten matching, ya que ambas matchean el EXISTS — tie cae a step 3 rowid ASC); este test verifica que el `EXISTS` SQL clause realmente discrimina cuando UN grupo tiene performed y otro NO. Setup: dos grupos `(h1, '2026-03-15')` y `(h2, '2026-03-15')`, `withPerformedLink: true` solo para h1; verificar que en grupo h2 (sin performed) sobrevive la primera-rowid; verificar que en h1 sobrevive correctamente
    - Test 3 (REQ-04-04 D-03 step 3 oldest): seedDuplicates default (3 rows, `withCompleted: false`, `withPerformedLink: false`) → sobrevive la primera insertada (rowid más bajo)
    - Test 4 (REQ-04-05 INDEX creado): post-migration, `SELECT name FROM sqlite_master WHERE type='index' AND name='idx_unique_habit_date'` retorna 1 row
    - Test 5 (REQ-04-05 INDEX rechaza duplicado regular): después de migration, intentar insertar un duplicado regular throws con `/UNIQUE constraint/`
    - Test 6 (REQ-04-08 INDEX permite spontaneous): después de migration, insertar 2 spontaneous (habit_id=null) con misma fecha → ambos persisten
    - Test 7 (REQ-04-06 idempotency): correr `runMigrations` 2 veces; segunda ejecución no throws; user_version sigue 1; INDEX sigue presente
    - Test 8 (REQ-04-06 user_version): post-migration, `PRAGMA user_version` devuelve 1
    - Test 9 (REQ-04-07 silent failure): forzar throw (e.g., mockear el dedup query para arrojar error) → `console.error` se llama; user_version queda en 0; tabla `daily_assignments` mantiene los duplicados (rollback completo)
  </behavior>
  <action>
Crear directorio `src/services/migrations/` (si no existe) y archivo `src/services/migrations/migrationV1.ts`:

```typescript
/**
 * migrationV1.ts — Migración v1: dedupe + partial UNIQUE INDEX en daily_assignments.
 *
 * Implementa REQ-04-04..REQ-04-07 (Phase 4):
 *   - Detecta y borra duplicados (habit_id, date) según heurística D-03
 *   - Crea partial UNIQUE INDEX idx_unique_habit_date (D-07)
 *   - Marca PRAGMA user_version = 1 (D-05)
 *
 * Atomicidad (D-08): todo dentro de un único withTransactionAsync.
 * Rollback automático en throw. Falla silenciosa con console.error (D-06).
 *
 * Esta es la PRIMERA migración versionada del proyecto. Convención:
 *   - PRAGMA user_version es la fuente de verdad
 *   - cada nueva migración suma 1 al target_version
 *   - se ejecutan en orden creciente; si version >= target, skip
 */

import * as SQLite from 'expo-sqlite';
import {
  SQL_DEDUPE_VIA_CTE,
  SQL_ASSERT_NO_DUPLICATES,
  SQL_CREATE_UNIQUE_INDEX,
} from '../../repositories/assignmentRepository';

const TARGET_VERSION = 1;

/**
 * Punto de entrada del sistema de migraciones versionadas.
 * Lee PRAGMA user_version y dispatcha a las migraciones pendientes en orden.
 */
export async function runMigrations(db: SQLite.SQLiteDatabase): Promise<void> {
  const result = await db.getFirstAsync<{ user_version: number }>('PRAGMA user_version');
  const current = result?.user_version ?? 0;

  if (current < 1) {
    await migrationV1_dedupeAndIndex(db);
  }
  // Nota: cuando se agregue v2, agregar aquí: if (current < 2) await migrationV2_*(db)
}

/**
 * Migración v1: dedupe (habit_id, date) regulares + crea partial UNIQUE INDEX.
 *
 * Orden estricto (D-08):
 *   1. DELETE losers via CTE+ROW_NUMBER (single statement)
 *   2. SELECT COUNT(*) sobre grupos con count>1 — DEBE ser 0 (Pitfall #1)
 *   3. CREATE UNIQUE INDEX (IF NOT EXISTS — idempotente)
 *   4. PRAGMA user_version = 1
 *
 * Si cualquier paso falla, withTransactionAsync hace rollback completo.
 * console.error y continuar boot — D-06.
 */
async function migrationV1_dedupeAndIndex(db: SQLite.SQLiteDatabase): Promise<void> {
  try {
    await db.withTransactionAsync(async () => {
      await db.execAsync(SQL_DEDUPE_VIA_CTE);
      await assertNoDuplicatesRemain(db);
      await db.execAsync(SQL_CREATE_UNIQUE_INDEX);
      await db.execAsync(`PRAGMA user_version = ${TARGET_VERSION}`);
    });
  } catch (err) {
    // D-06: silent failure, NO bloquear boot. La DB queda en versión 0;
    // se reintentará en el próximo arranque.
    console.error('[migration v1] dedupe+index falló — la DB queda en versión 0', err);
  }
}

/**
 * Invariante post-DELETE (RESEARCH §Pitfall #1): si quedan grupos con count>1,
 * el CREATE INDEX siguiente fallaría — es preferible abortar acá con error
 * descriptivo para que el rollback preserve la DB y el log indique el problema.
 */
async function assertNoDuplicatesRemain(db: SQLite.SQLiteDatabase): Promise<void> {
  const remaining = await db.getFirstAsync<{ count: number }>(SQL_ASSERT_NO_DUPLICATES);
  const count = remaining?.count ?? 0;
  if (count > 0) {
    throw new Error(`migration v1: ${count} grupos siguen duplicados post-DELETE`);
  }
}
```

Crear `src/__tests__/migrationV1.test.ts`:

```typescript
import type Database from 'better-sqlite3';
import {
  createPreMigrationTestDatabase,
  resetTestDatabase,
  insertTestHabit,
  insertTestAssignment,
  insertTestPerformed,
  seedDuplicates,
} from './setup/testDatabase';
import { runMigrations } from '../services/migrations/migrationV1';
import { getDatabase } from '../services/db';

let db: Database.Database;

beforeEach(() => {
  db = createPreMigrationTestDatabase();
});

afterEach(() => {
  resetTestDatabase();
  jest.clearAllMocks();
});

function getUserVersion(): number {
  return (db.prepare('PRAGMA user_version').get() as { user_version: number }).user_version;
}

function hasUniqueIndex(): boolean {
  const row = db.prepare(
    "SELECT name FROM sqlite_master WHERE type='index' AND name='idx_unique_habit_date'",
  ).get();
  return row !== undefined;
}

function countAssignmentsForHabitDate(habitId: string, date: string): number {
  return (db.prepare(
    'SELECT COUNT(*) as c FROM daily_assignments WHERE habit_id = ? AND date = ?',
  ).get(habitId, date) as { c: number }).c;
}

function getSurvivor(habitId: string, date: string): { id: string; is_completed: number } {
  return db.prepare(
    'SELECT id, is_completed FROM daily_assignments WHERE habit_id = ? AND date = ?',
  ).get(habitId, date) as { id: string; is_completed: number };
}

describe('migration v1 — REQ-04-04..09', () => {
  test('REQ-04-04 D-03 step 1: completed wins', async () => {
    insertTestHabit(db, { id: 'h1', name: 'Meditar' });
    seedDuplicates(db, 'h1', '2026-03-15', { count: 2, withCompleted: true });
    expect(countAssignmentsForHabitDate('h1', '2026-03-15')).toBe(2);

    await runMigrations(await getDatabase());

    expect(countAssignmentsForHabitDate('h1', '2026-03-15')).toBe(1);
    expect(getSurvivor('h1', '2026-03-15').is_completed).toBe(1);
  });

  test('REQ-04-04 D-03 step 2: has_performed wins on completion tie', async () => {
    insertTestHabit(db, { id: 'h1', name: 'Meditar' });
    insertTestHabit(db, { id: 'h2', name: 'Caminar' });
    // h1: dos rows uncompleted, una con performed_link
    seedDuplicates(db, 'h1', '2026-03-15', { count: 2, withPerformedLink: true });
    // h2: dos rows uncompleted, sin performed_link
    seedDuplicates(db, 'h2', '2026-03-15', { count: 2, withPerformedLink: false });

    await runMigrations(await getDatabase());

    // Ambos grupos quedan en 1 row
    expect(countAssignmentsForHabitDate('h1', '2026-03-15')).toBe(1);
    expect(countAssignmentsForHabitDate('h2', '2026-03-15')).toBe(1);
    // Ambas siguen uncompleted (step 1 no discriminó)
    expect(getSurvivor('h1', '2026-03-15').is_completed).toBe(0);
    expect(getSurvivor('h2', '2026-03-15').is_completed).toBe(0);
  });

  test('REQ-04-04 D-03 step 3 oldest: rowid ASC en full tie', async () => {
    insertTestHabit(db, { id: 'h1', name: 'Meditar' });
    const ids = seedDuplicates(db, 'h1', '2026-03-15', { count: 3 });
    // ids[0] insertado primero → rowid más bajo

    await runMigrations(await getDatabase());

    expect(countAssignmentsForHabitDate('h1', '2026-03-15')).toBe(1);
    expect(getSurvivor('h1', '2026-03-15').id).toBe(ids[0]);
  });

  test('REQ-04-05: crea partial UNIQUE INDEX idx_unique_habit_date', async () => {
    expect(hasUniqueIndex()).toBe(false);
    await runMigrations(await getDatabase());
    expect(hasUniqueIndex()).toBe(true);
  });

  test('REQ-04-05/09: post-migration, INDEX rechaza duplicado regular', async () => {
    insertTestHabit(db, { id: 'h1', name: 'Meditar' });
    insertTestAssignment(db, { id: 'a1', habit_id: 'h1', date: '2026-03-15' });

    await runMigrations(await getDatabase());

    expect(() => {
      insertTestAssignment(db, { id: 'a2', habit_id: 'h1', date: '2026-03-15' });
    }).toThrow(/UNIQUE constraint/);
  });

  test('REQ-04-08: post-migration, INDEX permite múltiples spontaneous (habit_id=null)', async () => {
    await runMigrations(await getDatabase());

    insertTestAssignment(db, {
      id: 's1', habit_id: null, date: '2026-03-15', is_spontaneous: 1,
    });
    insertTestAssignment(db, {
      id: 's2', habit_id: null, date: '2026-03-15', is_spontaneous: 1,
    });
    const c = (db.prepare('SELECT COUNT(*) as c FROM daily_assignments WHERE date = ?').get('2026-03-15') as { c: number }).c;
    expect(c).toBe(2);
  });

  test('REQ-04-06 idempotency: PRAGMA user_version = 1 post-migration', async () => {
    expect(getUserVersion()).toBe(0);
    await runMigrations(await getDatabase());
    expect(getUserVersion()).toBe(1);
  });

  test('REQ-04-06 idempotency: 2x runMigrations no throws ni cambia estado', async () => {
    insertTestHabit(db, { id: 'h1', name: 'Meditar' });
    seedDuplicates(db, 'h1', '2026-03-15', { count: 2, withCompleted: true });

    await runMigrations(await getDatabase());
    expect(getUserVersion()).toBe(1);
    expect(countAssignmentsForHabitDate('h1', '2026-03-15')).toBe(1);

    await runMigrations(await getDatabase()); // segunda corrida
    expect(getUserVersion()).toBe(1);
    expect(countAssignmentsForHabitDate('h1', '2026-03-15')).toBe(1);
  });

  test('REQ-04-07 silent failure: error en migration → console.error + DB queda en v0', async () => {
    insertTestHabit(db, { id: 'h1', name: 'Meditar' });
    seedDuplicates(db, 'h1', '2026-03-15', { count: 2 });

    const errSpy = jest.spyOn(console, 'error').mockImplementation(() => undefined);

    // Forzar fallo: stubbear getFirstAsync del mock para retornar count > 0
    // tras el DELETE — simulamos que el invariante post-DELETE detecta inconsistencia.
    // Implementación concreta dependerá del shape del mock; alternativa:
    // mockear withTransactionAsync para que arroje.
    const realDb = await getDatabase();
    const original = realDb.withTransactionAsync.bind(realDb);
    (realDb as unknown as { withTransactionAsync: typeof original }).withTransactionAsync =
      jest.fn(async () => { throw new Error('forced failure'); });

    await runMigrations(realDb);

    expect(errSpy).toHaveBeenCalledWith(
      expect.stringContaining('[migration v1]'),
      expect.any(Error),
    );
    expect(getUserVersion()).toBe(0);
    expect(countAssignmentsForHabitDate('h1', '2026-03-15')).toBe(2); // duplicados intactos

    // Restore
    (realDb as unknown as { withTransactionAsync: typeof original }).withTransactionAsync = original;
    errSpy.mockRestore();
  });
});
```

**Notas importantes para el executor:**
1. El test "silent failure" depende de cómo está mockeado `expo-sqlite` en `__mocks__/expo-sqlite.ts`. Si el mock NO expone `withTransactionAsync` mutable, usar una alternativa: mockear el módulo `assignmentRepository` para que `SQL_DEDUPE_VIA_CTE` (al ejecutarse via execAsync) arroje. Si necesitas adaptar el test, mantén la verificación esencial: (a) `console.error` llamado con prefix `[migration v1]`, (b) user_version queda en 0, (c) data preservada.
2. NO instalar nuevas dependencias.
3. Si `getDatabase()` no resuelve en tests porque `setMockDatabase` no se invocó antes, asegurate de que el test usa `createPreMigrationTestDatabase()` en beforeEach (que SÍ llama `setMockDatabase`).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=migrationV1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/services/migrations/migrationV1.ts` (archivo existe)
    - `grep -E "^export async function runMigrations" src/services/migrations/migrationV1.ts` retorna 1 línea
    - `grep "withTransactionAsync" src/services/migrations/migrationV1.ts` retorna ≥1 línea
    - `grep "PRAGMA user_version" src/services/migrations/migrationV1.ts` retorna ≥2 líneas (read + write)
    - `grep "console.error\\('\\[migration v1\\]" src/services/migrations/migrationV1.ts` retorna 1 línea
    - `grep "TARGET_VERSION = 1" src/services/migrations/migrationV1.ts` retorna 1 línea
    - `grep -c "REQ-04-0" src/__tests__/migrationV1.test.ts` ≥ 9
    - `npm test -- --testPathPattern=migrationV1` exit 0; al menos 9 tests passed
    - Cada función ≤ 25 líneas (CONVENTIONS.md)
  </acceptance_criteria>
  <done>migrationV1 + tests committed; suite scoped GREEN; runMigrations es exportable e importable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integrar runMigrations en initDatabase + verificar boot order en App.tsx</name>
  <files>src/services/db.ts, App.tsx</files>
  <read_first>
    - src/services/db.ts (archivo completo — modificar initDatabase secuencia)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Pitfall #4: addAssignmentForHabit llamado durante boot ANTES de la migración"
    - .planning/phases/04-habit-creation-audit/04-CONTEXT.md §"D-05" (migración en initDatabase)
    - App.tsx (líneas 102-118 — useEffect chain con initDatabase y silentSignInIfPossible — verificar que no hay path donde un assignment write corra antes de initDatabase resolver)
    - .planning/phases/04-habit-creation-audit/04-PATTERNS.md §"src/services/db.ts (MOD)" (orden recomendado)
  </read_first>
  <behavior>
    - Después del cambio: en `initDatabase()`, runMigrations corre ENTRE `migrateSchema(db)` (legacy ad-hoc) y `sanitizeCategories(db)`
    - App.tsx: la chain `initDatabase().then(checkAndBackfillHistory)` se preserva tal cual; ningún store action puede invocarse antes de initDatabase resolver porque (a) los componentes que llaman `fetchHabitsForDate` se renderizan después del boot effect, (b) el silent sign-in effect (segundo useEffect) no toca daily_assignments
    - El segundo useEffect de App.tsx (`silentSignInIfPossible`) NO depende de initDatabase, por lo cual sigue corriendo en paralelo — esto es seguro porque no escribe daily_assignments
  </behavior>
  <action>
**Modificar** `src/services/db.ts`:

1. Agregar import al inicio (después del import de constants y parsing):
```typescript
import { runMigrations } from './migrations/migrationV1';
```

2. Modificar `initDatabase()` insertando `runMigrations(db)` ENTRE `migrateSchema(db)` y `sanitizeCategories(db)`:
```typescript
export async function initDatabase(): Promise<void> {
  const db = await getDatabase();
  await executeSchema(db);
  await migrateSchema(db);
  await runMigrations(db);          // ← REQ-04-06: versioned migrations (Phase 4)
  await sanitizeCategories(db);
  await seedHabits(db);
}
```

**Verificación de boot order en App.tsx (sin modificar lógica)**:

Leer App.tsx líneas 102-118 y CONFIRMAR:
- Existe exactamente una chain `initDatabase().then(() => checkAndBackfillHistory()).then(...).catch(...)` en el primer useEffect.
- El segundo useEffect (silentSignInIfPossible) NO importa ni llama a `addAssignmentForHabit`, `ensureAssignmentsForDate`, ni ninguna función de assignmentService.
- Si la verificación falla (alguien introdujo un path nuevo), agregar un comentario `// REQ-04-06: el orden initDatabase → checkAndBackfillHistory es crítico — no agregar writes a daily_assignments antes` en App.tsx línea 102, sin cambiar el código.
- Si la verificación pasa, agregar SOLO ese mismo comentario como documentación protectora antes de `initDatabase()`:

```typescript
useEffect(() => {
  // REQ-04-06: El orden initDatabase → checkAndBackfillHistory es crítico.
  // initDatabase corre runMigrations (PRAGMA user_version + UNIQUE INDEX).
  // No agregar writes a daily_assignments antes de que esta cadena resuelva.
  initDatabase()
    .then(() => checkAndBackfillHistory())
    .then(() => console.log('DB inicializada y backfill completado'))
    .catch((err) => console.error('Error inicializando DB:', err));
}, []);
```

**No** agregar lógica nueva en App.tsx; sólo el comentario documentativo y la verificación.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from './migrations/migrationV1'" src/services/db.ts` retorna 1 línea
    - `grep "await runMigrations(db)" src/services/db.ts` retorna 1 línea
    - El orden en initDatabase es: executeSchema → migrateSchema → runMigrations → sanitizeCategories → seedHabits (verificar con `grep -n -E "(executeSchema|migrateSchema|runMigrations|sanitizeCategories|seedHabits)\\(db\\)" src/services/db.ts` — los números de línea deben ser estrictamente crecientes en ese orden)
    - `grep "REQ-04-06" App.tsx` retorna ≥1 línea (comentario documentativo presente)
    - `grep -E "^\\s*initDatabase\\(\\)\\s*$" App.tsx` retorna 1 línea (chain preservada — no se duplicó ni rompió)
    - `grep "addAssignmentForHabit\\|ensureAssignmentsForDate" App.tsx` retorna 0 líneas (App.tsx no llama write-paths directamente — el único call que afecta daily_assignments es el `checkAndBackfillHistory` ya en su lugar)
    - `npm test` exit 0 (suite completa GREEN — migrations + integración + tests preexistentes)
  </acceptance_criteria>
  <done>initDatabase ejecuta runMigrations en el orden correcto; comentario documentativo en App.tsx; suite completa GREEN; ningún write-path bypassa la migración.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App boot ↔ SQLite DB | Migración modifica tabla del usuario; cualquier corrupción persiste en disco |
| performed_habits ↔ daily_assignments | El JOIN del CTE asume schema actual (timestamp 'YYYY-MM-DD HH:MM:SS') — un dato malformado podría inducir winner equivocado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering / Data Loss | migrationV1 hard-delete | mitigate | (a) D-08 atomic transaction → rollback completo en throw, (b) backup JSON pre-migración del usuario es la red de seguridad explícita, (c) invariante post-DELETE (assertNoDuplicatesRemain) impide CREATE INDEX si quedan duplicados, evitando estado intermedio |
| T-04-02-02 | Denial of Service | runMigrations en cada boot frio | mitigate | PRAGMA user_version short-circuit: si `current >= 1`, skip inmediatamente (un solo SELECT en el hot path post-primer-boot) |
| T-04-02-03 | Tampering | substr(timestamp, 1, 10) en CTE asume formato exacto | accept | RESEARCH §Assumption A3: si formato inesperado, paso 2 cae a paso 3 (oldest) — no rompe la migración, sólo elige peor heurística |
| T-04-02-04 | Repudiation | Falla silenciosa por D-06 | accept | console.error con prefix `[migration v1]` queda en logcat / Metro logs; el usuario notará duplicados persistentes y puede reportar |
| T-04-02-05 | Elevation of Privilege | n/a | accept | App local-first, single-user, sin escalation de privilegios posible |
</threat_model>

<verification>
- `npm test -- --testPathPattern='migrationV1|dailyAssignments'` (suite scoped — ~5s)
- `npm test` (full — debe ser GREEN)
- Manual smoke (post-execute, no en este plan): `npm run start`; observar logs de Metro: una sola línea `'DB inicializada y backfill completado'`; cero `[migration v1]` errors. Re-abrir app: idem (idempotencia).
</verification>

<success_criteria>
- [ ] `src/services/migrations/migrationV1.ts` existe con `runMigrations` + `migrationV1_dedupeAndIndex`
- [ ] SQL constants exportados desde assignmentRepository: SQL_DEDUPE_VIA_CTE, SQL_FIND_DUPLICATES, SQL_ASSERT_NO_DUPLICATES, SQL_CREATE_UNIQUE_INDEX
- [ ] `findDuplicates()` y `countByHabitAndDate()` exportados como named functions
- [ ] `initDatabase` ejecuta `runMigrations(db)` entre migrateSchema y sanitizeCategories
- [ ] App.tsx tiene comentario documentativo REQ-04-06 protegiendo el boot order
- [ ] Suite completa `npm test` GREEN (cubre REQ-04-04..09)
- [ ] Tests scoped: 9+ tests passed en migrationV1.test.ts
- [ ] Funciones nuevas ≤ 25 líneas (CONVENTIONS.md)
- [ ] Threat T-04-02-01 mitigado (atomicidad verificada por test "2x runMigrations")
</success_criteria>

<output>
After completion, create `.planning/phases/04-habit-creation-audit/04-02-SUMMARY.md` documentando:
- runMigrations() y firma de migrationV1_dedupeAndIndex
- SQL constants exportados (consumibles por futuros plans / migrations v2+)
- Resultados de los 9 tests de migration
- Cualquier cambio al schema de tests (mock de expo-sqlite, withTransactionAsync mutability)
- Notas sobre boot order verification en App.tsx
</output>
