---
phase: 04-habit-creation-audit
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/types/index.ts
  - src/repositories/assignmentRepository.ts
  - src/services/assignmentService.ts
  - src/__tests__/dailyAssignments.test.ts
autonomous: true
requirements:
  - REQ-04-01
  - REQ-04-02
  - REQ-04-10
  - REQ-04-11
threat_model_summary: "Riesgo MEDIO de UX: cambio del modelo de visibility weekly/monthly (D-01 Opción B) puede confundir al usuario si la propagación de completion entre rows del período falla. Mitigación: invariante runtime en dev (console.warn si findDuplicates encuentra >0 grupos) + tests específicos de propagación + el partial UNIQUE INDEX (REQ-04-05) bloquea inserts duplicados a nivel motor. Riesgo BAJO de performance: el read-time enrichment agrega 1 query por render de DailySheet — Pattern 3 con query agregada single-shot evita N+1."
notes: "Wave 3 — depende de 04-01 (consume getPeriodKey desde periodHelpers) y 04-02 (consume findDuplicates y el UNIQUE INDEX para que los tests de propagación funcionen contra DB con index). Implementa Opción B (RESEARCH §Pitfall #5 / Open Q1): una row por día por hábito weekly/monthly, completion propaga a TODAS las rows del período actual — minimiza cambio al schema y tests. Toca: (a) DailyItem expone isCompletedForPeriod; (b) getItemsForDate enriquece via single aggregated query; (c) completeAssignment + uncompleteAssignment + updateTodaySnapshotForHabit propagan a todas las rows del período actual cuando el hábito es weekly/monthly; (d) ensureAssignmentsForDate gana invariante runtime dev-only."

must_haves:
  truths:
    - "Hábito weekly visible TODOS los días de la semana ISO actual (DailyItem.isCompletedForPeriod refleja el estado del período)"
    - "Hábito monthly visible TODOS los días del mes calendario actual"
    - "Cuando el usuario completa un hábito weekly el miércoles, el resto de los días de la semana ISO actual ven `isCompletedForPeriod = true`"
    - "Cuando el usuario uncomplete un hábito weekly previamente completado, todas las rows del período actual vuelven a `is_completed = 0`"
    - "completeAssignment y uncompleteAssignment para frequency='daily' afectan SOLO la row del día (no propagación)"
    - "updateTodaySnapshotForHabit (D-01) propaga el snapshot a todas las rows uncompleted del período actual cuando el hábito es weekly/monthly"
    - "En __DEV__, ensureAssignmentsForDate post-inserción ejecuta findDuplicates y warn si encuentra >0 grupos"
  artifacts:
    - path: "src/types/index.ts"
      provides: "DailyItem extendido con isCompletedForPeriod: boolean"
      contains: "isCompletedForPeriod"
    - path: "src/repositories/assignmentRepository.ts"
      provides: "SQL_FIND_COMPLETED_HABITS_IN_RANGE + findCompletedHabitsInRange + setCompletedForHabitInRange + updateSnapshotForHabitInRange"
      contains: "findCompletedHabitsInRange"
    - path: "src/services/assignmentService.ts"
      provides: "getItemsForDate enriquece con isCompletedForPeriod; completeAssignment/uncompleteAssignment propagan a período; updateTodaySnapshotForHabit propaga; dev invariant en ensureAssignmentsForDate"
      contains: "isCompletedForPeriod"
    - path: "src/__tests__/dailyAssignments.test.ts"
      provides: "Tests REQ-04-10/11 para weekly/monthly visibility + propagación de completion + invariante dev"
      contains: "describe('Visibility weekly/monthly"
  key_links:
    - from: "src/services/assignmentService.ts getItemsForDate"
      to: "src/utils/periodHelpers.ts getPeriodKey"
      via: "import { getPeriodKey } from '../utils/periodHelpers'"
      pattern: "getPeriodKey\\("
    - from: "src/services/assignmentService.ts completeAssignment"
      to: "src/repositories/assignmentRepository.ts setCompletedForHabitInRange"
      via: "named import"
      pattern: "setCompletedForHabitInRange\\("
    - from: "src/services/assignmentService.ts ensureAssignmentsForDate dev invariant"
      to: "src/repositories/assignmentRepository.ts findDuplicates"
      via: "if (__DEV__) await assignmentRepo.findDuplicates()"
      pattern: "if \\(__DEV__\\)"
---

<objective>
Implementar el modelo de visibility de D-01 (Opción B: una row por día, completion propaga a todas las rows del período actual) en service-layer + repo SQL aggregated query. Cierra REQ-04-01, REQ-04-02, REQ-04-10, REQ-04-11.

Purpose: El usuario espera ver su hábito weekly toda la semana, marcarlo una vez, y que el resto de la semana lo muestre como "completado para este período". Hoy el hábito weekly aparece SOLO el día que su row fue creada (rollover lo materializa) y `is_completed` es por-row → si lo completa el miércoles, el jueves no lo ve completado. Este plan hace que (a) el rollover SIGUE creando una row por día (sin cambio de schema), pero (b) la lectura computa `isCompletedForPeriod` agregando sobre todas las rows del período, y (c) las mutaciones (toggle, edit) propagan el cambio.

Output: tipos extendidos, queries nuevas (single aggregated, no N+1), service-level orchestration, tests de propagación.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-habit-creation-audit/04-CONTEXT.md
@.planning/phases/04-habit-creation-audit/04-RESEARCH.md
@.planning/phases/04-habit-creation-audit/04-PATTERNS.md
@.planning/phases/04-habit-creation-audit/04-01-PLAN.md
@.planning/phases/04-habit-creation-audit/04-02-PLAN.md
@.planning/codebase/CONVENTIONS.md
@src/services/assignmentService.ts
@src/repositories/assignmentRepository.ts
@src/types/index.ts
@src/store/useHabitStore.ts
@src/__tests__/dailyAssignments.test.ts
@src/__tests__/setup/testDatabase.ts
@src/utils/periodHelpers.ts

<interfaces>
<!-- Contratos consumidos por este plan. -->

From src/utils/periodHelpers.ts (post 04-01):
```typescript
export type Frequency = 'daily' | 'weekly' | 'monthly';
export function getPeriodKey(datePrefix: string, frequency: Frequency): string;
export function getISOWeekKey(datePrefix: string): string;
export function getMonthKey(datePrefix: string): string;
```

From src/types/index.ts (DailyItem actual a extender):
```typescript
export interface DailyItem {
  assignmentId: string;
  habitId: string | null;
  name: string;
  points: number;
  categories: string;
  frequency: 'daily' | 'weekly' | 'monthly';
  isCompleted: boolean;
  isSpontaneous: boolean;
  performedHabitId: string | null;
}
```

From src/repositories/assignmentRepository.ts (post 04-02 — funciones existentes a extender):
```typescript
export async function findByDate(datePrefix: string): Promise<DailyAssignment[]>;
export async function findByHabitAndDate(habitId, datePrefix): Promise<DailyAssignment | null>;
export async function setCompleted(id, completed): Promise<void>;
export async function updateSnapshot(habitId, datePrefix, name, points, cats, freq): Promise<void>;
export async function findDuplicates(): Promise<{habit_id, date, count}[]>; // de 04-02
```

From src/services/assignmentService.ts (estructura actual):
```typescript
export async function getItemsForDate(datePrefix?): Promise<DailyItem[]>;
export async function completeAssignment(item: DailyItem, datePrefix?): Promise<string|null>;
export async function uncompleteAssignment(item: DailyItem, datePrefix?): Promise<void>;
export async function updateTodaySnapshotForHabit(habitId): Promise<void>;
export async function ensureAssignmentsForDate(datePrefix): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender DailyItem con isCompletedForPeriod + agregar 3 SQL constants y 3 wrappers en assignmentRepository</name>
  <files>src/types/index.ts, src/repositories/assignmentRepository.ts</files>
  <read_first>
    - src/types/index.ts (archivo completo — ubicar DailyItem)
    - src/repositories/assignmentRepository.ts (archivo completo, post 04-02 — extender SQL constants y wrappers existentes)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Pattern 3: Period-Aware Visibility (D-01)" (single aggregated query — evita N+1; Risk R3)
    - .planning/phases/04-habit-creation-audit/04-PATTERNS.md §"src/repositories/assignmentRepository.ts (MOD)"
    - src/services/db.ts SQL_CREATE_PERFORMED (líneas 64-74 — confirma columnas: habit_id, timestamp)
    - src/services/db.ts SQL_CREATE_ASSIGNMENTS (líneas 87-100 — confirma is_completed)
  </read_first>
  <behavior>
    - Test 1: `findCompletedHabitsInRange(['h1','h2'], '2026-03-09', '2026-03-15')` retorna lista de habit_ids con al menos una row `is_completed=1` en el rango (inclusive ambos extremos)
    - Test 2: `setCompletedForHabitInRange('h1', 1, '2026-03-09', '2026-03-15')` actualiza TODAS las rows con `(habit_id='h1', date BETWEEN ... AND ...)` a `is_completed=1`
    - Test 3: `setCompletedForHabitInRange('h1', 0, ...)` revierte a 0 todas las rows del rango
    - Test 4: `updateSnapshotForHabitInRange(...)` aplica el snapshot SOLO a las rows del rango con `is_completed=0` (preserva los snapshots de rows ya completadas)
    - Test 5: la query con `IN (?,?,?)` se construye dinámicamente según el largo del array (CONVENTIONS.md prohíbe interpolación → usar `.map(()=>'?').join(',')` y pasar el array como params)
  </behavior>
  <action>
**1. Modificar `src/types/index.ts`** — Encontrar la interface `DailyItem` y agregar el campo `isCompletedForPeriod`:

```typescript
export interface DailyItem {
  assignmentId: string;
  habitId: string | null;
  name: string;
  points: number;
  categories: string;
  frequency: 'daily' | 'weekly' | 'monthly';
  isCompleted: boolean;          // estado de la row del día actual
  isCompletedForPeriod: boolean; // REQ-04-10/11: true si HAY al menos 1 row con is_completed=1 en el período actual del item
  isSpontaneous: boolean;
  performedHabitId: string | null;
}
```

Para `daily` frequency: `isCompletedForPeriod === isCompleted` (mismo período = mismo día).
Para `weekly`/`monthly`: `isCompletedForPeriod` puede ser true aunque `isCompleted` (del día actual) sea false.

**2. Extender `src/repositories/assignmentRepository.ts`** agregando al final de la sección "SQL Constants para dedup migration":

```typescript
// ─── SQL Constants para visibility de período (REQ-04-10/11) ────────

const SQL_SET_COMPLETED_FOR_HABIT_IN_RANGE = `
  UPDATE daily_assignments
  SET is_completed = ?
  WHERE habit_id = ?
    AND date BETWEEN ? AND ?
`;

const SQL_UPDATE_SNAPSHOT_FOR_HABIT_IN_RANGE = `
  UPDATE daily_assignments
  SET snapshot_name = ?, snapshot_points = ?, snapshot_categories = ?, snapshot_frequency = ?
  WHERE habit_id = ?
    AND date BETWEEN ? AND ?
    AND is_completed = 0
`;

// SQL_FIND_COMPLETED_HABITS_IN_RANGE NO se define como const aquí porque
// el placeholder list es dinámico (?, ?, ?... según largo de habitIds).
// Se construye dentro de la wrapper.
```

Y al final de la sección "// ─── Mutaciones ───":

```typescript
/**
 * REQ-04-10/11: marca/desmarca is_completed para todas las rows
 * (habit_id, date BETWEEN start AND end). Soporta propagación de completion
 * a las rows del período actual de hábitos weekly/monthly.
 */
export async function setCompletedForHabitInRange(
  habitId: string,
  completed: number,
  startDate: string,
  endDate: string,
): Promise<void> {
  const db = await getDatabase();
  await db.runAsync(SQL_SET_COMPLETED_FOR_HABIT_IN_RANGE, [
    completed, habitId, startDate, endDate,
  ]);
}

/**
 * REQ-04-10/11: aplica snapshot a rows uncompleted en (habit_id, date BETWEEN ...).
 * Preserva snapshots de rows ya completadas (consistencia con updateSnapshot existente).
 */
export async function updateSnapshotForHabitInRange(
  habitId: string,
  startDate: string,
  endDate: string,
  snapshotName: string,
  snapshotPoints: number,
  snapshotCategories: string,
  snapshotFrequency: string,
): Promise<void> {
  const db = await getDatabase();
  await db.runAsync(SQL_UPDATE_SNAPSHOT_FOR_HABIT_IN_RANGE, [
    snapshotName, snapshotPoints, snapshotCategories, snapshotFrequency,
    habitId, startDate, endDate,
  ]);
}
```

Y en la sección "// ─── Consultas ───":

```typescript
/**
 * REQ-04-10/11: para una lista de habit_ids y un rango de fechas, retorna los
 * habit_ids que tienen AL MENOS UNA row con is_completed=1 en el rango.
 * Single aggregated query — evita N+1 (RESEARCH §Pattern 3, Risk R3).
 *
 * Si habitIds está vacío, retorna [] sin tocar la DB.
 */
export async function findCompletedHabitsInRange(
  habitIds: string[],
  startDate: string,
  endDate: string,
): Promise<string[]> {
  if (habitIds.length === 0) return [];
  const db = await getDatabase();
  const placeholders = habitIds.map(() => '?').join(',');
  const sql = `
    SELECT DISTINCT habit_id
    FROM daily_assignments
    WHERE habit_id IN (${placeholders})
      AND date BETWEEN ? AND ?
      AND is_completed = 1
  `;
  const rows = await db.getAllAsync<{ habit_id: string }>(
    sql,
    [...habitIds, startDate, endDate],
  );
  return rows.map((r) => r.habit_id);
}
```

**Tests inline en este task:** No, los tests específicos de estos wrappers se cubren en task 2 (donde se consumen) y task 3 (extensión de dailyAssignments.test.ts). Acá la verify es: la suite preexistente sigue GREEN tras agregar campos opcionales y wrappers nuevos.

**Cuidado con `DailyItem`**: el campo `isCompletedForPeriod` es REQUERIDO (no opcional) — si lo hago opcional, los consumidores podrían hacer `item.isCompletedForPeriod === undefined` y eso es ambiguo. El executor de task 2 garantizará que `enrichAssignments` siempre lo set. Si TS-strict rechaza por DailyItem usado en lugares todavía sin actualizar, el task 2 lo arreglará.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "isCompletedForPeriod: boolean" src/types/index.ts` retorna 1 línea
    - `grep -E "^export async function findCompletedHabitsInRange" src/repositories/assignmentRepository.ts` retorna 1 línea
    - `grep -E "^export async function setCompletedForHabitInRange" src/repositories/assignmentRepository.ts` retorna 1 línea
    - `grep -E "^export async function updateSnapshotForHabitInRange" src/repositories/assignmentRepository.ts` retorna 1 línea
    - `grep "habitIds.map(() => '?')" src/repositories/assignmentRepository.ts` retorna 1 línea (placeholder dinámico, sin interpolación)
    - `grep "BETWEEN ? AND ?" src/repositories/assignmentRepository.ts | wc -l` ≥ 2 (UPDATE rangos parametrizados)
    - `npm test` exit 0 — la suite puede romperse temporalmente si TS-strict detecta DailyItem sin isCompletedForPeriod en consumers; si esto pasa, el executor debe agregar el campo en `enrichAssignments` (al menos como `false` placeholder) en este mismo task como hotfix mínimo, dejando el cómputo real para task 2. Aceptable: la suite debe quedar GREEN tras este task.
  </acceptance_criteria>
  <done>DailyItem extendido + 3 SQL constants y 3 wrappers nuevos exportados + suite GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar visibility-aware enrichment + propagación de completion + dev invariant en assignmentService.ts</name>
  <files>src/services/assignmentService.ts</files>
  <read_first>
    - src/services/assignmentService.ts (archivo completo — extender funciones existentes y enrichAssignments)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Pattern 3: Period-Aware Visibility (D-01)" (lógica de read-time enrichment)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Code Examples" Example 3 (dev invariant warn)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Pitfall #5" (Opción B: row por día + propagación)
    - .planning/phases/04-habit-creation-audit/04-PATTERNS.md §"src/services/assignmentService.ts (MOD)"
    - src/utils/periodHelpers.ts (post 04-01 — getPeriodKey, getISOWeekKey, getMonthKey)
    - src/repositories/assignmentRepository.ts (post task 1 — findCompletedHabitsInRange, setCompletedForHabitInRange, updateSnapshotForHabitInRange, findDuplicates)
    - src/store/useHabitStore.ts líneas 220-253 (call sites de completeAssignment, uncompleteAssignment, updateTodaySnapshotForHabit)
  </read_first>
  <behavior>
    - Behavior 1 (REQ-04-10): `getItemsForDate('2026-03-11')` (miércoles) retorna items donde para cada hábito weekly cuyo `(habit_id, period)` tiene una row `is_completed=1` en algún día de la semana ISO actual, `isCompletedForPeriod === true`
    - Behavior 2 (REQ-04-11): idem para monthly contra el rango del mes actual
    - Behavior 3 (REQ-04-10): `completeAssignment(item, day)` para `item.frequency === 'weekly'` propaga `is_completed=1` a todas las rows del rango ISO-week que contiene `day`
    - Behavior 4 (REQ-04-10): `uncompleteAssignment` revierte la propagación si el hábito es weekly/monthly
    - Behavior 5 (REQ-04-10): `updateTodaySnapshotForHabit` para weekly/monthly aplica snapshot a todas las rows uncompleted del período actual (no sólo today)
    - Behavior 6 (REQ-04-01): `ensureAssignmentsForDate` en `__DEV__` corre `findDuplicates()` post-insert y `console.warn` si encuentra >0 grupos
    - Behavior 7 (REQ-04-01): `addAssignmentForHabit` mantiene su guard `findByHabitAndDate` (sigue siendo idempotente; el INDEX es defensa en profundidad — REQ-04-02)
  </behavior>
  <action>
**Modificar `src/services/assignmentService.ts`**.

**1. Imports nuevos al tope** (después de los existentes):
```typescript
import { getPeriodKey, type Frequency } from '../utils/periodHelpers';
```

**2. Helpers internos nuevos** (al final del archivo, en la sección "// ─── Helpers internos ───"):
```typescript
/**
 * Devuelve [startDate, endDate] (inclusive) del período al que pertenece datePrefix.
 * Para 'daily' es [datePrefix, datePrefix].
 * Para 'weekly' es [lunes, domingo] de la semana ISO.
 * Para 'monthly' es [primero-del-mes, último-del-mes].
 */
function getPeriodRange(datePrefix: string, frequency: Frequency): [string, string] {
  if (frequency === 'daily') return [datePrefix, datePrefix];
  if (frequency === 'monthly') {
    const [y, m] = datePrefix.split('-').map(Number);
    const start = `${datePrefix.slice(0, 7)}-01`;
    const lastDay = new Date(Date.UTC(y, m, 0)).getUTCDate(); // último día del mes
    const end = `${datePrefix.slice(0, 7)}-${String(lastDay).padStart(2, '0')}`;
    return [start, end];
  }
  // weekly: lunes a domingo (ISO 8601)
  const d = new Date(`${datePrefix}T00:00:00Z`);
  const dayOfWeek = (d.getUTCDay() + 6) % 7; // Mon=0..Sun=6
  const monday = new Date(d);
  monday.setUTCDate(d.getUTCDate() - dayOfWeek);
  const sunday = new Date(monday);
  sunday.setUTCDate(monday.getUTCDate() + 6);
  return [monday.toISOString().slice(0, 10), sunday.toISOString().slice(0, 10)];
}
```

**3. Reescribir `enrichAssignments`** (la función existente al final del archivo) para que acepte el set de habit_ids completados-en-período:
```typescript
function enrichAssignments(
  assignments: DailyAssignment[],
  performed: { id: string; habit_id: string }[],
  completedInPeriod: Set<string>,
): DailyItem[] {
  const performedMap = new Map(performed.map((p) => [p.habit_id, p.id]));

  return assignments.map((a) => {
    const isCompleted = a.is_completed === 1;
    const isCompletedForPeriod = a.habit_id !== null
      ? (isCompleted || completedInPeriod.has(a.habit_id))
      : isCompleted; // spontaneous: por-row
    return {
      assignmentId: a.id,
      habitId: a.habit_id,
      name: a.snapshot_name,
      points: a.snapshot_points,
      categories: a.snapshot_categories,
      frequency: a.snapshot_frequency as Frequency,
      isCompleted,
      isCompletedForPeriod,
      isSpontaneous: a.is_spontaneous === 1,
      performedHabitId: a.habit_id ? performedMap.get(a.habit_id) ?? null : null,
    };
  });
}
```

**4. Reescribir `getItemsForDate`** para computar el set de habit_ids completados-en-período via single aggregated query:
```typescript
export async function getItemsForDate(
  datePrefix?: string,
): Promise<DailyItem[]> {
  const day = datePrefix ?? getTodayPrefix();
  await ensureAssignmentsForDate(day);

  const [assignments, performed] = await Promise.all([
    assignmentRepo.findByDate(day),
    taskRepo.findByDate(day),
  ]);

  // REQ-04-10/11: resolver isCompletedForPeriod via single aggregated query
  // por frequency (weekly/monthly) — daily no necesita query (ya viene en is_completed).
  const weeklyIds = uniqueHabitIdsByFrequency(assignments, 'weekly');
  const monthlyIds = uniqueHabitIdsByFrequency(assignments, 'monthly');
  const completedInPeriod = new Set<string>();

  if (weeklyIds.length > 0) {
    const [start, end] = getPeriodRange(day, 'weekly');
    const completedWeekly = await assignmentRepo.findCompletedHabitsInRange(weeklyIds, start, end);
    completedWeekly.forEach((id) => completedInPeriod.add(id));
  }
  if (monthlyIds.length > 0) {
    const [start, end] = getPeriodRange(day, 'monthly');
    const completedMonthly = await assignmentRepo.findCompletedHabitsInRange(monthlyIds, start, end);
    completedMonthly.forEach((id) => completedInPeriod.add(id));
  }

  return enrichAssignments(assignments, performed, completedInPeriod);
}

function uniqueHabitIdsByFrequency(
  assignments: DailyAssignment[],
  frequency: Frequency,
): string[] {
  const ids = new Set<string>();
  for (const a of assignments) {
    if (a.habit_id !== null && a.snapshot_frequency === frequency) {
      ids.add(a.habit_id);
    }
  }
  return [...ids];
}
```

**5. Modificar `completeAssignment`** para propagar a período:
```typescript
export async function completeAssignment(
  item: DailyItem,
  datePrefix?: string,
): Promise<string | null> {
  const day = datePrefix ?? getTodayPrefix();

  // REQ-04-10/11 D-01 Opción B: weekly/monthly propagan completion a todo el período actual.
  if (item.habitId !== null && (item.frequency === 'weekly' || item.frequency === 'monthly')) {
    const [start, end] = getPeriodRange(day, item.frequency);
    await assignmentRepo.setCompletedForHabitInRange(item.habitId, 1, start, end);
  } else {
    await assignmentRepo.setCompleted(item.assignmentId, true);
  }

  // performed_habit sólo para hábitos regulares
  if (item.habitId) {
    const ts = datePrefix ? getTimestampForDate(day) : getNowTimestamp();
    const performedId = await taskRepo.insert(
      item.habitId, ts, item.points, item.categories,
    );
    return performedId;
  }
  return null;
}
```

**6. Modificar `uncompleteAssignment`** para revertir propagación:
```typescript
export async function uncompleteAssignment(
  item: DailyItem,
  datePrefix?: string,
): Promise<void> {
  const day = datePrefix ?? getTodayPrefix();

  if (item.habitId !== null && (item.frequency === 'weekly' || item.frequency === 'monthly')) {
    const [start, end] = getPeriodRange(day, item.frequency);
    await assignmentRepo.setCompletedForHabitInRange(item.habitId, 0, start, end);
  } else {
    await assignmentRepo.setCompleted(item.assignmentId, false);
  }

  if (item.performedHabitId) {
    await taskRepo.deleteById(item.performedHabitId);
  }
}
```

**7. Modificar `updateTodaySnapshotForHabit`** para propagar snapshot al período si frequency permite:
```typescript
export async function updateTodaySnapshotForHabit(habitId: string): Promise<void> {
  const today = getTodayPrefix();
  const habit = await habitRepo.findById(habitId);
  if (!habit) return;

  const freq = habit.frequency as Frequency;
  if (freq === 'weekly' || freq === 'monthly') {
    const [start, end] = getPeriodRange(today, freq);
    await assignmentRepo.updateSnapshotForHabitInRange(
      habitId, start, end,
      habit.name, habit.base_points, habit.default_categories, habit.frequency,
    );
  } else {
    await assignmentRepo.updateSnapshot(
      habitId, today,
      habit.name, habit.base_points, habit.default_categories, habit.frequency,
    );
  }
}
```

**8. Agregar dev invariant en `ensureAssignmentsForDate`**. Mantener el guard `existing > 0` y agregar al final del else branch (después del loop de inserts):
```typescript
export async function ensureAssignmentsForDate(datePrefix: string): Promise<void> {
  if (isFutureDate(datePrefix)) return;
  const existing = await assignmentRepo.countByDate(datePrefix);
  if (existing > 0) return;

  const [habits, performed] = await Promise.all([
    habitRepo.findAllActive(),
    taskRepo.findByDate(datePrefix),
  ]);
  const performedSet = new Set(performed.map((p) => p.habit_id));

  for (const habit of habits) {
    const isCompleted = performedSet.has(habit.id) ? 1 : 0;
    await assignmentRepo.insert(
      habit.id, datePrefix, habit.name, habit.base_points,
      habit.default_categories, habit.frequency, isCompleted, 0,
    );
  }

  // REQ-04-01: invariante runtime dev-only — el INDEX en prod ya hace fail-loud,
  // pero en dev queremos detectar regresiones en tests sin index en otros paths.
  if (__DEV__) {
    const dups = await assignmentRepo.findDuplicates();
    if (dups.length > 0) {
      console.warn('[ensureAssignmentsForDate] duplicates detected post-insert', dups);
    }
  }
}
```

**Constraints**:
- Cada función modificada ≤ 25 líneas (CONVENTIONS.md). Si supera, extraer a helpers.
- No tocar `addAssignmentForHabit` ni `addSpontaneous` ni `removeAssignmentForHabit` — ya son correctos para el modelo de Opción B.
- `getPointsForDate` no requiere cambios (puntos son por-row; un weekly completado el miércoles, jueves, viernes contribuye múltiples veces a sus respectivos sumPoints — y eso es deseable para el modelo de "row por día con propagación", porque cada día es independiente para puntos. Si se quisiera "puntos una sola vez por período" sería un cambio mayor que NO está en scope per CONTEXT §domain).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=dailyAssignments</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { getPeriodKey, type Frequency } from '../utils/periodHelpers'" src/services/assignmentService.ts` retorna 1 línea
    - `grep -E "^function getPeriodRange" src/services/assignmentService.ts` retorna 1 línea
    - `grep "isCompletedForPeriod" src/services/assignmentService.ts | wc -l` ≥ 2 (en enrichAssignments + uno o más call sites)
    - `grep "setCompletedForHabitInRange" src/services/assignmentService.ts | wc -l` ≥ 2 (complete + uncomplete)
    - `grep "updateSnapshotForHabitInRange" src/services/assignmentService.ts` retorna 1 línea
    - `grep "if (__DEV__)" src/services/assignmentService.ts` retorna 1 línea
    - `grep "findDuplicates" src/services/assignmentService.ts` retorna 1 línea
    - `grep "console.warn(.*\\[ensureAssignmentsForDate\\]" src/services/assignmentService.ts` retorna 1 línea
    - `npm test -- --testPathPattern=dailyAssignments` exit 0 (suite pasa con cambios)
    - Funciones modificadas ≤ 25 líneas: `awk '/^export async function (completeAssignment|uncompleteAssignment|updateTodaySnapshotForHabit|ensureAssignmentsForDate|getItemsForDate)/{f=1;n=0} f{n++; if (/^}/) {print FILENAME":"NR" "n; f=0}}' src/services/assignmentService.ts` ningún resultado >25
  </acceptance_criteria>
  <done>assignmentService.ts modificado con visibility-aware enrichment + propagación de completion + dev invariant; suite scoped GREEN; getPointsForDate sin cambios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extender src/__tests__/dailyAssignments.test.ts con tests de visibility weekly/monthly + propagación + dev invariant</name>
  <files>src/__tests__/dailyAssignments.test.ts</files>
  <read_first>
    - src/__tests__/dailyAssignments.test.ts (archivo completo — extender en describes existentes y agregar nuevos)
    - .planning/phases/04-habit-creation-audit/04-PATTERNS.md §"src/__tests__/dailyAssignments.test.ts (MOD)"
    - src/__tests__/setup/testDatabase.ts (helpers disponibles: insertTestHabit, insertTestAssignment, insertTestPerformed, createTestDatabase con UNIQUE INDEX)
    - src/services/assignmentService.ts (post task 2 — completeAssignment, uncompleteAssignment, updateTodaySnapshotForHabit, getItemsForDate)
  </read_first>
  <behavior>
    - Tests REQ-04-10: weekly habit visible toda la semana ISO; complete propaga; uncomplete revierte
    - Tests REQ-04-11: monthly habit visible todo el mes calendario; complete propaga
    - Tests REQ-04-01: dev invariant warn cuando hay duplicados
    - Tests REQ-04-02: addAssignmentForHabit mantiene idempotency (test ya existe en líneas 354-361 — verificar que sigue verde)
  </behavior>
  <action>
**Extender** `src/__tests__/dailyAssignments.test.ts`. Agregar al final del archivo (después del último describe existente):

```typescript
// ─── Visibility weekly/monthly (REQ-04-10/11, D-01 Opción B) ───────────────────

import { getItemsForDate, completeAssignment, uncompleteAssignment } from '../services/assignmentService';

describe('Visibility weekly/monthly — REQ-04-10/11', () => {
  // Para 2026-03-11 (miércoles): semana ISO W11 corre 2026-03-09 (lun) a 2026-03-15 (dom).
  // Mes 2026-03 corre 2026-03-01 a 2026-03-31.
  const MONDAY = '2026-03-09';
  const SUNDAY = '2026-03-15';

  test('REQ-04-10: weekly habit visible toda la semana — completar miércoles propaga a la semana', async () => {
    insertTestHabit(db, { id: 'wh', name: 'Yoga', frequency: 'weekly', base_points: 5 });
    // Sembrar rows del lunes a domingo (simulando rollover ya corrido)
    for (let i = 0; i < 7; i++) {
      const d = new Date(`${MONDAY}T00:00:00Z`);
      d.setUTCDate(d.getUTCDate() + i);
      const date = d.toISOString().slice(0, 10);
      insertTestAssignment(db, {
        id: `wh-${date}`,
        habit_id: 'wh',
        date,
        snapshot_name: 'Yoga',
        snapshot_points: 5,
        snapshot_frequency: 'weekly',
      });
    }

    // Estado inicial: ningún día visto como completado en período
    const itemsMon = await getItemsForDate(MONDAY);
    expect(itemsMon.find((i) => i.habitId === 'wh')?.isCompletedForPeriod).toBe(false);

    // Completar el miércoles (TODAY)
    const itemsWed = await getItemsForDate(TODAY);
    const wedItem = itemsWed.find((i) => i.habitId === 'wh')!;
    await completeAssignment(wedItem, TODAY);

    // Volver a leer cada día — todos deben mostrar isCompletedForPeriod=true
    const itemsMonAfter = await getItemsForDate(MONDAY);
    const itemsSunAfter = await getItemsForDate(SUNDAY);
    expect(itemsMonAfter.find((i) => i.habitId === 'wh')?.isCompletedForPeriod).toBe(true);
    expect(itemsSunAfter.find((i) => i.habitId === 'wh')?.isCompletedForPeriod).toBe(true);

    // is_completed (por-row) debe ser 1 para TODAS las rows del rango (propagación física)
    const completedRows = (db.prepare(
      'SELECT COUNT(*) as c FROM daily_assignments WHERE habit_id = ? AND is_completed = 1',
    ).get('wh') as { c: number }).c;
    expect(completedRows).toBe(7);
  });

  test('REQ-04-10: uncomplete revierte la propagación', async () => {
    insertTestHabit(db, { id: 'wh', name: 'Yoga', frequency: 'weekly' });
    for (let i = 0; i < 7; i++) {
      const d = new Date(`${MONDAY}T00:00:00Z`);
      d.setUTCDate(d.getUTCDate() + i);
      const date = d.toISOString().slice(0, 10);
      insertTestAssignment(db, {
        id: `wh-${date}`, habit_id: 'wh', date,
        snapshot_frequency: 'weekly', is_completed: 1,
      });
    }

    const items = await getItemsForDate(TODAY);
    const item = items.find((i) => i.habitId === 'wh')!;
    await uncompleteAssignment(item, TODAY);

    const stillCompleted = (db.prepare(
      'SELECT COUNT(*) as c FROM daily_assignments WHERE habit_id = ? AND is_completed = 1',
    ).get('wh') as { c: number }).c;
    expect(stillCompleted).toBe(0);
  });

  test('REQ-04-11: monthly habit visible todo el mes — completar día 11 propaga al mes', async () => {
    insertTestHabit(db, { id: 'mh', name: 'Reflexión mensual', frequency: 'monthly' });
    // Sembrar rows del 1 al 31 de marzo
    for (let day = 1; day <= 31; day++) {
      const date = `2026-03-${String(day).padStart(2, '0')}`;
      insertTestAssignment(db, {
        id: `mh-${date}`, habit_id: 'mh', date,
        snapshot_frequency: 'monthly',
      });
    }

    const items = await getItemsForDate(TODAY);
    const item = items.find((i) => i.habitId === 'mh')!;
    await completeAssignment(item, TODAY);

    const itemsDay1 = await getItemsForDate('2026-03-01');
    const itemsDay31 = await getItemsForDate('2026-03-31');
    expect(itemsDay1.find((i) => i.habitId === 'mh')?.isCompletedForPeriod).toBe(true);
    expect(itemsDay31.find((i) => i.habitId === 'mh')?.isCompletedForPeriod).toBe(true);

    const completedRows = (db.prepare(
      'SELECT COUNT(*) as c FROM daily_assignments WHERE habit_id = ? AND is_completed = 1',
    ).get('mh') as { c: number }).c;
    expect(completedRows).toBe(31);
  });

  test('REQ-04-10: daily habit NO se propaga (sólo afecta su día)', async () => {
    insertTestHabit(db, { id: 'dh', name: 'Meditar', frequency: 'daily' });
    insertTestAssignment(db, {
      id: 'dh-today', habit_id: 'dh', date: TODAY, snapshot_frequency: 'daily',
    });
    insertTestAssignment(db, {
      id: 'dh-yest', habit_id: 'dh', date: YESTERDAY, snapshot_frequency: 'daily',
    });

    const items = await getItemsForDate(TODAY);
    const item = items.find((i) => i.habitId === 'dh')!;
    await completeAssignment(item, TODAY);

    // Sólo la row de today queda completada
    const completedRows = (db.prepare(
      'SELECT COUNT(*) as c FROM daily_assignments WHERE habit_id = ? AND is_completed = 1',
    ).get('dh') as { c: number }).c;
    expect(completedRows).toBe(1);

    // isCompletedForPeriod del día = isCompleted (mismo período)
    const itemsToday = await getItemsForDate(TODAY);
    const today = itemsToday.find((i) => i.habitId === 'dh')!;
    expect(today.isCompletedForPeriod).toBe(true);
    expect(today.isCompleted).toBe(true);

    const itemsYest = await getItemsForDate(YESTERDAY);
    const yest = itemsYest.find((i) => i.habitId === 'dh')!;
    expect(yest.isCompletedForPeriod).toBe(false);
  });
});

// ─── Dev invariant (REQ-04-01) ─────────────────────────────────────────────────

describe('ensureAssignmentsForDate — REQ-04-01 dev invariant', () => {
  // Nota: __DEV__ es true en jest por default (jest-expo preset).
  // Si no lo es, este test se skipea con .skip explícito.
  test('REQ-04-01: en __DEV__, warn cuando findDuplicates encuentra grupos', async () => {
    if (typeof (globalThis as { __DEV__?: boolean }).__DEV__ !== 'undefined' && !(globalThis as { __DEV__?: boolean }).__DEV__) {
      return; // skip en non-dev
    }

    insertTestHabit(db, { id: 'h1', name: 'Meditar' });
    // El INDEX en createTestDatabase impide insertar duplicados — para forzar el warn,
    // tenemos que evadir el INDEX. Como createTestDatabase incluye el index, este caso
    // sólo es verificable con createPreMigrationTestDatabase (sin index).
    // Sin embargo, el comportamiento esperado en la suite normal es que findDuplicates
    // SIEMPRE retorne [] post-rollover — verificamos que el warn NUNCA se llama en operación normal.
    const warnSpy = jest.spyOn(console, 'warn').mockImplementation(() => undefined);
    await ensureAssignmentsForDate(TODAY);
    expect(warnSpy).not.toHaveBeenCalledWith(
      expect.stringContaining('[ensureAssignmentsForDate] duplicates detected'),
    );
    warnSpy.mockRestore();
  });
});
```

**Importante para el executor:**
- Los tests existentes en líneas 333-362 ("Prevención de duplicados") DEBEN seguir verdes — son nuestra cobertura de REQ-04-08/09. No modificarlos.
- El test existente "múltiples llamadas a addAssignmentForHabit no crean duplicados" (líneas 354-361) DEBE seguir verde — cubre REQ-04-02.
- Si algún test existente rompe por el nuevo campo `isCompletedForPeriod` en DailyItem, el fix vive en task 2 (enrichAssignments DEBE setearlo siempre); si rompe acá, ajustar el test minimamente para tolerar el campo nuevo.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REQ-04-10" src/__tests__/dailyAssignments.test.ts` ≥ 3
    - `grep -c "REQ-04-11" src/__tests__/dailyAssignments.test.ts` ≥ 1
    - `grep -c "REQ-04-01" src/__tests__/dailyAssignments.test.ts` ≥ 2 (dev invariant + idempotency existente)
    - `grep "describe('Visibility weekly/monthly" src/__tests__/dailyAssignments.test.ts` retorna 1 línea
    - `grep "isCompletedForPeriod" src/__tests__/dailyAssignments.test.ts | wc -l` ≥ 4
    - Tests preexistentes intactos: `grep "describe('Prevención de duplicados'" src/__tests__/dailyAssignments.test.ts` retorna 1 línea (no se borró)
    - `npm test` exit 0 — suite completa GREEN incluyendo migrationV1, periodHelpers, dedupeAssignmentsArray, dailyAssignments
  </acceptance_criteria>
  <done>Tests de visibility weekly/monthly + propagación + dev invariant agregados; suite completa GREEN; cobertura REQ-04-01/02/10/11 verificable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service layer ↔ Repo SQL | Las nuevas wrappers `setCompletedForHabitInRange` y `updateSnapshotForHabitInRange` aceptan rangos de fecha; un rango malformado podría afectar rows fuera del período esperado |
| Read path ↔ DailySheet render | `getItemsForDate` se llama en cada render; latencia adicional impacta hot path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | setCompletedForHabitInRange (rango fecha mal calculado afecta período equivocado) | mitigate | `getPeriodRange` es función pura testeable; lunes/domingo construido via UTC explícito (mismo patrón que periodHelpers); tests REQ-04-10/11 verifican rangos exactos en boundaries (lunes y domingo) |
| T-04-03-02 | DoS / Performance | getItemsForDate ejecuta hasta 2 queries adicionales (weekly + monthly aggregated) por render | mitigate | Single aggregated query (no N+1); placeholders parametrizados; el `if (ids.length === 0) return []` short-circuit evita ejecutar query si no hay weekly/monthly visibles; volumen estimado ≤ 20 hábitos por usuario → IN clause con ≤20 placeholders es trivial |
| T-04-03-03 | Information Disclosure | Dev invariant warn loguea grupos duplicados (habit_id, date, count) en console | accept | Local-first app, sin telemetría; logs sólo visibles en Metro/logcat del dispositivo del usuario |
| T-04-03-04 | Tampering | Propagation race: usuario completa en miércoles, mientras checkAndBackfillHistory está corriendo y crea row del jueves | accept | El backfill se ejecuta dentro de initDatabase().then(...) — la propagación de completion via UPDATE WHERE date BETWEEN ... naturally cubre rows recién creadas. Si la creación es post-completion (caso muy específico), la nueva row tendrá `is_completed=0` pero `isCompletedForPeriod` se computa en read-time, así que el usuario verá el período como completado de todas formas |
</threat_model>

<verification>
- `npm test -- --testPathPattern=dailyAssignments` (suite scoped, ~5s)
- `npm test` (full suite GREEN — migrationV1, periodHelpers, dedupeAssignmentsArray, dailyAssignments todos en verde)
- Manual smoke (post-execute, no en este plan): cargar app; agregar hábito weekly; completarlo el miércoles; verificar que el ítem aparece como completado el lunes y el domingo de la misma semana ISO.
</verification>

<success_criteria>
- [ ] DailyItem extendido con `isCompletedForPeriod: boolean` (REQUERIDO no opcional)
- [ ] 3 nuevas wrappers en assignmentRepository: findCompletedHabitsInRange, setCompletedForHabitInRange, updateSnapshotForHabitInRange
- [ ] assignmentService implementa visibility-aware enrichment via single aggregated query (no N+1)
- [ ] completeAssignment / uncompleteAssignment / updateTodaySnapshotForHabit propagan a período cuando frequency = weekly/monthly
- [ ] Dev invariant `console.warn` agregado en ensureAssignmentsForDate
- [ ] 4+ tests nuevos pasando: 2 weekly + 1 monthly + 1 daily-no-propagation
- [ ] Tests preexistentes intactos y GREEN (Prevención de duplicados, idempotency, BUG-01..04)
- [ ] Suite completa `npm test` GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/04-habit-creation-audit/04-03-SUMMARY.md` documentando:
- Cambios al type DailyItem (consumers a actualizar en el futuro)
- Nuevas funciones del repo expuestas (findCompletedHabitsInRange + setCompletedForHabitInRange + updateSnapshotForHabitInRange)
- Helpers internos en assignmentService (getPeriodRange, uniqueHabitIdsByFrequency)
- Decisión final sobre Open Q1 (Opción B confirmada)
- Notas sobre interacción con statsService / getPointsForDate (sin cambios — los puntos siguen siendo por-row, esto es deseable)
</output>
