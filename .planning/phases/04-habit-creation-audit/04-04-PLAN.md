---
phase: 04-habit-creation-audit
plan: 04
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/services/backupService.ts
  - src/__tests__/restorePreClean.test.ts
  - .planning/codebase/ARCHITECTURE.md
  - .planning/phases/04-habit-creation-audit/04-VALIDATION.md
autonomous: true
requirements:
  - REQ-04-03
threat_model_summary: "Riesgo MEDIO de inconsistencia en restore: un backup JSON pre-fix con duplicados haría fallar el UNIQUE INDEX (REQ-04-05) durante el bulk insert, y `withTransactionAsync` rollback dejaría la DB en estado pre-restore (con la data del usuario actual perdida desde su perspectiva, ya que esperaba que el restore aplicara el backup). Mitigación: pre-clean del array via dedupeAssignmentsArray ANTES de pasarlo al bulk insert, garantizando que el INDEX nunca se viola. driveBackupService.applyRestore reusa `restoreData` directamente — el fix es transparente. Riesgo BAJO en docs: ARCHITECTURE.md falsamente afirma UNIQUE constraint existente — corregir cierra la divergencia entre realidad y docs."
notes: "Wave 4 — depende de 04-01 (consume dedupeAssignmentsArray) y 04-02 (el INDEX que justifica este pre-clean ya existe). Tareas: (1) integrar dedupeAssignmentsArray en backupService.restoreData; (2) test que un JSON con duplicados se restaura sin fallar el INDEX; (3) doc-fix en ARCHITECTURE.md (Regla 3.4 CLAUDE.md); (4) actualizar 04-VALIDATION.md con per-task map de los 4 plans."

must_haves:
  truths:
    - "Si el JSON de backup tiene 2+ rows con mismo (habit_id, date), restoreData inserta sólo 1 (el winner por D-03) sin fallar el UNIQUE INDEX"
    - "Si el JSON tiene spontaneous (habit_id=null) duplicados visualmente, todos se preservan (passthrough — el INDEX los excluye)"
    - "driveBackupService.applyRestore (que llama restoreData) automáticamente hereda el pre-clean — sin cambios en driveBackupService"
    - "ARCHITECTURE.md línea ~281 refleja la realidad post-Phase-4: 'Partial UNIQUE INDEX idx_unique_habit_date ON daily_assignments(habit_id, date) WHERE habit_id IS NOT NULL (introducido en Phase 4 migration v1)'"
    - "04-VALIDATION.md tiene la per-task map filled con cada task ID del phase y `nyquist_compliant: true`"
  artifacts:
    - path: "src/services/backupService.ts"
      provides: "restoreData con pre-clean via dedupeAssignmentsArray"
      contains: "dedupeAssignmentsArray"
    - path: "src/__tests__/restorePreClean.test.ts"
      provides: "Test REQ-04-03 — JSON con duplicados se restaura sin error UNIQUE"
      contains: "describe('restoreData pre-clean"
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Línea ~281 corregida con descripción real del partial UNIQUE INDEX"
      contains: "idx_unique_habit_date"
    - path: ".planning/phases/04-habit-creation-audit/04-VALIDATION.md"
      provides: "Per-task verification map filled + nyquist_compliant: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "src/services/backupService.ts restoreData"
      to: "src/utils/dedupeAssignmentsArray.ts"
      via: "import { dedupeAssignmentsArray }"
      pattern: "dedupeAssignmentsArray\\("
    - from: "src/services/driveBackupService.ts applyRestore"
      to: "src/services/backupService.ts restoreData"
      via: "Plan 03-03 ya creó esta dependencia (driveBackupService importa restoreData)"
      pattern: "restoreData\\(payload\\.data\\)"
---

<objective>
Cerrar el círculo del phase: (1) hacer que `restoreData` deduplique el array de daily_assignments antes del bulk insert para evitar romper el UNIQUE INDEX cuando se restaura un backup pre-fix; (2) corregir la documentación falsa en ARCHITECTURE.md sobre UNIQUE constraint; (3) actualizar VALIDATION.md con el per-task map del phase y flippear el flag de Nyquist compliance.

Purpose: Sin este plan, un usuario que restaure un backup creado antes de Phase 4 (que puede tener duplicados) recibiría un error críptico de SQLite. Con pre-clean, el restore es robusto. Y la actualización de ARCHITECTURE.md cierra la divergencia entre docs y realidad (Regla 3.4 CLAUDE.md).

Output: backupService modificado + 1 test nuevo + 1 línea de ARCHITECTURE.md actualizada + per-task map de validación llena.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-habit-creation-audit/04-CONTEXT.md
@.planning/phases/04-habit-creation-audit/04-RESEARCH.md
@.planning/phases/04-habit-creation-audit/04-PATTERNS.md
@.planning/phases/04-habit-creation-audit/04-01-PLAN.md
@.planning/phases/04-habit-creation-audit/04-02-PLAN.md
@.planning/phases/04-habit-creation-audit/04-03-PLAN.md
@.planning/phases/04-habit-creation-audit/04-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@src/services/backupService.ts
@src/services/driveBackupService.ts
@src/repositories/backupRepository.ts
@src/__tests__/setup/testDatabase.ts

<interfaces>
<!-- Contratos consumidos por este plan. -->

From src/utils/dedupeAssignmentsArray.ts (post 04-01):
```typescript
export function dedupeAssignmentsArray(
  rows: DailyAssignment[],
  performed: PerformedHabit[],
): DailyAssignment[];
```

From src/services/backupService.ts (línea 126-133 — actual a modificar):
```typescript
export async function restoreData(data: BackupData): Promise<void> {
  await backupRepo.restoreAllData(
    data.habits,
    data.performed_habits,
    data.mood_entries,
    data.daily_assignments,
  );
}
```

From src/services/driveBackupService.ts línea 433 (consumer downstream):
```typescript
await restoreData(payload.data);  // hereda automáticamente el pre-clean
```

From src/repositories/backupRepository.ts (signature de restoreAllData — para confirmar shape de params):
```typescript
export async function restoreAllData(
  habits: Habit[],
  performed: PerformedHabit[],
  moods: MoodEntry[],
  assignments: DailyAssignment[],
): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrar dedupeAssignmentsArray en backupService.restoreData + test de regresión REQ-04-03</name>
  <files>src/services/backupService.ts, src/__tests__/restorePreClean.test.ts</files>
  <read_first>
    - src/services/backupService.ts (archivo completo — modificar restoreData línea 126-133)
    - src/utils/dedupeAssignmentsArray.ts (post 04-01 — firma y comportamiento)
    - src/services/driveBackupService.ts líneas 420-441 (applyRestore — verificar que solo llama restoreData y NO bypassa)
    - src/repositories/backupRepository.ts (entender restoreAllData — el TRUNCATE+INSERT pattern, para que el pre-clean tenga sentido en términos del bulk insert)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Pattern 4: Pre-Clean JSON Before Bulk Insert (Restore)" y §"Pitfall #3"
    - .planning/phases/04-habit-creation-audit/04-PATTERNS.md §"src/services/backupService.ts (MOD)"
    - src/__tests__/setup/testDatabase.ts (helpers para tests — createTestDatabase que YA tiene el UNIQUE INDEX, para reproducir el escenario de restore con index activo)
    - src/types/index.ts (BackupData, DailyAssignment shapes)
  </read_first>
  <behavior>
    - Test 1 (REQ-04-03 happy path): JSON con `daily_assignments` sin duplicados → restoreData se ejecuta normalmente, todas las rows persistidas
    - Test 2 (REQ-04-03 dedup activo): JSON con 2 rows mismo (habit_id, date), una completed → restoreData inserta sólo 1, la completed; no throws
    - Test 3 (REQ-04-03 spontaneous passthrough): JSON con 3 spontaneous mismo día → todas persisten post-restore
    - Test 4 (REQ-04-03 mixed): 2 duplicados regulares + 1 spontaneous → 1 regular + 1 spontaneous post-restore
    - Test 5 (REQ-04-03 driveBackupService): documentar que applyRestore hereda automáticamente — no requiere test específico, sólo grep verifies en acceptance
  </behavior>
  <action>
**Modificar** `src/services/backupService.ts`:

1. Agregar import al tope (después del import de types):
```typescript
import { dedupeAssignmentsArray } from '../utils/dedupeAssignmentsArray';
```

2. Reemplazar `restoreData` (línea 126-133) por:
```typescript
/**
 * Restaura un backup completo. Aplica pre-clean a daily_assignments via
 * dedupeAssignmentsArray (REQ-04-03) para que un backup pre-Phase-4 con
 * duplicados no rompa el partial UNIQUE INDEX (idx_unique_habit_date).
 *
 * Heurística D-03 aplicada en memoria; spontaneous (habit_id=null) se preservan.
 *
 * driveBackupService.applyRestore consume esta función — el pre-clean se
 * aplica transparentemente al restore desde Drive también.
 */
export async function restoreData(data: BackupData): Promise<void> {
  const dedupedAssignments = dedupeAssignmentsArray(
    data.daily_assignments,
    data.performed_habits,
  );
  await backupRepo.restoreAllData(
    data.habits,
    data.performed_habits,
    data.mood_entries,
    dedupedAssignments,
  );
}
```

**No tocar** `parseAndValidate`, `buildBackupData`, `exportBackup`, `importBackup`. El cambio es 5 líneas dentro de `restoreData`.

**Crear** `src/__tests__/restorePreClean.test.ts`:

```typescript
import type Database from 'better-sqlite3';
import {
  createTestDatabase,
  resetTestDatabase,
} from './setup/testDatabase';
import { restoreData } from '../services/backupService';
import type { BackupData } from '../types';

let db: Database.Database;

beforeEach(() => {
  db = createTestDatabase(); // INCLUYE el UNIQUE INDEX — escenario realista post-Phase-4
});

afterEach(() => {
  resetTestDatabase();
  jest.clearAllMocks();
});

function makeBackup(over: Partial<BackupData> = {}): BackupData {
  return {
    version: 1,
    exportedAt: '2026-03-15T10:00:00.000Z',
    habits: [
      { id: 'h1', name: 'Meditar', frequency: 'daily', base_points: 1, default_categories: '[]', is_active: 1 },
    ],
    performed_habits: [],
    mood_entries: [],
    daily_assignments: [],
    ...over,
  };
}

function countAssignments(): number {
  return (db.prepare('SELECT COUNT(*) as c FROM daily_assignments').get() as { c: number }).c;
}

function countAssignmentsForHabit(habitId: string): number {
  return (db.prepare(
    'SELECT COUNT(*) as c FROM daily_assignments WHERE habit_id = ?',
  ).get(habitId) as { c: number }).c;
}

describe('restoreData pre-clean — REQ-04-03', () => {
  test('REQ-04-03: JSON sin duplicados → todas las rows persistidas', async () => {
    const backup = makeBackup({
      daily_assignments: [
        {
          id: 'a1', habit_id: 'h1', date: '2026-03-15', snapshot_name: 'Meditar',
          snapshot_points: 1, snapshot_categories: '[]', snapshot_frequency: 'daily',
          is_completed: 0, is_spontaneous: 0,
        },
      ],
    });
    await restoreData(backup);
    expect(countAssignments()).toBe(1);
  });

  test('REQ-04-03: JSON con duplicados regulares (uno completed) → sobrevive el completed, sin UNIQUE error', async () => {
    const backup = makeBackup({
      daily_assignments: [
        {
          id: 'a1', habit_id: 'h1', date: '2026-03-15', snapshot_name: 'Meditar',
          snapshot_points: 1, snapshot_categories: '[]', snapshot_frequency: 'daily',
          is_completed: 0, is_spontaneous: 0,
        },
        {
          id: 'a2', habit_id: 'h1', date: '2026-03-15', snapshot_name: 'Meditar',
          snapshot_points: 1, snapshot_categories: '[]', snapshot_frequency: 'daily',
          is_completed: 1, is_spontaneous: 0,
        },
      ],
    });
    await restoreData(backup); // NO debe arrojar UNIQUE constraint
    expect(countAssignmentsForHabit('h1')).toBe(1);
    const survivor = db.prepare(
      'SELECT id, is_completed FROM daily_assignments WHERE habit_id = ?',
    ).get('h1') as { id: string; is_completed: number };
    expect(survivor.is_completed).toBe(1); // D-03 step 1: completed wins
  });

  test('REQ-04-03: spontaneous duplicados (habit_id=null) → todos persisten (passthrough)', async () => {
    const backup = makeBackup({
      daily_assignments: [
        {
          id: 's1', habit_id: null, date: '2026-03-15', snapshot_name: 'Logro 1',
          snapshot_points: 0, snapshot_categories: '[]', snapshot_frequency: 'daily',
          is_completed: 1, is_spontaneous: 1,
        },
        {
          id: 's2', habit_id: null, date: '2026-03-15', snapshot_name: 'Logro 2',
          snapshot_points: 0, snapshot_categories: '[]', snapshot_frequency: 'daily',
          is_completed: 1, is_spontaneous: 1,
        },
        {
          id: 's3', habit_id: null, date: '2026-03-15', snapshot_name: 'Logro 3',
          snapshot_points: 0, snapshot_categories: '[]', snapshot_frequency: 'daily',
          is_completed: 1, is_spontaneous: 1,
        },
      ],
    });
    await restoreData(backup);
    expect(countAssignments()).toBe(3);
  });

  test('REQ-04-03: mixed — 2 duplicados regulares + 1 spontaneous → 1 regular + 1 spontaneous', async () => {
    const backup = makeBackup({
      daily_assignments: [
        {
          id: 's1', habit_id: null, date: '2026-03-15', snapshot_name: 'Spontaneous',
          snapshot_points: 0, snapshot_categories: '[]', snapshot_frequency: 'daily',
          is_completed: 1, is_spontaneous: 1,
        },
        {
          id: 'a1', habit_id: 'h1', date: '2026-03-15', snapshot_name: 'Meditar',
          snapshot_points: 1, snapshot_categories: '[]', snapshot_frequency: 'daily',
          is_completed: 0, is_spontaneous: 0,
        },
        {
          id: 'a2', habit_id: 'h1', date: '2026-03-15', snapshot_name: 'Meditar',
          snapshot_points: 1, snapshot_categories: '[]', snapshot_frequency: 'daily',
          is_completed: 1, is_spontaneous: 0,
        },
      ],
    });
    await restoreData(backup);
    expect(countAssignments()).toBe(2);
    expect(countAssignmentsForHabit('h1')).toBe(1);
  });
});
```

**Notas para el executor:**
- El test usa `createTestDatabase()` que SÍ incluye el UNIQUE INDEX — esto reproduce el escenario realista post-Phase-4: si el pre-clean fallara, el INSERT del segundo duplicado throw `UNIQUE constraint failed`, lo cual el test detectaría como `await restoreData(...)` rechazando.
- El `BackupData.daily_assignments` shape exacto debe coincidir con `DailyAssignment` desde `src/types/index.ts`. Si el campo `is_active` u otros faltan en el mock helper, agregarlos para satisfacer TS-strict.
- No es necesario tocar driveBackupService — `applyRestore` ya llama `await restoreData(payload.data)` (línea 433), así que hereda el pre-clean automáticamente. La task de acceptance verifica esto vía grep.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=restorePreClean</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { dedupeAssignmentsArray }" src/services/backupService.ts` retorna 1 línea
    - `grep "dedupeAssignmentsArray(" src/services/backupService.ts | wc -l` ≥ 1 (consumido en restoreData)
    - `grep "dedupedAssignments" src/services/backupService.ts | wc -l` ≥ 2 (variable + uso)
    - `grep -c "REQ-04-03" src/__tests__/restorePreClean.test.ts` ≥ 4
    - `npm test -- --testPathPattern=restorePreClean` exit 0; al menos 4 tests passed
    - `grep "restoreData(payload.data)" src/services/driveBackupService.ts` retorna ≥1 línea (verificación: el path Drive→restore SIGUE pasando por backupService.restoreData; sin bypass)
    - `grep -E "(buildBackupData|parseAndValidate|exportBackup|importBackup)" src/services/backupService.ts | wc -l` ≥ 4 (funciones existentes intactas)
    - `npm test` exit 0 (suite completa GREEN — no rompe nada en backupService o driveBackupService)
  </acceptance_criteria>
  <done>backupService.restoreData con pre-clean integrado; test de regresión REQ-04-03 GREEN; driveBackupService sin cambios pero verificado que hereda el fix.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Corregir ARCHITECTURE.md y actualizar 04-VALIDATION.md per-task map (cierre del phase)</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/phases/04-habit-creation-audit/04-VALIDATION.md</files>
  <read_first>
    - .planning/codebase/ARCHITECTURE.md líneas 270-289 (sección Data Integrity — línea ~281 contiene el claim falso)
    - .planning/phases/04-habit-creation-audit/04-VALIDATION.md (archivo completo — la tabla per-task está vacía)
    - .planning/phases/04-habit-creation-audit/04-01-PLAN.md (tasks: REQ-04-03, REQ-04-12)
    - .planning/phases/04-habit-creation-audit/04-02-PLAN.md (tasks: REQ-04-04..09)
    - .planning/phases/04-habit-creation-audit/04-03-PLAN.md (tasks: REQ-04-01, REQ-04-02, REQ-04-10, REQ-04-11)
    - CLAUDE.md Regla 3.4 ("Actualizar archivos de documentación .md que hayan quedado desactualizados")
  </read_first>
  <behavior>
    - ARCHITECTURE.md línea ~281 reemplaza el claim falso de "Unique constraint on (habit_id, date)" por la descripción real del partial UNIQUE INDEX
    - 04-VALIDATION.md llena la tabla per-task verification map con todos los task IDs del phase, sus REQ-IDs, comandos automated y status
    - 04-VALIDATION.md flippea `nyquist_compliant: false` → `nyquist_compliant: true` en frontmatter
    - 04-VALIDATION.md llena Approval: pending → Approval: ready-for-execute
  </behavior>
  <action>
**1. Modificar `.planning/codebase/ARCHITECTURE.md`** — Reemplazar la línea 281 actual:

```
  - Unique constraint on `(habit_id, date)` for daily_assignments (prevents duplicate assignments)
```

por:

```
  - **Partial UNIQUE INDEX** `idx_unique_habit_date` ON `daily_assignments(habit_id, date) WHERE habit_id IS NOT NULL` — introducido en Phase 4 (migration v1, `src/services/migrations/migrationV1.ts`). Previene duplicados de hábitos regulares; los spontaneous (habit_id IS NULL) están exentos por design (D-07). Ver `.planning/phases/04-habit-creation-audit/` para detalles de la migración versionada via `PRAGMA user_version`.
```

Nota: el reemplazo debe ser exacto en la línea — usar Edit para precisión, no Write.

**2. Reemplazar `.planning/phases/04-habit-creation-audit/04-VALIDATION.md`** completo con esta versión actualizada:

```markdown
---
phase: 4
slug: habit-creation-audit
status: ready-for-execute
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-01
last_updated: 2026-05-01
---

# Phase 4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `.planning/phases/04-habit-creation-audit/04-RESEARCH.md` §"Validation Architecture"

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | jest 29.x (jest-expo preset) |
| **Config file** | `jest.config.js` (existing) |
| **Quick run command** | `npm test -- --testPathPattern='dailyAssignments|migrationV1|periodHelpers|dedupeAssignmentsArray|restorePreClean'` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~12s quick / ~45s full |

---

## Sampling Rate

- **After every task commit:** Run quick command (scoped pattern above)
- **After every plan wave:** Run `npm test` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04-01-T1 | 04-01 | 1 | REQ-04-12 | T-04-01-02 | getPeriodKey correcto en boundaries ISO/year/month | unit | `npm test -- --testPathPattern=periodHelpers` | ✅ (creado en task) | ⬜ pending |
| 04-01-T2 | 04-01 | 1 | REQ-04-03 | T-04-01-01 | dedupeAssignmentsArray respeta D-03 + spontaneous passthrough | unit | `npm test -- --testPathPattern=dedupeAssignmentsArray` | ✅ (creado en task) | ⬜ pending |
| 04-01-T3 | 04-01 | 1 | (infra) | — | createPreMigrationTestDatabase + seedDuplicates + insertTestPerformed disponibles | unit (smoke) | `npm test` | ✅ | ⬜ pending |
| 04-02-T1 | 04-02 | 2 | REQ-04-04, REQ-04-05 | T-04-02-01 | SQL constants para dedup CTE + INDEX exportadas | unit (smoke) | `npm test` | ✅ | ⬜ pending |
| 04-02-T2 | 04-02 | 2 | REQ-04-04..09 | T-04-02-01, T-04-02-02 | migration v1 atomicity + idempotency + silent failure | unit | `npm test -- --testPathPattern=migrationV1` | ✅ (creado en task) | ⬜ pending |
| 04-02-T3 | 04-02 | 2 | REQ-04-06 | T-04-02-01 | runMigrations integrado en initDatabase + boot order verified | unit (smoke) | `npm test` | ✅ | ⬜ pending |
| 04-03-T1 | 04-03 | 3 | REQ-04-10, REQ-04-11 | T-04-03-01 | DailyItem + repo wrappers para visibility de período | unit (smoke) | `npm test` | ✅ | ⬜ pending |
| 04-03-T2 | 04-03 | 3 | REQ-04-01, REQ-04-10, REQ-04-11 | T-04-03-01, T-04-03-02 | service-layer enrichment + propagación + dev invariant | unit | `npm test -- --testPathPattern=dailyAssignments` | ✅ | ⬜ pending |
| 04-03-T3 | 04-03 | 3 | REQ-04-01, REQ-04-02, REQ-04-10, REQ-04-11 | T-04-03-01 | tests REQ-04-10/11 + REQ-04-01 dev invariant | unit | `npm test -- --testPathPattern=dailyAssignments` | ✅ | ⬜ pending |
| 04-04-T1 | 04-04 | 4 | REQ-04-03 | (mitigado en 04-01) | restoreData pre-clean — backup pre-fix no rompe INDEX | unit | `npm test -- --testPathPattern=restorePreClean` | ✅ (creado en task) | ⬜ pending |
| 04-04-T2 | 04-04 | 4 | (docs) | — | ARCHITECTURE.md alineado + VALIDATION.md cierre | doc | `grep "idx_unique_habit_date" .planning/codebase/ARCHITECTURE.md` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 (Now Complete)

- [x] `src/__tests__/periodHelpers.test.ts` — REQ-04-12
- [x] `src/__tests__/dedupeAssignmentsArray.test.ts` — REQ-04-03 (utility-level)
- [x] `src/__tests__/setup/testDatabase.ts` extendido con createPreMigrationTestDatabase + seedDuplicates + insertTestPerformed
- [x] `src/__tests__/migrationV1.test.ts` — REQ-04-04..09
- [x] `src/__tests__/dailyAssignments.test.ts` extendido — REQ-04-10/11/01
- [x] `src/__tests__/restorePreClean.test.ts` — REQ-04-03 (integration-level)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Migración corre silenciosamente al primer boot post-update sin feedback visible (D-06) | REQ-04-07 | Comportamiento de UI/boot sequence; no asertable desde Jest | (1) Build APK con `build-apk-local` skill; (2) Instalar sobre versión previa con DB poblada (con duplicados conocidos sembrados manualmente via `adb shell sqlite3` o pre-cargando un backup pre-fix); (3) Abrir app; (4) Verificar que NO hay UI bloqueante (sólo el ActivityIndicator de fonts breve); (5) `adb logcat -s ReactNativeJS` debe mostrar `'DB inicializada y backfill completado'` y NO `[migration v1]` errors; (6) Re-abrir app: idem (idempotencia — ningún log de migration nueva). |
| Restore de backup pre-fix con duplicados no rompe el UNIQUE INDEX | REQ-04-03 | E2E flow involucra Drive/file pickers difíciles de unit-testear | (1) Generar JSON de backup pre-fix con duplicados conocidos (via export de un APK viejo o JSON sintético); (2) Reset DB local; (3) Restore via Drive backup UI o vía importBackup local; (4) Verificar post-restore: cero duplicados (`SELECT habit_id, date, COUNT(*) FROM daily_assignments WHERE habit_id IS NOT NULL GROUP BY habit_id, date HAVING COUNT(*) > 1` retorna 0 rows); la app abre normal sin alerta de error. |
| Visibility weekly/monthly en device — completar miércoles propaga visualmente al resto de la semana | REQ-04-10 | UX visual; los unit tests cubren la lógica pero no el rendering | (1) Crear hábito weekly desde Biblioteca; (2) Verificar que el ítem aparece en DailySheet hoy; (3) Cambiar viewDate al lunes y al domingo de la misma semana ISO — el ítem aparece en ambos; (4) Completar el ítem hoy; (5) Cambiar viewDate al lunes — el ítem se ve marcado como "completado para este período" (visual: tilde verde, opacidad reducida, etc., según el render de DailySheet); (6) Idem domingo. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (periodHelpers, migrationV1, restorePreClean test files)
- [x] No watch-mode flags (`--watch` prohibido en commands de plan)
- [x] Feedback latency < 15s para quick, < 60s para full
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready-for-execute
```

**Constraints**:
- En el ARCHITECTURE.md, usar Edit (no Write) para preservar el resto del archivo intacto.
- En el 04-VALIDATION.md, podemos hacer Write completo (el archivo está marcado como `status: draft` y este task lo cierra).
  </action>
  <verify>
    <automated>grep "idx_unique_habit_date" .planning/codebase/ARCHITECTURE.md && grep "nyquist_compliant: true" .planning/phases/04-habit-creation-audit/04-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Partial UNIQUE INDEX" .planning/codebase/ARCHITECTURE.md` retorna ≥1 línea (claim corregido)
    - `grep "idx_unique_habit_date" .planning/codebase/ARCHITECTURE.md` retorna ≥1 línea (nombre del index documentado)
    - `grep "Unique constraint on \\\`(habit_id, date)\\\`" .planning/codebase/ARCHITECTURE.md` retorna 0 líneas (claim falso eliminado)
    - `grep "nyquist_compliant: true" .planning/phases/04-habit-creation-audit/04-VALIDATION.md` retorna 1 línea
    - `grep "wave_0_complete: true" .planning/phases/04-habit-creation-audit/04-VALIDATION.md` retorna 1 línea
    - `grep "Approval: ready-for-execute" .planning/phases/04-habit-creation-audit/04-VALIDATION.md` retorna 1 línea
    - `grep -c "| 04-0" .planning/phases/04-habit-creation-audit/04-VALIDATION.md` ≥ 11 (11 tasks listadas en el per-task map)
    - `npm test` exit 0 (no rompe tests — sólo docs y validation map)
  </acceptance_criteria>
  <done>ARCHITECTURE.md refleja realidad post-Phase-4; 04-VALIDATION.md cerrado con per-task map filled y nyquist compliance verificada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backup JSON ↔ DB bulk insert | Datos no confiables (backup viejo) cruzan al motor; el INDEX rechaza duplicados; pre-clean los detecta y elimina antes |
| Drive REST ↔ backupService.restoreData | Plan 03-03 ya estableció esta confianza; este plan refina el handling de duplicados sin cambiar la barrera |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | restoreData (un backup malformado podría hacer crashear el helper o producir output erróneo) | mitigate | parseAndValidate ya valida shape antes de llegar a restoreData (plan 02-03/03-03); dedupeAssignmentsArray es puro y testeado para edge cases (array vacío, todas spontaneous, todas regulares) |
| T-04-04-02 | DoS | dedupeAssignmentsArray sobre backups muy grandes | accept | Volumen estimado ≤ 10k rows en backup realista (≈3 años de uso diario × ~10 hábitos); sort O(n log n) por grupo (k pequeño) — bajo impacto |
| T-04-04-03 | Information Disclosure | n/a (sólo docs y validation map) | accept | Ningún secreto en docs; los archivos ya están en git versioning |
</threat_model>

<verification>
- `npm test -- --testPathPattern=restorePreClean` (suite scoped, ~3s)
- `npm test` (full suite GREEN — TODA la fase: periodHelpers, dedupeAssignmentsArray, migrationV1, dailyAssignments, restorePreClean)
- `grep "idx_unique_habit_date" .planning/codebase/ARCHITECTURE.md` debe retornar ≥1 línea
- `grep -E "(false|pending)" .planning/phases/04-habit-creation-audit/04-VALIDATION.md | head -1` debe estar en contextos válidos (no en `nyquist_compliant`)
- Manual smoke (post-execute, no en este plan): restaurar un backup sintético con duplicados via importBackup local; verificar que post-restore la app abre, no hay alerta de error, y `findDuplicates()` (corrido manualmente desde un test temp) retorna 0.
</verification>

<success_criteria>
- [ ] backupService.restoreData aplica dedupeAssignmentsArray pre-bulk-insert
- [ ] Test restorePreClean cubre 4 escenarios: happy path, duplicados regulares, spontaneous passthrough, mixed
- [ ] driveBackupService SIN cambios pero verificado que hereda el pre-clean (importa restoreData de backupService)
- [ ] ARCHITECTURE.md línea ~281 corregida: "Partial UNIQUE INDEX `idx_unique_habit_date` ..." reemplaza el claim falso
- [ ] 04-VALIDATION.md frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`, `status: ready-for-execute`
- [ ] 04-VALIDATION.md per-task map filled con 11 tasks (3+3+3+2)
- [ ] Suite completa `npm test` GREEN
- [ ] Phase 4 cierra con: 12 REQs cubiertos, 4 plans aprobados, 4 waves planificadas, 14 archivos modificados
</success_criteria>

<output>
After completion, create `.planning/phases/04-habit-creation-audit/04-04-SUMMARY.md` documentando:
- Cambio en backupService.restoreData (5 líneas)
- Test count en restorePreClean.test.ts
- Diff exacto aplicado a ARCHITECTURE.md
- Estado final del 04-VALIDATION.md (nyquist_compliant flipped)
- Resumen de cobertura del phase: 12 REQs cerrados (REQ-04-01..12), 4 plans, 4 waves
- Recomendaciones para `/gsd-verify-work` (smoke manual a correr post-execute)
</output>
