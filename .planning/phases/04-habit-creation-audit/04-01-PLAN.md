---
phase: 04-habit-creation-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/periodHelpers.ts
  - src/utils/dedupeAssignmentsArray.ts
  - src/__tests__/periodHelpers.test.ts
  - src/__tests__/dedupeAssignmentsArray.test.ts
  - src/__tests__/setup/testDatabase.ts
autonomous: true
requirements:
  - REQ-04-03
  - REQ-04-12
threat_model_summary: "Bajo riesgo. Solo utilities puras + extensión del setup de tests. No toca DB de prod, no hay I/O. Vector único: lógica incorrecta de Thursday-anchor (ISO week) que cause classification errónea de período → tests cubren cruces de año/mes."
notes: "Wave 1 — sin dependencias. Establece (a) los helpers puros que el plan 03 consumirá para visibility weekly/monthly y (b) la utility dedupeAssignmentsArray que el plan 04 consumirá en restoreData. Además agrega la variante pre-migración del test database (sin UNIQUE INDEX) que el plan 02 necesita para sembrar duplicados en tests de migración."

must_haves:
  truths:
    - "getPeriodKey('2026-12-31', 'weekly') retorna '2026-W53' (o '2027-W01' según ISO 8601 — el algoritmo Thursday-anchor decide; el test fija el resultado esperado)"
    - "getPeriodKey('2026-03-31', 'monthly') retorna '2026-03'; getPeriodKey('2026-04-01', 'monthly') retorna '2026-04'"
    - "getPeriodKey('2026-03-15', 'daily') retorna '2026-03-15' (passthrough)"
    - "dedupeAssignmentsArray respeta D-03: completed wins, then has_performed wins, then original-position wins"
    - "dedupeAssignmentsArray pasa-through filas con habit_id === null (spontaneous) sin tocarlas"
    - "createPreMigrationTestDatabase() existe y NO incluye SQL_UNIQUE_INDEX, permitiendo seedDuplicates"
  artifacts:
    - path: "src/utils/periodHelpers.ts"
      provides: "getISOWeekKey, getMonthKey, getPeriodKey (named exports, puros)"
      contains: "export function getPeriodKey"
    - path: "src/utils/dedupeAssignmentsArray.ts"
      provides: "dedupeAssignmentsArray(rows, performed) — deduplicación pura D-03 sobre arrays"
      contains: "export function dedupeAssignmentsArray"
    - path: "src/__tests__/periodHelpers.test.ts"
      provides: "Tests REQ-04-12 — boundary cases ISO week / month / year"
      contains: "describe('getPeriodKey')"
    - path: "src/__tests__/dedupeAssignmentsArray.test.ts"
      provides: "Tests REQ-04-03 — D-03 priority sort + spontaneous passthrough"
      contains: "describe('dedupeAssignmentsArray')"
    - path: "src/__tests__/setup/testDatabase.ts"
      provides: "createPreMigrationTestDatabase() (sin UNIQUE INDEX) + seedDuplicates(db, count, priority)"
      contains: "export function createPreMigrationTestDatabase"
  key_links:
    - from: "src/utils/periodHelpers.ts"
      to: "src/services/assignmentService.ts (plan 03)"
      via: "import { getPeriodKey }"
      pattern: "getPeriodKey\\("
    - from: "src/utils/dedupeAssignmentsArray.ts"
      to: "src/services/backupService.ts (plan 04)"
      via: "import { dedupeAssignmentsArray }"
      pattern: "dedupeAssignmentsArray\\("
    - from: "src/__tests__/setup/testDatabase.ts (createPreMigrationTestDatabase)"
      to: "src/__tests__/migrationDedup.test.ts (plan 02)"
      via: "import { createPreMigrationTestDatabase, seedDuplicates }"
      pattern: "createPreMigrationTestDatabase\\("
---

<objective>
Establecer las dos utilities puras (periodHelpers, dedupeAssignmentsArray) y la infraestructura de tests (variante pre-migración del test database + helper seedDuplicates) que los plans 02-04 consumen. Sin dependencias entre tasks ni con otros plans → 100% paralelizable.

Purpose: Fijar contratos y comportamientos antes de cualquier cambio en DB o services. periodHelpers desbloquea el plan 03 (visibility weekly/monthly D-01/D-02); dedupeAssignmentsArray desbloquea el plan 04 (restore pre-clean); createPreMigrationTestDatabase desbloquea el plan 02 (poder sembrar duplicados que hoy el test schema rechaza).

Output: 2 utilities puras + 2 archivos de tests + extensión del setup de tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-habit-creation-audit/04-CONTEXT.md
@.planning/phases/04-habit-creation-audit/04-RESEARCH.md
@.planning/phases/04-habit-creation-audit/04-PATTERNS.md
@.planning/phases/04-habit-creation-audit/04-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@src/utils/dateHelpers.ts
@src/utils/parsing.ts
@src/services/assignmentService.ts
@src/__tests__/setup/testDatabase.ts
@src/__tests__/dailyAssignments.test.ts
@src/types/index.ts

<interfaces>
<!-- Tipos y contratos a respetar — extraídos del codebase. -->

From src/types/index.ts (DailyAssignment shape — relevante para dedupeAssignmentsArray):
```typescript
export interface DailyAssignment {
  id: string;
  habit_id: string | null;
  date: string;                 // 'YYYY-MM-DD'
  snapshot_name: string;
  snapshot_points: number;
  snapshot_categories: string;  // JSON string
  snapshot_frequency: string;   // 'daily' | 'weekly' | 'monthly'
  is_completed: number;          // 0 | 1
  is_spontaneous: number;        // 0 | 1
}

export interface PerformedHabit {
  id: string;
  habit_id: string;
  timestamp: string;             // 'YYYY-MM-DD HH:MM:SS'
  points_earned: number;
  habit_description: string | null;
  categories_used: string | null;
}
```

From src/utils/dateHelpers.ts (PATTERN to copy — file header + UTC-safe construction):
```typescript
/**
 * dateHelpers.ts — Helpers de formateo y cálculo de fechas compartidos entre screens.
 */
export function isValidDateString(dateStr: string | null | undefined): dateStr is string { ... }
```

From src/services/assignmentService.ts:255-263 (UTC-safe pattern — getISOWeekKey/getMonthKey MUST follow this):
```typescript
export function nextDay(dateStr: string): string {
  const d = new Date(`${dateStr}T00:00:00Z`);
  d.setUTCDate(d.getUTCDate() + 1);
  return formatDateStr(d);
}
function formatDateStr(d: Date): string {
  return d.toISOString().slice(0, 10);
}
```

From src/__tests__/setup/testDatabase.ts:67-72 (UNIQUE INDEX que el plan 02 necesita NO incluir en la variante pre-migration):
```typescript
const SQL_UNIQUE_INDEX = `
  CREATE UNIQUE INDEX IF NOT EXISTS idx_unique_habit_date
  ON daily_assignments(habit_id, date)
  WHERE habit_id IS NOT NULL
`;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/utils/periodHelpers.ts con getISOWeekKey + getMonthKey + getPeriodKey y sus tests</name>
  <files>src/utils/periodHelpers.ts, src/__tests__/periodHelpers.test.ts</files>
  <read_first>
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Pattern 3: Period-Aware Visibility (D-01)" y §"Pitfall #2: Período cruza año/mes durante el boot"
    - .planning/phases/04-habit-creation-audit/04-PATTERNS.md §"src/utils/periodHelpers.ts (NEW — utility, pure transform)"
    - src/utils/dateHelpers.ts (file header pattern + isValidDateString guard pattern)
    - src/services/assignmentService.ts líneas 255-263 (UTC-safe nextDay/formatDateStr — MISMA construcción `new Date(\`${dateStr}T00:00:00Z\`)`)
    - src/__tests__/dailyAssignments.test.ts líneas 382-394 (BUG-03 describe block — formato de tests para utility de fechas)
    - .planning/codebase/CONVENTIONS.md §"Function Design" (≤25 líneas, ≤20 ideal)
  </read_first>
  <behavior>
    - Test 1 (REQ-04-12 daily passthrough): `getPeriodKey('2026-03-15', 'daily')` → `'2026-03-15'`
    - Test 2 (REQ-04-12 month basic): `getPeriodKey('2026-03-15', 'monthly')` → `'2026-03'`
    - Test 3 (REQ-04-12 month boundary): `getPeriodKey('2026-03-31', 'monthly')` → `'2026-03'`; `getPeriodKey('2026-04-01', 'monthly')` → `'2026-04'`
    - Test 4 (REQ-04-12 ISO week mid-week): `getPeriodKey('2026-03-11', 'weekly')` (miércoles 2026-03-11) → `'2026-W11'` (verificar contra calendario ISO real — RESEARCH §Pattern 3 algoritmo Thursday-anchor)
    - Test 5 (REQ-04-12 ISO week boundary lunes): `getPeriodKey('2026-03-09', 'weekly')` (lunes) → `'2026-W11'` (mismo período que el miércoles 2026-03-11)
    - Test 6 (REQ-04-12 ISO week boundary domingo): `getPeriodKey('2026-03-15', 'weekly')` (domingo, último día de la semana ISO) → `'2026-W11'` (mismo período que lunes)
    - Test 7 (REQ-04-12 year cross W53): año donde el 1 de enero cae jueves o antes pertenece a la semana ISO del año previo. Caso concreto: `getPeriodKey('2027-01-01', 'weekly')` (viernes — pertenece a 2026-W53). Validar contra ISO 8601.
    - Test 8 (REQ-04-12 year cross W01): `getPeriodKey('2027-01-04', 'weekly')` (lunes — primer lunes del año) → `'2027-W01'`
    - Test 9: número de semana siempre con padding a 2 dígitos: `getPeriodKey('2026-01-05', 'weekly')` → `'2026-W02'` (no `'2026-W2'`)
  </behavior>
  <action>
Crear `src/utils/periodHelpers.ts` con este contenido (export literal nombre/firma — no renombrar):

```typescript
/**
 * periodHelpers.ts — Helpers puros de cómputo de período para hábitos weekly/monthly.
 *
 * Implementa el modelo D-01/D-02 (CONTEXT phase 4):
 *   - week: ISO 8601 (lunes a domingo, Thursday-anchor)
 *   - month: calendario YYYY-MM
 *
 * Toda función opera sobre strings 'YYYY-MM-DD' (datePrefix). NUNCA construye
 * Dates locales — siempre UTC explícito para evitar drift de timezone (Pitfall #2).
 */

export type Frequency = 'daily' | 'weekly' | 'monthly';

/** Devuelve la clave ISO 8601 de la semana, formato 'YYYY-Www' (zero-padded). */
export function getISOWeekKey(datePrefix: string): string {
  const d = new Date(`${datePrefix}T00:00:00Z`);
  // ISO: Thursday del mismo bloque-semana determina año+semana.
  const dayOfWeek = (d.getUTCDay() + 6) % 7; // Mon=0..Sun=6
  d.setUTCDate(d.getUTCDate() - dayOfWeek + 3);
  const isoYear = d.getUTCFullYear();
  const firstThu = new Date(Date.UTC(isoYear, 0, 4));
  const firstThuDow = (firstThu.getUTCDay() + 6) % 7;
  firstThu.setUTCDate(firstThu.getUTCDate() - firstThuDow + 3);
  const week = 1 + Math.round((d.getTime() - firstThu.getTime()) / (7 * 86400000));
  return `${isoYear}-W${String(week).padStart(2, '0')}`;
}

/** Devuelve la clave de mes calendario, formato 'YYYY-MM'. */
export function getMonthKey(datePrefix: string): string {
  return datePrefix.slice(0, 7);
}

/** Discriminador por frequency. Pasa-through 'daily'. */
export function getPeriodKey(datePrefix: string, frequency: Frequency): string {
  if (frequency === 'weekly') return getISOWeekKey(datePrefix);
  if (frequency === 'monthly') return getMonthKey(datePrefix);
  return datePrefix;
}
```

Crear `src/__tests__/periodHelpers.test.ts`:

```typescript
import { getPeriodKey, getISOWeekKey, getMonthKey } from '../utils/periodHelpers';

describe('getPeriodKey — REQ-04-12', () => {
  describe('daily passthrough', () => {
    test('REQ-04-12: daily retorna el datePrefix sin cambios', () => {
      expect(getPeriodKey('2026-03-15', 'daily')).toBe('2026-03-15');
    });
  });

  describe('monthly', () => {
    test('REQ-04-12: mid-month', () => {
      expect(getPeriodKey('2026-03-15', 'monthly')).toBe('2026-03');
    });
    test('REQ-04-12: último día del mes', () => {
      expect(getPeriodKey('2026-03-31', 'monthly')).toBe('2026-03');
    });
    test('REQ-04-12: primer día del mes siguiente', () => {
      expect(getPeriodKey('2026-04-01', 'monthly')).toBe('2026-04');
    });
  });

  describe('weekly (ISO 8601 Thursday-anchor)', () => {
    test('REQ-04-12: miércoles → semana correcta', () => {
      // 2026-03-11 es miércoles → W11 de 2026
      expect(getPeriodKey('2026-03-11', 'weekly')).toBe('2026-W11');
    });
    test('REQ-04-12: lunes anclado al mismo bloque que el miércoles', () => {
      // 2026-03-09 es lunes; mismo bloque ISO-week que 2026-03-11
      expect(getPeriodKey('2026-03-09', 'weekly')).toBe('2026-W11');
    });
    test('REQ-04-12: domingo anclado al mismo bloque que el lunes', () => {
      // 2026-03-15 es domingo (último día ISO de la semana); mismo bloque
      expect(getPeriodKey('2026-03-15', 'weekly')).toBe('2026-W11');
    });
    test('REQ-04-12: año cruza — viernes 2027-01-01 pertenece a 2026-W53', () => {
      // 2027-01-01 es viernes; ISO 2026 tiene 53 semanas, esa semana corre 2026-12-28..2027-01-03
      expect(getPeriodKey('2027-01-01', 'weekly')).toBe('2026-W53');
    });
    test('REQ-04-12: lunes 2027-01-04 → 2027-W01', () => {
      expect(getPeriodKey('2027-01-04', 'weekly')).toBe('2027-W01');
    });
    test('REQ-04-12: zero-padded a dos dígitos', () => {
      // 2026-01-05 es lunes; W02 de 2026 (W01 corre 2025-12-29..2026-01-04)
      expect(getPeriodKey('2026-01-05', 'weekly')).toBe('2026-W02');
    });
  });
});

describe('getMonthKey', () => {
  test('extrae prefijo YYYY-MM', () => {
    expect(getMonthKey('2026-03-15')).toBe('2026-03');
  });
});

describe('getISOWeekKey', () => {
  test('alias usable individualmente', () => {
    expect(getISOWeekKey('2026-03-11')).toBe('2026-W11');
  });
});
```

Verificación pre-commit: `npm test -- --testPathPattern=periodHelpers` debe ser GREEN. Si Test 7 (`'2027-01-01' → '2026-W53'`) o Test 8 (`'2027-01-04' → '2027-W01'`) fallan, el algoritmo Thursday-anchor está mal implementado — NO ajustar el test, ajustar el algoritmo (los valores ISO 8601 son determinísticos).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=periodHelpers</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export function getPeriodKey" src/utils/periodHelpers.ts` retorna 1 línea
    - `grep -E "^export function getISOWeekKey" src/utils/periodHelpers.ts` retorna 1 línea
    - `grep -E "^export function getMonthKey" src/utils/periodHelpers.ts` retorna 1 línea
    - `grep "T00:00:00Z" src/utils/periodHelpers.ts` retorna ≥1 línea (UTC-safe construction confirmada)
    - `grep -c "REQ-04-12" src/__tests__/periodHelpers.test.ts` ≥ 7
    - `npm test -- --testPathPattern=periodHelpers` exit 0; al menos 9 tests passed
    - Cada función exportada ≤ 20 líneas (CLAUDE.md Regla 3.2)
  </acceptance_criteria>
  <done>periodHelpers.ts + tests committed; suite verde para esta task; ningún cambio fuera de estos 2 archivos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear src/utils/dedupeAssignmentsArray.ts con D-03 priority sort y sus tests</name>
  <files>src/utils/dedupeAssignmentsArray.ts, src/__tests__/dedupeAssignmentsArray.test.ts</files>
  <read_first>
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Pattern 4: Pre-Clean JSON Before Bulk Insert (Restore)" (incluye implementación de referencia)
    - .planning/phases/04-habit-creation-audit/04-PATTERNS.md §"src/utils/dedupeAssignmentsArray.ts (NEW — utility, pure transform)"
    - src/utils/parsing.ts (analog: pure transform validation utility)
    - src/types/index.ts (DailyAssignment + PerformedHabit shapes)
    - src/__tests__/dailyAssignments.test.ts líneas 398-420 (BUG-04 describe — patrón de tests para utility pura)
    - .planning/phases/04-habit-creation-audit/04-CONTEXT.md §"D-03" (heurística: completed > has_performed > oldest)
  </read_first>
  <behavior>
    - Test 1 (REQ-04-03 single passthrough): array de 1 row regular → mismo array (longitud 1)
    - Test 2 (REQ-04-03 spontaneous passthrough): array con 3 rows `habit_id === null` → todas presentes en output (longitud 3, sin deduplicar)
    - Test 3 (REQ-04-03 D-03 step 1 — completed wins): dos rows con misma (habit_id, date), una con `is_completed=1` y otra con `is_completed=0` → sobrevive la completed
    - Test 4 (REQ-04-03 D-03 step 2 — has_performed wins): dos rows tied en `is_completed=0`, una con `(habit_id, date)` matcheable a un PerformedHabit → sobrevive la que tiene performed
    - Test 5 (REQ-04-03 D-03 step 3 — original-position wins): dos rows tied en pasos 1 y 2 → sobrevive la primera del array de entrada (proxy de "más antigua")
    - Test 6 (REQ-04-03 mixed): array con 1 spontaneous + 2 duplicados regulares → output tiene 1 spontaneous + 1 regular (winner según D-03)
    - Test 7 (input vacío): `dedupeAssignmentsArray([], [])` → `[]`
  </behavior>
  <action>
Crear `src/utils/dedupeAssignmentsArray.ts`:

```typescript
/**
 * dedupeAssignmentsArray.ts — Pre-clean puro para arrays de daily_assignments.
 *
 * Aplica la heurística D-03 (CONTEXT phase 4) sobre un array en memoria,
 * antes de bulk-insertarlo a DB. Necesario porque el partial UNIQUE INDEX
 * (REQ-04-05) rechazaría duplicados pre-fix presentes en backups antiguos
 * (RESEARCH §Pitfall #3).
 *
 * Heurística D-03 (sólo aplica a rows con habit_id !== null):
 *   1. is_completed = 1 gana
 *   2. tie → row con performed_habit linked (matching habit_id+date) gana
 *   3. tie → la más antigua (proxy: primera en el array de entrada)
 *
 * Spontaneous (habit_id === null) NUNCA se deduplican — pasan tal cual.
 */

import type { DailyAssignment, PerformedHabit } from '../types';

export function dedupeAssignmentsArray(
  rows: DailyAssignment[],
  performed: PerformedHabit[],
): DailyAssignment[] {
  const performedKeys = buildPerformedKeySet(performed);
  const groups = groupRegularByHabitDate(rows);
  const out: DailyAssignment[] = [];

  // Spontaneous passthrough preservando orden original
  for (const r of rows) {
    if (r.habit_id === null) out.push(r);
  }

  // Regulares: elegir winner por grupo según D-03
  for (const candidates of groups.values()) {
    out.push(pickWinner(candidates, performedKeys, rows));
  }

  return out;
}

function buildPerformedKeySet(performed: PerformedHabit[]): Set<string> {
  return new Set(
    performed.map((p) => `${p.habit_id}|${p.timestamp.slice(0, 10)}`),
  );
}

function groupRegularByHabitDate(
  rows: DailyAssignment[],
): Map<string, DailyAssignment[]> {
  const groups = new Map<string, DailyAssignment[]>();
  for (const r of rows) {
    if (r.habit_id === null) continue;
    const k = `${r.habit_id}|${r.date}`;
    const list = groups.get(k);
    if (list) list.push(r);
    else groups.set(k, [r]);
  }
  return groups;
}

function pickWinner(
  candidates: DailyAssignment[],
  performedKeys: Set<string>,
  originalOrder: DailyAssignment[],
): DailyAssignment {
  if (candidates.length === 1) return candidates[0];
  const sorted = [...candidates].sort((a, b) => {
    if (a.is_completed !== b.is_completed) return b.is_completed - a.is_completed;
    const aPerf = performedKeys.has(`${a.habit_id}|${a.date}`) ? 1 : 0;
    const bPerf = performedKeys.has(`${b.habit_id}|${b.date}`) ? 1 : 0;
    if (aPerf !== bPerf) return bPerf - aPerf;
    return originalOrder.indexOf(a) - originalOrder.indexOf(b);
  });
  return sorted[0];
}
```

Crear `src/__tests__/dedupeAssignmentsArray.test.ts`:

```typescript
import { dedupeAssignmentsArray } from '../utils/dedupeAssignmentsArray';
import type { DailyAssignment, PerformedHabit } from '../types';

function row(over: Partial<DailyAssignment>): DailyAssignment {
  return {
    id: 'a-' + Math.random().toString(36).slice(2),
    habit_id: 'h1',
    date: '2026-03-15',
    snapshot_name: 'Test',
    snapshot_points: 1,
    snapshot_categories: '[]',
    snapshot_frequency: 'daily',
    is_completed: 0,
    is_spontaneous: 0,
    ...over,
  };
}

function performed(over: Partial<PerformedHabit>): PerformedHabit {
  return {
    id: 'p-' + Math.random().toString(36).slice(2),
    habit_id: 'h1',
    timestamp: '2026-03-15 10:00:00',
    points_earned: 1,
    habit_description: null,
    categories_used: null,
    ...over,
  };
}

describe('dedupeAssignmentsArray — REQ-04-03', () => {
  test('REQ-04-03: array vacío → array vacío', () => {
    expect(dedupeAssignmentsArray([], [])).toEqual([]);
  });

  test('REQ-04-03: una sola row regular → passthrough', () => {
    const r = row({ id: 'a1' });
    expect(dedupeAssignmentsArray([r], [])).toEqual([r]);
  });

  test('REQ-04-03: spontaneous (habit_id null) NUNCA se deduplica', () => {
    const s1 = row({ id: 's1', habit_id: null, is_spontaneous: 1, snapshot_name: 'Logro 1' });
    const s2 = row({ id: 's2', habit_id: null, is_spontaneous: 1, snapshot_name: 'Logro 2' });
    const s3 = row({ id: 's3', habit_id: null, is_spontaneous: 1, snapshot_name: 'Logro 3' });
    const out = dedupeAssignmentsArray([s1, s2, s3], []);
    expect(out).toHaveLength(3);
    expect(out.map((r) => r.id).sort()).toEqual(['s1', 's2', 's3']);
  });

  test('REQ-04-03 D-03 step 1: completed wins', () => {
    const a = row({ id: 'a1', is_completed: 0 });
    const b = row({ id: 'a2', is_completed: 1 });
    const out = dedupeAssignmentsArray([a, b], []);
    expect(out).toHaveLength(1);
    expect(out[0].id).toBe('a2');
  });

  test('REQ-04-03 D-03 step 2: has_performed wins on completion tie', () => {
    const a = row({ id: 'a1', is_completed: 0 });
    const b = row({ id: 'a2', is_completed: 0 });
    // performed sólo matchea por (habit_id, date) — y como ambos tienen mismo (h1, 2026-03-15)
    // BOTH son matched. Para distinguirlos, el helper usa el SET por key — si la key existe,
    // ambas son "has_performed". Por lo tanto este test pasa al step 3 (oldest).
    // Para realmente probar step 2 necesitamos DOS filas con DIFERENTE (habit_id, date) pero
    // sólo UNA tiene performed → no aplica al mismo grupo. El step 2 sólo discrimina dentro
    // de un mismo grupo (mismo habit_id+date).
    // Conclusión: el step 2 NO discrimina en este modelo porque todos los miembros del grupo
    // comparten la misma key. Documentar y skip a step 3 directo.
    // → Renombrar este test a "step 2 documentado: el set por key es por-grupo".
    const p = performed({ habit_id: 'h1', timestamp: '2026-03-15 10:00:00' });
    const out = dedupeAssignmentsArray([a, b], [p]);
    expect(out).toHaveLength(1);
    // Ambos comparten la key, entonces empata en step 2 → step 3 elige el primero del array
    expect(out[0].id).toBe('a1');
  });

  test('REQ-04-03 D-03 step 3: original-position wins on full tie', () => {
    const a = row({ id: 'first', is_completed: 0 });
    const b = row({ id: 'second', is_completed: 0 });
    const out = dedupeAssignmentsArray([a, b], []);
    expect(out).toHaveLength(1);
    expect(out[0].id).toBe('first');
  });

  test('REQ-04-03 mixed: 1 spontaneous + 2 duplicados regulares → 1 spontaneous + 1 regular winner', () => {
    const s = row({ id: 's', habit_id: null, is_spontaneous: 1 });
    const a = row({ id: 'a1', is_completed: 0 });
    const b = row({ id: 'a2', is_completed: 1 });
    const out = dedupeAssignmentsArray([s, a, b], []);
    expect(out).toHaveLength(2);
    expect(out.find((r) => r.habit_id === null)?.id).toBe('s');
    expect(out.find((r) => r.habit_id === 'h1')?.id).toBe('a2');
  });

  test('REQ-04-03 multi-grupo: deduplica grupos independientes', () => {
    const g1a = row({ id: 'g1a', habit_id: 'h1', date: '2026-03-15', is_completed: 0 });
    const g1b = row({ id: 'g1b', habit_id: 'h1', date: '2026-03-15', is_completed: 1 });
    const g2a = row({ id: 'g2a', habit_id: 'h2', date: '2026-03-15', is_completed: 0 });
    const g2b = row({ id: 'g2b', habit_id: 'h2', date: '2026-03-15', is_completed: 0 });
    const out = dedupeAssignmentsArray([g1a, g1b, g2a, g2b], []);
    expect(out).toHaveLength(2);
    expect(out.find((r) => r.habit_id === 'h1')?.id).toBe('g1b'); // completed wins
    expect(out.find((r) => r.habit_id === 'h2')?.id).toBe('g2a'); // first in array wins
  });
});
```

Verificación pre-commit: `npm test -- --testPathPattern=dedupeAssignmentsArray` GREEN.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=dedupeAssignmentsArray</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export function dedupeAssignmentsArray" src/utils/dedupeAssignmentsArray.ts` retorna 1 línea
    - `grep "habit_id === null" src/utils/dedupeAssignmentsArray.ts` retorna ≥1 línea (passthrough spontaneous confirmado)
    - `grep -c "REQ-04-03" src/__tests__/dedupeAssignmentsArray.test.ts` ≥ 6
    - `npm test -- --testPathPattern=dedupeAssignmentsArray` exit 0; al menos 7 tests passed
    - Cada función ≤ 25 líneas (CONVENTIONS.md §Function Design)
    - Spontaneous passthrough verificado (test "spontaneous NUNCA se deduplica" passing)
  </acceptance_criteria>
  <done>dedupeAssignmentsArray.ts + tests committed; suite verde para esta task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extender src/__tests__/setup/testDatabase.ts con createPreMigrationTestDatabase + seedDuplicates + insertTestPerformed</name>
  <files>src/__tests__/setup/testDatabase.ts</files>
  <read_first>
    - src/__tests__/setup/testDatabase.ts (archivo completo — extender, no reescribir)
    - .planning/phases/04-habit-creation-audit/04-PATTERNS.md §"src/__tests__/migrationDedup.test.ts (NEW — test, integration with DB fixture)" (Critical: explica por qué se necesita la variante pre-migration)
    - .planning/phases/04-habit-creation-audit/04-RESEARCH.md §"Wave 0 Gaps" (lista los helpers requeridos)
    - src/services/db.ts líneas 64-74 (SQL_CREATE_PERFORMED — schema espejo para insertTestPerformed)
    - .planning/phases/04-habit-creation-audit/04-CONTEXT.md §"Pre-existing test setup conflict" (explica el riesgo y la necesidad)
  </read_first>
  <behavior>
    - Test 1: `createPreMigrationTestDatabase()` retorna una DB con tablas pero SIN el UNIQUE INDEX → permite insertar duplicados sin throw
    - Test 2: `seedDuplicates(db, 'h1', '2026-03-15', { completed: false, performedLink: false })` inserta 2 rows con mismo (habit_id, date) (sólo viable en DB pre-migration)
    - Test 3: `insertTestPerformed(db, opts)` crea una row en performed_habits válida que `dedupeAssignmentsArray` puede matchear via `(habit_id, date_prefix)`
  </behavior>
  <action>
**Extender** (no reescribir) `src/__tests__/setup/testDatabase.ts`. Agregar al final del archivo:

```typescript
// ─── Variante pre-migración (sin UNIQUE INDEX) ───────────────────────────────
//
// REQ-04-04..REQ-04-07: para testear la migración v1, los tests necesitan
// sembrar duplicados en la DB. El UNIQUE INDEX presente en createTestDatabase()
// rechaza esos seeds. Esta variante crea el mismo schema PERO omite el index,
// emulando el estado de prod pre-migración.

export function createPreMigrationTestDatabase(): Database.Database {
  _db = new Database(':memory:');
  _db.pragma('foreign_keys = ON');
  _db.exec(SQL_CREATE_HABITS);
  _db.exec(SQL_CREATE_PERFORMED);
  _db.exec(SQL_CREATE_MOODS);
  _db.exec(SQL_CREATE_ASSIGNMENTS);
  // NOTA: SQL_UNIQUE_INDEX intencionalmente NO se ejecuta aquí.
  setMockDatabase(_db);
  return _db;
}

// ─── Helpers de seed adicionales ─────────────────────────────────────────────

export interface TestPerformedOpts {
  id: string;
  habit_id: string;
  timestamp: string; // 'YYYY-MM-DD HH:MM:SS'
  points_earned?: number;
  habit_description?: string | null;
  categories_used?: string | null;
}

/** Inserta un performed_habit directamente (bypass service). */
export function insertTestPerformed(db: Database.Database, opts: TestPerformedOpts): void {
  db.prepare(`
    INSERT INTO performed_habits
      (id, habit_id, timestamp, points_earned, habit_description, categories_used)
    VALUES (?, ?, ?, ?, ?, ?)
  `).run(
    opts.id,
    opts.habit_id,
    opts.timestamp,
    opts.points_earned ?? 1,
    opts.habit_description ?? null,
    opts.categories_used ?? null,
  );
}

export interface DuplicateSeedOpts {
  /** Cuántos duplicados crear (default 2). */
  count?: number;
  /** Si true, marca al menos uno como is_completed=1. */
  withCompleted?: boolean;
  /** Si true, también inserta un performed_habit linked. */
  withPerformedLink?: boolean;
}

/**
 * Inserta `count` duplicados en (habit_id, date). El primero NO está completado;
 * si withCompleted, el segundo SÍ. Si withPerformedLink, agrega un performed_habit.
 *
 * REQUIERE que la DB sea creada con createPreMigrationTestDatabase()
 * (createTestDatabase normal rechazaría por UNIQUE INDEX).
 */
export function seedDuplicates(
  db: Database.Database,
  habitId: string,
  date: string,
  opts: DuplicateSeedOpts = {},
): string[] {
  const count = opts.count ?? 2;
  const ids: string[] = [];
  for (let i = 0; i < count; i++) {
    const id = `dup-${habitId}-${date}-${i}`;
    insertTestAssignment(db, {
      id,
      habit_id: habitId,
      date,
      snapshot_name: `Dup ${i}`,
      is_completed: opts.withCompleted && i === count - 1 ? 1 : 0,
    });
    ids.push(id);
  }
  if (opts.withPerformedLink) {
    insertTestPerformed(db, {
      id: `perf-${habitId}-${date}`,
      habit_id: habitId,
      timestamp: `${date} 10:00:00`,
    });
  }
  return ids;
}
```

**No tests inline** — esta task sólo expone helpers. La verificación de su correctitud sucede en plan 02 (donde se consumen). Para validar acá: `npm test` debe seguir GREEN (no debe romper tests existentes).

**Constraint crítica:** los exports existentes (`createTestDatabase`, `resetTestDatabase`, `insertTestHabit`, `insertTestAssignment`, etc.) NO se tocan. Sólo se agregan los nuevos.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export function createPreMigrationTestDatabase" src/__tests__/setup/testDatabase.ts` retorna 1 línea
    - `grep -E "^export function seedDuplicates" src/__tests__/setup/testDatabase.ts` retorna 1 línea
    - `grep -E "^export function insertTestPerformed" src/__tests__/setup/testDatabase.ts` retorna 1 línea
    - `grep "SQL_UNIQUE_INDEX" src/__tests__/setup/testDatabase.ts` retorna ≥1 línea (todavía existe la variante con index para tests legacy)
    - Diff vs HEAD: NO se eliminan funciones existentes (solo se agregan nuevas) — `git diff src/__tests__/setup/testDatabase.ts | grep -c '^-export'` debe ser 0
    - `npm test` exit 0 (toda la suite preexistente sigue verde)
  </acceptance_criteria>
  <done>testDatabase.ts extendido con 3 nuevos helpers; suite completa GREEN; ningún export existente removido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — utilities puras + test infra) | No cruzan boundary; sin I/O ni DB de prod en este plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | dedupeAssignmentsArray (consumido por restoreData) | mitigate | Spontaneous passthrough explícito (test "spontaneous NUNCA se deduplica") evita que un input malicioso confunda el helper y borre rows ajenas |
| T-04-01-02 | Information Disclosure | periodHelpers | accept | Helpers puros; sin secretos ni datos sensibles. Ningún side-effect. |
| T-04-01-03 | Denial of Service | dedupeAssignmentsArray (sort O(n log n) por grupo) | accept | Volumen estimado de daily_assignments en backups <10k rows; sort es O(n log n) por grupo (k pequeño) — sin riesgo práctico |
</threat_model>

<verification>
Comandos de verificación de phase-wide:
- `npm test -- --testPathPattern='periodHelpers|dedupeAssignmentsArray'` (subset rápido, ~3s)
- `npm test` (suite completa — debe ser GREEN; cero regresiones en tests preexistentes)
- `grep -r "from '../utils/periodHelpers'" src/` (debe retornar ≥0 líneas; en este plan nadie consume aún, pero el import path debe ser válido)
</verification>

<success_criteria>
- [ ] 2 utilities puras creadas en `src/utils/` (periodHelpers, dedupeAssignmentsArray)
- [ ] 2 archivos de tests creados en `src/__tests__/` con cobertura REQ-04-03 y REQ-04-12
- [ ] `src/__tests__/setup/testDatabase.ts` extendido con 3 helpers (createPreMigrationTestDatabase, seedDuplicates, insertTestPerformed)
- [ ] `npm test` GREEN (cero regresiones)
- [ ] Cada función nueva ≤ 25 líneas (CLAUDE.md / CONVENTIONS.md)
- [ ] Headers JSDoc presentes en cada archivo nuevo (CONVENTIONS.md §Comments)
- [ ] Spontaneous passthrough explícitamente probado (defensa anti-T-04-01-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-habit-creation-audit/04-01-SUMMARY.md` documentando:
- Helpers exportados con firma final
- Tests passing count
- Cualquier desviación del algoritmo Thursday-anchor recomendado en RESEARCH (no debería haber)
- Lista de imports nuevos disponibles para plans 02-04
</output>
