---
phase: 02-tech-debt
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/hooks/useSpeechRecognition.ts
  - src/services/backupService.ts
  - src/__tests__/speechRecognition.test.ts
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02

must_haves:
  truths:
    - "useSpeechRecognition.ts ya no contiene ningún `any` explícito (DEBT-01 success criterion)"
    - "useSpeechRecognition.ts define inline una interfaz SpeechModuleInterface y una interfaz SpeechRecognitionEvent"
    - "El loader `require('expo-speech-recognition')` + try/catch se mantiene exactamente como está (D-13), solo se le agrega el cast tipado"
    - "backupService.parseAndValidate ya no contiene `as Partial<BackupData>` ni `as BackupData` (D-04, alcance ampliado de DEBT-02)"
    - "El parser de BackupData usa type guards de runtime (parsea a unknown, narrowing inline) para retornar BackupData sin assertions"
    - "Los mensajes de error en español del parser de backup se preservan: 'Formato de respaldo inválido', 'Falta performed_habits en el respaldo', 'Falta mood_entries en el respaldo'"
    - "speechRecognition.test.ts ejerce los dos paths del hook (módulo ausente vs presente) sin requerir react-test-renderer"
  artifacts:
    - path: "src/hooks/useSpeechRecognition.ts"
      provides: "Hook con SpeechModuleInterface tipada y cero any explícitos"
      contains: "SpeechModuleInterface"
      min_lines: 80
    - path: "src/services/backupService.ts"
      provides: "parseAndValidate sin assertions `as` — retorna BackupData via type guards"
      contains: "parseAndValidate"
    - path: "src/__tests__/speechRecognition.test.ts"
      provides: "Tests para los dos paths del hook (módulo ausente, módulo presente)"
      contains: "useSpeechRecognition"
  key_links:
    - from: "src/hooks/useSpeechRecognition.ts"
      to: "expo-speech-recognition (optional native module)"
      via: "require + try/catch tipado con SpeechModuleInterface"
      pattern: "SpeechModuleInterface"
    - from: "src/services/backupService.ts"
      to: "src/types/index.ts (BackupData interface)"
      via: "type guards sobre unknown para construir BackupData sin as"
      pattern: "function parseAndValidate"
---

<objective>
Eliminar los dos `any` explícitos restantes del codebase (líneas 16 y 41 de `useSpeechRecognition.ts`) introduciendo `SpeechModuleInterface` y `SpeechRecognitionEvent` inline, y eliminar las dos `as` assertions de `backupService.parseAndValidate` (cast `as Partial<BackupData>` y cast final `as BackupData`) reemplazándolas por type guards sobre `unknown`.

Purpose: Cerrar DEBT-01 completo (cero `any` en el archivo del hook) y la última pieza de DEBT-02 (alcance ampliado D-04 — el parse de backup también es JSON parsing tipado, dejando el archivo listo para que Phase 3 monte Drive sobre `backupService` sin deuda heredada).
Output: Hook tipado, parser de backup sin `as`, tests reales para `useSpeechRecognition` (reemplazo de los `it.todo` stubs creados en plan 02-01).
</objective>

<execution_context>
@/Users/facu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/facu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tech-debt/02-CONTEXT.md
@.planning/phases/02-tech-debt/02-RESEARCH.md
@.planning/phases/02-tech-debt/02-PATTERNS.md
@.planning/phases/02-tech-debt/02-VALIDATION.md
@.planning/phases/02-tech-debt/02-01-SUMMARY.md

<interfaces>
Current src/hooks/useSpeechRecognition.ts líneas 16-22 (a TIPAR):
- `let SpeechModule: any = null;` — línea 16 (any #1)
- try { SpeechModule = require('expo-speech-recognition'); } catch ...

Uso del callback en línea 41 (a TIPAR):
- `const sub = SpeechModule.addResultListener?.((event: any) => { ... });` (any #2)

API real usada por el hook (verificada en código):
- `SpeechModule.addResultListener(cb)` retorna `{ remove(): void } | undefined`
- `SpeechModule.ExpoSpeechRecognitionModule.start({ lang: string })` retorna `Promise<void>`
- `SpeechModule.ExpoSpeechRecognitionModule.stop()` retorna `Promise<void>`
- `event.results[0].transcript` es `string`

Current src/services/backupService.ts parseAndValidate (líneas 80-99) — CAST 1 y CAST 2 a eliminar:
- `const data = JSON.parse(json) as Partial<BackupData>;` — línea 81 (cast #1)
- return statement final con sufijo `} as BackupData;` — línea 98 (cast #2)
- Mensajes de error a preservar exactamente: "Formato de respaldo inválido", "Falta performed_habits en el respaldo", "Falta mood_entries en el respaldo"

Tipo BackupData (de src/types/index.ts):
- `version: number; exportedAt: string; habits: Habit[]; performed_habits: PerformedHabit[]; mood_entries: MoodEntry[]; daily_assignments: DailyAssignment[];`

Wave 0 stub creado en plan 02-01:
- src/__tests__/speechRecognition.test.ts contiene 2 `it.todo` que este plan reemplaza por tests reales.

Test infrastructure NOTE: el proyecto NO tiene `@testing-library/react-native` ni `react-test-renderer` instalados (verificado en package.json). Por lo tanto los tests de useSpeechRecognition NO renderizan el hook como tal; usan jest.resetModules + jest.doMock para verificar que el archivo carga correctamente con módulo presente y ausente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tipar useSpeechRecognition.ts y agregar tests del módulo</name>
  <files>
    - src/hooks/useSpeechRecognition.ts (MODIFY — agregar 2 interfaces inline, tipar `let SpeechModule`, tipar callback de addResultListener)
    - src/__tests__/speechRecognition.test.ts (REWRITE — reemplazar `it.todo` por tests reales que cargan el módulo bajo dos mocks distintos)
  </files>
  <read_first>
    - src/hooks/useSpeechRecognition.ts (archivo completo, 80 líneas)
    - .planning/phases/02-tech-debt/02-RESEARCH.md (sección DEBT-01, líneas 154-220)
    - .planning/phases/02-tech-debt/02-PATTERNS.md (sección useSpeechRecognition, líneas 199-230)
    - src/__tests__/speechRecognition.test.ts (creado en 02-01 con `it.todo` — base a reescribir)
    - package.json (verificar ausencia de @testing-library/react-native antes de elegir test approach)
  </read_first>
  <behavior>
    - `grep -nE "\bany\b" src/hooks/useSpeechRecognition.ts` retorna 0 líneas
    - `grep -c "interface SpeechModuleInterface" src/hooks/useSpeechRecognition.ts` retorna 1
    - `grep -c "interface SpeechRecognitionEvent" src/hooks/useSpeechRecognition.ts` retorna 1
    - El comportamiento del hook es idéntico al actual: si require falla, isAvailable=false; si require pasa, isAvailable depende de voiceEnabled
    - El loader sigue siendo `require('expo-speech-recognition')` + try/catch — NO migrar a `await import()` (D-13 bloqueado)
    - speechRecognition.test.ts corre 4+ tests verdes; ninguno requiere react-test-renderer
  </behavior>
  <action>
**Modificación 1 — `src/hooks/useSpeechRecognition.ts`**:

Insertar las dos interfaces locales ANTES de la declaración de `let SpeechModule` (entre la línea 14 actual `import { useSettingsStore } from '../store/useSettingsStore';` y la línea 16 actual `let SpeechModule: any = null;`). El bloque a insertar:

```ts
// Interfaces locales — no importar del paquete (puede no estar instalado en runtime).
// Solo se tipa la superficie usada por el hook (D-11).
interface SpeechRecognitionEvent {
  results?: Array<{ transcript: string }>;
}

interface SpeechModuleInterface {
  addResultListener(
    cb: (event: SpeechRecognitionEvent) => void,
  ): { remove(): void } | undefined;
  ExpoSpeechRecognitionModule?: {
    start(opts: { lang: string }): Promise<void>;
    stop(): Promise<void>;
  };
}
```

Modificar la línea 16 actual:
- Antes: `let SpeechModule: any = null;`
- Después: `let SpeechModule: SpeechModuleInterface | null = null;`

Modificar la línea 19 actual (dentro del try):
- Antes: `SpeechModule = require('expo-speech-recognition');`
- Después: `SpeechModule = require('expo-speech-recognition') as SpeechModuleInterface;`

Modificar la línea 41 actual (callback de addResultListener):
- Antes: `const sub = SpeechModule.addResultListener?.((event: any) => {`
- Después: `const sub = SpeechModule.addResultListener?.((event: SpeechRecognitionEvent) => {`

NO modificar:
- Las líneas con useState, useCallback, useRef, useEffect — la lógica es idéntica.
- Los métodos start, stop, toggle — los `?.` siguen siendo necesarios porque `ExpoSpeechRecognitionModule` es opcional en la interfaz.
- El comentario `// eslint-disable-next-line @typescript-eslint/no-var-requires` sobre el require — mantenerlo.
- Los Alert con ALERT_VOICE_UNAVAILABLE — sin cambios.

**Modificación 2 — `src/__tests__/speechRecognition.test.ts`** (reemplazar el contenido completo del archivo creado en 02-01).

El proyecto NO tiene `@testing-library/react-native` instalado (verificar con `grep -q "@testing-library/react-native" package.json` antes de empezar). Por tanto los tests verifican el módulo a nivel de carga, no renderizan el hook. Patrón: `jest.resetModules` + `jest.doMock` con distintos shapes para el módulo nativo virtual.

Estructura del archivo:

1. Header JSDoc en español: "Tests para useSpeechRecognition (DEBT-01). El proyecto no tiene react-test-renderer; los tests cargan el módulo bajo distintos mocks de expo-speech-recognition para verificar que (a) el archivo no crashea cuando el módulo falta, (b) el archivo carga limpio cuando el módulo está, y (c) las interfaces locales aceptan el shape esperado."

2. Tests:

```ts
describe('useSpeechRecognition (DEBT-01)', () => {
  beforeEach(() => {
    jest.resetModules();
    // Mock del settings store por si se llega a importar transitively
    jest.doMock('../store/useSettingsStore', () => ({
      useSettingsStore: (selector: (s: { voiceDictationEnabled: boolean; language: string }) => unknown) =>
        selector({ voiceDictationEnabled: true, language: 'es' }),
    }));
  });

  afterEach(() => {
    jest.resetModules();
  });

  test('el archivo carga sin crashear cuando expo-speech-recognition falta (Expo Go path)', () => {
    jest.doMock(
      'expo-speech-recognition',
      () => { throw new Error('module not installed'); },
      { virtual: true },
    );
    expect(() => require('../hooks/useSpeechRecognition')).not.toThrow();
  });

  test('el archivo carga sin crashear cuando expo-speech-recognition retorna shape válido (dev build path)', () => {
    jest.doMock(
      'expo-speech-recognition',
      () => ({
        addResultListener: () => ({ remove: () => undefined }),
        ExpoSpeechRecognitionModule: {
          start: async (_o: { lang: string }) => undefined,
          stop: async () => undefined,
        },
      }),
      { virtual: true },
    );
    expect(() => require('../hooks/useSpeechRecognition')).not.toThrow();
  });

  test('el módulo exporta useSpeechRecognition como function', () => {
    jest.doMock(
      'expo-speech-recognition',
      () => ({
        addResultListener: () => undefined,
        ExpoSpeechRecognitionModule: { start: async () => undefined, stop: async () => undefined },
      }),
      { virtual: true },
    );
    const mod = require('../hooks/useSpeechRecognition');
    expect(typeof mod.useSpeechRecognition).toBe('function');
  });

  test('SpeechRecognitionEvent type-shape: results opcional, transcript string (sanity check)', () => {
    type Event = { results?: Array<{ transcript: string }> };
    const e1: Event = {};
    const e2: Event = { results: [{ transcript: 'hola' }] };
    expect(e1.results).toBeUndefined();
    expect(e2.results?.[0].transcript).toBe('hola');
  });
});
```

**Notas:**
- `{ virtual: true }` es necesario porque `expo-speech-recognition` puede no estar en `node_modules` durante tests; Jest lo tratará como módulo virtual.
- Los tests NO renderizan el hook (no se puede sin renderer). Verifican que el archivo carga sin crash en los dos paths críticos: módulo ausente y módulo presente.
- El `useSettingsStore` mock se hace con `jest.doMock` dentro de `beforeEach` para no interferir con otros tests si la suite se expande.
- Si en el futuro se instala `@testing-library/react-native`, estos tests pueden expandirse para usar `renderHook` y verificar el comportamiento real (start, stop, callback).
  </action>
  <verify>
    <automated>npx jest --testPathPattern="speechRecognition"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -nE "\bany\b" src/hooks/useSpeechRecognition.ts` succeeds (zero `any` in file — DEBT-01 success criterion)
    - `grep -c "interface SpeechModuleInterface" src/hooks/useSpeechRecognition.ts` returns 1
    - `grep -c "interface SpeechRecognitionEvent" src/hooks/useSpeechRecognition.ts` returns 1
    - `grep -c "let SpeechModule: SpeechModuleInterface | null" src/hooks/useSpeechRecognition.ts` returns 1
    - `grep -q "as SpeechModuleInterface" src/hooks/useSpeechRecognition.ts` succeeds (typed cast on require)
    - `grep -q "(event: SpeechRecognitionEvent)" src/hooks/useSpeechRecognition.ts` succeeds (typed callback param)
    - `grep -q "require('expo-speech-recognition')" src/hooks/useSpeechRecognition.ts` succeeds (loader pattern preserved per D-13)
    - `npx jest --testPathPattern="speechRecognition"` returns exit code 0 with all tests passing
    - `! grep -q "it.todo" src/__tests__/speechRecognition.test.ts` succeeds (no more placeholders)
    - `npx jest` (full suite) returns exit code 0 (no regressions)
  </acceptance_criteria>
  <done>useSpeechRecognition.ts contiene SpeechModuleInterface + SpeechRecognitionEvent inline; los dos `any` originales eliminados; loader require/try-catch preservado per D-13; tests reales reemplazan los `it.todo` stubs; suite completa verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Eliminar `as Partial&lt;BackupData&gt;` y `as BackupData` de backupService.parseAndValidate</name>
  <files>
    - src/services/backupService.ts (MODIFY — reemplazar la función parseAndValidate completa, conservando mensajes de error)
  </files>
  <read_first>
    - src/services/backupService.ts (archivo completo, prestar atención a líneas 80-99 — la función parseAndValidate)
    - src/types/index.ts (interfaz BackupData: campos exactos)
    - .planning/phases/02-tech-debt/02-RESEARCH.md (sección DEBT-04 ampliado, líneas 458-545)
  </read_first>
  <behavior>
    - `grep -nE "as (Partial<)?BackupData" src/services/backupService.ts` retorna 0 resultados
    - La función parseAndValidate sigue exportando un valor de tipo `BackupData` (o lanzando un Error)
    - Los mensajes de error en español se preservan EXACTAMENTE: "Formato de respaldo inválido", "Falta performed_habits en el respaldo", "Falta mood_entries en el respaldo"
    - JSON malformado → throw "Formato de respaldo inválido: JSON malformado" (extensión leve y útil del mensaje)
    - Datos válidos → retorna BackupData con todos los campos requeridos; daily_assignments=[] si está ausente o no es array
    - El resto del archivo (`buildBackupData`, `restoreData`, exportaciones) NO se modifica
    - npx jest (suite completa) sigue verde
  </behavior>
  <action>
Reemplazar la función `parseAndValidate` completa (líneas 80-99 actuales) en `src/services/backupService.ts` por la implementación con type guards. Mantener el resto del archivo intacto.

**Función reemplazante:**

```ts
function parseAndValidate(json: string): BackupData {
  let raw: unknown;
  try {
    raw = JSON.parse(json);
  } catch {
    throw new Error('Formato de respaldo inválido: JSON malformado');
  }

  if (raw == null || typeof raw !== 'object') {
    throw new Error('Formato de respaldo inválido');
  }

  const data = raw as Record<string, unknown>;

  if (typeof data.version !== 'number' || !Array.isArray(data.habits)) {
    throw new Error('Formato de respaldo inválido');
  }
  if (!Array.isArray(data.performed_habits)) {
    throw new Error('Falta performed_habits en el respaldo');
  }
  if (!Array.isArray(data.mood_entries)) {
    throw new Error('Falta mood_entries en el respaldo');
  }

  return {
    version: data.version,
    exportedAt: typeof data.exportedAt === 'string' ? data.exportedAt : new Date().toISOString(),
    habits: data.habits as BackupData['habits'],
    performed_habits: data.performed_habits as BackupData['performed_habits'],
    mood_entries: data.mood_entries as BackupData['mood_entries'],
    daily_assignments: Array.isArray(data.daily_assignments)
      ? (data.daily_assignments as BackupData['daily_assignments'])
      : [],
  };
}
```

**Notas críticas sobre los casts:**

1. `raw as Record<string, unknown>` — este cast NO es una assertion `as BackupData`; es un narrowing legítimo desde `unknown` a `Record<string, unknown>` después de haber validado en runtime que `raw != null && typeof raw === 'object'`. El acceptance criterion verifica que NO existan `as Partial<BackupData>` ni `as BackupData` (no que existan cero `as` totales) — el cast a `Record<string, unknown>` es esperado y aceptable.

2. `data.habits as BackupData['habits']` — estos son los campos que el contrato de Phase 3 va a refinar (Phase 3 puede agregar validación profunda de cada item). Por ahora basta con `Array.isArray` para satisfacer DEBT-02; la deuda profunda queda fuera de scope (no hay requirement Phase 2 para validar shape de cada Habit/PerformedHabit/MoodEntry/DailyAssignment).

3. El cambio de `data.version` chequeo — ANTES: `if (!data.version || !Array.isArray(data.habits))`; AHORA: `if (typeof data.version !== 'number' || !Array.isArray(data.habits))`. Razón: la versión `0` debería ser inválida pero `!data.version` también dispararía con cualquier número distinto de 0 — el patrón `typeof !== 'number'` es más correcto y semánticamente equivalente para datos reales (versiones siempre >= 1).

4. El mensaje "Formato de respaldo inválido: JSON malformado" es una EXTENSIÓN del mensaje base original — agregar el sufijo es informativo y no rompe consumidores de la cadena (los Alert.alert de la UI muestran la cadena completa).

**Verificación post-cambio (a correr antes de cerrar la task):**
- `! grep -nE "as (Partial<)?BackupData" src/services/backupService.ts` debe pasar.
- `grep -c "Formato de respaldo inválido" src/services/backupService.ts` debe retornar 2 (1 en el catch del JSON, 1 en el chequeo de version/habits).
- `grep -q "Falta performed_habits en el respaldo" src/services/backupService.ts` debe pasar.
- `grep -q "Falta mood_entries en el respaldo" src/services/backupService.ts` debe pasar.
- `npx jest` debe seguir verde (no hay tests existentes que ejerciten parseAndValidate; si hay alguno, debe seguir pasando).
  </action>
  <verify>
    <automated>bash -c 'cd "$(pwd)" && ! grep -nE "as (Partial<)?BackupData" src/services/backupService.ts && grep -q "Falta performed_habits en el respaldo" src/services/backupService.ts && grep -q "Falta mood_entries en el respaldo" src/services/backupService.ts && npx jest'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -nE "as (Partial<)?BackupData" src/services/backupService.ts` succeeds (no `as Partial<BackupData>` and no `as BackupData` casts remain)
    - `grep -c "function parseAndValidate" src/services/backupService.ts` returns 1 (function still exists with same name)
    - `grep -q "Formato de respaldo inválido" src/services/backupService.ts` succeeds (Spanish error preserved)
    - `grep -q "Falta performed_habits en el respaldo" src/services/backupService.ts` succeeds
    - `grep -q "Falta mood_entries en el respaldo" src/services/backupService.ts` succeeds
    - `grep -q "JSON.parse(json)" src/services/backupService.ts` succeeds (parse step intact)
    - `grep -q "raw as Record<string, unknown>" src/services/backupService.ts` succeeds (narrowing pattern present)
    - `grep -q "typeof data.version" src/services/backupService.ts` succeeds (runtime version check)
    - `npx jest` returns exit code 0 (no regressions)
  </acceptance_criteria>
  <done>parseAndValidate refactorizada sin `as Partial<BackupData>` y sin `as BackupData`; mensajes de error en español preservados; suite completa verde; archivo listo para que Phase 3 promueva parseAndValidate y buildBackupData a named exports sin heredar deuda.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional native module → app | useSpeechRecognition consume `expo-speech-recognition` via require + try/catch. La interfaz local tipa la superficie usada; el cast `as SpeechModuleInterface` no valida runtime (limitación documentada, ver Pitfall 5 en RESEARCH). |
| backup file → app | parseAndValidate recibe un string JSON externo (archivo local o, en Phase 3, Drive). Validación shape-by-shape via type guards garantiza que un archivo malformado o incompleto no llegue a restoreData con datos undefined. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-spoof-01 | Spoofing (tipo) | useSpeechRecognition (`any` permitía cualquier shape) | mitigate | Task 1: SpeechModuleInterface acota la superficie aceptada por el compilador; runtime sigue defendido por optional chaining (`?.`) preservado del código actual. |
| T-02-tamper-04 | Tampering | backupService.parseAndValidate (cast `as Partial<BackupData>` permitía datos malformados al pipeline de restore) | mitigate | Task 2: parseo a `unknown`, chequeos de tipo en runtime para version/habits/performed_habits/mood_entries, fallback explícito para daily_assignments ausente. Mensajes de error en español preservados. |

T-02-01 (categorías inválidas en DB), T-02-02 (SQL injection en sanitize) y T-02-03 (validación pre-write de habits) ya están mitigados en planes 02-01 y 02-02.
</threat_model>

<verification>
- `! grep -nE "\bany\b" src/hooks/useSpeechRecognition.ts` → cero `any` (DEBT-01 success criterion verde)
- `! grep -nE "as (Partial<)?BackupData" src/services/backupService.ts` → cero casts problemáticos (D-04 verde)
- `npx jest --testPathPattern="speechRecognition"` → tests reales corren verdes
- `npx jest` (suite completa) → 29 Phase 1 + 5 parsing + 4 habitService + 6 sanitize + 4+ speechRecognition = 48+ tests verdes
- Mensajes de error en español del backup preservados (3 grep checks en Task 2 acceptance)
</verification>

<success_criteria>
- DEBT-01 cerrado: `useSpeechRecognition.ts` tiene `SpeechModuleInterface` + `SpeechRecognitionEvent` inline y cero `any` explícitos.
- DEBT-02 cerrado totalmente con D-04 incluido: ningún archivo del proyecto tiene cast `as` para JSON parsing de categorías o backup.
- Phase 1 sigue verde, plan 02-01 sigue verde, plan 02-02 sigue verde.
- Phase 3 (Google Drive Backup) puede empezar sobre un `backupService` ya saneado — no hereda deuda de Phase 2.
- T-02-tamper-04 mitigado: backups malformados se rechazan con mensaje en español accionable, no se cuelan al pipeline de restore.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tech-debt/02-03-SUMMARY.md` documentando:
- Las dos interfaces locales agregadas a useSpeechRecognition.ts (firmas exactas)
- Confirmación de que el loader require/try-catch quedó intacto per D-13
- Resumen del refactor de parseAndValidate (cuántas líneas, qué guards se introdujeron, qué mensajes se preservaron)
- Decisión sobre tests del hook: se usó approach módulo-level porque @testing-library/react-native NO está instalado; nota para futuro: si se instala, expandir a renderHook + act
- Hand-off para Phase 3: backupService.parseAndValidate y backupService.buildBackupData están listas para promoverse a named exports en plan 03-02 sin heredar deuda
- Cierre de la fase: confirmar que los 3 success criteria del ROADMAP §Phase 2 están todos verdes (DEBT-01, DEBT-02, DEBT-03)
</output>
