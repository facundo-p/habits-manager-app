---
phase: 02-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/parsing.ts
  - src/services/habitService.ts
  - src/__tests__/parsing.test.ts
  - src/__tests__/sanitize.test.ts
  - src/__tests__/habitService.test.ts
  - src/__tests__/speechRecognition.test.ts
autonomous: true
requirements:
  - DEBT-02

must_haves:
  truths:
    - "parseAndValidateCategories exists in src/utils/parsing.ts and filters invalid IDs against VALID_AREA_IDS"
    - "parseAndValidateCategories emits console.warn listing discarded invalid IDs (D-14)"
    - "parseAndValidateCategories returns [] for malformed JSON, non-arrays, or empty arrays"
    - "createHabit and updateHabit in habitService throw a descriptive error listing invalid category IDs before any DB write (D-15)"
    - "Four Wave 0 test stub files exist and parser-related tests run GREEN against the new function"
    - "parseJsonArray is removed from src/utils/parsing.ts (replaced by parseAndValidateCategories)"
  artifacts:
    - path: "src/utils/parsing.ts"
      provides: "Central category parser parseAndValidateCategories(json: string): string[]"
      contains: "parseAndValidateCategories"
      min_lines: 20
    - path: "src/services/habitService.ts"
      provides: "Pre-write category validation in createHabit and updateHabit"
      contains: "VALID_AREA_IDS"
    - path: "src/__tests__/parsing.test.ts"
      provides: "Unit tests for parseAndValidateCategories (happy path, invalid IDs, malformed JSON, empty array, console.warn assertion)"
      contains: "parseAndValidateCategories"
    - path: "src/__tests__/sanitize.test.ts"
      provides: "Wave 0 stub for the two per-table sanitize functions (placeholder tests RED until db.ts is refactored in plan 02-02)"
      contains: "sanitize"
    - path: "src/__tests__/habitService.test.ts"
      provides: "Tests for D-15 write validation in createHabit and updateHabit"
      contains: "createHabit"
    - path: "src/__tests__/speechRecognition.test.ts"
      provides: "Wave 0 stub for useSpeechRecognition (placeholder tests RED until plan 02-03 types the hook)"
      contains: "useSpeechRecognition"
  key_links:
    - from: "src/utils/parsing.ts"
      to: "src/config/constants.ts"
      via: "import { VALID_AREA_IDS }"
      pattern: "import.*VALID_AREA_IDS.*from.*config/constants"
    - from: "src/services/habitService.ts"
      to: "src/config/constants.ts"
      via: "import { VALID_AREA_IDS }"
      pattern: "import.*VALID_AREA_IDS.*from.*config/constants"
    - from: "src/__tests__/parsing.test.ts"
      to: "src/utils/parsing.ts"
      via: "import { parseAndValidateCategories }"
      pattern: "import.*parseAndValidateCategories.*from.*utils/parsing"
---

<objective>
Crear el parser central de categorías (`parseAndValidateCategories`) en `src/utils/parsing.ts`, agregar validación de escritura en `createHabit`/`updateHabit` (D-15), y escribir las 4 stubs de tests Wave 0 que validan DEBT-01, DEBT-02 y DEBT-03.

Purpose: Establecer el bloque base sobre el que se construyen los planes 02-02 y 02-03. El parser es el único punto de parsing+validación de categorías; la validación de escritura cierra la última puerta abierta para que IDs inválidos lleguen a la DB; los 4 stubs de tests dejan los hooks de verificación listos para que los planes downstream usen TDD.
Output: `parseAndValidateCategories` exportada con filtrado + warn, `parseJsonArray` eliminada, validación pre-write en habits, 4 archivos de test creados con cobertura inicial verde para parser+habitService y RED placeholder para sanitize+speech.
</objective>

<execution_context>
@/Users/facu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/facu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tech-debt/02-CONTEXT.md
@.planning/phases/02-tech-debt/02-RESEARCH.md
@.planning/phases/02-tech-debt/02-PATTERNS.md
@.planning/phases/02-tech-debt/02-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs without exploring the codebase. -->

From src/config/constants.ts (line 150):
```typescript
// Set con 9 IDs válidos: salud_fisica, mental, relaciones, proposito, economia, crecimiento, espiritualidad, recreacion, entorno
export const VALID_AREA_IDS = new Set(HABIT_AREAS.map((a) => a.id));
```

Current src/utils/parsing.ts (entire file, to be REPLACED):
```typescript
/**
 * parsing.ts — Utilidades de parseo compartidas entre services, screens y componentes.
 */

/** Parsea un JSON de array de strings de forma segura. Devuelve [] ante cualquier error. */
export function parseJsonArray(json: string): string[] {
  try {
    const arr = JSON.parse(json);
    return Array.isArray(arr) ? arr : [];
  } catch {
    return [];
  }
}
```

Current src/services/habitService.ts createHabit/updateHabit (lines 96-113):
```typescript
export async function createHabit(
  name: string,
  frequency: string,
  basePoints: number,
  categories: string[],
): Promise<string> {
  return habitRepo.insert(name, frequency, basePoints, JSON.stringify(categories));
}

export async function updateHabit(
  id: string,
  name: string,
  frequency: string,
  basePoints: number,
  categories: string[],
): Promise<void> {
  return habitRepo.update(id, name, frequency, basePoints, JSON.stringify(categories));
}
```

Reference pattern from src/services/assignmentService.ts lines 91-97 (BUG-04 — copy verbatim, swap prefix):
```typescript
const invalidIds = categories.filter((id) => !VALID_AREA_IDS.has(id));
if (invalidIds.length > 0) {
  throw new Error(
    `addSpontaneous: categorias invalidas — ${invalidIds.join(', ')}`,
  );
}
```

Test infrastructure from src/__tests__/setup/testDatabase.ts:
```typescript
export function createTestDatabase(): Database.Database;
export function resetTestDatabase(): void;
export interface TestHabitOpts { id: string; name: string; frequency?: string; base_points?: number; default_categories?: string; is_active?: number; }
export function insertTestHabit(db: Database.Database, opts: TestHabitOpts): void;
```

Jest config: `testMatch: ['<rootDir>/src/__tests__/**/*.test.ts']`. Run command: `npx jest --testPathPattern="<pattern>"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear los 4 stubs de tests Wave 0 (parsing, sanitize, habitService, speechRecognition)</name>
  <files>
    - src/__tests__/parsing.test.ts (NEW)
    - src/__tests__/sanitize.test.ts (NEW)
    - src/__tests__/habitService.test.ts (NEW)
    - src/__tests__/speechRecognition.test.ts (NEW)
  </files>
  <read_first>
    - src/__tests__/dailyAssignments.test.ts (reference test pattern — better-sqlite3 in-memory + jest.mock)
    - src/__tests__/setup/testDatabase.ts (createTestDatabase, insertTestHabit signatures)
    - __mocks__/expo-sqlite.ts (mock structure)
    - .planning/phases/02-tech-debt/02-VALIDATION.md (Wave 0 Requirements section)
    - .planning/phases/02-tech-debt/02-PATTERNS.md (Test File Patterns section, lines 326-432)
  </read_first>
  <behavior>
    - parsing.test.ts: 5 tests (RED initially, become GREEN in Task 2):
      1. "retorna solo IDs válidos de un array bien formado" — input `'["salud_fisica","mental"]'` → output `['salud_fisica','mental']`
      2. "filtra IDs inválidos y emite console.warn" — input `'["salud_fisica","fake_id"]'` → output `['salud_fisica']`, console.warn called once with array containing `'fake_id'`
      3. "retorna [] para JSON malformado" — input `'{not json'` → output `[]`
      4. "retorna [] para JSON que no es array" — input `'{"foo":1}'` → output `[]`
      5. "retorna [] para array vacío" — input `'[]'` → output `[]`
    - sanitize.test.ts: 2 placeholder tests using `it.todo` — these are intentional RED stubs that plan 02-02 will replace with real assertions when sanitizeHabitDefaultCategories and sanitizePerformedCategoriesUsed are implemented
    - habitService.test.ts: 4 tests (RED initially, become GREEN in Task 3):
      1. "createHabit: throws descriptive error when categories contain an invalid ID" — `await expect(createHabit('H','daily',1,['fake_id'])).rejects.toThrow(/fake_id/)`
      2. "createHabit: throws error listing ALL invalid IDs" — `['salud_fisica','fake_id']` → error message contains `fake_id` but not `salud_fisica`
      3. "createHabit: succeeds with all valid categories" — `['salud_fisica','mental']` → resolves with a non-empty string id
      4. "updateHabit: throws on invalid IDs" — `await expect(updateHabit('id1','H','daily',1,['fake_id'])).rejects.toThrow(/fake_id/)`
    - speechRecognition.test.ts: 2 placeholder tests using `it.todo` — intentional RED stubs that plan 02-03 will replace
  </behavior>
  <action>
Crear los 4 archivos de test. Seguir EXACTAMENTE el patrón de imports y mock de `dailyAssignments.test.ts` (lines 16-63).

**1. `src/__tests__/parsing.test.ts`** — sin DB, sin mocks:
```typescript
import { parseAndValidateCategories } from '../utils/parsing';

describe('parseAndValidateCategories', () => {
  test('retorna solo IDs válidos de un array bien formado', () => {
    const result = parseAndValidateCategories('["salud_fisica","mental"]');
    expect(result).toEqual(['salud_fisica', 'mental']);
  });

  test('filtra IDs inválidos y emite console.warn', () => {
    const warnSpy = jest.spyOn(console, 'warn').mockImplementation(() => {});
    const result = parseAndValidateCategories('["salud_fisica","fake_id"]');
    expect(result).toEqual(['salud_fisica']);
    expect(warnSpy).toHaveBeenCalledTimes(1);
    expect(warnSpy.mock.calls[0][1]).toEqual(expect.arrayContaining(['fake_id']));
    warnSpy.mockRestore();
  });

  test('retorna [] para JSON malformado', () => {
    expect(parseAndValidateCategories('{not json')).toEqual([]);
  });

  test('retorna [] para JSON que no es array', () => {
    expect(parseAndValidateCategories('{"foo":1}')).toEqual([]);
  });

  test('retorna [] para array vacío', () => {
    expect(parseAndValidateCategories('[]')).toEqual([]);
  });
});
```

**2. `src/__tests__/sanitize.test.ts`** — stub RED con `it.todo`:
```typescript
/**
 * Wave 0 stub — RED placeholder.
 * Plan 02-02 va a reemplazar estos todos por tests reales que importan
 * sanitizeHabitDefaultCategories y sanitizePerformedCategoriesUsed desde db.ts.
 */
describe('sanitize (DEBT-03)', () => {
  it.todo('sanitizeHabitDefaultCategories elimina IDs inválidos en habits.default_categories');
  it.todo('sanitizePerformedCategoriesUsed elimina IDs inválidos en performed_habits.categories_used');
});
```

**3. `src/__tests__/habitService.test.ts`** — DB in-memory + mocks idénticos a dailyAssignments.test.ts:
```typescript
import type Database from 'better-sqlite3';
import { createTestDatabase, resetTestDatabase } from './setup/testDatabase';

const TODAY = '2026-04-26';

jest.mock('../services/db', () => {
  const actual = jest.requireActual('../services/db');
  return {
    ...actual,
    getTodayPrefix: jest.fn(() => TODAY),
    getNowTimestamp: jest.fn(() => `${TODAY} 10:00:00`),
    getTimestampForDate: jest.fn((date: string) => `${date} 10:00:00`),
  };
});

import { createHabit, updateHabit } from '../services/habitService';

let db: Database.Database;

beforeEach(() => {
  db = createTestDatabase();
});

afterEach(() => {
  resetTestDatabase();
  jest.clearAllMocks();
});

describe('habitService — D-15: write-time category validation', () => {
  test('createHabit: throws descriptive error when categories contain an invalid ID', async () => {
    await expect(createHabit('Habito', 'daily', 1, ['fake_id']))
      .rejects.toThrow(/fake_id/);
  });

  test('createHabit: throws error listing ALL invalid IDs', async () => {
    await expect(createHabit('Habito', 'daily', 1, ['salud_fisica', 'fake_id']))
      .rejects.toThrow(/fake_id/);
  });

  test('createHabit: succeeds with all valid categories', async () => {
    const id = await createHabit('Habito', 'daily', 1, ['salud_fisica', 'mental']);
    expect(typeof id).toBe('string');
    expect(id.length).toBeGreaterThan(0);
  });

  test('updateHabit: throws on invalid IDs', async () => {
    const id = await createHabit('Habito', 'daily', 1, ['salud_fisica']);
    await expect(updateHabit(id, 'Habito', 'daily', 1, ['fake_id']))
      .rejects.toThrow(/fake_id/);
  });
});
```

**4. `src/__tests__/speechRecognition.test.ts`** — stub RED con `it.todo`:
```typescript
/**
 * Wave 0 stub — RED placeholder.
 * Plan 02-03 va a reemplazar estos todos por tests reales que importan
 * useSpeechRecognition tras eliminar los `any` de useSpeechRecognition.ts.
 */
describe('useSpeechRecognition (DEBT-01)', () => {
  it.todo('isAvailable=false cuando expo-speech-recognition no está instalado');
  it.todo('addResultListener recibe SpeechRecognitionEvent tipado, sin any');
});
```

NOTA — Estos tests con `it.todo` aparecen en la salida de Jest como "todo" (no fallan ni pasan). Son marcadores trazables para los planes downstream.

NOTA — Tests de `parsing.test.ts` y `habitService.test.ts` van a fallar al ejecutar `npx jest` antes de las Tasks 2 y 3 (RED esperado). Eso valida que los tests realmente ejercen el código que se va a escribir.
  </action>
  <verify>
    <automated>npx jest --listTests --testPathPattern="parsing|sanitize|habitService|speechRecognition" 2>&1 | grep -E "parsing\.test|sanitize\.test|habitService\.test|speechRecognition\.test" | wc -l | grep -q "^[[:space:]]*4$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `src/__tests__/parsing.test.ts` exists and `grep -c "test(" src/__tests__/parsing.test.ts` returns 5
    - File `src/__tests__/sanitize.test.ts` exists and `grep -c "it.todo(" src/__tests__/sanitize.test.ts` returns 2
    - File `src/__tests__/habitService.test.ts` exists and `grep -c "test(" src/__tests__/habitService.test.ts` returns 4
    - File `src/__tests__/speechRecognition.test.ts` exists and `grep -c "it.todo(" src/__tests__/speechRecognition.test.ts` returns 2
    - `npx jest --listTests --testPathPattern="parsing|sanitize|habitService|speechRecognition"` lists 4 files
    - `grep -q "import.*parseAndValidateCategories.*from.*utils/parsing" src/__tests__/parsing.test.ts` succeeds
    - `grep -q "import.*createHabit.*updateHabit.*from.*services/habitService\|import.*createHabit.*from.*services/habitService" src/__tests__/habitService.test.ts` succeeds
  </acceptance_criteria>
  <done>4 archivos de test creados; parsing.test.ts y habitService.test.ts contienen tests reales que fallarán hasta Task 2/3; sanitize.test.ts y speechRecognition.test.ts contienen `it.todo` placeholders; jest --listTests reporta los 4 archivos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reemplazar parseJsonArray por parseAndValidateCategories en parsing.ts</name>
  <files>
    - src/utils/parsing.ts (REWRITE — replace parseJsonArray)
  </files>
  <read_first>
    - src/utils/parsing.ts (current implementation, 13 líneas)
    - src/config/constants.ts (línea 150 — VALID_AREA_IDS Set)
    - src/__tests__/parsing.test.ts (creado en Task 1 — los 5 tests deben pasar tras esta task)
    - .planning/phases/02-tech-debt/02-RESEARCH.md (sección DEBT-02, líneas 271-303)
  </read_first>
  <behavior>
    Después de esta task:
    - `import { parseAndValidateCategories } from '../utils/parsing'` funciona desde cualquier archivo
    - `import { parseJsonArray } from '../utils/parsing'` falla en compile (función eliminada — los call sites se migran en plan 02-02)
    - `parseAndValidateCategories('["salud_fisica","mental"]')` → `['salud_fisica','mental']`
    - `parseAndValidateCategories('["salud_fisica","fake_id"]')` → `['salud_fisica']` y `console.warn` se llama 1 vez con prefijo `[parseAndValidateCategories] IDs de área inválidos descartados:` y array `['fake_id']`
    - `parseAndValidateCategories('{not json')` → `[]` sin warn
    - `parseAndValidateCategories('{"foo":1}')` → `[]` sin warn
    - `parseAndValidateCategories('[]')` → `[]` sin warn
    - El orden del array de salida preserva el orden del array de entrada (filter, no sort)
    - Items que no sean strings se descartan y aparecen en el warn convertidos via `String(id)`
  </behavior>
  <action>
Reemplazar el contenido completo de `src/utils/parsing.ts` con la nueva implementación. Eliminar `parseJsonArray` (los 4 call sites quedarán rotos hasta plan 02-02 — esto es intencional y se ve a nivel de TypeScript en los archivos consumidores; no se ejecuta el código en CI esta wave).

Nuevo contenido completo de `src/utils/parsing.ts`:
```typescript
/**
 * parsing.ts — Utilidades de parseo compartidas entre services, screens y componentes.
 *
 * Único punto de parsing + validación de categorías de hábitos contra VALID_AREA_IDS.
 */

import { VALID_AREA_IDS } from '../config/constants';

/**
 * Parsea un JSON de array de categorías de forma segura.
 * - Filtra IDs inválidos contra VALID_AREA_IDS.
 * - Emite console.warn con los IDs descartados (D-14).
 * - Retorna [] ante JSON malformado, no-array, o array vacío.
 *
 * @param json String JSON crudo desde la DB (puede ser malformado en datos legacy).
 * @returns Array de IDs de área válidos en el orden original; [] si nada válido.
 */
export function parseAndValidateCategories(json: string): string[] {
  try {
    const arr = JSON.parse(json);
    if (!Array.isArray(arr)) return [];
    const valid: string[] = [];
    const invalid: string[] = [];
    for (const id of arr) {
      if (typeof id === 'string' && VALID_AREA_IDS.has(id)) {
        valid.push(id);
      } else {
        invalid.push(String(id));
      }
    }
    if (invalid.length > 0) {
      console.warn(
        '[parseAndValidateCategories] IDs de área inválidos descartados:',
        invalid,
      );
    }
    return valid;
  } catch {
    return [];
  }
}
```

**Importante:**
- NO mantener `parseJsonArray` como alias temporal. Eliminarla del archivo. Plan 02-02 migra todos los call sites en una sola wave.
- El string del warn debe ser EXACTAMENTE `'[parseAndValidateCategories] IDs de área inválidos descartados:'` (Task 1's test verifica el segundo argumento del warn — el array — vía `warnSpy.mock.calls[0][1]`).
- Mantener el JSDoc en español siguiendo CONVENTIONS.md.
- No exportar nada más desde este archivo (la firma del helper de validación de escritura vive en habitService — D-15 — no en parsing.ts; ver Claude's Discretion en CONTEXT.md, optamos por NO exportar a parsing.ts para mantener el helper junto a su consumidor).
  </action>
  <verify>
    <automated>npx jest --testPathPattern="parsing"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --testPathPattern="parsing"` returns exit code 0 with 5 passing tests
    - `grep -c "export function parseAndValidateCategories" src/utils/parsing.ts` returns 1
    - `grep -c "parseJsonArray" src/utils/parsing.ts` returns 0 (function removed)
    - `grep -q "import.*VALID_AREA_IDS.*from.*config/constants" src/utils/parsing.ts` succeeds
    - `grep -q "console.warn" src/utils/parsing.ts` succeeds
    - `grep -q "\\[parseAndValidateCategories\\] IDs de área inválidos descartados:" src/utils/parsing.ts` succeeds
  </acceptance_criteria>
  <done>parseAndValidateCategories implementada y exportada; parseJsonArray eliminada; los 5 tests de parsing.test.ts pasan; el archivo importa VALID_AREA_IDS desde config/constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Agregar validación pre-write de categorías en createHabit y updateHabit (D-15)</name>
  <files>
    - src/services/habitService.ts (MODIFY — agregar import VALID_AREA_IDS, insertar bloque de validación en createHabit y updateHabit)
  </files>
  <read_first>
    - src/services/habitService.ts (estado actual — líneas 96-113 son los targets)
    - src/services/assignmentService.ts (líneas 91-97 — patrón BUG-04 a copiar)
    - src/config/constants.ts (línea 150 — VALID_AREA_IDS export)
    - src/__tests__/habitService.test.ts (creado en Task 1 — los 4 tests deben pasar tras esta task)
    - .planning/phases/02-tech-debt/02-PATTERNS.md (sección "Error throw style for invalid category IDs")
  </read_first>
  <behavior>
    Después de esta task:
    - `createHabit('Test', 'daily', 1, ['fake_id'])` → throw `Error` cuyo message contiene `'createHabit'`, `'categorias invalidas'`, y `'fake_id'`
    - `createHabit('Test', 'daily', 1, ['salud_fisica', 'fake_id'])` → throw cuyo message lista `'fake_id'` (puede o no listar `salud_fisica`; el test sólo exige que `fake_id` aparezca)
    - `createHabit('Test', 'daily', 1, ['salud_fisica', 'mental'])` → resolves con un string id no vacío
    - `createHabit('Test', 'daily', 1, [])` → resolves (array vacío es válido)
    - `updateHabit('id1', 'Test', 'daily', 1, ['fake_id'])` → throw cuyo message contiene `'updateHabit'`, `'categorias invalidas'`, `'fake_id'`
    - La validación corre ANTES del `JSON.stringify(categories)` y antes de cualquier llamada a `habitRepo` — es decir, el repo nunca recibe IDs inválidos
  </behavior>
  <action>
Modificar `src/services/habitService.ts` con dos cambios:

**Cambio 1 — agregar import** (al final del bloque de imports existente, después de `import { buildStats } from '../utils/statsHelpers';` en línea 15):
```typescript
import { VALID_AREA_IDS } from '../config/constants';
```

**Cambio 2 — insertar bloque de validación en `createHabit`** (línea 96-103). El bloque va inmediatamente DESPUÉS de la apertura de llave de la función y ANTES del `return habitRepo.insert(...)`. Resultado:
```typescript
export async function createHabit(
  name: string,
  frequency: string,
  basePoints: number,
  categories: string[],
): Promise<string> {
  const invalidIds = categories.filter((id) => !VALID_AREA_IDS.has(id));
  if (invalidIds.length > 0) {
    throw new Error(
      `createHabit: categorias invalidas — ${invalidIds.join(', ')}`,
    );
  }
  return habitRepo.insert(name, frequency, basePoints, JSON.stringify(categories));
}
```

**Cambio 3 — insertar bloque de validación en `updateHabit`** (línea 105-113). Mismo patrón, prefijo `updateHabit:`:
```typescript
export async function updateHabit(
  id: string,
  name: string,
  frequency: string,
  basePoints: number,
  categories: string[],
): Promise<void> {
  const invalidIds = categories.filter((id) => !VALID_AREA_IDS.has(id));
  if (invalidIds.length > 0) {
    throw new Error(
      `updateHabit: categorias invalidas — ${invalidIds.join(', ')}`,
    );
  }
  return habitRepo.update(id, name, frequency, basePoints, JSON.stringify(categories));
}
```

**Notas:**
- Copiar EXACTAMENTE el formato del mensaje de assignmentService.ts línea 92-95: `<funcName>: categorias invalidas — <ids.join(', ')>`. Mismo em-dash `—`, mismo "categorias" sin tilde (style del proyecto en BUG-04 que el usuario validó).
- No extraer a un helper compartido (dos call sites internos al mismo archivo no justifican abstracción; ≤6 líneas de duplicación, dentro del límite del proyecto).
- No modificar los otros métodos del archivo.
- No modificar el repository — la validación vive en service layer (CONVENTIONS.md: layer separation).
  </action>
  <verify>
    <automated>npx jest --testPathPattern="habitService"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --testPathPattern="habitService"` returns exit code 0 with 4 passing tests
    - `grep -q "import.*VALID_AREA_IDS.*from.*config/constants" src/services/habitService.ts` succeeds
    - `grep -c "categorias invalidas —" src/services/habitService.ts` returns 2 (one per function)
    - `grep -q "createHabit: categorias invalidas —" src/services/habitService.ts` succeeds
    - `grep -q "updateHabit: categorias invalidas —" src/services/habitService.ts` succeeds
    - `grep -B1 "habitRepo.insert" src/services/habitService.ts | grep -q "JSON.stringify(categories)"` succeeds (the existing insert call is preserved)
  </acceptance_criteria>
  <done>createHabit y updateHabit validan categorías contra VALID_AREA_IDS antes del INSERT/UPDATE, throwean con mensaje en formato BUG-04, los 4 tests de habitService.test.ts pasan, no se modifica habitRepository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Repository | Único cruce relevante en este plan: habitService valida antes de delegar al repo. El repo asume datos ya validados (CRUD puro). |
| User input → Service | `createHabit`/`updateHabit` reciben `categories: string[]` desde la UI (HabitFormModal). El service NO confía — valida contra whitelist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `parseAndValidateCategories` (src/utils/parsing.ts) | mitigate | Task 2: parser filtra contra `VALID_AREA_IDS.has()` y emite `console.warn` con IDs descartados (D-14). Test 2 de parsing.test.ts verifica que el filtro y el warn ocurren. |
| T-02-03 | Tampering | `createHabit`/`updateHabit` (src/services/habitService.ts) | mitigate | Task 3: validación pre-INSERT/pre-UPDATE con `categories.filter((id) => !VALID_AREA_IDS.has(id))` que throwea error descriptivo (D-15). Tests 1, 2, 4 de habitService.test.ts verifican el throw. Cierra la última puerta abierta para que IDs inválidos lleguen a la DB. |

T-02-02 (SQL injection en sanitizeTable) se mitiga en plan 02-02 (que refactoriza `sanitizeTable` a dos funciones con SQL estático).
</threat_model>

<verification>
- `npx jest --testPathPattern="parsing"` → 5 tests passing
- `npx jest --testPathPattern="habitService"` → 4 tests passing
- `npx jest --testPathPattern="sanitize|speechRecognition"` → tests "todo" (ni passing ni failing — Wave 0 stubs intencionales)
- `npx jest --testPathPattern="dailyAssignments"` → 29 tests passing (regression — Phase 1 no se rompe)
- `! grep -q "parseJsonArray" src/utils/parsing.ts` → función eliminada del archivo
- `grep -c "categorias invalidas —" src/services/habitService.ts` → 2 (validación en createHabit y updateHabit)
- TypeScript compile en este archivo: `npx tsc --noEmit src/utils/parsing.ts` no debe fallar (los call sites de parseJsonArray sí van a fallar — eso es esperado y se arregla en plan 02-02)
</verification>

<success_criteria>
- DEBT-02 parcial: parser central existe y reemplaza completamente a parseJsonArray. (Migración de los 4 call sites es responsabilidad de plan 02-02.)
- D-14 implementado: parser filtra IDs inválidos silenciosamente Y emite console.warn.
- D-15 implementado: validación pre-write en createHabit y updateHabit.
- Wave 0 completa: 4 archivos de test creados, dos de ellos con tests reales que pasan, dos con `it.todo` listos para reemplazo en planes downstream.
- Phase 1 sigue verde (29 tests de dailyAssignments).
</success_criteria>

<output>
After completion, create `.planning/phases/02-tech-debt/02-01-SUMMARY.md` documentando:
- parseAndValidateCategories firma final, ubicación, comportamiento ante input inválido
- Decisión sobre helper de validación de escritura: NO se exporta desde parsing.ts (vive inline en habitService — Claude's Discretion en CONTEXT.md)
- Estado de los 4 archivos de test (cuáles green, cuáles todo, qué plan los completa)
- Hand-off para 02-02 (parser disponible para call sites + sanitize) y 02-03 (speech tests stub disponible)
- Nota: parseJsonArray ya no existe → los 4 call sites están temporalmente rotos a nivel de TypeScript hasta que 02-02 los migre. Esto es intencional y planificado.
</output>
